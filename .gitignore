node_modules/
.vercel/
.env
.env.local
*.log
.DS_Store
Thumbs.db
.vercel
.env*.local

# Firebase service account keys — NEVER commit
*-firebase-adminsdk-*.json
firebase-*.json
serviceAccount*.json

# Extension zip — distribute via GitHub Releases
*.zip

# NB N-gram corpus cache (1 GB+) — never commit; build script re-downloads on demand
corpus/*
!corpus/.gitignore

# Package-time staging dir (used by scripts/package-extension.js)
.package-staging/

# Claude Code settings
.claude/

# graphify knowledge-graph build output (run `graphify update .` to regenerate)
graphify-out/

# Local screenshots dir — user workflow, not shipped artefacts
screenshots/
