---
phase: 02-data-layer-frequency-bigrams-typo-bank
plan: 05
type: execute
wave: 3
depends_on: [02-04]
files_modified:
  - extension/data/en.json
  - extension/data/grammarfeatures-en.json
  - extension/manifest.json
  - extension/popup/popup.js
  - extension/popup/popup.html
  - extension/content/floating-widget.js
  - extension/content/word-prediction.js
  - extension/content/vocab-seam-core.js
  - extension/content/vocab-seam.js
  - extension/background/service-worker.js
  - scripts/sync-vocab.js
autonomous: false
gap_closure: true
requirements: []  # gap closure — closes Phase 2 SC-4 (roadmap success criterion), no new REQUIREMENTS.md row
user_setup: []

must_haves:
  truths:
    - "npm run check-bundle-size exits 0 on the packaged zip"
    - "npm run check-fixtures still exits 0 at 138/138"
    - "German, Spanish, French dictionary lookup still work in the unpacked extension (manual smoke)"
    - "Zero references to extension/data/en.json (or 'en' as a vocabulary language code) remain in shipped extension source"
  artifacts:
    - path: "extension/data/en.json"
      provides: "REMOVED from repo"
      contains: "(file should not exist after this plan lands)"
    - path: ".planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-05-SUMMARY.md"
      provides: "Audit findings + removal record + verification evidence"
      min_lines: 40
  key_links:
    - from: "backend/public/lexi-extension.zip"
      to: "10 MiB cap"
      via: "scripts/check-bundle-size.js"
      pattern: "PASS.*headroom"
    - from: "extension runtime (popup + content + service worker)"
      to: "vocab data loading"
      via: "NO fetch/read of en.json remains"
      pattern: "en\\.json"
---

<objective>
Close Phase 2 Success Criterion #4 (10 MiB bundle-size cap). The packaged zip is 10,599,772 bytes — 114,012 bytes over the 10,485,760-byte ceiling. Per the locked user decision in 02-CONTEXT.md, this plan audits `extension/data/en.json` (4.65 MB) for runtime references and, if unreferenced, deletes it together with any dead-code paths that would reach for it. Expected outcome: zip drops to ~5.5 MiB (~4.5 MiB headroom), `check-bundle-size` turns green, Phase 2 closes.

Purpose: English is NOT in CLAUDE.md's supported-language list (de/es/fr for foreign-language learning; nb/nn for spell-check), so en.json is suspected dead weight. The gate infrastructure from Plan 02-04 correctly reports FAIL; only the data payload needs to come down.

Output:
- Audit finding (captured inline in 02-05-SUMMARY.md)
- `extension/data/en.json` deleted (if audit is clean)
- Any dead-code paths that named `en.json` or `'en'` as a vocab language removed
- `npm run check-bundle-size` exits 0
- `npm run check-fixtures` still exits 0 at 138/138

Non-goals:
- Do NOT touch `extension/data/grammarfeatures-en.json` unless the audit proves it too is unreferenced. It is small (~7 KB); leave it alone unless actively harmful.
- Do NOT bump `CEILING_BYTES` in `scripts/check-bundle-size.js`. Outcome-B rejection is the whole point of this plan.
- Do NOT cut a GitHub Release. That is Release Workflow work, outside this plan's scope.
- Do NOT modify the sibling `papertek-vocabulary` repo. If its sync output still emits en.json, that's fine — Leksihjelp simply stops bundling it.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-CONTEXT.md
@.planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-VERIFICATION.md
@.planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Release gate contract the executor needs. Extracted from scripts/check-bundle-size.js (Plan 02-04). -->
<!-- Executor does NOT modify the gate; it runs it and reads exit code. -->

From scripts/check-bundle-size.js:
- Constant: CEILING_BYTES = 10 * 1024 * 1024 (= 10,485,760)
- Behavior: runs `npm run package`, reads backend/public/lexi-extension.zip size, shells `unzip -l` for per-dir breakdown, exits 0 if ≤ cap (with headroom report) or 1 if > cap (with overage + remediation hint).
- Current state: exits 1 at 10,599,772 bytes (114,012 over cap).

From scripts/package-extension.js:
- Behavior: uses `.package-staging/` (gitignored) scratch copy of `extension/`, minifies `data/*.json` via JSON.parse/stringify, zips via `zip -r -X -q`. Source tree `extension/data/*.json` stays pretty-printed.
- Executor does NOT modify this script.

From scripts/check-fixtures.js (Plan 01-03):
- Behavior: runs 138 hand-authored NB/NN fixture cases, exits 0 if all pass (any hard mismatch = exit 1).
- Current state: 138/138 pass at F1=1.000.
- Executor runs it to confirm no regression from en.json removal.
</interfaces>

<audit_surfaces>
<!-- The exact surfaces the audit task must grep. From 02-CONTEXT.md "en.json Audit Scope". -->
<!-- Grep patterns: en.json   'en'   "en"   data/en   i18n and vocab-language-map contexts. -->

- extension/manifest.json — web_accessible_resources, content_scripts.matches
- extension/popup/popup.js — language picker, fetch paths
- extension/popup/popup.html — language picker markup
- extension/content/floating-widget.js — language detection
- extension/content/word-prediction.js — prediction dictionaries
- extension/content/vocab-seam-core.js — language map (LOOKS MOST LIKELY TO LIST 'en')
- extension/content/vocab-seam.js — browser-side loader; may fetch data/en.json
- extension/background/service-worker.js — context menus, language handoff
- scripts/sync-vocab.js — language sync list (may explicitly enumerate 'en')
- scripts/build-frequencies.js, scripts/build-bigrams.js — corpus language switches (these are NB/NN only, so 'en' references would be noise)
- extension/i18n/*.json — UI translation strings. DIFFERENT from vocab en.json. Distinguish carefully.

Distinguish these two namespaces:
- VOCAB data: extension/data/{lang}.json — this is what we're auditing for removal
- UI i18n strings: extension/i18n/{lang}.json — unrelated, leave alone
</audit_surfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit en.json references across shipped extension + scripts</name>
  <files>
    .planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-05-AUDIT.md
  </files>
  <action>
    Produce a complete, disciplined audit of every reference to the English vocabulary payload. Write findings to `.planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-05-AUDIT.md` (a working scratch file for this plan; it can be merged into 02-05-SUMMARY.md at plan-end).

    **Step A — File presence grep (hard references to `en.json`):**
    ```
    rg -n "en\.json" extension/ scripts/ backend/ 2>/dev/null
    ```
    Expected: only hits in `extension/data/en.json` itself, `extension/data/grammarfeatures-en.json` (different file), possibly `extension/i18n/en.json` (also different file — UI strings, not vocab). Flag any extension runtime hit.

    **Step B — Language-code audit (soft references to `'en'` or `"en"` as a vocab language):**
    ```
    rg -n "['\"]en['\"]" extension/manifest.json extension/popup/ extension/content/ extension/background/ scripts/sync-vocab.js 2>/dev/null
    ```
    Every hit must be triaged into one of:
    - (a) vocab data load path — HARD REFERENCE, surfaces a checkpoint
    - (b) i18n UI strings path (e.g., `locales/en/messages.json` or `i18n/en.json` load) — leave alone
    - (c) HTML lang attribute, documentation, comments — leave alone
    - (d) web_accessible_resources manifest entries mentioning `data/en.json` — HARD REFERENCE, surfaces a checkpoint

    **Step C — Cross-language link audit inside vocab data:**
    Check if `extension/data/de.json`, `es.json`, `fr.json`, `nb.json`, `nn.json` have cross-language translation links pointing at English entries. A quick probe:
    ```
    rg -n '"en"\s*:' extension/data/de.json extension/data/es.json extension/data/fr.json 2>/dev/null | head -20
    ```
    If cross-language links exist, note them — removing en.json may surface 404s if the runtime tries to follow those links. Usually these links are data-only and never resolved unless a specific lookup asks for them; document the risk in the audit file regardless.

    **Step D — sync-vocab.js enumeration:**
    Open `scripts/sync-vocab.js` and confirm whether it lists `'en'` in its languages-to-sync array. If yes, document it — Task 2 will decide whether to drop `'en'` from that array.

    **Step E — Service-worker context-menu audit:**
    Open `extension/background/service-worker.js` and grep for `'en'`, `English`, `Engelsk`. Document any context-menu item, default-language constant, or fallback path that names English.

    **Step F — Package-staging sanity check:**
    Run `rg -n "en\.json" .package-staging/ 2>/dev/null` if the directory exists (it should be gone between runs, but just in case). No hits expected.

    **Audit output format (write to `02-05-AUDIT.md`):**
    ```markdown
    # en.json Audit — 2026-04-19

    ## Hard references (file-name grep)
    | File | Line | Context | Classification |
    |------|------|---------|----------------|
    | ... | ... | ... | data-vocab / i18n / doc-only |

    ## Soft references ('en' as vocab language code)
    | File | Line | Context | Classification |
    | ... | ... | ... | data-vocab / i18n / html-lang / comment |

    ## Cross-language links
    | File | Sample key | Count | Risk assessment |
    | ... | ... | ... | ... |

    ## sync-vocab.js languages array
    - Lists 'en': YES / NO
    - Line: N

    ## service-worker English references
    - (list each hit with classification)

    ## Verdict
    - ☐ CLEAN — zero runtime refs, safe to delete en.json (Task 2 proceeds to deletion)
    - ☐ BLOCKED — N runtime refs found, surface checkpoint in Task 2 (Task 2 does NOT delete)

    ## If CLEAN, dead-code paths to also clean up
    - (list files + specific constructs, e.g., "remove 'en' from sync-vocab.js:line N languages array")
    ```

    **Interpretation gate — important:**
    - A "runtime reference" is code inside `extension/` or `scripts/sync-vocab.js` that, if executed on the packaged extension with en.json absent, would cause a 404, crash, broken feature, or user-visible error.
    - An entry in `extension/data/de.json` that says `"en": "english word"` as a translation pair is **data, not code**. It does not count as a runtime reference unless vocab-seam-core.js actively fetches en.json to resolve it. If the runtime never tries to resolve English translations, the data link is dormant and can remain.
    - When in doubt, add it to the audit table and let the audit verdict be BLOCKED — Task 2 surfaces a checkpoint to the user, not a silent delete.
  </action>
  <verify>
    <automated>test -f /Users/geirforbord/Papertek/leksihjelp/.planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-05-AUDIT.md &amp;&amp; grep -qE "^## Verdict" /Users/geirforbord/Papertek/leksihjelp/.planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-05-AUDIT.md</automated>
  </verify>
  <done>
    02-05-AUDIT.md exists. All six audit steps (A–F) have output captured in their respective tables. The "Verdict" section is filled in with either CLEAN or BLOCKED and cites specific evidence (file:line) for the call. If CLEAN, the "dead-code paths to also clean up" section enumerates concrete edits Task 2 will make.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Audit verdict checkpoint — CLEAN or BLOCKED</name>
  <decision>Does 02-05-AUDIT.md conclude the audit is CLEAN (safe to delete en.json) or BLOCKED (runtime references found)?</decision>
  <context>
    Per 02-CONTEXT.md's hard requirement: "If the audit finds en.json IS referenced at runtime, the planner/executor MUST NOT silently remove it." This checkpoint is the enforcement point.

    The executor must read 02-05-AUDIT.md's Verdict section and the referenced evidence, then choose:
    - CLEAN → proceed to Task 3 (delete + dead-code removal + verify)
    - BLOCKED → do NOT proceed. Surface the findings to the user. Phase 2 gap closure needs a fresh plan or a revised remediation path (e.g., strip audio/de/, trim data/de.json conjugations, explicit ceiling bump).

    A BLOCKED outcome is NOT a plan failure — it is the plan working as designed. The gate infrastructure from 02-04 still ships; the next remediation is a product decision.
  </context>
  <options>
    <option id="clean">
      <name>CLEAN — proceed to Task 3</name>
      <pros>Expected outcome per 02-CONTEXT.md. Unblocks Phase 2 completion.</pros>
      <cons>None if audit is thorough.</cons>
    </option>
    <option id="blocked">
      <name>BLOCKED — stop, surface findings, do NOT delete</name>
      <pros>Prevents silent removal of a file in active use. Hard requirement from 02-CONTEXT.md honored.</pros>
      <cons>Phase 2 SC-4 remains unmet; a new plan is needed with a different remediation path.</cons>
    </option>
  </options>
  <resume-signal>
    Report the Verdict line from 02-05-AUDIT.md. If CLEAN, Task 3 proceeds autonomously. If BLOCKED, the plan halts here and the executor surfaces the finding to the user (who must then pick a different remediation path — not covered by this plan).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Delete en.json + clean dead-code paths + re-verify gates</name>
  <files>
    extension/data/en.json
    extension/data/grammarfeatures-en.json
    scripts/sync-vocab.js
    extension/manifest.json
    extension/popup/popup.js
    extension/popup/popup.html
    extension/content/vocab-seam-core.js
    extension/content/vocab-seam.js
    extension/content/word-prediction.js
    extension/content/floating-widget.js
    extension/background/service-worker.js
  </files>
  <action>
    **PRECONDITION:** Task 2 resolved to CLEAN. If Task 2 resolved to BLOCKED, skip this task entirely — the plan has halted at the checkpoint.

    **Step 1 — Delete the vocab file:**
    ```
    rm /Users/geirforbord/Papertek/leksihjelp/extension/data/en.json
    ```

    **Step 2 — Apply the "dead-code paths to also clean up" list from 02-05-AUDIT.md.** For each enumerated edit:
    - Open the file
    - Make the specific edit (remove `'en'` from language arrays, remove English context-menu items, remove `en.json` from manifest `web_accessible_resources` if listed, etc.)
    - Preserve UI i18n (`extension/i18n/en.json` or similar) — those are different files and are out of scope

    Handling guidance:
    - `scripts/sync-vocab.js` — if it listed `'en'`, remove it from the languages array. Also remove `'en'` from any similar per-language-code array (e.g., translation pair codes if the script fetches `-en` pairs).
    - `extension/content/vocab-seam-core.js` — if it listed `'en'` in a language map, remove the `'en'` entry. Ensure the resulting structure still parses (no trailing comma issues).
    - `extension/manifest.json` — if `web_accessible_resources` names `data/en.json`, remove that specific entry. Leave other data files.
    - Other files — apply edits strictly per the audit list. Do NOT refactor adjacent code.

    **Step 3 — `grammarfeatures-en.json` handling:**
    If the audit found zero runtime references to `grammarfeatures-en.json` (check same surfaces as Task 1), delete it too. It's small (~7 KB) but dead weight. If any reference exists, leave it.
    ```
    rg -n "grammarfeatures-en" extension/ scripts/ 2>/dev/null
    ```

    **Step 4 — Re-run the three verification gates:**

    4a. Regression fixture (no functional regression expected — en.json is not in the fixture scope, but this is cheap insurance):
    ```
    cd /Users/geirforbord/Papertek/leksihjelp && npm run check-fixtures
    ```
    Expected: exit 0, 138/138 pass. If any fail, ROLL BACK the deletions (`git checkout -- extension/data/en.json extension/data/grammarfeatures-en.json <any-other-modified-files>`) and halt with a BLOCKED status. The rollback exists precisely because an unexpected fixture failure means the audit missed a reference.

    4b. Bundle-size gate (the whole point of this plan):
    ```
    cd /Users/geirforbord/Papertek/leksihjelp && npm run check-bundle-size
    ```
    Expected: exit 0, PASS with headroom. Zip expected to be ~5.5 MiB. Capture the full per-directory breakdown output.

    4c. Confirm en.json is genuinely gone from the shipped zip:
    ```
    cd /Users/geirforbord/Papertek/leksihjelp && unzip -l backend/public/lexi-extension.zip | grep -E "en\.json|grammarfeatures-en" || echo "NO EN REFERENCES IN ZIP"
    ```
    Expected: "NO EN REFERENCES IN ZIP" (or only matches for `i18n/en.json` if that file exists — which is UI strings and irrelevant). A match for `data/en.json` is a failure: the staging-dir copy included it somehow.

    **Step 5 — Final residual grep:**
    ```
    cd /Users/geirforbord/Papertek/leksihjelp && rg -n "data/en\.json|data\\\\en\.json" extension/ scripts/ backend/ || echo "ZERO RESIDUAL REFS"
    ```
    Expected: "ZERO RESIDUAL REFS" (or equivalent no-match output). Any remaining reference is a dead-code path missed in Step 2 — fix it before finishing.

    **Step 6 — Write 02-05-SUMMARY.md:**
    Follow the project's SUMMARY template. Capture:
    - Audit verdict (CLEAN)
    - Files deleted with byte counts
    - Dead-code edits with file:line before/after (abbreviated if many)
    - Bundle-size before/after (10,599,772 → new value) with per-dir breakdown diff
    - Fixture result (138/138 pass)
    - Git commit messages used

    Commit changes atomically — at minimum one commit for the deletion + dead-code cleanup, one commit for the SUMMARY file. Commit messages follow project convention:
    ```
    feat(02-05): delete en.json + dead-code cleanup — zip now PASSES bundle-size gate
    docs(02-05): complete SC-4 gap closure summary
    ```

    **If bundle-size gate STILL reports FAIL after deletion:**
    This is an unexpected outcome (deleting a 4.65 MB file should clear a 114 KB overage by a wide margin). Do NOT modify `CEILING_BYTES`. Instead, capture the new per-directory breakdown, write a BLOCKED SUMMARY file explaining the discrepancy, and halt. The most likely cause is en.json somehow not being in the staging-dir zip to begin with (in which case the audit correctly found zero refs because it was already dead weight that wasn't being packaged — and some other directory is pushing over cap, needing a fresh remediation plan).
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp && npm run check-fixtures && npm run check-bundle-size && test ! -f extension/data/en.json</automated>
  </verify>
  <done>
    extension/data/en.json is deleted. All dead-code paths identified in 02-05-AUDIT.md are removed. `npm run check-fixtures` exits 0 at 138/138. `npm run check-bundle-size` exits 0 with the zip under 10,485,760 bytes. `unzip -l` on the packaged zip shows zero `data/en.json` entries. `rg "data/en\.json"` across `extension/` and `scripts/` returns zero matches. 02-05-SUMMARY.md exists with all required sections. Changes committed.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Automated gates (must all exit 0):**
   - `npm run check-fixtures` — 138/138 pass, F1=1.000 across all rule classes
   - `npm run check-bundle-size` — packaged zip under 10,485,760 bytes, prints PASS with headroom

2. **Residual reference grep (must return zero matches):**
   - `rg -n "data/en\.json" extension/ scripts/ backend/`
   - `unzip -l backend/public/lexi-extension.zip | grep "data/en.json"`

3. **Manual smoke test (checkpoint:human-verify — NOT automated; document this as the human step for Release Workflow):**
   - Load `extension/` unpacked in Chrome via chrome://extensions/
   - Open popup.html → change language picker to German → search for a common word (e.g., `Haus`) → confirm definition and conjugations render
   - Repeat for Spanish (e.g., `casa`) and French (e.g., `maison`)
   - Confirm the language picker no longer offers English (expected, since en.json is gone)
   - Confirm no console errors in DevTools

   This manual smoke is not gated inside the plan because the executor is Claude, not the human. It IS gated at Release Workflow time (step after `check-bundle-size`). The plan's job is to make the automated gates green and to document this human-verify step in 02-05-SUMMARY.md so the developer knows it exists before cutting a release.
</verification>

<success_criteria>
- SC-4 (ROADMAP): Total packaged extension zip under 10 MiB ceiling — VERIFIED
- `npm run check-bundle-size` exits 0 — VERIFIED
- `npm run check-fixtures` exits 0 at 138/138 — VERIFIED
- Zero residual references to `extension/data/en.json` in shipped extension code or scripts — VERIFIED
- Phase 2 closes fully (SC-1, SC-2, SC-3 already VERIFIED; SC-4 now VERIFIED) — VERIFIED

**Release Workflow implication (documented, not executed by this plan):**
Once the gate turns green, the developer can cut a GitHub Release following CLAUDE.md Release Workflow steps 3–5 (version bump + repackage + upload). That work is NOT part of this plan.
</success_criteria>

<output>
After completion, the executor creates:
- `.planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-05-SUMMARY.md` (final plan summary)

The intermediate `.planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-05-AUDIT.md` is retained as the audit artifact (evidence for reviewers that the file deletion was justified). It is NOT in `files_modified` because it lives under `.planning/` and is a planning artifact, not a shipped file.
</output>
