---
phase: 35-v3.1-uat-followups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/content/spell-rules/fr-aspect-hint.js
  - extension/content/spell-rules/de-prep-case.js
  - extension/content/spell-rules/es-gustar.js
  - extension/content/spell-popover.js
  - extension/content/spell-check.js
  - extension/data/es.json
  - extension/data/fr.json
  - extension/data/de.json
  - extension/manifest.json
  - package.json
  - backend/public/index.html
  - .planning/phases/35-v3.1-uat-followups/35-VERIFICATION.md
autonomous: false
gap_closure: true
requirements: []

must_haves:
  truths:
    - "F1: fr-aspect-hint fires on 'Hier il mangeait une pomme' producing a P3 hint dot; fixture suite remains P=R=F1=1.000"
    - "F2: es-gustar fires on 'El encanta la música' (encantar) and at least one other extended-class verb"
    - "F3: 'duele' is recognized as a valid form of doler (no out-of-dictionary marker)"
    - "F5: de-prep-case either fires Wechselpräposition pedagogy on 'in der Schule' OR an alternate canonical trigger sentence is documented for Phase 26 walkthroughs"
    - "F6: Tab navigation between spell-check markers no longer auto-collapses the Lær mer panel; only Esc or explicit user action collapses it"
    - "F7: NN (3.5) and EN (3.6) Lær mer locale walkthroughs executed in vanilla extension and recorded in 35-VERIFICATION.md"
    - "All 11 release-workflow gates exit 0; version bumped in three places; cross-repo papertek-vocabulary PRs (F2/F3, possibly F1/F5) landed and synced"
  artifacts:
    - path: ".planning/phases/35-v3.1-uat-followups/35-VERIFICATION.md"
      provides: "Per-finding pass/fail with NN/EN locale evidence"
    - path: "extension/data/es.json"
      provides: "Updated ES vocab with doler conjugations + extended gustar-class verb_class markers (post-sync)"
    - path: "extension/data/fr.json"
      provides: "Updated FR vocab if F1 root cause is data-side (post-sync)"
    - path: "extension/manifest.json"
      provides: "Bumped version (e.g. 2.9.13 -> 2.9.14 or 2.10.0)"
  key_links:
    - from: "extension/content/spell-rules/es-gustar.js"
      to: "extension/data/es.json verbbank verb_class markers"
      via: "verbClass lookup against synced verbbank"
      pattern: "verb_class.*gustar"
    - from: "extension/content/spell-popover.js"
      to: "Tab keydown handler / focus restoration"
      via: "blur/focusout timer that currently auto-collapses panel"
      pattern: "panel.*collapse|setTimeout.*hide"
    - from: "extension/content/spell-rules/fr-aspect-hint.js"
      to: "extension/data/fr.json verb conjugation index"
      via: "imparfait detection on 'mangeait'"
      pattern: "imparfait|aspect"
---

<objective>
Resolve all six Phase 34 UAT findings (F1, F2, F3, F5, F6, F7) so v3.1 can be archived. Mix of leksihjelp logic fixes, papertek-vocabulary cross-repo data fixes, and a deferred extension-side manual UAT. F4 already shipped in-flight (lockdown commit 5830fcc) and is NOT in scope.

Purpose: Close the v3.1 audit gap chain — Phase 34 verified the milestone surfaces and identified six concrete defects; Phase 35 lands the fixes so no `human_needed` items roll forward.

Output:
- Code/data fixes for F1, F2, F3, F5, F6
- Extension UAT for F7 with screenshots
- 35-VERIFICATION.md with per-finding pass/fail
- Cross-repo papertek-vocabulary PR(s) merged + synced into extension/data/
- Version bump signaling lockdown + skriveokt-zero downstream consumers to re-pin
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-v3.1-browser-uat-sweep/34-VERIFICATION.md
@CLAUDE.md
@extension/content/spell-rules/fr-aspect-hint.js
@extension/content/spell-rules/es-gustar.js
@extension/content/spell-rules/de-prep-case.js

**Project principle (CLAUDE.md):** Prefer fixing data issues at the API source (papertek-vocabulary at `/Users/geirforbord/Papertek/papertek-vocabulary`) over client-side workarounds. For each finding, evaluate data-vs-logic root cause before patching extension code.

**Cross-repo path:** `/Users/geirforbord/Papertek/papertek-vocabulary` (NOT `/papertek` — that path does not exist).

**Sync command after vocab changes:** `npm run sync-vocab:es` (or `:fr`, `:de`) from the leksihjelp root, which writes to `extension/data/{lang}.json`.

**Phase 34 findings recap:**
- F1: FR `fr-aspect-hint` silent on `Hier il mangeait une pomme` (findingsCount: 0). Curated typo rule fires on gibberish so engine isn't dead.
- F2: ES `es-gustar` does not fire on extended-class verbs like `encantar` (`El encanta la música` → no marker).
- F3: `duele` (correct conjugation of doler) flagged out-of-dictionary — doler conjugations missing from ES vocab.
- F5: DE `de-prep-case` does not fire on `in der Schule` (Wechselpräposition movement→accusative trigger).
- F6: Tab navigation between markers moves focus correctly but Lær mer panel auto-collapses after ~1s.
- F7: NN (3.5) + EN (3.6) Lær mer locale walkthroughs deferred — no UI-language toggle in lockdown sidepanel by design; needs vanilla extension UAT.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage F1, F2, F3, F5 root cause (data vs logic) and land cross-repo papertek-vocabulary fixes</name>
  <files>
    /Users/geirforbord/Papertek/papertek-vocabulary/data/es/verbbank.json (or equivalent — locate via repo conventions)
    /Users/geirforbord/Papertek/papertek-vocabulary/data/fr/* (if F1 is data-side)
    /Users/geirforbord/Papertek/papertek-vocabulary/data/de/* (if F5 is data-side)
    extension/content/spell-rules/fr-aspect-hint.js (if F1 is logic-side)
    extension/content/spell-rules/es-gustar.js (if F2 is logic-side)
    extension/content/spell-rules/de-prep-case.js (if F5 is logic-side)
  </files>
  <action>
For each of F1, F2, F3, F5:

1. **Diagnose root cause** with a small repro:
   - F1: read `extension/content/spell-rules/fr-aspect-hint.js`; check the rule's trigger predicate against the canonical sentence `Hier il mangeait une pomme`. Inspect `extension/data/fr.json` verbbank for `manger` imparfait conjugation entry. Determine if rule misses the trigger (logic gap) or `mangeait` is unknown to the index (data gap).
   - F2: read `extension/content/spell-rules/es-gustar.js`; trace how it identifies gustar-class verbs (Phase 32-03 used `verb_class: gustar-class` marker on verbbank entries). Inspect `extension/data/es.json` for `encantar` — does the verbbank entry have `verb_class: gustar-class`?
   - F3: inspect `extension/data/es.json` verbbank for `doler` — are present-tense conjugations (duele, duelen, etc.) present? If absent, this is a vocab data gap.
   - F5: read `extension/content/spell-rules/de-prep-case.js`; check whether `in` is in the Wechselpräposition list, whether the rule checks for accusative-vs-dative case mismatch, and whether the test sentence `in der Schule` matches the trigger logic for movement→accusative. If the rule doesn't model semantic movement detection (which is hard), document the gap and pick a trigger sentence that does fire (e.g. `Ich gehe in der Schule` — explicit motion verb + dative article — pure case mismatch path).

2. **Apply fixes at the data layer when possible** (per CLAUDE.md data-logic separation principle):
   - **F2/F3 are almost certainly papertek-vocabulary data fixes.** Edit the ES verbbank in `/Users/geirforbord/Papertek/papertek-vocabulary` to: (a) add `verb_class: "gustar-class"` to extended verbs (encantar, interesar, doler, faltar, importar, molestar, parecer, quedar, sobrar, tocar — match Phase 32-03's verb list); (b) add full present-tense conjugations to `doler` if missing.
   - **F1**: if vocab gap, add `manger` (and other common -er verbs) imparfait conjugations to FR verbbank. If logic gap, fix the rule's trigger.
   - **F5**: if logic gap is genuinely hard (semantic movement detection), DO NOT extend the rule scope. Instead, document the rule's intentional case-mismatch-only scope in a code comment + update Phase 26 canonical trigger sentence in `.planning/phases/26-laer-mer-pedagogy-ui/` (search for `in der Schule` references and replace with the new trigger).

3. **Commit + push papertek-vocabulary changes** in that repo (one commit per language, atomic):
   ```
   cd /Users/geirforbord/Papertek/papertek-vocabulary
   git add data/es/* && git commit -m "feat(es): extend gustar-class verb_class markers + add doler conjugations (leksihjelp F2/F3)"
   git push
   ```

4. **Sync into leksihjelp** and stage:
   ```
   cd /Users/geirforbord/Papertek/leksihjelp
   npm run sync-vocab:es   # and :fr, :de as needed
   git add extension/data/es.json extension/data/fr.json extension/data/de.json
   ```

5. **Verify each finding fires correctly** by running the rule against the canonical trigger via `npm run check-fixtures` (add fixture entries under `extension/content/spell-rules/__fixtures__/` for any new verbs/triggers you address). For F5, verify the chosen trigger fires the popover correctly.

If a rule's logic genuinely needs extending (e.g. F2 might need rule-side widening if the verbbank already had verb_class but the rule was hardcoded to gustar), make minimal logic edits in the corresponding spell-rule file with code comments referencing F2/F3.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp && npm run check-fixtures</automated>
  </verify>
  <done>
    - F1 trigger sentence produces a finding (verified via fixture or rule unit test)
    - F2 produces findings on `El encanta la música` + at least one other extended-class verb
    - F3 `duele` is no longer out-of-dictionary
    - F5 either fires correctly OR an alternate trigger is documented + fires correctly
    - papertek-vocabulary commits pushed; vocab synced into extension/data/
    - check-fixtures still exits 0 with P=R=F1=1.000 on existing rules
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix F6 — Lær mer panel auto-collapses on Tab nav</name>
  <files>
    extension/content/spell-popover.js
    extension/content/spell-check.js
  </files>
  <action>
Investigate the focus/blur handler that auto-collapses the Lær mer panel ~1s after Tab navigation moves focus to the next marker.

1. **Locate the offending timer/handler:**
   - Read `extension/content/spell-popover.js` and grep for `setTimeout`, `blur`, `focusout`, `mouseleave` event listeners.
   - Also check `extension/content/spell-check.js` Tab navigation handler — Phase 25 SPELL-01 added Tab cycling between markers; suspect this handler dismisses the popover on each Tab to "reset state" before re-rendering on the next marker.

2. **Diagnose the race:** typical pattern is a blur timer that calls `hidePopover()` after ~1000ms when the popover loses focus, but Tab nav fires blur (popover loses focus) → timer scheduled → focus moves to next marker → popover re-shown for new marker → timer from PREVIOUS marker still pending → fires → collapses the now-current popover/panel.

3. **Fix:** the cleanest fix is one of:
   - (a) Cancel the pending blur timer when a new popover is shown for a different marker; OR
   - (b) Make panel-collapse-on-blur opt-out — only collapse on Esc, click-outside, or explicit dismiss; NOT on blur from Tab nav within the spell-check overlay; OR
   - (c) Track panel expanded state per marker (stored on the marker DOM node, not on the singleton popover) so re-rendering for a new marker preserves the "Lær mer expanded" state cleanly.

   Pick the simplest patch consistent with existing code (likely option (a) — clear the timer in the show-popover path). Add a code comment referencing F6.

4. **Verify manually in the extension** since this is a focus-timing UI bug — load the unpacked extension, type a NB sentence with multiple typos (or DE prep errors), click Aa to enter spell-check mode, click "Lær mer" on the first marker, press Tab → confirm the panel stays expanded (or behaves per chosen fix). Capture before/after evidence in 35-VERIFICATION.md.

5. **Do NOT change behavior of Esc**, click-outside, or click-on-different-marker dismissal paths.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp && npm run check-fixtures && npm run check-network-silence</automated>
  </verify>
  <done>
    - Tab navigation between markers no longer auto-collapses the Lær mer panel
    - Esc still collapses
    - Click-outside still dismisses popover
    - Manual UAT recorded in 35-VERIFICATION.md with before/after note (and ideally screenshots)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: F7 — Manual extension UAT for NN (3.5) and EN (3.6) Lær mer locales + 35-VERIFICATION.md authoring + version bump + full release-gate sweep</name>
  <what-built>
    - F1/F2/F3/F5 data + (minimal) logic fixes from Task 1
    - F6 focus-timer fix from Task 2
    - papertek-vocabulary cross-repo PR(s) merged + synced into extension/data/
  </what-built>
  <how-to-verify>
    **Part A — F7 manual UAT (only doable in the vanilla extension; lockdown sidepanel intentionally lacks the UI-language toggle):**

    1. Build the extension: `cd /Users/geirforbord/Papertek/leksihjelp && npm run package`
    2. Load the unpacked `extension/` dir into Chrome via chrome://extensions (Developer mode → Load unpacked)
    3. Open the leksihjelp popup, go to Settings, switch UI language to **NN (Nynorsk)**
    4. Visit any web page with an editable text area; type a DE sentence that triggers `de-prep-case` (e.g. `mit der Hund`)
    5. Click Aa, click the marker dot, click "Lær mer"
    6. **Verify all panel strings render in Nynorsk** (case label badge, summary, explanation, correct/incorrect example labels, colloquial_note if shown). Note any strings that fall back to NB or are missing.
    7. Repeat steps 3-6 with UI language switched to **EN (English)**.
    8. Capture screenshots and notes per locale.

    **Part B — Author 35-VERIFICATION.md** at `.planning/phases/35-v3.1-uat-followups/35-VERIFICATION.md` with frontmatter `phase: 35`, `status: complete` (or `gaps_found` if any sub-finding fails), and per-finding (F1, F2, F3, F5, F6, F7) pass/fail with evidence pointers (screenshots, fixture outputs, before/after notes).

    **Part C — Version bump in three places:**
    - `extension/manifest.json`
    - `package.json`
    - `backend/public/index.html`
    Bump from current 2.9.13 to 2.9.14 (patch) or 2.10.0 (minor) — pick patch unless any rule logic widened materially.

    **Part D — Run all 11 release gates and confirm each exits 0:**
    ```
    npm run check-fixtures
    npm run check-explain-contract && npm run check-explain-contract:test
    npm run check-rule-css-wiring && npm run check-rule-css-wiring:test
    npm run check-spellcheck-features
    npm run check-network-silence
    npm run check-exam-marker && npm run check-exam-marker:test
    npm run check-popup-deps && npm run check-popup-deps:test
    npm run check-bundle-size
    npm run check-baseline-bundle-size && npm run check-baseline-bundle-size:test
    npm run check-benchmark-coverage && npm run check-benchmark-coverage:test
    npm run check-governance-data && npm run check-governance-data:test
    ```

    **Part E — Repackage:** `npm run package` (produces release zip). Verify zip is under 20 MiB cap.

    Confirm verification by typing "approved" with a brief note on any per-finding outcomes; or describe issues for Claude to fix and re-verify.
  </how-to-verify>
  <resume-signal>Type "approved" with notes, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Phase-level checks:
- [ ] All six findings (F1, F2, F3, F5, F6, F7) have explicit dispositions in 35-VERIFICATION.md
- [ ] Cross-repo papertek-vocabulary PRs landed (at minimum for F2/F3); synced into extension/data/
- [ ] All 11 release gates exit 0
- [ ] Version bumped consistently across the three files
- [ ] `npm run package` produces a valid zip under the 20 MiB cap
- [ ] No regression to existing fixture P=R=F1=1.000 numbers
- [ ] F6 fix verified manually (focus-timer race)
- [ ] F7 NN + EN locale evidence captured in 35-VERIFICATION.md
</verification>

<success_criteria>
Maps to roadmap Phase 35 success criteria 1-6:
1. (F1) FR `fr-aspect-hint` fires on `Hier il mangeait une pomme`; fixture suite stays at P=R=F1=1.000
2. (F2/F3) `doler` conjugations present; `es-gustar` fires on `El encanta la música` + extended verbs; `duele` no longer out-of-dictionary
3. (F5) `in der Schule` fires `de-prep-case` Wechselpräposition pedagogy OR alternate canonical trigger documented
4. (F6) Tab nav no longer auto-collapses Lær mer panel
5. (F7) NN (3.5) + EN (3.6) executed in vanilla extension UAT; results in 35-VERIFICATION.md
6. All release gates exit 0; cross-repo PR(s) landed; version bumped (signals lockdown + skriveokt-zero to re-pin)
</success_criteria>

<output>
After completion, create `.planning/phases/35-v3.1-uat-followups/35-01-SUMMARY.md` documenting:
- Per-finding root cause + fix (data vs logic)
- papertek-vocabulary commit SHAs (cross-repo)
- Version bump details
- Gate sweep results
- Any deferred items or follow-up needed
</output>
