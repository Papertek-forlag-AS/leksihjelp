---
phase: 37-hygiene-templates-pre-flight
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/deferred/lockdown-resync-pending.md
  - CLAUDE.md
autonomous: true
requirements: [HYG-06]
must_haves:
  truths:
    - "The [lockdown-resync-needed] commit-message convention is documented in the CLAUDE.md downstream-consumers section: when a commit modifies any synced-surface path (extension/content/, popup/views/, exam-registry.js, styles/content.css, data/, i18n/), include the marker in the commit message body so downstream consumers (lockdown webapp + skriveokt-zero) can scope their re-sync windows"
    - "A retroactive catch-up doc exists at .planning/deferred/lockdown-resync-pending.md listing the convention for future commits AND tabulating any v3.1 synced-surface commits that landed without the marker"
    - "The retroactive scan for v3.1..HEAD synced-surface commits produces a documented result (likely EMPTY per RESEARCH verification — only docs churn since v3.1)"
    - "Future synced-surface commits land using the convention; the marker hint also appears in the check-synced-surface-version gate failure diagnostic (Plan 02 wires this — Plan 04 only documents)"
  artifacts:
    - path: ".planning/deferred/lockdown-resync-pending.md"
      provides: "Doc-based catch-up list: convention restated + retroactive table for v3.1..HEAD synced-surface commits without the marker"
      contains: "lockdown-resync-needed"
    - path: "CLAUDE.md"
      provides: "Downstream-consumers section gains a [lockdown-resync-needed] convention block adjacent to existing version-bump rules"
      contains: "lockdown-resync-needed"
  key_links:
    - from: ".planning/deferred/lockdown-resync-pending.md"
      to: "CLAUDE.md downstream-consumers section"
      via: "Convention text references CLAUDE.md as canonical doc; deferred file is the retroactive ledger only"
      pattern: "CLAUDE.md"
    - from: "CLAUDE.md downstream-consumers convention block"
      to: "scripts/check-synced-surface-version.js failure diagnostic (Plan 02)"
      via: "Convention text notes the gate prints copy-paste hint with the marker on failure"
      pattern: "check-synced-surface-version"
---

<objective>
Document the `[lockdown-resync-needed]` commit-message convention in CLAUDE.md (downstream-consumers section) and produce a doc-based retroactive catch-up list at `.planning/deferred/lockdown-resync-pending.md`. Per CONTEXT and RESEARCH: no git history rewrite — just a documented convention going forward + a one-time scan tabulating v3.1..HEAD synced-surface commits that landed without the marker (verified empty by RESEARCH; doc still gets created so future commits have a landing place).

Purpose: Downstream re-sync trigger graduates from tribal knowledge to documented repo standard. Coupled with HYG-05 gate (Plan 02), the convention shows up as a failure-mode hint exactly where someone needs it.

Output: One new `.planning/deferred/lockdown-resync-pending.md`, one new CLAUDE.md subsection in the downstream-consumers area.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-hygiene-templates-pre-flight/37-CONTEXT.md
@.planning/phases/37-hygiene-templates-pre-flight/37-RESEARCH.md

<!-- CLAUDE.md downstream-consumers section is the canonical insertion point -->
@CLAUDE.md

<interfaces>
<!-- Synced-paths trigger set (single source of truth, matches CLAUDE.md downstream-consumer list + Plan 02 SYNCED_PATHS) -->
SYNCED_PATHS = [
  'extension/content/',
  'extension/popup/views/',
  'extension/exam-registry.js',
  'extension/styles/content.css',
  'extension/data/',
  'extension/i18n/',
]

<!-- Retroactive scan command (per RESEARCH Open Question 4 + verification) -->
git log v3.1..HEAD --oneline -- extension/content/ extension/popup/views/ extension/exam-registry.js extension/styles/content.css extension/data/ extension/i18n/
# RESEARCH-verified result on 2026-05-01: EMPTY (no synced-surface commits since v3.1; only docs churn)

<!-- Convention text (CONTEXT-locked) -->
Marker: "[lockdown-resync-needed]" in commit message body
Trigger: any modification (incl. addition/deletion) to a SYNCED_PATHS entry
Purpose: downstream consumers (lockdown webapp, skriveokt-zero) scope their re-sync windows by scanning for the marker
Enforcement: check-synced-surface-version (Plan 02) prints the marker as a copy-paste hint on failure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document [lockdown-resync-needed] convention in CLAUDE.md downstream-consumers section</name>
  <files>CLAUDE.md</files>
  <action>
Locate the existing "Downstream consumers" section in CLAUDE.md (it documents lockdown webapp + skriveokt-zero synced surfaces, version-bump signal, "downstream-only quick fixes" rules, etc.). Add a new subsection within it — adjacent to the existing "Bumping `package.json` version signals a downstream sync is needed" bullet — formalizing the `[lockdown-resync-needed]` commit-message convention.

Subsection MUST cover:

1. **Convention name + marker**: When a commit modifies any synced surface (list the 6 paths verbatim from interfaces block above), include `[lockdown-resync-needed]` in the commit message body (anywhere — title, body, trailer all acceptable).

2. **Why**: Downstream consumers (lockdown webapp's `node scripts/sync-leksihjelp.js`, skriveokt-zero's analogue) scope their re-sync windows by scanning git log for the marker since their last consumed version. Without it, downstream maintainers must manually diff every commit since their last sync — error-prone, gets skipped under time pressure (root cause of the Phase 30-02 4-orphan-file class).

3. **Enforcement**: `npm run check-synced-surface-version` (HYG-05 release gate, Plan 02 in this phase) prints the `[lockdown-resync-needed]` marker as a copy-paste hint when it fires on an un-bumped synced-surface diff. The gate doesn't enforce the marker itself (it enforces the version bump), but it surfaces the marker recommendation at the exact moment someone is about to forget it.

4. **Retroactive scope**: Pre-Phase-37 commits don't carry the marker (convention didn't exist). The retroactive catch-up list lives at `.planning/deferred/lockdown-resync-pending.md` — a doc-based ledger, no git history rewrite.

5. **Example**: Provide one short example commit message with the marker in the body, e.g.:
   ```
   fix(content): correct spell-check dot color for nb-sarskriving rule

   [lockdown-resync-needed]

   Closes F38-3.
   ```

Length budget: ~15-20 lines added. Use the same Markdown sub-heading style as adjacent subsections in the downstream-consumers area.

Do NOT modify the Release Workflow numbered list (Plan 02 already inserted both gates there; this plan only touches the downstream-consumers section).
  </action>
  <verify>
    <automated>grep -q "lockdown-resync-needed" CLAUDE.md && grep -q "lockdown-resync-pending.md" CLAUDE.md && grep -q "check-synced-surface-version" CLAUDE.md && echo OK</automated>
  </verify>
  <done>CLAUDE.md downstream-consumers section contains the `[lockdown-resync-needed]` convention block: marker, trigger paths, why, enforcement (HYG-05 gate hint coupling), retroactive catch-up reference (`.planning/deferred/lockdown-resync-pending.md`), and a short example commit message. Stays adjacent to existing version-bump signal text.</done>
</task>

<task type="auto">
  <name>Task 2: Create .planning/deferred/lockdown-resync-pending.md with retroactive scan</name>
  <files>.planning/deferred/lockdown-resync-pending.md</files>
  <action>
Create `.planning/deferred/` directory if missing (mkdir -p). Author `lockdown-resync-pending.md` per the skeleton in RESEARCH.md "lockdown-resync-pending.md skeleton".

File MUST contain (Markdown):

1. `# Lockdown Re-Sync Pending` heading + 1-paragraph description: doc-based ledger for synced-surface commits that landed without the `[lockdown-resync-needed]` marker before the convention was adopted in Phase 37 (HYG-06).

2. `## Convention (going forward)` section: state the marker, trigger paths (verbatim list of 6), enforcement (HYG-05 prints the hint on failure). This is a deliberate duplication with CLAUDE.md — the deferred file is self-contained for future maintainers who might land here without context.

3. `## Retroactive catch-up (v3.1 → HEAD)` section: run `git log v3.1..HEAD --oneline -- extension/content/ extension/popup/views/ extension/exam-registry.js extension/styles/content.css extension/data/ extension/i18n/` LIVE during execution and tabulate results.
   - Per RESEARCH verification (2026-05-01), result is EMPTY (no synced-surface commits since v3.1; only docs churn). If empty, record explicitly: "Result on <ISO-date>: EMPTY — no synced-surface commits since v3.1; only docs churn. Convention starts clean for v3.2."
   - If non-empty (state may have shifted by execution time), tabulate as: `| Commit | Date | Synced files | Re-sync status |`. For each row, "Re-sync status" defaults to "needs documentation" (the catch-up state); future re-sync activity flips to "synced".

4. `## v3.0 → v3.1 retroactive scope` section: per RESEARCH Open Question 4, document the decision NOT to extend retroactive scope further than v3.1..HEAD. Cite reason: CONTEXT scopes retroactive scan as "v3.1 synced-surface commits that landed without the marker" — i.e., v3.1..HEAD only. Earlier eras (v3.0..v3.1, Phases 24-36) are out-of-scope; if downstream consumers report drift, a one-off scan can extend the table at that point. Keep this paragraph short (~3-5 lines).

5. `## Future commits` section: 1-line directive — future commits use the marker per CLAUDE.md downstream-consumers convention; this file remains the source of truth for tracking any backfill needed.

The file's tone is operational, not aspirational — it's a maintenance ledger. Date the document at top with `**Created:** <ISO-date>` and `**Last scan:** <ISO-date>`.
  </action>
  <verify>
    <automated>test -f .planning/deferred/lockdown-resync-pending.md && grep -q "Convention" .planning/deferred/lockdown-resync-pending.md && grep -q "Retroactive catch-up" .planning/deferred/lockdown-resync-pending.md && grep -q "v3.1" .planning/deferred/lockdown-resync-pending.md && grep -q "lockdown-resync-needed" .planning/deferred/lockdown-resync-pending.md && echo OK</automated>
  </verify>
  <done>File exists at `.planning/deferred/lockdown-resync-pending.md` with the 5 required sections: convention restatement, retroactive v3.1..HEAD scan result (live-recorded — EMPTY per RESEARCH or tabulated otherwise), v3.0→v3.1 scope decision, future-commits directive. Created+last-scan dates recorded.</done>
</task>

</tasks>

<verification>
Confirm both files exist with required content. Confirm CLAUDE.md and the deferred file are mutually consistent (both reference the same 6 synced paths, the same marker, the same HYG-05 gate coupling).

```bash
grep -A1 "lockdown-resync-needed" CLAUDE.md | head -20
grep -A1 "lockdown-resync-needed" .planning/deferred/lockdown-resync-pending.md | head -20
```
</verification>

<success_criteria>
- [ ] CLAUDE.md downstream-consumers section contains the `[lockdown-resync-needed]` convention block (marker, paths, why, enforcement, example)
- [ ] `.planning/deferred/lockdown-resync-pending.md` exists with convention restatement + retroactive scan + scope decision + future-commits directive
- [ ] Retroactive `git log v3.1..HEAD -- <synced-paths>` was run live and the result is recorded in the deferred file
- [ ] CLAUDE.md and the deferred file are consistent on the 6 synced paths, the marker, and the HYG-05 gate coupling
</success_criteria>

<output>
After completion, create `.planning/phases/37-hygiene-templates-pre-flight/37-04-SUMMARY.md`
</output>
