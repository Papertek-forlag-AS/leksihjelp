---
phase: 37-hygiene-templates-pre-flight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/uat/TEMPLATE-walkthrough.md
  - .planning/uat/TEMPLATE-finding.md
  - CLAUDE.md
autonomous: true
requirements: [HYG-01, HYG-02, HYG-03]
must_haves:
  truths:
    - "A new UAT walkthrough rendered from .planning/uat/TEMPLATE-walkthrough.md carries mandatory pre-flight YAML frontmatter (ext_version, idb_revision, preset_profile, browser_version, reload_ts, target_browsers, verification_kind), a numbered-step body, and a defects-observed section"
    - "A finding committed against .planning/uat/TEMPLATE-finding.md carries f_id, severity, sync_status, regression_fixture_id frontmatter — formalising the F36-1 convention as repo standard"
    - "Any agent running /gsd:auto can detect verification_kind: human-browser-walk on a phase plan via gsd-tools.cjs and hard-pause instead of auto-advancing; convention is documented in CLAUDE.md"
  artifacts:
    - path: ".planning/uat/TEMPLATE-walkthrough.md"
      provides: "Walkthrough scaffold with locked YAML frontmatter + numbered-step body"
      contains: "verification_kind: human-browser-walk"
    - path: ".planning/uat/TEMPLATE-finding.md"
      provides: "Finding scaffold with locked YAML frontmatter (f_id, severity, sync_status, regression_fixture_id)"
      contains: "f_id:"
    - path: "CLAUDE.md"
      provides: "Auto-mode pause convention documented for verification_kind: human-browser-walk"
      contains: "verification_kind"
  key_links:
    - from: "TEMPLATE-walkthrough.md"
      to: "scripts/check-vocab-deployment.js"
      via: "Pre-flight section instructs walker to run the script and paste output"
      pattern: "check-vocab-deployment"
    - from: "/gsd:auto agent loop"
      to: "gsd-tools.cjs frontmatter get"
      via: "Documented in CLAUDE.md as the query mechanism"
      pattern: "frontmatter get.*verification_kind"
---

<objective>
Create the two UAT templates that Phase 38 walkthroughs will instantiate, and document the verification_kind: human-browser-walk auto-mode hard-pause convention so /gsd:auto stops on human-walk phases instead of steamrolling past them (root cause of v3.1's six-walkthrough deferral, per CONTEXT Pitfall 2).

Purpose: UAT discipline becomes auditable and repeatable; auto-mode learns to defer to humans on browser-walk phases.

Output: Two YAML-frontmatter templates at .planning/uat/, plus a CLAUDE.md subsection documenting the auto-mode pause convention.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-hygiene-templates-pre-flight/37-CONTEXT.md
@.planning/phases/37-hygiene-templates-pre-flight/37-RESEARCH.md

<!-- Closest in-tree precedent for UAT template body shape — mirror this style, distill into reusable scaffold -->
@.planning/phases/36-v3.1-uat-sweep-2/36-VERIFICATION.md

<interfaces>
<!-- gsd-tools.cjs frontmatter API already exists. No parser to author. -->
<!-- Verified at ~/.claude/get-shit-done/bin/gsd-tools.cjs lines 304-321 -->

Existing subcommands (callable from any agent's bash step):
  node ~/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter get <file> --field <key>
  node ~/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter set <file> --field <key> --value <val>
  node ~/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter merge <file> --data <json>
  node ~/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter validate <file> --schema <name>

For HYG-03 hard-pause: agents running auto-mode read each phase's plan files
and check `verification_kind` via `frontmatter get`. If "human-browser-walk",
they pause and surface the walkthrough requirement to the user instead of
advancing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .planning/uat/ directory and TEMPLATE-walkthrough.md</name>
  <files>.planning/uat/TEMPLATE-walkthrough.md</files>
  <action>
Create directory `.planning/uat/` (mkdir -p) if missing, then write `TEMPLATE-walkthrough.md` with EXACTLY the locked frontmatter keys per CONTEXT.md decisions and RESEARCH.md Pattern 4. Use snake_case keys. Frontmatter MUST include (in this order):

```yaml
---
walkthrough_id: UAT-EXT-XX               # matches REQUIREMENTS.md ID
phase: 38-extension-uat-batch
verification_kind: human-browser-walk    # HYG-03 hook trigger — auto-mode pauses on this value
ext_version: <fill from manifest.json>   # guards stale-zip walks
idb_revision: <fill from chrome.storage> # guards stale-data walks
preset_profile: default                  # default | basic | full — guards Phase 05.1 feature-gating regression class
browser_version: <fill>                  # e.g. "Chrome 138.0.6962.42"
reload_ts: <ISO-8601>                    # chrome://extensions reload timestamp
target_browsers: [chrome, edge, brave]   # list
walker: <name>
date: <ISO-8601>
---
```

Body MUST contain (Markdown):
1. `## Pre-flight evidence (paste before walking)` section with checklist of: `node scripts/check-vocab-deployment.js` exit code, manifest.json version matches frontmatter `ext_version`, IDB revision captured, reload timestamp recorded, browser+version recorded.
2. `## Steps` section: numbered step-list with `<step> → expected: … → observed: … → ✅/❌` shape (per RESEARCH Pattern 4).
3. `## Defects observed` section: bullet template `F<phase>-<seq>: <one-line> → see .planning/uat/findings/<id>.md`.
4. `## Outcome` section: checkbox list with "All steps pass" + "Findings filed: <list>".

Add a top-of-file HTML comment explaining: "This is a TEMPLATE. Copy to `.planning/uat/<walkthrough_id>.md` and fill placeholders before walking. The `verification_kind: human-browser-walk` field triggers /gsd:auto hard-pause per CLAUDE.md GSD references."

Do NOT include implementation guidance beyond CONTEXT-locked fields. Do NOT add deferred fields.
  </action>
  <verify>
    <automated>test -f .planning/uat/TEMPLATE-walkthrough.md && grep -q "verification_kind: human-browser-walk" .planning/uat/TEMPLATE-walkthrough.md && grep -q "ext_version:" .planning/uat/TEMPLATE-walkthrough.md && grep -q "idb_revision:" .planning/uat/TEMPLATE-walkthrough.md && grep -q "preset_profile:" .planning/uat/TEMPLATE-walkthrough.md && grep -q "reload_ts:" .planning/uat/TEMPLATE-walkthrough.md && grep -q "target_browsers:" .planning/uat/TEMPLATE-walkthrough.md && grep -q "Defects observed" .planning/uat/TEMPLATE-walkthrough.md && grep -q "check-vocab-deployment" .planning/uat/TEMPLATE-walkthrough.md && echo OK</automated>
  </verify>
  <done>File exists at .planning/uat/TEMPLATE-walkthrough.md with all 7 mandatory frontmatter pre-flight fields, body contains pre-flight evidence + numbered steps + defects-observed + outcome sections, pre-flight references check-vocab-deployment.js.</done>
</task>

<task type="auto">
  <name>Task 2: Write TEMPLATE-finding.md</name>
  <files>.planning/uat/TEMPLATE-finding.md</files>
  <action>
Write `.planning/uat/TEMPLATE-finding.md` with EXACTLY the locked frontmatter keys per CONTEXT.md HYG-02 decisions. Use snake_case. Frontmatter MUST include:

```yaml
---
f_id: F38-1                              # F<phase>-<seq>; formalises F36-1 precedent
severity: blocker                        # blocker | major | minor | trivial
sync_status: extension-only              # synced-upstream | needs-resync | extension-only
regression_fixture_id: fixtures/<lang>/<rule>.jsonl#<id>
                                         # OR benchmark-texts/expectations.json#<id> — HARD requirement, school-year stakes
walkthrough_id: UAT-EXT-XX               # back-reference to walkthrough
discovered: <ISO-8601>
status: open                             # open | fixing | closed
---
```

Body sections (Markdown free-form, mirror Phase 36-VERIFICATION.md F36-1 narrative shape):
1. `## Reproduction` — minimal repro steps + observed output.
2. `## Root cause hypothesis` — short paragraph.
3. `## Fix tracking` — checklist: extension fix landed, regression artifact added (`regression_fixture_id`), all release gates green, version bumped in 3 places, `npm run package` rebuild, lockdown re-sync (if `sync_status: needs-resync`).

Add a top HTML comment: "This is a TEMPLATE. Copy to `.planning/uat/findings/F<phase>-<seq>.md` and fill placeholders. The `regression_fixture_id` field is HARD-required per FIX-02 — school-year stakes, no exceptions."
  </action>
  <verify>
    <automated>test -f .planning/uat/TEMPLATE-finding.md && grep -q "f_id:" .planning/uat/TEMPLATE-finding.md && grep -q "severity:" .planning/uat/TEMPLATE-finding.md && grep -q "sync_status:" .planning/uat/TEMPLATE-finding.md && grep -q "regression_fixture_id:" .planning/uat/TEMPLATE-finding.md && grep -q "Reproduction" .planning/uat/TEMPLATE-finding.md && grep -q "Fix tracking" .planning/uat/TEMPLATE-finding.md && echo OK</automated>
  </verify>
  <done>File exists at .planning/uat/TEMPLATE-finding.md with all 4 mandatory frontmatter fields plus walkthrough_id/discovered/status, body has Reproduction + Root cause + Fix tracking sections.</done>
</task>

<task type="auto">
  <name>Task 3: Document verification_kind auto-pause convention in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Add a new short subsection in CLAUDE.md documenting the verification_kind: human-browser-walk convention. Place it adjacent to the existing "Downstream consumers" section (which already covers cross-cutting GSD/release conventions), or as a new subsection at end of the file before any trailing content — Claude's discretion on exact placement, optimise for findability by an agent reading top-to-bottom.

Section text MUST cover:
1. **Convention name**: `verification_kind` frontmatter field on phase plan files.
2. **Trigger value**: `human-browser-walk` — phase requires real browser verification by a human walker; cannot be auto-advanced.
3. **How agents detect it**: `node ~/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter get <plan-file> --field verification_kind` returns "human-browser-walk" (or null when absent).
4. **Required behavior**: Any agent running auto-mode (`/gsd:auto`, /gsd:execute-phase auto-loop, /gsd:plan-phase auto-loop) MUST query each plan's verification_kind before advancing. If "human-browser-walk", the agent halts the loop, surfaces the walkthrough requirement (URL/template path to instantiate), and waits for explicit user resume — does NOT auto-advance.
5. **Why**: Phase 38's six v3.1 walkthroughs deferred because auto-mode never paused for browser verification (CONTEXT Pitfall 2 = root cause). This convention closes that class.
6. **Example**: Phase 38 plan files declare `verification_kind: human-browser-walk` in their frontmatter; running `/gsd:auto` on Phase 38 surfaces "Phase 38 requires human browser walk — instantiate .planning/uat/TEMPLATE-walkthrough.md and resume after walking."

Use Markdown subsection heading (`## Auto-mode pause convention: `verification_kind: human-browser-walk`` or similar — pick a heading-level consistent with the surrounding subsections in the file). Do NOT modify the Release Workflow numbered list (HYG-04/HYG-05 will land that change in Plan 02).

Keep the addition compact — under 30 lines of prose.
  </action>
  <verify>
    <automated>grep -q "verification_kind" CLAUDE.md && grep -q "human-browser-walk" CLAUDE.md && grep -q "frontmatter get" CLAUDE.md && echo OK</automated>
  </verify>
  <done>CLAUDE.md contains a documented convention block referencing verification_kind, human-browser-walk, and gsd-tools.cjs frontmatter get; auto-mode hard-pause rationale stated; under 30 lines.</done>
</task>

</tasks>

<verification>
Run all three task automated checks. Confirm `.planning/uat/` directory exists with both templates. Sanity-check by simulating a frontmatter query:

```bash
node ~/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter get .planning/uat/TEMPLATE-walkthrough.md --field verification_kind
# Expected output: "human-browser-walk"
```
</verification>

<success_criteria>
- [ ] `.planning/uat/TEMPLATE-walkthrough.md` exists with all 7 locked frontmatter pre-flight fields + body sections
- [ ] `.planning/uat/TEMPLATE-finding.md` exists with all 4 locked frontmatter fields + body sections
- [ ] CLAUDE.md documents the verification_kind: human-browser-walk auto-mode pause convention
- [ ] `node ~/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter get .planning/uat/TEMPLATE-walkthrough.md --field verification_kind` returns "human-browser-walk"
</success_criteria>

<output>
After completion, create `.planning/phases/37-hygiene-templates-pre-flight/37-01-SUMMARY.md`
</output>
