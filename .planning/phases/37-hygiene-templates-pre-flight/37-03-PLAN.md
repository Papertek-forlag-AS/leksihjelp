---
phase: 37-hygiene-templates-pre-flight
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-vocab-deployment.js
  - package.json
autonomous: true
requirements: [HYG-07]
must_haves:
  truths:
    - "Running `node scripts/check-vocab-deployment.js` (or `npm run check-vocab-deployment`) verifies that the local sibling repo at /Users/geirforbord/Papertek/papertek-vocabulary is git-clean, in sync with origin, AND that papertek-vocabulary.vercel.app/api/vocab/v1/revisions reflects the same HEAD sha"
    - "Drift between local HEAD and Vercel-deployed revision exits 1 with a clear diagnostic and a remediation hint"
    - "Vercel API unreachable (timeout/connect-error) exits 1 with a clear 'verify manually before proceeding' message — does NOT silently bypass (Pitfall 4)"
    - "Side-patch reconciliation guidance is printed on success: recommends `npm run sync-vocab` then `git diff extension/data/` (per CLAUDE.md data-logic separation philosophy)"
    - "The script is callable but is NOT inserted into the CLAUDE.md Release Workflow numbered list (per CONTEXT decision: pre-flight only, not a release gate)"
  artifacts:
    - path: "scripts/check-vocab-deployment.js"
      provides: "Pre-flight script verifying papertek-vocabulary deployment is at-HEAD before UAT runs"
      contains: "papertek-vocabulary.vercel.app"
    - path: "package.json"
      provides: "npm script: check-vocab-deployment (no :test pair — pre-flight, not a release gate)"
      contains: "check-vocab-deployment"
  key_links:
    - from: "scripts/check-vocab-deployment.js"
      to: "https://papertek-vocabulary.vercel.app/api/vocab/v1/revisions"
      via: "fetch with AbortController + 10s timeout (Pitfall 4 — never hang forever)"
      pattern: "revisions"
    - from: "scripts/check-vocab-deployment.js"
      to: "/Users/geirforbord/Papertek/papertek-vocabulary git HEAD"
      via: "execSync git rev-parse + git status + git fetch + git rev-list HEAD...@{upstream}"
      pattern: "rev-parse HEAD"
    - from: ".planning/uat/TEMPLATE-walkthrough.md (Plan 01)"
      to: "scripts/check-vocab-deployment.js"
      via: "Pre-flight checklist instructs walker to run the script and paste exit code"
      pattern: "check-vocab-deployment"
---

<objective>
Add a pre-flight script at `scripts/check-vocab-deployment.js` (and matching `npm run check-vocab-deployment`) that verifies the papertek-vocabulary sibling repo + Vercel deployment are aligned at HEAD before any Phase 38 UAT walkthrough runs.

Purpose: Closes Pitfall 1 (stale-artifact failure mode — testing against stale vocab data). HYG-07 is a hard pre-condition for UAT-EXT-* and UAT-LOCK-*; without it, walkthrough findings could be vocab-data artifacts misattributed to extension logic. Per CONTEXT decision, this is a pre-flight only — NOT promoted to a release gate (avoids coupling extension releases to Vercel + sibling-repo state on every release).

Output: One zero-deps Node script + one npm script entry. No CLAUDE.md Release Workflow modification.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-hygiene-templates-pre-flight/37-CONTEXT.md
@.planning/phases/37-hygiene-templates-pre-flight/37-RESEARCH.md

<!-- Existing zero-deps gate exemplar — borrow style (header comment, exit codes, stderr formatting) but NOT the release-gate framing -->
@scripts/check-popup-deps.js

<!-- Existing sync mechanism (called out as side-patch reconciliation action) -->
@scripts/sync-vocab.js

@package.json

<interfaces>
<!-- Sibling repo location (CONSTANT — do not parameterize) -->
PAPERTEK_REPO = '/Users/geirforbord/Papertek/papertek-vocabulary'

<!-- Vercel revisions endpoint -->
REVISIONS_URL = 'https://papertek-vocabulary.vercel.app/api/vocab/v1/revisions'

<!-- Timeout — Pitfall 4 -->
TIMEOUT_MS = 10000

<!-- Response shape: UNVERIFIED — per RESEARCH Open Question 1, first task in this plan is to curl the endpoint and freeze the parser against the actual shape -->
<!-- Likely candidates: { revision: '<sha>' } | { head: '<sha>' } | { sha: '<sha>' } | { languages: { de: '<sha>', es: '<sha>', ... } } -->
<!-- Do NOT speculate — verify first, then implement -->

<!-- Git introspection commands -->
git rev-parse HEAD              # local HEAD sha
git status --porcelain          # empty == clean
git fetch --quiet               # update remote-tracking
git rev-list --left-right --count HEAD...@{upstream}   # ahead<TAB>behind counts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe Vercel revisions endpoint to freeze response-shape contract</name>
  <files>scripts/check-vocab-deployment.js</files>
  <action>
Per RESEARCH Open Question 1, the response shape of `https://papertek-vocabulary.vercel.app/api/vocab/v1/revisions` is unverified. BEFORE writing the script, probe it live:

```bash
curl -sS https://papertek-vocabulary.vercel.app/api/vocab/v1/revisions
```

Capture the exact JSON shape. Common likely shapes (do NOT assume):
- `{ "revision": "<sha>" }` — single global revision
- `{ "head": "<sha>" }` — git-style single field
- `{ "languages": { "de": "<sha>", "es": "<sha>", ... } }` — per-language
- Some hybrid

Once observed, write the script's parser against the EXACT observed shape. If shape is per-language and there's no global revision, the script must compare each language's revision against the local sibling-repo HEAD (the sibling repo serves all languages from one git tree, so they should all match HEAD on a clean deploy).

Document the observed shape in a top-of-file comment so future maintainers know what was assumed:
```js
// Vercel API response shape (verified <ISO-date>):
//   { ... actual shape ... }
// If shape changes, update parser and bump this comment.
```

If the curl probe FAILS (Vercel down, network unreachable, 4xx/5xx), do NOT speculate. Surface the failure to the user and stop — this plan cannot proceed against an unknown shape (executing with a guessed parser would ship a script that's wrong-by-construction). Document the probe failure in the verification output and request user input.

Save the probed JSON in a comment at the top of `scripts/check-vocab-deployment.js` for posterity.
  </action>
  <verify>
    <automated>test -f scripts/check-vocab-deployment.js && head -30 scripts/check-vocab-deployment.js | grep -q "Vercel API response shape" && echo OK</automated>
  </verify>
  <done>scripts/check-vocab-deployment.js exists with a top-of-file comment recording the verified response shape from a live curl probe (date-stamped). If probe failed, plan halts and surfaces failure to user before proceeding.</done>
</task>

<task type="auto">
  <name>Task 2: Implement check-vocab-deployment.js script body + register npm script</name>
  <files>scripts/check-vocab-deployment.js, package.json</files>
  <action>
Implement the script body using the verbatim skeleton in RESEARCH.md "check-vocab-deployment.js skeleton" as starting point, ADAPTED to the response shape verified in Task 1.

**`scripts/check-vocab-deployment.js`:**
- Strict CommonJS, zero npm deps. Use built-in `fetch` (Node ≥18) per RESEARCH stack decision.
- Header comment: identifies this as the HYG-07 pre-flight script, declares it is NOT a release gate (do not call from CLAUDE.md Release Workflow), records the verified Vercel API response shape.
- main() async function — execution flow:
  1. **Local repo introspection** at `PAPERTEK_REPO = '/Users/geirforbord/Papertek/papertek-vocabulary'`:
     - `git rev-parse HEAD` → localHead
     - `git status --porcelain` → must be empty (else FAIL with diagnostic listing dirty files)
     - `git fetch --quiet`
     - `git rev-list --left-right --count HEAD...@{upstream}` → tab-separated ahead/behind; both must be 0 (else FAIL)
  2. **Vercel API fetch** with AbortController + 10s timeout (Pitfall 4):
     ```js
     const ctrl = new AbortController();
     const timer = setTimeout(() => ctrl.abort(), TIMEOUT_MS);
     try {
       const res = await fetch(REVISIONS_URL, { signal: ctrl.signal });
       if (!res.ok) { ... FAIL ... }
       const body = await res.json();
       // parse per Task-1-verified shape
     } catch (e) {
       // AbortError or network — FAIL with "Vercel API unreachable — verify manually before proceeding"
     } finally { clearTimeout(timer); }
     ```
  3. **Compare**: extract apiHead from body using the Task-1-verified shape. If `apiHead !== localHead` → FAIL with both shas printed + remediation hint ("wait for Vercel deploy to finish, or trigger redeploy").
  4. **Success path**: stdout PASS message with truncated sha (first 8 chars) + recommendation to next step:
     - `npm run sync-vocab` to refresh `extension/data/*.json` from upstream
     - `git diff extension/data/` — empty means side-patches reconciled; non-empty means investigate (probably an upstream gap, document as deferred)

- All FAIL paths: stderr per RESEARCH skeleton format (`[check-vocab-deployment] FAIL: <reason>` + indented details + `fix:` line where applicable). Exit 1.
- Success: stdout `[check-vocab-deployment] PASS: ...` + exit 0.

**`package.json`:**
- Add ONE new entry to `scripts` block:
  ```json
  "check-vocab-deployment": "node scripts/check-vocab-deployment.js"
  ```
- DO NOT add a `:test` pair. Per CONTEXT decision, this is a pre-flight script not a release gate; the gate-pair convention does not apply. (Future maintainers may add a self-test if drift bites; explicitly out of scope here.)
- Group with other check-* entries for discoverability.

DO NOT modify CLAUDE.md Release Workflow numbered list — pre-flight script does not appear there per CONTEXT lock.
  </action>
  <verify>
    <automated>node scripts/check-vocab-deployment.js && grep -q "check-vocab-deployment" package.json && echo OK</automated>
  </verify>
  <done>scripts/check-vocab-deployment.js exits 0 against current state (assuming sibling repo is clean + Vercel up-to-date — likely true; if drift exists this surfaces it for manual reconciliation BEFORE Phase 38 starts, which is the entire point of HYG-07). package.json has the npm script. CLAUDE.md Release Workflow is unchanged.</done>
</task>

</tasks>

<verification>
- `node scripts/check-vocab-deployment.js` exit 0 OR exit 1 with a clear actionable diagnostic (drift caught early is success-mode for the gate; a PASS-result and a clean-fail-with-remediation are both acceptable outcomes).
- `npm run check-vocab-deployment` works as alias.
- `grep -c "check-vocab-deployment" CLAUDE.md` is 0 (verify it was NOT inserted into Release Workflow).
- Top-of-file comment records the verified Vercel API response shape with a date.

If the script reports drift on first run, that's expected pre-Phase-38 work — surface to user with the recommended remediation (`npm run sync-vocab`, then resolve any side-patch reconciliation per CONTEXT HYG-07 reconciliation policy). Do NOT silently fix during Plan 03 execution.
</verification>

<success_criteria>
- [ ] `scripts/check-vocab-deployment.js` exists, zero npm deps, uses Node 18+ built-in fetch with 10s timeout
- [ ] Script verifies sibling-repo cleanliness + remote sync + Vercel-deployed revision matches local HEAD
- [ ] On drift: exits 1 with actionable per-condition diagnostic and remediation hint
- [ ] On unreachable Vercel: exits 1 with "verify manually before proceeding" — does NOT silently bypass
- [ ] On success: prints PASS + recommendation to run `npm run sync-vocab` + `git diff extension/data/` for side-patch reconciliation check
- [ ] `npm run check-vocab-deployment` works
- [ ] CLAUDE.md Release Workflow numbered list is unchanged (pre-flight only, not a release gate)
- [ ] Top-of-file comment records the verified Vercel API response shape with date-stamp
</success_criteria>

<output>
After completion, create `.planning/phases/37-hygiene-templates-pre-flight/37-03-SUMMARY.md`
</output>
