---
phase: 05.1-close-ux-01-gaps-from-phase-5-smoke-test
plan: 02
plan_id: "05.1-02"
plan_number: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - "papertek-vocabulary/vocabulary/lexicon/nb/languagesbank.json"
  - "papertek-vocabulary/vocabulary/lexicon/nn/languagesbank.json"
  - "papertek-vocabulary/vocabulary/lexicon/nb/nationalitiesbank.json"
  - "papertek-vocabulary/vocabulary/lexicon/nn/nationalitiesbank.json"
  - "papertek-vocabulary/vocabulary/lexicon/nb/manifest.json"
  - "papertek-vocabulary/vocabulary/lexicon/nn/manifest.json"
  - "papertek-vocabulary/api/vocab/v3/lookup/[language]/[wordId].js"
autonomous: true
requirements: [UX-01]
working_directory: "/Users/geirforbord/Papertek/papertek-vocabulary/"
gap_closure: true
gaps_addressed: ["Gap B — NB + NN wordLists missing language and nationality nouns"]

must_haves:
  truths:
    - "papertek-vocabulary/vocabulary/lexicon/nb/languagesbank.json contains ~20 language entries (norsk, nynorsk, bokmål, engelsk, tysk, spansk, fransk, italiensk, portugisisk, russisk, arabisk, kinesisk, japansk, polsk, ukrainsk, samisk, islandsk, færøysk, svensk, dansk)"
    - "papertek-vocabulary/vocabulary/lexicon/nn/languagesbank.json contains NN-orthographic equivalents of the NB list"
    - "nationalitiesbank.json (NB + NN) contains corresponding demonyms where grammatically distinct from language names"
    - "Bank files are auto-discovered by existing *bank.json glob patterns (manifest endpoint + search-index generator)"
    - "Entries carry type: 'language' / 'nationality' (NOT type: 'noun') so vocab-seam-core's nounGenus pipe doesn't pollute on the leksihjelp side"
    - "TYPE_TO_BANK in api/vocab/v3/lookup routes language/nationality types to their new banks for fast lookup"
  artifacts:
    - path: "papertek-vocabulary/vocabulary/lexicon/nb/languagesbank.json"
      provides: "NB language-name entries with type: 'language', hand-authored ~20 entries"
      contains: "norsk"
    - path: "papertek-vocabulary/vocabulary/lexicon/nn/languagesbank.json"
      provides: "NN language-name entries"
      contains: "norsk"
    - path: "papertek-vocabulary/vocabulary/lexicon/nb/nationalitiesbank.json"
      provides: "NB nationality-demonym entries with type: 'nationality'"
      contains: "nordmann"
    - path: "papertek-vocabulary/vocabulary/lexicon/nn/nationalitiesbank.json"
      provides: "NN nationality-demonym entries"
      contains: "nordmann"
    - path: "papertek-vocabulary/api/vocab/v3/lookup/[language]/[wordId].js"
      provides: "TYPE_TO_BANK updated with language + nationality types"
      contains: "languagesbank"
  key_links:
    - from: "entry.type === 'language' | 'nationality'"
      to: "*bank.json files"
      via: "auto-discovery glob in manifest.js + build-lexicon-search-index.js"
      pattern: "endsWith\\(['\\\"]bank\\.json['\\\"]\\)"
    - from: "per-lang manifest.json banks block"
      to: "new bank entry counts"
      via: "hand-updated bank count"
      pattern: "languagesbank"
---

<objective>
Close Gap B: Introduce `languagesbank` and `nationalitiesbank` as new bank types in `papertek-vocabulary`, hand-author ~20 targeted entries per language (NB + NN) for language names (`norsk`, `nynorsk`, `bokmål`, `engelsk`, `tysk`, `spansk`, `fransk`, `italiensk`, `portugisisk`, `russisk`, `arabisk`, `kinesisk`, `japansk`, `polsk`, `ukrainsk`, `samisk`, `islandsk`, `færøysk`, `svensk`, `dansk`) plus the corresponding nationality demonyms where grammatically distinct (`nordmann`, `svenske`, `dansk`, etc.). All work is cross-repo in `papertek-vocabulary`; Plan 05 pulls into leksihjelp via `npm run sync-vocab` and wires the new banks through the four extension-side registries.

Purpose: A student writing "skrive på nynorsk" in an NN document currently gets "nynorsk står ikkje i ordboka" because `nynorsk` is missing from the vocabulary source. The two official written standards of Norwegian — and common foreign-language names Norwegian students write about daily — must be first-class vocabulary. CONTEXT.md locks new bank types (not cramming into `nounbank`) for schema clarity and future-phase expandability. Targeted ~20 per language now; comprehensive ISO 639 expansion is deferred.

Output: Four new bank JSON files (NB languages, NN languages, NB nationalities, NN nationalities), updated per-lang manifest.json counts, TYPE_TO_BANK entries added to the v3 lookup endpoint for fast-path routing, a migration note in `papertekvocabulary.md` if that file exists, and a single atomic commit pushed to papertek-vocabulary main.

Resolves research open question #1 (type field design: `type: 'language'` / `type: 'nationality'` chosen to keep entries OUT of nounGenus pollution) and open question #5 (bokmål UTF-8 spot-check during Task 2 authoring).
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/geirforbord/Papertek/leksihjelp/CLAUDE.md
@/Users/geirforbord/Papertek/leksihjelp/.planning/phases/05.1-close-ux-01-gaps-from-phase-5-smoke-test/05.1-CONTEXT.md
@/Users/geirforbord/Papertek/leksihjelp/.planning/phases/05.1-close-ux-01-gaps-from-phase-5-smoke-test/05.1-RESEARCH.md

<working_directory>
ALL tasks in this plan run from `/Users/geirforbord/Papertek/papertek-vocabulary/`. No changes to Leksihjelp repo in this plan — data flows through via Plan 05 sync.
</working_directory>

<interfaces>
Research-verified existing bank auto-discovery points — these flow additively when new `*bank.json` files land:

```javascript
// /Users/geirforbord/Papertek/papertek-vocabulary/api/vocab/v3/manifest.js:16-26
//   per-lang manifest endpoint uses .filter(f => f.endsWith('bank.json'))
// /Users/geirforbord/Papertek/papertek-vocabulary/scripts/build-lexicon-search-index.js:42
//   readdirSync(langPath).filter(f => f.endsWith('bank.json'))
// /Users/geirforbord/Papertek/papertek-vocabulary/api/vocab/v3/lookup/[language]/[wordId].js:41-47
//   findWordInAllBanks iterates every *bank.json as fallback
```

TYPE_TO_BANK fast-path routing (existing pattern):

```javascript
// /Users/geirforbord/Papertek/papertek-vocabulary/api/vocab/v3/lookup/[language]/[wordId].js:16-25 (approx)
const TYPE_TO_BANK = {
  'verb': 'verbbank',
  'noun': 'nounbank',
  'adj': 'adjectivebank',
  'article': 'articlesbank',
  ...
};
```

Per-lang manifest.json structure (current NB example — inspect before editing):
```json
{
  "language": "nb",
  "banks": {
    "verbbank": { "entries": N },
    "nounbank": { "entries": N },
    "adjectivebank": { "entries": N },
    "generalbank": { "entries": N }
  },
  ...
}
```

Existing NB nounbank entry shape (template to follow):
```json
"<word>_noun": {
  "word": "<word>",
  "type": "noun",
  "genus": "m|f|n",
  "cefr": "A1",
  "frequency": <int>,
  "translation": "<gloss>",
  "plural": "<plural form>",
  "definite": "<definite form>"
}
```

For Gap B, DO NOT use `type: 'noun'` in languagesbank entries — that would pollute leksihjelp's `nounGenus` Map (vocab-seam-core.js:473 adds entries with `entry.bank === 'nounbank' || entry.type === 'nounform' || entry.type === 'plural'` to nounGenus). Use `type: 'language'`.

For nationalities (nordmann, svenske, tysker): these ARE nouns grammatically. Shape them as full noun entries (with `genus`, `plural`) but in `nationalitiesbank.json` with `type: 'nationality'`. Per leksihjelp vocab-seam-core.js:473, `type: 'nationality'` will NOT enter nounGenus because it's neither 'nounform' nor 'plural' nor bank='nounbank' — so gender-rule won't flag "Jeg er en nordmann" as gender-mismatched on the new nationalities data (intended; gender rule fires off nounGenus lookup). Trade-off: nationality nouns lose gender tracking for the gender rule in leksihjelp. If this matters for future phases, a separate `nounGenus` feed for nationalitiesbank would be needed — out of scope for 05.1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema design + NB languagesbank + NB nationalitiesbank author</name>
  <files>/Users/geirforbord/Papertek/papertek-vocabulary/vocabulary/lexicon/nb/languagesbank.json, /Users/geirforbord/Papertek/papertek-vocabulary/vocabulary/lexicon/nb/nationalitiesbank.json</files>
  <action>
Working directory: `/Users/geirforbord/Papertek/papertek-vocabulary/`.

Step 1 — Create `vocabulary/lexicon/nb/languagesbank.json`. Follow the `_metadata` + entries structure used by existing banks (inspect `vocabulary/lexicon/nb/nounbank.json` first 15 lines for the pattern). Shape:

```json
{
  "_metadata": {
    "language": "nb",
    "bank": "languagesbank",
    "description": "Language names — primary adjectival form used to refer to a language. Substantivized adjectival use (Norwegian 'norsk' can mean both 'Norwegian' adj and 'Norwegian language' noun). Added in Phase 05.1 to close wordList coverage for language names. Type is 'language' (not 'adj' or 'noun') to avoid polluting downstream genus/plural indexes.",
    "generatedAt": "<ISO-8601 date>",
    "version": "1.0"
  },
  "norsk_lang": {
    "word": "norsk",
    "type": "language",
    "cefr": "A1",
    "frequency": 5000,
    "translation": "Norwegian (language)",
    "typos": [],
    "acceptedForms": []
  },
  "nynorsk_lang": {
    "word": "nynorsk",
    "type": "language",
    "cefr": "A2",
    "frequency": 800,
    "translation": "Nynorsk (one of two written standards of Norwegian)",
    "typos": [],
    "acceptedForms": []
  },
  "bokmal_lang": {
    "word": "bokmål",
    "type": "language",
    "cefr": "A2",
    "frequency": 800,
    "translation": "Bokmål (one of two written standards of Norwegian)",
    "typos": [],
    "acceptedForms": []
  },
  "engelsk_lang": {
    "word": "engelsk",
    "type": "language",
    "cefr": "A1",
    "frequency": 4000,
    "translation": "English (language)"
  },
  "tysk_lang": {
    "word": "tysk",
    "type": "language",
    "cefr": "A1",
    "frequency": 2500,
    "translation": "German (language)"
  },
  "spansk_lang": { "word": "spansk", "type": "language", "cefr": "A1", "frequency": 2000, "translation": "Spanish (language)" },
  "fransk_lang": { "word": "fransk", "type": "language", "cefr": "A1", "frequency": 2000, "translation": "French (language)" },
  "italiensk_lang": { "word": "italiensk", "type": "language", "cefr": "A2", "frequency": 800, "translation": "Italian (language)" },
  "portugisisk_lang": { "word": "portugisisk", "type": "language", "cefr": "A2", "frequency": 400, "translation": "Portuguese (language)" },
  "russisk_lang": { "word": "russisk", "type": "language", "cefr": "A2", "frequency": 600, "translation": "Russian (language)" },
  "arabisk_lang": { "word": "arabisk", "type": "language", "cefr": "A2", "frequency": 500, "translation": "Arabic (language)" },
  "kinesisk_lang": { "word": "kinesisk", "type": "language", "cefr": "A2", "frequency": 500, "translation": "Chinese (language)" },
  "japansk_lang": { "word": "japansk", "type": "language", "cefr": "A2", "frequency": 400, "translation": "Japanese (language)" },
  "polsk_lang": { "word": "polsk", "type": "language", "cefr": "A2", "frequency": 400, "translation": "Polish (language)" },
  "ukrainsk_lang": { "word": "ukrainsk", "type": "language", "cefr": "B1", "frequency": 300, "translation": "Ukrainian (language)" },
  "samisk_lang": { "word": "samisk", "type": "language", "cefr": "A2", "frequency": 600, "translation": "Sámi (language)" },
  "islandsk_lang": { "word": "islandsk", "type": "language", "cefr": "B1", "frequency": 200, "translation": "Icelandic (language)" },
  "faeroysk_lang": { "word": "færøysk", "type": "language", "cefr": "B1", "frequency": 150, "translation": "Faroese (language)" },
  "svensk_lang": { "word": "svensk", "type": "language", "cefr": "A1", "frequency": 2500, "translation": "Swedish (language)" },
  "dansk_lang": { "word": "dansk", "type": "language", "cefr": "A1", "frequency": 2500, "translation": "Danish (language)" }
}
```

Notes:
- Word-id suffix is `_lang` (researcher recommendation, resolves open question #1a).
- Keys use ASCII-safe transliteration (`bokmal_lang`, `faeroysk_lang`) but the `word` value carries the correct Norwegian orthography (`bokmål`, `færøysk`). This matches `blå_adj` / `blå` precedent.
- `type: 'language'` avoids polluting nounGenus in leksihjelp.
- 20 entries — CONTEXT.md-locked targeted hand-authored list.
- `typos: []` and `acceptedForms: []` on the three highest-value entries (norsk, nynorsk, bokmål) for future typo-bank expansion; omitted on others to keep file size down.

Step 2 — UTF-8 spot-check (resolves open question #5):

```bash
cd /Users/geirforbord/Papertek/papertek-vocabulary
python3 -c "import json; d = json.load(open('vocabulary/lexicon/nb/languagesbank.json')); print('bokmal_lang word bytes:', d['bokmal_lang']['word'].encode('utf-8'))"
# Expected: b'bokm\xc3\xa5l' — 6 bytes for 5 chars (å = 2 bytes)
```

Sanity check: the file re-parses as valid JSON; no BOM; UTF-8 encoding is preserved.

Step 3 — Create `vocabulary/lexicon/nb/nationalitiesbank.json`. Nationalities ARE grammatical nouns (they have gender, plural, definite forms). Use type `'nationality'` to shield from nounGenus pollution (see Context.interfaces note). Shape:

```json
{
  "_metadata": {
    "language": "nb",
    "bank": "nationalitiesbank",
    "description": "Nationality demonyms — nouns referring to people of a nationality. Type 'nationality' distinguishes from regular nouns. Forms include genus + plural + definite like a regular noun, but the type prevents downstream gender/plural index pollution in consumers that opt out.",
    "generatedAt": "<ISO-8601 date>",
    "version": "1.0"
  },
  "nordmann_nat": {
    "word": "nordmann",
    "type": "nationality",
    "genus": "m",
    "plural": "nordmenn",
    "definite": "nordmannen",
    "plural_definite": "nordmennene",
    "cefr": "A2",
    "frequency": 1500,
    "translation": "Norwegian (person)"
  },
  "svenske_nat": { "word": "svenske", "type": "nationality", "genus": "m", "plural": "svensker", "definite": "svensken", "plural_definite": "svenskene", "cefr": "A2", "frequency": 800, "translation": "Swede" },
  "danske_nat": { "word": "danske", "type": "nationality", "genus": "m", "plural": "dansker", "definite": "dansken", "plural_definite": "danskene", "cefr": "A2", "frequency": 600, "translation": "Dane" },
  "tysker_nat": { "word": "tysker", "type": "nationality", "genus": "m", "plural": "tyskere", "definite": "tyskeren", "plural_definite": "tyskerne", "cefr": "A2", "frequency": 700, "translation": "German (person)" },
  "englendar_nat": { "word": "englender", "type": "nationality", "genus": "m", "plural": "englendere", "definite": "englenderen", "plural_definite": "englenderne", "cefr": "A2", "frequency": 400, "translation": "English person" },
  "brite_nat": { "word": "brite", "type": "nationality", "genus": "m", "plural": "briter", "definite": "briten", "plural_definite": "britene", "cefr": "A2", "frequency": 400, "translation": "Briton" },
  "amerikaner_nat": { "word": "amerikaner", "type": "nationality", "genus": "m", "plural": "amerikanere", "definite": "amerikaneren", "plural_definite": "amerikanerne", "cefr": "A1", "frequency": 800, "translation": "American" },
  "franskmann_nat": { "word": "franskmann", "type": "nationality", "genus": "m", "plural": "franskmenn", "definite": "franskmannen", "plural_definite": "franskmennene", "cefr": "A2", "frequency": 400, "translation": "French person" },
  "spanjol_nat": { "word": "spanjol", "type": "nationality", "genus": "m", "plural": "spanjoler", "definite": "spanjolen", "plural_definite": "spanjolene", "cefr": "A2", "frequency": 300, "translation": "Spaniard" },
  "italiener_nat": { "word": "italiener", "type": "nationality", "genus": "m", "plural": "italienere", "definite": "italieneren", "plural_definite": "italienerne", "cefr": "A2", "frequency": 300, "translation": "Italian" },
  "polakk_nat": { "word": "polakk", "type": "nationality", "genus": "m", "plural": "polakker", "definite": "polakken", "plural_definite": "polakkene", "cefr": "B1", "frequency": 300, "translation": "Pole" },
  "russer_nat": { "word": "russer", "type": "nationality", "genus": "m", "plural": "russere", "definite": "russeren", "plural_definite": "russerne", "cefr": "B1", "frequency": 300, "translation": "Russian person" },
  "ukrainer_nat": { "word": "ukrainer", "type": "nationality", "genus": "m", "plural": "ukrainere", "definite": "ukraineren", "plural_definite": "ukrainerne", "cefr": "B1", "frequency": 200, "translation": "Ukrainian person" },
  "same_nat": { "word": "same", "type": "nationality", "genus": "m", "plural": "samer", "definite": "samen", "plural_definite": "samene", "cefr": "A2", "frequency": 400, "translation": "Sami person" },
  "islending_nat": { "word": "islending", "type": "nationality", "genus": "m", "plural": "islendinger", "definite": "islendingen", "plural_definite": "islendingene", "cefr": "B1", "frequency": 150, "translation": "Icelander" },
  "faeroying_nat": { "word": "færøying", "type": "nationality", "genus": "m", "plural": "færøyinger", "definite": "færøyingen", "plural_definite": "færøyingene", "cefr": "B1", "frequency": 100, "translation": "Faroese person" },
  "araber_nat": { "word": "araber", "type": "nationality", "genus": "m", "plural": "arabere", "definite": "araberen", "plural_definite": "araberne", "cefr": "A2", "frequency": 400, "translation": "Arab" },
  "kineser_nat": { "word": "kineser", "type": "nationality", "genus": "m", "plural": "kinesere", "definite": "kineseren", "plural_definite": "kineserne", "cefr": "A2", "frequency": 400, "translation": "Chinese person" },
  "japaner_nat": { "word": "japaner", "type": "nationality", "genus": "m", "plural": "japanere", "definite": "japaneren", "plural_definite": "japanerne", "cefr": "A2", "frequency": 300, "translation": "Japanese person" },
  "portugiser_nat": { "word": "portugiser", "type": "nationality", "genus": "m", "plural": "portugisere", "definite": "portugiseren", "plural_definite": "portugiserne", "cefr": "B1", "frequency": 200, "translation": "Portuguese person" }
}
```

20 entries total. All masculine `genus: 'm'` per Norwegian default for mixed-gender demonyms (Språkrådet convention). Frequencies hand-estimated from casual common-usage. Word-id suffix `_nat`.

Step 4 — Parse both files through python3/node to confirm valid JSON:

```bash
cd /Users/geirforbord/Papertek/papertek-vocabulary
node -e "JSON.parse(require('fs').readFileSync('vocabulary/lexicon/nb/languagesbank.json', 'utf8')); console.log('NB languages OK')"
node -e "JSON.parse(require('fs').readFileSync('vocabulary/lexicon/nb/nationalitiesbank.json', 'utf8')); console.log('NB nationalities OK')"
```
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/papertek-vocabulary && node -e "const nb_lang = JSON.parse(require('fs').readFileSync('vocabulary/lexicon/nb/languagesbank.json','utf8')); const nb_nat = JSON.parse(require('fs').readFileSync('vocabulary/lexicon/nb/nationalitiesbank.json','utf8')); const lang_entries = Object.keys(nb_lang).filter(k => !k.startsWith('_')); const nat_entries = Object.keys(nb_nat).filter(k => !k.startsWith('_')); console.log('lang:', lang_entries.length, 'nat:', nat_entries.length); console.log('bokmal present:', !!nb_lang['bokmal_lang']); console.log('nynorsk present:', !!nb_lang['nynorsk_lang']); console.log('nordmann present:', !!nb_nat['nordmann_nat']); if (lang_entries.length < 18 || nat_entries.length < 18) process.exit(1);"</automated>
  </verify>
  <done>NB languagesbank.json + nationalitiesbank.json exist, parse as valid JSON, contain ≥18 entries each, include all CONTEXT.md-listed languages (norsk, nynorsk, bokmål, + 17 others) and demonyms (nordmann, svenske, + others).</done>
</task>

<task type="auto">
  <name>Task 2: NN languagesbank + NN nationalitiesbank + manifest count updates</name>
  <files>/Users/geirforbord/Papertek/papertek-vocabulary/vocabulary/lexicon/nn/languagesbank.json, /Users/geirforbord/Papertek/papertek-vocabulary/vocabulary/lexicon/nn/nationalitiesbank.json, /Users/geirforbord/Papertek/papertek-vocabulary/vocabulary/lexicon/nb/manifest.json, /Users/geirforbord/Papertek/papertek-vocabulary/vocabulary/lexicon/nn/manifest.json</files>
  <action>
Working directory: `/Users/geirforbord/Papertek/papertek-vocabulary/`.

Step 1 — Create `vocabulary/lexicon/nn/languagesbank.json`. NN orthography notes:
- Most language names are IDENTICAL in NB and NN (norsk, engelsk, tysk, spansk, fransk, italiensk, russisk, arabisk, kinesisk, japansk, polsk, samisk, svensk, dansk, bokmål, nynorsk).
- NN-specific spelling differences: `portugisisk` (same), `ukrainsk` (same), `islandsk` (same), `færøysk` (same) — these are adjective forms identical in both.
- Linguistic check: some NN references use `søamisk` vs `samisk` — use `samisk` (the official majority form).

Mirror the NB shape but with the `"language": "nn"` metadata. Keep the 20 entries. Keep word-id suffixes `_lang`.

Step 2 — Create `vocabulary/lexicon/nn/nationalitiesbank.json`. NN-specific nationality demonyms:
- `nordmann` → same in NN
- `svenske` → same (or `svensk` used adjectivally)
- `tyskar` vs NB `tysker` — NN uses `-ar` plural pattern, so `tysker` becomes `tyskar` in NN. Verify against existing NN nounbank convention before choosing.

Inspect existing NN nounbank pattern for `-er` vs `-ar` suffix:

```bash
cd /Users/geirforbord/Papertek/papertek-vocabulary
grep -A 5 '"lærar"' vocabulary/lexicon/nn/nounbank.json | head -20
grep -A 5 '"spelar"' vocabulary/lexicon/nn/nounbank.json | head -20
```

If NN uses `-ar` / `-arar` (typical NN pattern), author NN nationalities as `tyskar`, `englendar`, `amerikanar`, `spanjol` (same), `italienar`, `polakk` (same), `russar`, `ukrainar`, `arabar`, `kinesar`, `japanar`, `portugisar`. Plurals `-ar` → `-arar` or `-arane`.

Minimum shape per entry (use NB nationality shape as template, adjust forms):

```json
"nordmann_nat": {
  "word": "nordmann",
  "type": "nationality",
  "genus": "m",
  "plural": "nordmenn",
  "definite": "nordmannen",
  "plural_definite": "nordmennene",
  "cefr": "A2",
  "frequency": 1500,
  "translation": "Norwegian (person)"
}
```

Default plan: if you cannot confidently author NN-specific form for an entry, use the NB form as a fallback AND note in SUMMARY. Linguistic correctness is important but not every NN demonym has a widely-accepted form — err on the side of the most common Språkrådet-documented shape. Maintain 20 entries with correct word values; plural accuracy can be improved in a follow-up if needed.

Step 3 — Update per-lang manifest counts. Inspect current shape:

```bash
cd /Users/geirforbord/Papertek/papertek-vocabulary
cat vocabulary/lexicon/nb/manifest.json
cat vocabulary/lexicon/nn/manifest.json
```

Add new bank entries to the `banks` object. Example NB manifest.json edit:

```json
"banks": {
  "verbbank": { "entries": N },
  "nounbank": { "entries": N },
  "adjectivebank": { "entries": N + 1 },  // +1 for fin_adj from Plan 01
  "generalbank": { "entries": N },
  "languagesbank": { "entries": 20 },       // NEW
  "nationalitiesbank": { "entries": 20 }    // NEW
},
```

NN manifest.json: add same two new entries with count 20 each. Adjectivebank count also +1 if Plan 01 added NN fin_adj.

**Plan 01 + Plan 02 manifest coordination:** Both plans edit the same per-lang manifest.json. To avoid merge conflicts:
- Plan 01 will update adjectivebank counts ONLY.
- Plan 02 (this plan) adds languagesbank + nationalitiesbank entries ONLY.
- Edit the file section-by-section, preserving Plan 01's in-progress adjectivebank edits.
- If this plan's task executes AFTER Plan 01's Task 3 commit, no coordination is needed — just pull and re-apply. If concurrent, use a planner checkpoint: verify the file's current state with `git diff` before committing.

Validate JSON after edits:

```bash
node -e "JSON.parse(require('fs').readFileSync('vocabulary/lexicon/nb/manifest.json','utf8')); console.log('nb manifest ok')"
node -e "JSON.parse(require('fs').readFileSync('vocabulary/lexicon/nn/manifest.json','utf8')); console.log('nn manifest ok')"
```

Step 4 — Optional search-index rebuild. If there's a `build:search-index` npm script that includes the new banks, run it:

```bash
cd /Users/geirforbord/Papertek/papertek-vocabulary
ls scripts/build-lexicon-search-index.js scripts/build-search-index.js 2>/dev/null
npm run build:search-index 2>/dev/null || echo "skipped"
```

The sync-vocab endpoint in leksihjelp hits the v3 list endpoint which reads from `search-index.json` — if that's a build artefact, it must be rebuilt. If it's generated on-demand by the API, skip.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/papertek-vocabulary && node -e "const nn_lang = JSON.parse(require('fs').readFileSync('vocabulary/lexicon/nn/languagesbank.json','utf8')); const nn_nat = JSON.parse(require('fs').readFileSync('vocabulary/lexicon/nn/nationalitiesbank.json','utf8')); const nb_man = JSON.parse(require('fs').readFileSync('vocabulary/lexicon/nb/manifest.json','utf8')); const nn_man = JSON.parse(require('fs').readFileSync('vocabulary/lexicon/nn/manifest.json','utf8')); console.log('nn lang:', Object.keys(nn_lang).filter(k=>!k.startsWith('_')).length); console.log('nn nat:', Object.keys(nn_nat).filter(k=>!k.startsWith('_')).length); console.log('nb manifest lang count:', nb_man.banks && nb_man.banks.languagesbank && nb_man.banks.languagesbank.entries); console.log('nn manifest lang count:', nn_man.banks && nn_man.banks.languagesbank && nn_man.banks.languagesbank.entries); if (!(nb_man.banks && nb_man.banks.languagesbank && nb_man.banks.nationalitiesbank)) process.exit(1);"</automated>
  </verify>
  <done>NN languagesbank.json + nationalitiesbank.json exist with ≥18 entries each. Per-lang manifest.json files contain `banks.languagesbank` and `banks.nationalitiesbank` entries with count 20. All files parse as valid JSON.</done>
</task>

<task type="auto">
  <name>Task 3: TYPE_TO_BANK lookup routing + commit + push</name>
  <files>/Users/geirforbord/Papertek/papertek-vocabulary/api/vocab/v3/lookup/[language]/[wordId].js</files>
  <action>
Working directory: `/Users/geirforbord/Papertek/papertek-vocabulary/`.

Step 1 — Update `api/vocab/v3/lookup/[language]/[wordId].js` TYPE_TO_BANK map. Inspect the current shape:

```bash
cd /Users/geirforbord/Papertek/papertek-vocabulary
sed -n '10,30p' 'api/vocab/v3/lookup/[language]/[wordId].js'
```

Expected current (research line 16-25):
```javascript
const TYPE_TO_BANK = {
  'verb': 'verbbank',
  'noun': 'nounbank',
  'adj': 'adjectivebank',
  // ...
};
```

Add two entries:

```javascript
  'language': 'languagesbank',
  'nationality': 'nationalitiesbank',
```

This gives the v3 lookup endpoint fast-path routing for `_lang` / `_nat` suffixed IDs. Without this, requests fall through to `findWordInAllBanks` (slower but still works via the existing `*bank.json` glob on line 41-47).

Step 2 — Run any existing validator scripts that would catch schema regressions from new banks. Inspect scripts block:

```bash
cd /Users/geirforbord/Papertek/papertek-vocabulary
grep -E '^\s+"(validate|verify):' package.json
```

Run `npm run validate:all` if present. If it errors on the new banks (e.g., schema validator), the new banks likely need a schema file. Either:
- (Preferred, per CONTEXT.md Claude's Discretion) Skip schema files for 05.1 — the validator can be extended later.
- Add minimal JSON-schema files at `vocabulary/schema/languages.schema.json` and `vocabulary/schema/nationalities.schema.json` if `validate:all` hard-fails.

Expected outcome: existing validators don't touch the new banks (they typically filter to specific bank names). If a new validator surface is needed, note in SUMMARY as a follow-up and skip in this plan.

Step 3 — Commit everything to papertek-vocabulary. Single atomic commit:

```bash
cd /Users/geirforbord/Papertek/papertek-vocabulary
git add vocabulary/lexicon/nb/languagesbank.json vocabulary/lexicon/nn/languagesbank.json \
  vocabulary/lexicon/nb/nationalitiesbank.json vocabulary/lexicon/nn/nationalitiesbank.json \
  vocabulary/lexicon/nb/manifest.json vocabulary/lexicon/nn/manifest.json \
  'api/vocab/v3/lookup/[language]/[wordId].js'
git status
git commit -m "feat(lexicon): add languagesbank + nationalitiesbank (Leksihjelp Phase 05.1 Gap B)

- NEW: NB + NN languagesbank.json (20 entries each) — norsk, nynorsk, bokmål, engelsk, tysk, spansk, fransk, italiensk, portugisisk, russisk, arabisk, kinesisk, japansk, polsk, ukrainsk, samisk, islandsk, færøysk, svensk, dansk. Type 'language' avoids polluting downstream nounGenus/plural indexes.
- NEW: NB + NN nationalitiesbank.json (20 entries each) — nordmann, svenske, danske, tysker, englender, brite, amerikaner, + 13 others. Type 'nationality' distinguishes from regular nouns while carrying full genus/plural/definite data.
- MODIFIED: Per-lang manifest.json bank counts updated.
- MODIFIED: api/vocab/v3/lookup TYPE_TO_BANK adds 'language' → 'languagesbank' and 'nationality' → 'nationalitiesbank' fast-path routing.

Cross-app impact: Additive only. Existing *bank.json glob in manifest.js, build-lexicon-search-index.js, and findWordInAllBanks auto-discovers the new banks. Sibling consumers (papertek-webapps, papertek-nativeapps) see more vocabulary, no schema changes. Leksihjelp sync-vocab pipeline will pull new banks in Plan 05.1-05.

Resolves Leksihjelp Phase 5 smoke-test Gap B surfaced by 'skrive på nynorsk' false-flag.

Leksihjelp Phase 05.1 Plan 05.1-02 Gap B."
git push origin main
```

Step 4 — Capture commit SHA for SUMMARY. Note the `search-index.json` regeneration behavior (on-demand vs artefact) for Plan 05's sync-vocab run.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/papertek-vocabulary && grep -c "languagesbank" 'api/vocab/v3/lookup/[language]/[wordId].js'; echo "---"; git log -1 --oneline</automated>
  </verify>
  <done>TYPE_TO_BANK in lookup endpoint contains `'language': 'languagesbank'` and `'nationality': 'nationalitiesbank'`. Single atomic commit pushed to papertek-vocabulary main containing all four bank files + manifests + lookup routing.</done>
</task>

</tasks>

<verification>
Cross-repo data verification:
- All four bank JSON files parse as valid JSON (node `JSON.parse` check)
- Per-lang manifest.json bank counts match actual entries (no drift)
- `*bank.json` glob patterns will discover the new files (inherent to bank-file naming)
- TYPE_TO_BANK routes `_lang` / `_nat` suffixed IDs to the correct banks

End-to-end integration verification deferred to Plan 05 (runs `npm run sync-vocab` in leksihjelp, then exercises `extension/data/{nb,nn}.json` for the new top-level banks + tests dictionary popup POS labels + tests spell-check NOT flagging `nynorsk` / `bokmål`).
</verification>

<success_criteria>
1. Four new bank JSON files (NB + NN × languages + nationalities), each with ≥18 entries.
2. CONTEXT.md-locked language list (norsk, nynorsk, bokmål, engelsk, tysk, spansk, fransk, italiensk, portugisisk, russisk, arabisk, kinesisk, japansk, polsk, ukrainsk, samisk, islandsk, færøysk, svensk, dansk) present in both NB and NN banks.
3. Nationality demonyms present in both NB and NN banks.
4. `type: 'language'` and `type: 'nationality'` — NOT `type: 'noun'`/`'adj'` — so downstream genus/plural indexes in consumers are not polluted.
5. Per-lang manifest.json bank counts updated.
6. TYPE_TO_BANK in v3 lookup endpoint contains the two new type→bank mappings.
7. Single atomic commit pushed to papertek-vocabulary main.
8. No changes to leksihjelp repo (data flows through Plan 05's sync-vocab).
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-close-ux-01-gaps-from-phase-5-smoke-test/05.1-02-SUMMARY.md` with:
- Final entry counts per bank (NB + NN × languages + nationalities)
- Any NN linguistic notes (forms that fell back to NB shape with Språkrådet citation)
- Manifest coordination with Plan 01 (whether the adjectivebank count bump happened before or after)
- Bokmål UTF-8 spot-check result (resolves open question #5)
- Commit SHA in papertek-vocabulary
- Handoff note for Plan 05: after `npm run sync-vocab`, `extension/data/nb.json` and `extension/data/nn.json` should contain top-level `languagesbank` and `nationalitiesbank` objects. Four files in leksihjelp need BANKS list updates (sync-vocab.js, vocab-seam-core.js, popup.js, floating-widget.js) + i18n keys (`pos_language`, `pos_nationality` in nb/nn/en blocks).
</output>
