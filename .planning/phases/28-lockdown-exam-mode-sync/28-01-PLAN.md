---
phase: 28-lockdown-exam-mode-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/geirforbord/Papertek/lockdown/scripts/sync-leksihjelp.js
  - /Users/geirforbord/Papertek/lockdown/public/js/leksihjelp-loader.js
  - /Users/geirforbord/Papertek/lockdown/public/leksihjelp/exam-registry.js
  - /Users/geirforbord/Papertek/lockdown/public/leksihjelp/floating-widget.js
  - /Users/geirforbord/Papertek/lockdown/public/leksihjelp/word-prediction.js
  - /Users/geirforbord/Papertek/lockdown/public/leksihjelp/spell-check.js
  - /Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js
  - /Users/geirforbord/Papertek/leksihjelp/CLAUDE.md
autonomous: false
gap_closure: true
requirements: [EXAM-08]

must_haves:
  truths:
    - "lockdown sync script copies extension/exam-registry.js into public/leksihjelp/exam-registry.js"
    - "leksihjelp-loader.js LEKSI_BUNDLE includes 'leksihjelp/exam-registry.js' before 'leksihjelp/spell-check-core.js'"
    - "After re-running sync, public/leksihjelp/{floating-widget,word-prediction,spell-check}.js all reference examMode (Phase 27 commits present downstream)"
    - "Lockdown writes chrome.storage.local.set({ examModeLocked: true, examMode: true }) when teacher selects an exam-locked resource profile"
    - "End-to-end: exam profile in lockdown → popup shows toggle ON+disabled+lock caption; widget amber border; word-prediction suppressed; grammar-lookup dots gone; typo dots + dictionary remain"
  artifacts:
    - path: "/Users/geirforbord/Papertek/lockdown/scripts/sync-leksihjelp.js"
      provides: "Sync pipeline that now also copies exam-registry.js"
      contains: "exam-registry.js"
    - path: "/Users/geirforbord/Papertek/lockdown/public/js/leksihjelp-loader.js"
      provides: "LEKSI_BUNDLE entry for exam-registry.js"
      contains: "leksihjelp/exam-registry.js"
    - path: "/Users/geirforbord/Papertek/lockdown/public/leksihjelp/exam-registry.js"
      provides: "Synced copy of extension/exam-registry.js (proves sync ran)"
    - path: "/Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js"
      provides: "Teacher-lock writer that sets chrome.storage.local.examModeLocked / examMode"
      contains: "examModeLocked"
  key_links:
    - from: "lockdown sync-leksihjelp.js"
      to: "extension/exam-registry.js"
      via: "explicit fs.copyFileSync of the registry file"
      pattern: "exam-registry\\.js"
    - from: "leksihjelp-loader.js LEKSI_BUNDLE"
      to: "exam-registry.js → spell-check-core.js"
      via: "ordered script injection"
      pattern: "'leksihjelp/exam-registry\\.js'"
    - from: "writing-environment.js exam profile path"
      to: "chrome.storage.local"
      via: "chrome.storage.local.set({ examModeLocked, examMode })"
      pattern: "examModeLocked"
---

<objective>
Close the lockdown half of EXAM-08: extend the sync pipeline to ship `extension/exam-registry.js` to lockdown, prepend it to the loader's bundle so the registry exists before any consumer initialises, refresh the stale Phase 27 content scripts downstream, and wire a teacher-lock writer in lockdown's exam-profile path so the popup's "Slått på av lærer" caption activates and the runtime suppression takes effect.

Purpose: Phase 27 made exam mode work in the extension, but on stb-lockdown.app / papertek.app the registry never arrives, the loader never wires it, the synced content scripts predate Phase 27 (no `examMode` references), and no lockdown code writes `chrome.storage.local.examModeLocked`. Until this plan ships, exam mode in lockdown is functionally broken — the safety net the v3.1 milestone promised schools.

Output: Updated lockdown sync script + loader + writing-environment + a refreshed `public/leksihjelp/` tree containing Phase 27 commits and the new exam-registry, plus a CLAUDE.md note documenting that exam-registry.js is now part of the cross-app sync surface.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/geirforbord/Papertek/leksihjelp/.planning/STATE.md
@/Users/geirforbord/Papertek/leksihjelp/.planning/ROADMAP.md
@/Users/geirforbord/Papertek/leksihjelp/.planning/REQUIREMENTS.md
@/Users/geirforbord/Papertek/leksihjelp/.planning/v3.1-MILESTONE-AUDIT.md
@/Users/geirforbord/Papertek/leksihjelp/CLAUDE.md
@/Users/geirforbord/Papertek/leksihjelp/extension/exam-registry.js

# Lockdown sibling files this plan modifies
@/Users/geirforbord/Papertek/lockdown/scripts/sync-leksihjelp.js
@/Users/geirforbord/Papertek/lockdown/public/js/leksihjelp-loader.js
@/Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js
@/Users/geirforbord/Papertek/lockdown/public/js/writing-test/shared/resource-profile.js

<interfaces>
<!-- Phase 27 contract: every consumer reads exam state from chrome.storage.local
     and from the global registry exposed by extension/exam-registry.js. The
     lockdown shim's chrome.storage.local is in-memory but matches the API
     leksihjelp content scripts expect.

     Manifest order (extension side, from STATE.md):
       exam-registry.js MUST register __lexiExamRegistry on host BEFORE
       floating-widget.js / word-prediction.js / spell-check.js read it.
       Same ordering MUST be enforced in lockdown's LEKSI_BUNDLE; otherwise
       the fail-safe path in those scripts hides every surface even when
       examMode is off.

     Storage keys consumers read (Phase 27-03):
       chrome.storage.local.examMode        (boolean — runtime gate)
       chrome.storage.local.examModeLocked  (boolean — student cannot toggle off)
-->

From extension/exam-registry.js:
  globalThis.__lexiExamRegistry = {
    [surfaceId]: { exam: { safe: boolean, reason: string, category: string } },
    ...
  }

From lockdown/public/js/leksihjelp-loader.js (line ~372):
  const LEKSI_BUNDLE = [
    'leksihjelp/spell-check-core.js',   // ← exam-registry.js must come BEFORE this
    ...
  ];

Lockdown profile resolver (writing-test/shared/resource-profile.js):
  PROFILES.EXAM = 'exam'
  resolveResourceProfile(test, participant) -> 'full'|'exam'|'lexin'|'none'

Boot/teardown call sites in writing-environment.js:
  ~line 908: initial bootLeksihjelp(...) when initialEnvelope.leksihjelp
  ~line 931: re-boot on profile change
  ~line 936: teardownLeksihjelp() on profile change away from full
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend sync-leksihjelp.js to copy exam-registry.js</name>
  <files>/Users/geirforbord/Papertek/lockdown/scripts/sync-leksihjelp.js</files>
  <action>
    After the existing copy block that walks `extension/content/` (which lands files in `public/leksihjelp/`), add an explicit single-file copy step for `extension/exam-registry.js` → `public/leksihjelp/exam-registry.js`.

    Why a separate step: the registry lives at `extension/exam-registry.js` (NOT under `extension/content/`), so the existing recursive walk skips it. Match the file-existence pattern used elsewhere in the script (skip-if-missing rather than throw, so a pre-Phase-27 leksihjelp tree still syncs cleanly).

    Reference implementation:
    ```js
    // Exam registry → public/leksihjelp/exam-registry.js (Phase 28, EXAM-08)
    // Phase 27 introduced extension/exam-registry.js as the source of truth for
    // non-rule exam surfaces. It must be synced as a top-level file because the
    // loader injects it BEFORE spell-check-core.js so __lexiExamRegistry exists
    // before any consumer reads it.
    const registrySrc = path.join(pkgDir, 'extension', 'exam-registry.js');
    if (fs.existsSync(registrySrc)) {
        fs.copyFileSync(registrySrc, path.join(destDir, 'exam-registry.js'));
    }
    ```

    Place this block AFTER the content-script copy loop and BEFORE the `Data files` block, so the console output remains readable.

    Do NOT change the source-resolution logic, the data-file copy, the styles rename, or the i18n copy.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/lockdown &amp;&amp; LEKSIHJELP_LOCAL=/Users/geirforbord/Papertek/leksihjelp node scripts/sync-leksihjelp.js &amp;&amp; test -f public/leksihjelp/exam-registry.js &amp;&amp; echo OK</automated>
  </verify>
  <done>Running the sync script produces `public/leksihjelp/exam-registry.js` whose content matches `extension/exam-registry.js` byte-for-byte.</done>
</task>

<task type="auto">
  <name>Task 2: Prepend exam-registry.js to LEKSI_BUNDLE in leksihjelp-loader.js</name>
  <files>/Users/geirforbord/Papertek/lockdown/public/js/leksihjelp-loader.js</files>
  <action>
    Edit the `LEKSI_BUNDLE` array (around line 372). Insert `'leksihjelp/exam-registry.js'` as the FIRST entry, BEFORE `'leksihjelp/spell-check-core.js'`.

    Replace:
    ```js
    const LEKSI_BUNDLE = [
        'leksihjelp/spell-check-core.js',
        // nb rules
    ```

    With:
    ```js
    const LEKSI_BUNDLE = [
        // Phase 28 / EXAM-08: exam-registry must register __lexiExamRegistry
        // BEFORE any consumer (spell-check, widget, prediction) initialises.
        // Otherwise the fail-safe path in those scripts hides every surface
        // even when examMode is off (see leksihjelp STATE.md Phase 27-03 note).
        'leksihjelp/exam-registry.js',
        'leksihjelp/spell-check-core.js',
        // nb rules
    ```

    Do NOT touch the rest of the bundle, the boot/teardown logic, the chrome shim, or any other part of the loader.
  </action>
  <verify>
    <automated>grep -n "leksihjelp/exam-registry.js" /Users/geirforbord/Papertek/lockdown/public/js/leksihjelp-loader.js | head -1 &amp;&amp; awk '/LEKSI_BUNDLE = \[/,/spell-check-core/' /Users/geirforbord/Papertek/lockdown/public/js/leksihjelp-loader.js | grep -E "exam-registry|spell-check-core"</automated>
  </verify>
  <done>`exam-registry.js` appears in LEKSI_BUNDLE on a line strictly before `spell-check-core.js`.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run sync to refresh stale Phase 27 content scripts</name>
  <files>/Users/geirforbord/Papertek/lockdown/public/leksihjelp/floating-widget.js, /Users/geirforbord/Papertek/lockdown/public/leksihjelp/word-prediction.js, /Users/geirforbord/Papertek/lockdown/public/leksihjelp/spell-check.js, /Users/geirforbord/Papertek/lockdown/public/leksihjelp/exam-registry.js</files>
  <action>
    Run the sync script from the lockdown repo root with `LEKSIHJELP_LOCAL` pointing at this leksihjelp checkout (the Task-1 edit is now in place, so the registry will land alongside the existing files):

    ```bash
    cd /Users/geirforbord/Papertek/lockdown
    LEKSIHJELP_LOCAL=/Users/geirforbord/Papertek/leksihjelp node scripts/sync-leksihjelp.js
    ```

    The audit found the existing synced files mtime 2026-04-27 — before Phase 27 commits 9e99357, d02cc24, 3b9dc25. Re-running with the upgraded source will overwrite them with the Phase 27 versions that include `examMode` runtime gating.

    After the run, verify:
    1. `public/leksihjelp/exam-registry.js` exists (Task 1's new behaviour).
    2. `grep -l examMode public/leksihjelp/*.js` returns at minimum `floating-widget.js`, `word-prediction.js`, `spell-check.js`. (`spell-check-core.js` may also appear — that's fine.)

    If any of those files are missing `examMode`, halt and report — it means the upstream Phase 27 commits did not actually land in `extension/content/` (which would be a leksihjelp-side gap, not a lockdown gap).

    Do NOT hand-edit files under `public/leksihjelp/` — the contents must come exclusively from the sync.
  </action>
  <verify>
    <automated>test -f /Users/geirforbord/Papertek/lockdown/public/leksihjelp/exam-registry.js &amp;&amp; for f in floating-widget.js word-prediction.js spell-check.js; do grep -q examMode "/Users/geirforbord/Papertek/lockdown/public/leksihjelp/$f" || { echo "MISSING examMode in $f"; exit 1; }; done &amp;&amp; echo OK</automated>
  </verify>
  <done>`public/leksihjelp/exam-registry.js` exists and the three listed content scripts contain `examMode` references (proving Phase 27 commits landed downstream).</done>
</task>

<task type="auto">
  <name>Task 4: Wire teacher-lock writer in lockdown exam-profile path</name>
  <files>/Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js</files>
  <action>
    Discover the exact integration point and add a teacher-lock writer that sets `chrome.storage.local.examModeLocked = true` and `chrome.storage.local.examMode = true` whenever the resolved resource profile activates exam-mode-on-leksihjelp behaviour, and clears both flags whenever it does not.

    Discovery steps (do these IN ORDER before editing):
    1. `grep -n "bootLeksihjelp\|teardownLeksihjelp\|resolveResourceProfile\|EXAM\b\|profile === 'exam'\|profile === 'full'" /Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js` to find the existing profile-driven boot/teardown branches (around lines 908, 931, 936 per the STATE-level interfaces note above).
    2. `cat /Users/geirforbord/Papertek/lockdown/public/js/writing-test/shared/resource-profile.js` if needed — confirms the canonical profile values (`full`, `exam`, `lexin`, `none`) and the `getSpellEngineEnvelope()` helper.
    3. Identify the place where `bootLeksihjelp({ profile, ... })` is called and the matching teardown path on profile changes.

    Locking semantics (the part the v3.1 audit flagged):

    The audit's success criterion 4 states: "Lockdown's resource-profile / writing-test resolver writes `chrome.storage.local.set({ examModeLocked: true, examMode: true })` when teacher selects exam profile". In Phase 27-03 the storage flags work as follows: `examModeLocked: true` greys out the popup toggle and shows the "Slått på av lærer" caption; `examMode: true` activates the runtime suppression (widget amber border, grammar-lookup dot suppression, word-prediction suppression). Both are needed together for the lockdown teacher-lock UX.

    Implementation choice — there are two reasonable ways to read "exam profile" here:

    Option A (preferred — matches v3.1 audit phrasing literally): write the lock pair when the resolved profile is `'exam'`. Caveat: today the `'exam'` envelope sets `leksihjelp: false`, so leksihjelp would not boot at all and the runtime gating has nothing to suppress. To make the lock visible, Task 4 must ALSO ensure leksihjelp boots when profile === 'exam' AND the teacher has explicitly opted into "leksihjelp + exam-mode" (i.e. examModeLocked). Wire this as: when profile === 'exam', call `bootLeksihjelp({ profile: 'exam' })` and write the lock pair; when profile transitions away, write `{ examModeLocked: false, examMode: false }` and call `teardownLeksihjelp()`.

    Option B (fallback if Option A breaks the existing exam-profile contract — i.e. tests/locales depend on `'exam'` meaning "browser-spellcheck only, NO leksihjelp"): introduce a new profile-modifier path. Since this is intrusive, prefer Option A unless `grep -rn "profile === 'exam'\|PROFILES.EXAM" /Users/geirforbord/Papertek/lockdown/public/js/writing-test/` reveals contracts that would break.

    PICK ONE OPTION based on the discovery output. Document the choice in a header comment of the new code block, e.g.:

    ```js
    // Phase 28 / EXAM-08: When the resolved resource profile is 'exam', the
    // teacher has opted students into leksihjelp's exam-mode lock. Set the
    // storage flags Phase 27-03 reads, AND boot leksihjelp under that profile
    // so the runtime suppression has something to gate. On profile transitions
    // away from 'exam' (or when leksihjelp tears down), clear the flags.
    function applyExamModeLock(profile) {
        const isExamLocked = (profile === 'exam');
        try {
            window.chrome?.storage?.local?.set?.({
                examModeLocked: isExamLocked,
                examMode: isExamLocked
            });
        } catch (e) {
            console.warn('[28/EXAM-08] failed to write exam-mode flags', e);
        }
    }
    ```

    Then call `applyExamModeLock(profile)` at every site where the profile is resolved or changes (initial boot path ~line 908, profile-change path ~line 931, teardown path ~line 936). The teardown call should pass a non-exam value (e.g. `'full'` or `null`) so the flags clear.

    If Option A requires extending the spell-engine envelope, also touch `/Users/geirforbord/Papertek/lockdown/public/js/writing-test/shared/resource-profile.js` `getSpellEngineEnvelope('exam')` to return `{ leksihjelp: true }`. Be minimal — do not touch the other profile envelopes. Add this file to `files_modified` in your SUMMARY if you go this route.

    Do NOT modify `public/leksihjelp/**` — those are sync targets, not edit targets.
  </action>
  <verify>
    <automated>grep -n "examModeLocked\|examMode" /Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js | head -20 &amp;&amp; node --check /Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js</automated>
  </verify>
  <done>writing-environment.js contains an `examModeLocked` write keyed on the exam profile, the file parses, and grep confirms the lock pair is set+cleared symmetrically across boot/teardown.</done>
</task>

<task type="auto">
  <name>Task 5: Update CLAUDE.md to document the new sync surface</name>
  <files>/Users/geirforbord/Papertek/leksihjelp/CLAUDE.md</files>
  <action>
    In the "Downstream consumer: Papertek Lockdown webapp" section, update the bullet list of synced files to include `extension/exam-registry.js` → `lockdown/public/leksihjelp/exam-registry.js`.

    Find this block:
    ```
    - `extension/content/*.js` → `lockdown/public/leksihjelp/*.js`
    - `extension/styles/content.css` → `lockdown/public/leksihjelp/styles/leksihjelp.css` (renamed)
    - `extension/data/*` → `lockdown/public/leksihjelp/data/`
    - `extension/i18n/*` → `lockdown/public/leksihjelp/i18n/`
    ```

    Add a fifth bullet:
    ```
    - `extension/exam-registry.js` → `lockdown/public/leksihjelp/exam-registry.js` (Phase 28 / EXAM-08 — must be loaded BEFORE spell-check-core.js so `__lexiExamRegistry` exists when consumers initialise)
    ```

    Also add a one-line note under "Implications for changes here" saying that adding new entries to `extension/exam-registry.js` (i.e. new non-rule surfaces) requires re-running the lockdown sync so the downstream registry stays in step.

    Do NOT touch any other CLAUDE.md section.
  </action>
  <verify>
    <automated>grep -n "exam-registry.js" /Users/geirforbord/Papertek/leksihjelp/CLAUDE.md &amp;&amp; grep -c "exam-registry" /Users/geirforbord/Papertek/leksihjelp/CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md "Downstream consumer" section lists exam-registry.js as a synced file and notes the load-order requirement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: End-to-end browser verification of lockdown exam-mode lock</name>
  <files>(none — human verification of running browser state)</files>
  <what-built>
    Lockdown sync now copies exam-registry.js, the loader bundles it before spell-check-core, the synced content scripts include Phase 27 examMode gating, and writing-environment writes `chrome.storage.local.examModeLocked` + `examMode` when the exam profile is active.
  </what-built>
  <action>Human verification only — Tasks 1-5 already shipped the code. The user loads lockdown locally, switches the resource profile to `exam`, and confirms the Phase 27 lock UX (toggle disabled+ON+caption, EKSAMENMODUS badge, amber widget border, suppressed grammar-lookup dots and prediction, retained typo dots and dictionary lookups). See `<how-to-verify>` for the exact steps.</action>
  <verify>Manual — see `<how-to-verify>` block. The user types "approved" or describes deviation.</verify>
  <done>User explicitly approves all expected behaviours from `<how-to-verify>`, or the deviation is captured for follow-up.</done>
  <how-to-verify>
    1. From `/Users/geirforbord/Papertek/lockdown`, start the local dev server (the way you normally do for lockdown — typically `npm run dev` or open `public/elev.html` via the lockdown harness).
    2. Create or open a writing test, then in the teacher dashboard set the resource profile to **Eksamen-modus** (`exam`).
    3. Join the test as a student (or use the existing pretest harness).
    4. Open the leksihjelp popup (Fest button or popup target — whatever the lockdown shim exposes).
       - **Expected:** Exam-mode toggle is shown ON, disabled (greyed), with the caption "Slått på av lærer" (Phase 27 EXAM-03).
       - **Expected:** "EKSAMENMODUS" badge visible near the logo (EXAM-05).
    5. Hover the floating widget on the writing area.
       - **Expected:** Widget shows the amber border tint (EXAM-05).
    6. Type a word that would normally trigger word-prediction (e.g. start typing "huse" in an NB writing area).
       - **Expected:** Prediction dropdown does NOT open (EXAM-04).
    7. Type a sentence that would trigger a grammar-lookup rule (e.g. a `de-prep-case` sentence if the test language is German; for NB use a `nb-modal-verb` trigger).
       - **Expected:** No spell-check dot for the grammar rule appears (EXAM-04 — grammar-lookup rules are exam.safe=false).
       - **Expected:** Typo dots (e.g. `nb-typo-curated`) STILL appear — those are exam.safe=true.
       - **Expected:** Dictionary lookup via the floating widget STILL works — also exam-safe.
    8. Switch the test profile back to `full`. Reload the student view.
       - **Expected:** Toggle becomes interactive again, "Slått på av lærer" caption gone, EKSAMENMODUS badge gone, widget amber border gone, prediction dropdown reappears, grammar-lookup dots reappear.
    9. Open DevTools console; check for any `[28/EXAM-08]` warnings or `__lexiExamRegistry undefined` errors. There should be none.

    If any of steps 4-8 fail, describe what you saw and which expectation broke. The most likely failure modes:
    - Toggle interactive (not locked) → Task 4 wiring missed the storage write or wrote on the wrong profile.
    - All surfaces hidden including typo dots → exam-registry.js loaded AFTER consumers (Task 2 ordering wrong) or sync didn't run (Task 3).
    - Lock caption shows but widget/prediction still active → synced content scripts predate Phase 27 (Task 3 didn't refresh them — verify mtime).
  </how-to-verify>
  <resume-signal>Type "approved" if all expected behaviours match. Otherwise describe the deviation.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `test -f /Users/geirforbord/Papertek/lockdown/public/leksihjelp/exam-registry.js` succeeds
- [ ] `grep -n "leksihjelp/exam-registry.js" /Users/geirforbord/Papertek/lockdown/public/js/leksihjelp-loader.js` returns a hit on a line strictly above `'leksihjelp/spell-check-core.js'`
- [ ] `for f in floating-widget.js word-prediction.js spell-check.js; do grep -q examMode "/Users/geirforbord/Papertek/lockdown/public/leksihjelp/$f"; done` exits 0
- [ ] `grep -n "examModeLocked" /Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js` returns at least one hit
- [ ] `node --check /Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js` passes
- [ ] `node --check /Users/geirforbord/Papertek/lockdown/public/js/leksihjelp-loader.js` passes
- [ ] `grep -n "exam-registry.js" /Users/geirforbord/Papertek/leksihjelp/CLAUDE.md` returns a hit
- [ ] Task 6 human-verify completes with "approved"
</verification>

<success_criteria>
1. Sync script copies `extension/exam-registry.js` → `public/leksihjelp/exam-registry.js`.
2. `LEKSI_BUNDLE` injects `exam-registry.js` before `spell-check-core.js`.
3. Re-running sync produces `public/leksihjelp/{floating-widget,word-prediction,spell-check}.js` that contain `examMode` references (Phase 27 commits propagated).
4. Lockdown's exam-profile path writes `{ examModeLocked: true, examMode: true }` to `chrome.storage.local` and clears them on profile transition away.
5. End-to-end (human-verify): exam profile in lockdown shows the Phase 27 lock UX (toggle ON+disabled+caption, EKSAMENMODUS badge, amber widget border, suppressed grammar-lookup dots and prediction dropdown, while typo dots and dictionary lookups remain).
</success_criteria>

<output>
After completion, create `.planning/phases/28-lockdown-exam-mode-sync/28-01-SUMMARY.md` documenting:
- Which option (A or B from Task 4) was chosen and why
- Whether `getSpellEngineEnvelope('exam')` was modified
- File mtimes before/after the sync re-run (proof Phase 27 propagated)
- Any DevTools console output observed during human verify
- Outstanding follow-ups (e.g. lockdown-side version pin bump, if applicable)
</output>
