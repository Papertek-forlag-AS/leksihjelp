---
phase: 34-v3.1-browser-uat-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34-v3.1-browser-uat-sweep/34-VERIFICATION.md
autonomous: false
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Lockdown exam-mode E2E walkthrough completed on staging-lockdown with documented pass/fail per sub-check (lock UI, amber border, prediction suppression, grammar-dot suppression, typo dots remain, dictionary remains, profile transition clears flags, dev-button regression)"
    - "Lockdown sidepanel dictionary parity verified inside EKSAMENMODUS: ordbok tab visible, full conjugations/declensions render, language switcher works, direction toggle works, no audio buttons render"
    - "All six Phase 26 Lær mer DE walkthroughs executed and documented (dativ badge colour, Wechsel layout wide+narrow, Esc collapse, NN locale, EN locale, Tab nav state reset)"
    - "All three Phase 32 walkthroughs executed and documented (FR fr-aspect-hint pedagogy.aspect_choice render, ES por/para data-sourced render, ES gustar-class render across extended verbs)"
    - "Every UAT finding either landed as an in-flight fix in this session OR filed as a discrete follow-up phase entry — no `human_needed` deferrals roll forward into v3.1 archive"
    - "VERIFICATION.md at .planning/phases/34-v3.1-browser-uat-sweep/34-VERIFICATION.md committed with one section per surface, screenshots/notes per sub-check, and an explicit pass/fail line per criterion"
  artifacts:
    - path: ".planning/phases/34-v3.1-browser-uat-sweep/34-VERIFICATION.md"
      provides: "Consolidated UAT report covering all 6 roadmap success criteria"
      contains: "## Lockdown Exam-Mode E2E"
  key_links:
    - from: "VERIFICATION.md"
      to: "ROADMAP.md Phase 34 success criteria"
      via: "explicit pass/fail line per criterion (1-6)"
      pattern: "Criterion [1-6].*: (PASS|FAIL|PARTIAL)"
    - from: "Findings during UAT"
      to: "ROADMAP.md (new phase entry) OR commit during session"
      via: "no `human_needed` rollforward — every finding has a disposition"
      pattern: "Disposition:.*(in-flight fix .* commit|new phase .* entry|no action needed)"
---

<objective>
Run the consolidated v3.1 browser UAT walkthrough in one human-driven session and author 34-VERIFICATION.md so v3.1 can be archived without `human_needed` deferrals rolling forward.

Purpose: Phase 33 closed the v3.1 audit's code/data debt; this phase closes the *flow gap* — manual browser walkthroughs that have piled up across Phases 26, 29-03, 30, and 32 because each phase's own verification was deferred (auto-mode shipped without UAT). One QA session, one report.

Output: 34-VERIFICATION.md with pass/fail per roadmap success criterion (1-6), screenshots/notes per sub-check, and a disposition line for every finding (in-flight fix landed | new phase filed | no action). Any in-flight fixes ship as ordinary commits during the session.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md

# Phase 26 deferred walkthrough script
@.planning/phases/26-laer-mer-pedagogy-ui/26-VERIFICATION.md

# Phase 29-03 deferred (rolled into Phase 30)
@.planning/phases/29-03-SUMMARY.md

# Phase 30-02 staging-lockdown UAT script
@.planning/phases/30-shared-popup-views/30-02-SUMMARY.md

# Phase 32 SUMMARYs (FR aspect / ES por-para / ES gustar)
@.planning/phases/32-fr-es-pedagogy/32-01-SUMMARY.md
@.planning/phases/32-fr-es-pedagogy/32-02-SUMMARY.md
@.planning/phases/32-fr-es-pedagogy/32-03-SUMMARY.md

<scope_note>
This is a manual UAT phase. Tasks below are walkthrough scripts the human executor follows at the keyboard. The Claude executor's job is to:
1. Open the appropriate environment (extension reload, staging-lockdown URL, etc.) per task
2. Drive the human through each sub-check
3. Record observations + screenshots into 34-VERIFICATION.md
4. Triage findings into one of three buckets:
   - **In-flight fix** (small, clear cause, ≤2 files): patch + commit during the session, re-verify, mark PASS
   - **Discrete follow-up phase** (architectural, multi-file, or needs research): add a new phase entry to ROADMAP.md and link from VERIFICATION.md
   - **No action needed** (working as designed): annotate in VERIFICATION.md
5. NO release-gate run is required as part of this phase unless an in-flight fix is made (Release Workflow steps 1-11 only run when extension/ files change). If a fix lands, run the relevant gates + bump version per CLAUDE.md.

Environment setup the human handles:
- Extension reload from `extension/` unpacked in chrome://extensions
- Staging-lockdown access: `https://staging-lockdown.web.app` (or local lockdown-stb redirect) — credentials are in the user's password manager; do NOT prompt for them
- Test data: a teacher account that can create a LEKSIHJELP_EXAM test, plus a student account
</scope_note>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Lockdown exam-mode E2E + sidepanel dictionary parity (criteria 1-2)</name>
  <files>.planning/phases/34-v3.1-browser-uat-sweep/34-VERIFICATION.md</files>
  <what-built>
    Phase 27 exam-mode runtime gating (popup toggle + lockdown lock + amber border + prediction/grammar suppression) merged with Phase 30 shared sidepanel views (dictionary + settings) running on staging-lockdown.
  </what-built>
  <action>
    Drive the human through the staging-lockdown walkthrough described in `<how-to-verify>` below. For each sub-check, capture observation + screenshot and append a PASS/FAIL/PARTIAL line to `34-VERIFICATION.md` under sections `## 1` and `## 2`. For any FAIL, triage per the `<scope_note>` (in-flight fix | new phase | no action) and record disposition.
  </action>
  <how-to-verify>
    Driver opens staging-lockdown in two browser windows (teacher + student). For each sub-check below, observe the surface, capture a screenshot, and record PASS/FAIL with one-line note in `34-VERIFICATION.md` under `## 1. Lockdown Exam-Mode E2E (criterion 1)` and `## 2. Lockdown Sidepanel Dictionary Parity (criterion 2)`.

    **Criterion 1 sub-checks (8):**
    1.1 Teacher creates a test with profile `LEKSIHJELP_EXAM` ("Eksamen med Leksihjelp"). Test saves without firestore-rules error.
    1.2 Student joins. Leksihjelp side panel opens; popup-equivalent settings show exam-mode toggle ON + disabled + caption "Slått på av lærer" (or NN/EN equivalent per uiLanguage).
    1.3 Floating widget on selected text gains amber border tint.
    1.4 Word-prediction dropdown does NOT open while typing in the writing area.
    1.5 Spell-check grammar-lookup dots are suppressed; typo dots and dictionary lookups remain functional.
    1.6 Teacher transitions student off the LEKSIHJELP_EXAM profile → `chrome.storage.local.examModeLocked` and `examMode` both clear; toggle re-enables; amber border drops; prediction re-enables.
    1.7 Phase 28 dev-only button "Simuler lærer-lås" (in extension popup, dev mode) still works on a vanilla extension load (separate window): toggling it sets the flags and the same suppressions appear in extension context.
    1.8 No JS errors in DevTools console across the run (record any non-trivial warnings).

    **Criterion 2 sub-checks (5):**
    2.1 Inside EKSAMENMODUS, the ordbok (dictionary) tab IS visible in the sidepanel — Phase 33 fixed the B-blocker that previously hid it.
    2.2 Search a DE noun (e.g. "Schule"); full declension table renders (article, plural, cases per enabled grammar features).
    2.3 Search a DE verb (e.g. "sein"); full conjugation table renders (presens, preteritum, perfektum per enabled features).
    2.4 Language switcher cycles through nb/de/es/fr/en; results re-populate per language.
    2.5 Direction toggle (e.g. de→nb vs nb→de) reflects the actual selected pair (Phase 33 fixed it being hardcoded to ES).
    2.6 NO audio play buttons render in any view (lockdown sidepanel passes audioEnabled:false).

    For any FAIL, capture: error text, screenshot, what was expected, what happened. Triage into in-flight fix vs new phase per `<scope_note>` above and record disposition in VERIFICATION.md.
  </how-to-verify>
  <verify>
    `34-VERIFICATION.md` contains ## 1 and ## 2 sections with one entry per sub-check (1.1-1.8, 2.1-2.6), each annotated PASS/FAIL/PARTIAL plus disposition line for any FAIL.
  </verify>
  <done>
    All 14 sub-checks (8+6) recorded; every FAIL has a disposition (in-flight fix commit ref | new phase entry | no action needed); criteria 1 and 2 each have a final pass/fail line at the end of their section.
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 26 Lær mer DE walkthroughs (criterion 3, 6 sub-checks)</name>
  <files>.planning/phases/34-v3.1-browser-uat-sweep/34-VERIFICATION.md</files>
  <what-built>
    Phase 26 "Lær mer" pedagogy panel — DE preposition spell-check popover with case badges, summary, correct/incorrect example pair, Wechselpräposition motion-vs-location pair, colloquial_note aside, and trilingual nb/nn/en strings.
  </what-built>
  <action>
    Load the leksihjelp extension unpacked (vanilla, exam mode OFF) and drive the human through the 7 DE Lær mer sub-checks in `<how-to-verify>` below. Append entries 3.1-3.7 to `34-VERIFICATION.md` under `## 3`, each annotated PASS/FAIL/PARTIAL with disposition for any FAIL.
  </action>
  <how-to-verify>
    Driver loads the leksihjelp extension (vanilla, NOT lockdown — exam mode OFF) on a regular page with a DE text input. For each sub-check, trigger the surface, observe, screenshot, and record PASS/FAIL with one-line note in `34-VERIFICATION.md` under `## 3. Phase 26 Lær mer DE Walkthroughs (criterion 3)`.

    **Criterion 3 sub-checks (6) — all on `de-prep-case` rule:**
    3.1 **Dativ badge colour:** Type a sentence that triggers a Dativ-required preposition flag (e.g. "Ich gehe zu Schule" — should flag missing dat. article). Click "Lær mer". Verify the case badge in the panel is the dativ colour token (not accusative or genitive).
    3.2 **Wechsel pair side-by-side (wide):** Type a sentence with a Wechselpräposition (e.g. "Ich lege das Buch in den Tisch"). On a wide-enough text input/page, the motion vs location pair renders side-by-side.
    3.3 **Wechsel pair stacked (narrow):** Repeat 3.2 in a narrow text input (or resize). Pair stacks vertically.
    3.4 **Esc collapses panel:** With Lær mer panel open, press Esc. Panel collapses (popover may stay open or close per existing behaviour — record actual).
    3.5 **NN locale:** Set `chrome.storage.local.uiLanguage = 'nn'` via DevTools; reload; trigger Lær mer; panel text reads NN strings (case label, summary, examples).
    3.6 **EN locale:** Same as 3.5 with `uiLanguage = 'en'`. Panel text reads EN strings.
    3.7 **Tab nav resets state:** Open Lær mer on marker A. Press Tab to advance to marker B. The panel should close (or reset) so marker B's popover starts in collapsed state, not carrying marker A's open panel.

    Note: 3.7 brings the count to 7 — the roadmap says "6/6" but lists 6 distinct items; we count 3.7 as a sub-bullet of 3.1-3.6 collectively (it's the "Tab nav between markers resets panel state" item). Record all 7.

    For any FAIL, triage per `<scope_note>` and record disposition.
  </how-to-verify>
  <verify>
    `34-VERIFICATION.md` ## 3 section contains entries 3.1-3.7, each with PASS/FAIL/PARTIAL + disposition for FAILs.
  </verify>
  <done>
    All 7 sub-checks recorded; FAILs have dispositions; criterion 3 has a final pass/fail line.
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 32 FR/ES pedagogy walkthroughs + finalise VERIFICATION.md (criteria 4-6)</name>
  <files>.planning/phases/34-v3.1-browser-uat-sweep/34-VERIFICATION.md, .planning/ROADMAP.md (only if a finding files a new phase)</files>
  <what-built>
    Phase 32 cross-repo pedagogy: FR `fr-aspect-hint` rule + first FR pedagogy block; ES `es-por-para` migrated from inline strings to data; ES `es-gustar` class extended to ~10 verbs with shared pedagogy block.
  </what-built>
  <action>
    Drive the human through the 3 FR/ES pedagogy sub-checks (4.1-4.3) in `<how-to-verify>` below, then finalise `34-VERIFICATION.md` by adding `## 5. Disposition Roll-Up` and `## 6. Final Verdict` sections per the criterion 5-6 instructions. If any in-flight fixes shipped during the session, run the release-gate sweep + version bump per CLAUDE.md Release Workflow.
  </action>
  <how-to-verify>
    Driver loads the leksihjelp extension (vanilla, exam mode OFF). For each sub-check trigger the rule, click "Lær mer", observe, screenshot, record PASS/FAIL in `34-VERIFICATION.md` under `## 4. Phase 32 FR/ES Pedagogy Walkthroughs (criterion 4)`.

    **Criterion 4 sub-checks (3):**
    4.1 **FR aspect-hint:** Type a passé-composé sentence in a context that should suggest imparfait (e.g. "Quand j'étais petit, j'ai mangé tous les jours" — `j'ai mangé` flagged as aspect-hint). Click Lær mer. Panel renders `pedagogy.aspect_choice` content sourced from synced `extension/data/fr.json` (summary, explanation, contrastive example pair).
    4.2 **ES por/para:** Type "Estudio español por aprender" (should flag por→para for purpose). Click Lær mer. Panel renders pedagogy from `por_prep.pedagogy` / `para_prep.pedagogy` in synced `extension/data/es.json` (NOT inline strings — verify by checking subtype-specific text e.g. "purpose").
    4.3 **ES gustar-class extended:** Type one sentence per extended verb where the gustar-class rule should fire (encantar / interesar / doler / faltar — pick 2-3). For each, click Lær mer; panel renders the shared `pedagogy.gustar_class` block (same shape across verbs).

    **Criterion 5 (process check):** While running tasks 1-3, every FAIL has been classified in-flight-fix vs new-phase. After Task 3 sub-checks, scan VERIFICATION.md and confirm zero entries are marked "human_needed" or "deferred". Add a `## 5. Disposition Roll-Up` section listing all FAIL entries with their disposition. If any in-flight fixes shipped, list commit refs. If any new phases filed, list ROADMAP.md phase entry IDs.

    **Criterion 6 (artifact check):** VERIFICATION.md must contain:
    - `## 1. Lockdown Exam-Mode E2E` (PASS/FAIL line at end)
    - `## 2. Lockdown Sidepanel Dictionary Parity` (PASS/FAIL line at end)
    - `## 3. Phase 26 Lær mer DE Walkthroughs` (PASS/FAIL line at end)
    - `## 4. Phase 32 FR/ES Pedagogy Walkthroughs` (PASS/FAIL line at end)
    - `## 5. Disposition Roll-Up`
    - `## 6. Final Verdict` — explicit per-criterion pass/fail in a short table; one-line summary of v3.1 archive readiness.

    If any in-flight fix landed during the session, also run the release-gate sweep per CLAUDE.md (`npm run check-fixtures`, `check-explain-contract`, `check-rule-css-wiring`, `check-spellcheck-features`, `check-network-silence`, `check-exam-marker`, `check-popup-deps`, `check-bundle-size`, `check-baseline-bundle-size`, `check-benchmark-coverage`, `check-governance-data`, `check-pedagogy-shape`) and bump version (manifest.json + package.json + backend/public/index.html).
  </how-to-verify>
  <verify>
    `34-VERIFICATION.md` contains all six sections (## 1 through ## 6) populated. Final Verdict table has 6 rows (criteria 1-6) each marked PASS/FAIL. Zero `human_needed` deferrals. If fixes shipped, release gates pass and version is bumped.
  </verify>
  <done>
    VERIFICATION.md is complete with explicit per-criterion pass/fail; every FAIL has a disposition; v3.1 can be archived (or a clear blocker is documented with the new phase that addresses it).
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Phase-level checks:
- [ ] `34-VERIFICATION.md` exists at the planned path
- [ ] All 6 roadmap success criteria have explicit PASS/FAIL/PARTIAL annotations
- [ ] Zero `human_needed` deferrals — every FAIL has a disposition (in-flight fix | new phase | no action)
- [ ] If in-flight fixes landed, all 12 release-gates exit 0 and version is bumped per CLAUDE.md Release Workflow
- [ ] If new phases filed, they appear in ROADMAP.md under v3.1 (or as deferred entries)
</verification>

<success_criteria>
Mirrors the 6 roadmap success criteria for Phase 34:

1. Lockdown exam-mode E2E walkthrough on staging-lockdown — all 8 sub-checks documented, PASS or disposition.
2. Lockdown sidepanel dictionary parity — all 6 sub-checks documented, PASS or disposition.
3. Phase 26 Lær mer DE — all 7 sub-checks documented, PASS or disposition.
4. Phase 32 FR/ES — all 3 sub-checks documented, PASS or disposition.
5. Findings disposition: zero `human_needed` rollforward — every FAIL has an in-flight fix commit ref OR a new phase entry OR a "no action needed" annotation.
6. VERIFICATION.md authored at `.planning/phases/34-v3.1-browser-uat-sweep/34-VERIFICATION.md` with screenshots/notes per surface and explicit pass/fail per criterion.
</success_criteria>

<output>
After completion, create `.planning/phases/34-v3.1-browser-uat-sweep/34-01-SUMMARY.md` summarising:
- Per-criterion verdict (table)
- Findings landed as in-flight fixes (commit refs)
- Findings filed as new phases (ROADMAP.md entry IDs)
- v3.1 archive readiness statement
</output>
