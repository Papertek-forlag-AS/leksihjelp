---
phase: 25-ux-polish-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/manifest.json
  - package.json
  - backend/public/index.html
  - extension/content/vocab-seam.js
  - extension/content/word-prediction.js
autonomous: true
requirements: [DEBT-01, DEBT-05, SPELL-03]

must_haves:
  truths:
    - "extension/manifest.json, package.json, and backend/public/index.html all show the same version string"
    - "vocab-seam.js no longer references nn or en in BUNDLED_LANGS (only the actually-bundled baseline remains, if any)"
    - "Word prediction dropdown does not appear after 1 or 2 characters; appears only at 3+ characters for every supported language"
  artifacts:
    - path: "extension/manifest.json"
      provides: "Aligned version field"
      contains: "\"version\""
    - path: "package.json"
      provides: "Aligned version field"
      contains: "\"version\""
    - path: "backend/public/index.html"
      provides: "Aligned version display in landing page"
      contains: "Versjon"
    - path: "extension/content/vocab-seam.js"
      provides: "Cleaned BUNDLED_LANGS without stale nn/en entries"
      contains: "BUNDLED_LANGS"
    - path: "extension/content/word-prediction.js"
      provides: "3-character minimum gate for prediction dropdown"
      contains: "minChars"
  key_links:
    - from: "extension/content/word-prediction.js"
      to: "currentWord.length check"
      via: "minChars constant or comparison"
      pattern: "length\\s*>=\\s*3"
---

<objective>
Knock out three low-risk, single-file tech-debt items in one plan: align version numbers across the three sources of truth, remove stale BUNDLED_LANGS entries, and raise the word-prediction threshold to 3 characters.

Purpose: Quick wins that unblock release hygiene (DEBT-01) and polish a long-standing UX papercut (SPELL-03). DEBT-05 is a code-clarity fix carried over from Phase 23 data migration.

Output: Aligned version strings, cleaner vocab-seam constant, prediction dropdown that only fires at 3+ chars.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@extension/content/vocab-seam.js
@extension/content/word-prediction.js

<interfaces>
From extension/content/vocab-seam.js (current state):
```js
const BUNDLED_LANGS = ['nb', 'nn', 'en'];
// Used as: if (!BUNDLED_LANGS.includes(lang)) return null;
// Phase 23 removed bundled nn.json and en.json — only the NB baseline ships.
// The list should reflect what is actually in extension/data/ as a baseline file.
```

From extension/content/word-prediction.js (current state, ~line 496):
```js
const minChars = (currentLang === 'nb' || currentLang === 'nn' || currentLang === 'de') ? 4 : 3;
if (currentWord && currentWord.length >= minChars) {
```

Current versions:
- extension/manifest.json: 2.5.0
- package.json: 2.5.0
- backend/public/index.html: "Versjon 2.5.0"
All three already align at 2.5.0 today, but per CLAUDE.md release workflow, this plan bumps to 2.6.0 to publish v3.1 and locks in the alignment as a verifiable artifact. (STATE.md flagged 2.5.0 vs 2.4.1 skew historically; index.html and manifest already caught up.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump versions to 2.6.0 in all three sources of truth</name>
  <files>extension/manifest.json, package.json, backend/public/index.html</files>
  <action>
Update the version string to "2.6.0" in all three files:
1. extension/manifest.json — the top-level "version" field
2. package.json — the top-level "version" field
3. backend/public/index.html — find the `<p class="version">Chrome, Edge og Brave &bull; Versjon 2.5.0</p>` line and update to 2.6.0

Do not change anything else in these files. The 2.6.0 bump publishes v3.1 (Phases 24+25) and provides a verifiable artifact that DEBT-01 is closed.
  </action>
  <verify>
    <automated>grep -h '"version"' extension/manifest.json package.json | sort -u | wc -l | grep -q '^1$' && grep -q "Versjon 2.6.0" backend/public/index.html</automated>
  </verify>
  <done>All three files show version 2.6.0; the verify command exits 0 (single unique version string between manifest+package, and landing page matches).</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale BUNDLED_LANGS entries (DEBT-05)</name>
  <files>extension/content/vocab-seam.js</files>
  <action>
Open extension/content/vocab-seam.js. Locate `const BUNDLED_LANGS = ['nb', 'nn', 'en'];` (~line 61).

Phase 23 removed extension/data/nn.json and extension/data/en.json from the bundled-zip path; only the NB baseline ships. Replace the constant with `const BUNDLED_LANGS = ['nb'];` so callers correctly fall through for nn/en/de/es/fr to the IndexedDB/fetch path instead of fruitlessly trying to read a non-existent bundled file.

Verify by inspecting the consumer site (`if (!BUNDLED_LANGS.includes(lang)) return null;` ~line 123) — the change should preserve the early-return semantics for any non-baseline language. Do NOT change any other line. If a comment near the constant references `nn`/`en` as bundled, update it to reflect the post-Phase-23 reality (NB-only baseline).
  </action>
  <verify>
    <automated>grep -n "BUNDLED_LANGS" extension/content/vocab-seam.js | grep -v "'nn'" | grep -v "'en'" | grep -q "'nb'"</automated>
  </verify>
  <done>BUNDLED_LANGS contains only 'nb'; nn and en are gone from the array literal.</done>
</task>

<task type="auto">
  <name>Task 3: Raise word-prediction threshold to 3 chars across all languages (SPELL-03)</name>
  <files>extension/content/word-prediction.js</files>
  <action>
Open extension/content/word-prediction.js. Locate the existing `minChars` computation (~line 496):

```js
const minChars = (currentLang === 'nb' || currentLang === 'nn' || currentLang === 'de') ? 4 : 3;
if (currentWord && currentWord.length >= minChars) {
```

Replace the conditional with a flat `const minChars = 3;` so the dropdown never appears at 1 or 2 chars in any language. The existing `>= minChars` guard already enforces the threshold; do not duplicate the check elsewhere.

Search the rest of the file for any other length-based gating (e.g., `currentWord.length >= 2`, `length > 1`, etc.) that could let the dropdown surface at <3 chars. If found, raise to 3 to keep the contract. Do not lower the existing 4-char gate for nb/nn/de back below 3 — the requirement is "3+ chars minimum", not "exactly 3".

Per CLAUDE.md SC-06 (network silence), do NOT add any fetch/http reference; this is a pure threshold change.
  </action>
  <verify>
    <automated>grep -n "minChars" extension/content/word-prediction.js | head -5 && npm run check-network-silence</automated>
  </verify>
  <done>minChars is uniformly 3; check-network-silence still exits 0; dropdown contract documented at the single threshold site.</done>
</task>

</tasks>

<verification>
After all 3 tasks, run from repo root:
- `grep '"version"' extension/manifest.json package.json` — both show 2.6.0
- `grep "Versjon 2.6.0" backend/public/index.html` — matches
- `grep "BUNDLED_LANGS" extension/content/vocab-seam.js` — array has only 'nb'
- `grep "minChars" extension/content/word-prediction.js` — single flat assignment to 3
- `npm run check-network-silence` — exits 0
- `npm run check-fixtures` — does not regress (still produces the same pre-existing 5-suite failures, addressed by 25-04)
</verification>

<success_criteria>
- DEBT-01: All three version strings aligned to 2.6.0
- DEBT-05: BUNDLED_LANGS reflects post-Phase-23 reality (NB-only)
- SPELL-03: Prediction dropdown gated at 3+ chars uniformly
- No regressions in network-silence gate
</success_criteria>

<output>
After completion, create `.planning/phases/25-ux-polish-tech-debt/25-01-SUMMARY.md`
</output>
