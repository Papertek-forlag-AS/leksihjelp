---
phase: 25-ux-polish-tech-debt
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures
  - extension/content/spell-rules
  - scripts/check-fixtures.js
autonomous: true
requirements: [DEBT-02]

must_haves:
  truths:
    - "`npm run check-fixtures` exits 0 on main"
    - "Each of the 5 pre-existing failing suites is either (a) fixed so it passes at P=R=F1=1.0, or (b) explicitly quarantined with a documented expected-failure marker recognised by the gate"
    - "No quarantined suite is silently skipped — the gate prints which suites are quarantined and why on every run"
  artifacts:
    - path: "scripts/check-fixtures.js"
      provides: "Honors a quarantine marker (e.g., expected-failure annotation in fixture file or sidecar config)"
      contains: "quarantine"
    - path: ".planning/phases/25-ux-polish-tech-debt/25-04-TRIAGE.md"
      provides: "Per-suite triage report (fixed vs quarantined vs root cause)"
  key_links:
    - from: "scripts/check-fixtures.js"
      to: "quarantine list"
      via: "explicit allow-list with rationale"
      pattern: "quarantine|expected.failure"
---

<objective>
Make `npm run check-fixtures` exit 0 on main by triaging the five pre-existing failing suites:
- nb/homophone (P=0.857)
- nb/saerskriving (P=0.982)
- nn/typo (P=0.958)
- de/doc-drift-de-address (P=0.804)
- de/verb-final (P=0.981)

Each suite is either fixed (preferred — small precision gaps suggest single false positives) or quarantined with a written rationale tied to a deferred-data ticket on papertek-vocabulary.

Purpose: DEBT-02 unblocks the release workflow. Until check-fixtures exits 0, every release ships under "ignore the failing gate" — a habit that erodes the rest of the gate suite over time.

Output: Green check-fixtures gate + a 25-04-TRIAGE.md summarising what was fixed vs quarantined.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@scripts/check-fixtures.js

<interfaces>
Current failing suites (from `npm run check-fixtures` baseline):
- [nb/homophone] P=0.857 R=1.000 F1=0.923  5/6 pass — 1 false positive
- [nb/saerskriving] P=0.982 R=1.000 F1=0.991  86/87 pass — 1 false positive
- [nn/typo] P=0.958 R=1.000 F1=0.979  20/21 pass — 1 false positive
- [de/doc-drift-de-address] P=0.804 R=1.000 F1=0.892  42/51 pass — 9 false positives
- [de/verb-final] P=0.981 R=1.000 F1=0.990  140/141 pass — 1 false positive

All have R=1.000 (no missed expectations) — failures are unexpected findings = false positives. This is the easier class to triage: each FP is either (a) a fixture line that should also be marked as expected, (b) a rule heuristic that needs tightening, or (c) a data gap in papertek-vocabulary that warrants quarantine until the data ships.

CLAUDE.md release workflow Step 1: "If anything fails, fix the rule (or the fixture, if the expected answer was wrong) and re-run until exit is 0." This plan operationalises that.

CLAUDE.md "data-logic separation philosophy": prefer fixing data at papertek-vocabulary over hardcoding lists in spell-rules — but for the FP class here, the right fix is usually a rule-side tightening or a fixture-side annotation (the false positive should also be expected, OR the rule should not fire there).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage and fix or quarantine the 5 failing fixture suites</name>
  <files>test/fixtures/**/*, extension/content/spell-rules/**/*, scripts/check-fixtures.js, .planning/phases/25-ux-polish-tech-debt/25-04-TRIAGE.md</files>
  <action>
For each of the 5 failing suites:

1. Run `npm run check-fixtures` and capture the per-line FP detail. The runner already prints which line and what unexpected finding fired — extract that into a working list.

2. Classify each FP into one of three buckets:
   - **rule fix** — the rule is genuinely too aggressive; tighten the heuristic in extension/content/spell-rules/<rule>.js (must not regress other fixtures).
   - **fixture fix** — the fixture's expected list was wrong (e.g., the rule SHOULD fire there but the expectation forgot to include it). Update the fixture's expected annotations.
   - **quarantine** — the FP is rooted in papertek-vocabulary data that we do not control in this phase (e.g., missing pitfall metadata); document the upstream gap and quarantine the single fixture LINE (not the whole suite) where possible. If line-level quarantine is not supported by check-fixtures.js today, add it.

3. **Quarantine mechanism.** If `scripts/check-fixtures.js` doesn't yet support quarantine, add a minimal mechanism:
   - Recognise an inline annotation in fixture files (e.g., `# QUARANTINE: <ticket-or-rationale>` on the same line as the input), and exclude that line from the precision/recall accounting.
   - On every run, print a `[QUARANTINED]` summary block listing each quarantined line and its rationale, so the gate is loud about what's being skipped.
   - Update `npm run check-fixtures` script (if needed) to still exit 0 when only quarantined lines fail.

4. Write `.planning/phases/25-ux-polish-tech-debt/25-04-TRIAGE.md` documenting per-suite outcome:
   ```markdown
   # 25-04 Fixture Triage

   ## nb/homophone
   - 1 FP at line N: <input>
   - Disposition: rule-fix | fixture-fix | quarantine
   - Action: ...
   - Quarantine rationale (if applicable): <ticket / data gap>

   ## nb/saerskriving
   ...
   ```

5. Re-run `npm run check-fixtures` until it exits 0. Then re-run all other release-workflow gates from CLAUDE.md to ensure no regression:
   - check-explain-contract
   - check-rule-css-wiring
   - check-spellcheck-features
   - check-network-silence
   - check-baseline-bundle-size
   - check-benchmark-coverage
   - check-governance-data

6. Per CLAUDE.md "Data-Logic Separation Philosophy": for any quarantined FP that is data-shaped (missing typo entry, wrong genus, missing collocation), flag it in the TRIAGE.md as a follow-up task for papertek-vocabulary. Do NOT add inline data lists to leksihjelp spell-rules.

Fixture-side reminder: precision improvements that ALSO increase recall on other suites are fine. Precision improvements that drop recall below 1.0 anywhere are NOT fine — recall is the harder gate.
  </action>
  <verify>
    <automated>npm run check-fixtures</automated>
  </verify>
  <done>check-fixtures exits 0; 25-04-TRIAGE.md exists with per-suite disposition; all other release-workflow gates still pass; quarantined items have a written rationale.</done>
</task>

</tasks>

<verification>
- `npm run check-fixtures` exits 0
- All other gates from CLAUDE.md release workflow still exit 0
- 25-04-TRIAGE.md exists and lists all 5 suites with disposition
</verification>

<success_criteria>
- DEBT-02: check-fixtures gate is green on main
- Quarantine mechanism (if newly added) is loud (prints quarantined items every run)
- Any data-shaped quarantines are queued for papertek-vocabulary follow-up, not patched into leksihjelp spell-rules
</success_criteria>

<output>
After completion, create `.planning/phases/25-ux-polish-tech-debt/25-04-SUMMARY.md`
</output>
