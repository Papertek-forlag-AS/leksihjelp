---
phase: 29-lockdown-teacher-lock-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/geirforbord/Papertek/lockdown/public/js/writing-test/shared/resource-profile.js
  - /Users/geirforbord/Papertek/lockdown/public/js/writing-test/shared/classroom-illustrations.js
  - /Users/geirforbord/Papertek/lockdown/public/js/writing-test/locales/nb.js
  - /Users/geirforbord/Papertek/lockdown/public/js/writing-test/locales/nn.js
  - /Users/geirforbord/Papertek/lockdown/public/js/writing-test/locales/en.js
  - /Users/geirforbord/Papertek/lockdown/public/js/writing-test/teacher/question-builder.js
autonomous: false
requirements: [EXAM-10]

must_haves:
  truths:
    - "A new resource-profile value LEKSIHJELP_EXAM exists in lockdown's shared profile constants"
    - "Teacher dashboard offers the new option alongside Full pakke / Eksamen-modus / Kun Lexin / Ingen ressurser"
    - "All three locales (nb, nn, en) carry the label and the resource-profile envelope describes what it allows"
    - "Phase 28's 'Simuler lærer-lås' dev button keeps working (no regression in shared/resource-profile.js consumers)"
  artifacts:
    - path: "/Users/geirforbord/Papertek/lockdown/public/js/writing-test/shared/resource-profile.js"
      provides: "RESOURCE_PROFILES.LEKSIHJELP_EXAM constant + resolveResourceProfile envelope + PROFILE_LABELS_NB/NN/EN entries"
      contains: "LEKSIHJELP_EXAM"
    - path: "/Users/geirforbord/Papertek/lockdown/public/js/writing-test/locales/nb.js"
      provides: "Bokmål label + description for LEKSIHJELP_EXAM"
      contains: "leksihjelpExam"
  key_links:
    - from: "shared/resource-profile.js"
      to: "shared/classroom-illustrations.js"
      via: "RESOURCE_PROFILES enum consumed for the teacher dashboard illustration"
      pattern: "LEKSIHJELP_EXAM"
    - from: "teacher/question-builder.js"
      to: "shared/resource-profile.js"
      via: "PROFILE_LABELS_NB used to render the resource-profile picker"
      pattern: "PROFILE_LABELS_NB"
---

<objective>
Add the LEKSIHJELP_EXAM resource profile to the lockdown webapp's shared profile module, locales, teacher dashboard picker, and classroom illustration. This is the front-end UX surface that teachers will see and select.

Purpose: EXAM-10 requires a teacher-control surface for leksihjelp's Phase 27 exam-mode lock. Phase 28 verification chose Option B (new resource profile). Without this UX layer the writer in Plan 02 has nothing to react to.

Output: A new selectable profile that — when saved — flows through the existing Phase 63 envelope plumbing. The actual writer of `chrome.storage.local.examModeLocked + examMode` is wired in Plan 02; the firestore.rules/functions enum updates are also Plan 02. This plan is the JS-only client surface.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/geirforbord/Papertek/leksihjelp/.planning/ROADMAP.md
@/Users/geirforbord/Papertek/leksihjelp/.planning/REQUIREMENTS.md
@/Users/geirforbord/Papertek/leksihjelp/.planning/phases/28-lockdown-exam-mode-sync/28-VERIFICATION.md
@/Users/geirforbord/Papertek/leksihjelp/.planning/phases/28-lockdown-exam-mode-sync/28-01-SUMMARY.md

# Cross-repo files this plan modifies (lockdown sibling, NOT the leksihjelp tree):
@/Users/geirforbord/Papertek/lockdown/public/js/writing-test/shared/resource-profile.js
@/Users/geirforbord/Papertek/lockdown/public/js/writing-test/shared/classroom-illustrations.js

<interfaces>
<!-- Existing shape (from grep) — extracted so the executor doesn't go scavenging. -->

From shared/resource-profile.js (around line 31):
  export const RESOURCE_PROFILES = Object.freeze({ FULL: 'full', EXAM: 'exam', LEXIN: 'lexin', NONE: 'none' });
  export const PROFILE_LABELS_NB = Object.freeze({ full: 'Full pakke', exam: 'Eksamen-modus', lexin: 'Kun Lexin', none: 'Ingen ressurser' });
  // Also exports: PROFILE_LABELS_NN, PROFILE_LABELS_EN, resolveResourceProfile(profile), getSpellEngineEnvelope(profile)
  // The EXAM envelope today: { leksihjelp: false, spellEngineOptions: ['off','browser'] } — see Phase 28-VERIFICATION § Re-Scope.

From locales/nb.js line 149:
  exam: 'Eksamen-modus',          // existing entry under resourceProfile.label
  // and around line 901 a longer "heading" + description block in the teacher dashboard.

Existing tests (shared/resource-profile.test.js around line 32) assert PROFILE_LABELS_NB has the four labels and is frozen — adding a fifth entry will require this test to be updated alongside the source.
</interfaces>

**Cross-repo note:** All paths in this plan live under `/Users/geirforbord/Papertek/lockdown`. The leksihjelp tree is untouched.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Lock the LEKSIHJELP_EXAM UX shape</name>
  <action>Pause for user decision on the LEKSIHJELP_EXAM UX shape before writing any code in Task 2. Present the recommended default and the two alternatives, then proceed once the user replies. The decision locks Plan 02 (enum + writer) and Plan 03 (verification) so they inherit a stable contract.</action>
  <decision>Confirm the new resource-profile shape before writing code: name, label, what its envelope returns, and whether it lives alongside EXAM or replaces an unused slot.</decision>
  <context>
    Phase 28 verification recorded "Option B (new RESOURCE_PROFILES.LEKSIHJELP_EXAM)" as the user-selected direction, but the precise label, envelope shape, and whether it should also expose a per-test toggle are still open. The decision is locked here so Plan 02 (firestore enum + writer) and Plan 03 (verification) inherit a stable contract.

    Recommended shape (default if user just says "go"):
    - Enum value: 'leksihjelp_exam'  (matches naming style; underscored to keep enum simple)
    - Constant key: RESOURCE_PROFILES.LEKSIHJELP_EXAM
    - PROFILE_LABELS_NB: 'Eksamen med Leksihjelp'
    - PROFILE_LABELS_NN: 'Eksamen med Leksihjelp'
    - PROFILE_LABELS_EN: 'Exam with Leksihjelp'
    - Envelope (resolveResourceProfile): { leksihjelp: true, spellEngineOptions: ['off'], lexin: false } — leksihjelp on, browser/native spellcheck off (since Leksihjelp is the spell-check), Lexin off
    - No additional per-test toggle for v1 — keep surface minimal; revisit if teachers ask
  </context>
  <options>
    <option id="default-recommended">
      <name>Use the recommended shape above</name>
      <pros>Mirrors existing 4-profile pattern; minimal surface area; reuses Phase 63 envelope plumbing</pros>
      <cons>Locks Leksihjelp + spellEngine='off'; teachers can't combine "Leksihjelp + browser spellcheck"</cons>
    </option>
    <option id="dual-engine">
      <name>Allow both engines: spellEngineOptions: ['off','browser']</name>
      <pros>Teacher can opt into browser spellcheck alongside Leksihjelp (some exam regs allow it)</pros>
      <cons>Two engines flagging the same token confuses students; UX-double-flagging is a known footgun</cons>
    </option>
    <option id="custom">
      <name>User specifies a different shape</name>
      <pros>Tailored to actual school feedback</pros>
      <cons>Requires explicit description before continuing</cons>
    </option>
  </options>
  <resume-signal>Reply "default" to use recommended shape, "dual-engine" for two-engine variant, or describe custom shape.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add LEKSIHJELP_EXAM to shared/resource-profile.js + locales + classroom illustration + question-builder picker</name>
  <files>
    /Users/geirforbord/Papertek/lockdown/public/js/writing-test/shared/resource-profile.js,
    /Users/geirforbord/Papertek/lockdown/public/js/writing-test/shared/resource-profile.test.js,
    /Users/geirforbord/Papertek/lockdown/public/js/writing-test/shared/classroom-illustrations.js,
    /Users/geirforbord/Papertek/lockdown/public/js/writing-test/locales/nb.js,
    /Users/geirforbord/Papertek/lockdown/public/js/writing-test/locales/nn.js,
    /Users/geirforbord/Papertek/lockdown/public/js/writing-test/locales/en.js,
    /Users/geirforbord/Papertek/lockdown/public/js/writing-test/teacher/question-builder.js
  </files>
  <action>
    Apply the locked decision from Task 1. Default shape (override per Task 1 reply):

    1. **shared/resource-profile.js:**
       - Add `LEKSIHJELP_EXAM: 'leksihjelp_exam'` to RESOURCE_PROFILES (keep frozen)
       - Add `leksihjelp_exam: 'Eksamen med Leksihjelp'` to PROFILE_LABELS_NB (and nn/en variants — match locale convention used for the other four)
       - Extend `resolveResourceProfile()` and `getSpellEngineEnvelope()` to return `{ leksihjelp: true, spellEngineOptions: ['off'], lexin: false }` for the new profile (read existing EXAM branch to match shape exactly)

    2. **shared/resource-profile.test.js:**
       - Update the "PROFILE_LABELS_NB has the four Bokmål labels" test to reflect five labels; add a parallel assertion for `leksihjelp_exam` and that the object is still frozen
       - Add envelope assertion: `resolveResourceProfile('leksihjelp_exam').leksihjelp === true`

    3. **locales/nb.js, nn.js, en.js:**
       - Add `leksihjelp_exam: 'Eksamen med Leksihjelp'` (and translations) under the `resourceProfile.label` group (look around line 149 in nb.js)
       - Add a description string under `resourceProfile.description.leksihjelp_exam` mirroring the four existing description entries
       - If there is a teacher-dashboard heading section (around line 901 in nb.js), add a parallel entry there too

    4. **shared/classroom-illustrations.js:**
       - Mirror the existing four-profile pattern. Add an entry for the new profile so the teacher dashboard renders an illustration; the illustration metadata can reuse the EXAM illustration with a small annotation OR be a copy with the Leksihjelp logo. Use whichever is the smallest viable change — DO NOT generate new SVG art

    5. **teacher/question-builder.js:**
       - Find where the resource-profile picker is rendered (search for `PROFILE_LABELS_NB` or `RESOURCE_PROFILES.EXAM`) and append the new option. The dropdown is data-driven if it iterates `Object.entries(PROFILE_LABELS_NB)` — in that case the picker auto-updates and only a smoke check is needed; if hardcoded, add a fifth `<option>` matching the existing markup

    Keep changes minimal and additive. Do NOT modify the existing EXAM behavior (Phase 63 contract preserved).

    Reference: Phase 28-VERIFICATION § Re-Scope explains why this approach was chosen over flipping the EXAM envelope.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/lockdown && node --test public/js/writing-test/shared/resource-profile.test.js</automated>
  </verify>
  <done>
    - `node --test resource-profile.test.js` exits 0 with the updated 5-profile assertions passing
    - `grep -n "LEKSIHJELP_EXAM\|leksihjelp_exam" public/js/writing-test/shared/resource-profile.js` returns matches in RESOURCE_PROFILES, PROFILE_LABELS_NB/NN/EN, resolveResourceProfile envelope branch
    - All three locale files contain `leksihjelp_exam` label + description
    - question-builder picker shows the new option (visual check deferred to Plan 03 browser verify)
    - "Simuler lærer-lås" path in leksihjelp popup is untouched (no edits to leksihjelp tree)
  </done>
</task>

</tasks>

<verification>
- Unit test: `cd /Users/geirforbord/Papertek/lockdown && node --test public/js/writing-test/shared/resource-profile.test.js` exits 0
- Grep proof: `grep -rn "leksihjelp_exam" /Users/geirforbord/Papertek/lockdown/public/js | wc -l` returns ≥ 5 (resource-profile.js + 3 locales + classroom-illustrations OR question-builder)
- No leksihjelp-tree edits: `git -C /Users/geirforbord/Papertek/leksihjelp status` shows clean (or only this PLAN file)
</verification>

<success_criteria>
- LEKSIHJELP_EXAM enum value, three locale labels, classroom illustration entry, and teacher picker option all in place
- Existing four profiles still resolve identically (regression-free)
- resource-profile.test.js updated to cover the new entry
- No firestore.rules / Cloud Functions / writing-environment.js edits yet (those land in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/29-lockdown-teacher-lock-ux/29-01-SUMMARY.md` recording: locked UX shape from Task 1, files touched in lockdown, test command output, and any deviations from the recommended default.
</output>
