---
phase: 29-lockdown-teacher-lock-ux
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified: []
autonomous: false
requirements: [EXAM-10]

must_haves:
  truths:
    - "On stb-lockdown.app a teacher can pick the new 'Eksamen med Leksihjelp' profile and save a test successfully"
    - "A student joining that test sees: Leksihjelp popup toggle ON+disabled with 'Slått på av lærer' caption, EKSAMENMODUS badge, amber widget border, prediction dropdown suppressed, grammar-lookup dots suppressed, typo dots + dictionary lookups remain"
    - "When the teacher unselects the profile (or the test ends), examModeLocked + examMode flags clear and surfaces re-enable"
    - "Phase 28's 'Simuler lærer-lås' dev button in the leksihjelp popup still triggers the same lock state (no regression)"
    - "papertek.app (production) shows the same behavior after the prod deploy"
  artifacts: []
  key_links:
    - from: "stb-lockdown.app teacher dashboard"
      to: "stb-lockdown.app student writing environment"
      via: "Firestore test doc with resourceProfile: 'leksihjelp_exam'"
      pattern: "Eksamen med Leksihjelp"
    - from: "writing-environment.js"
      to: "leksihjelp popup toggle"
      via: "chrome.storage.local.examModeLocked propagated through Phase 28 LEKSI_BUNDLE shim"
      pattern: "Slått på av lærer"
---

<objective>
End-to-end browser verification of the LEKSIHJELP_EXAM teacher-lock path on staging (stb-lockdown.app), then on production (papertek.app), plus a regression check that Phase 28's dev-only "Simuler lærer-lås" button still works.

Purpose: Plans 01 and 02 wire the code; this plan proves the feature works against real Firebase + real browser. Without browser verification we cannot ship to schools.

Output: Three signed-off walkthroughs (staging, regression, prod) recorded in 29-03-SUMMARY.md.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/geirforbord/Papertek/leksihjelp/.planning/phases/28-lockdown-exam-mode-sync/28-VERIFICATION.md
@/Users/geirforbord/Papertek/leksihjelp/.planning/phases/28-lockdown-exam-mode-sync/28-01-SUMMARY.md

This plan is verification-only — no code edits. All work is browser-driven against deployed environments.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Staging end-to-end verification on stb-lockdown.app</name>
  <action>Pause for human-driven browser verification. Walk the 18-step checklist below against https://stb-lockdown.app with the 2.7.0 leksihjelp extension installed. Record outcome in 29-03-SUMMARY.md.</action>
  <what-built>
    Plans 01-02 added LEKSIHJELP_EXAM resource profile + locale labels + classroom illustration + teacher picker option, extended firestore.rules + Cloud Functions enums, wired applyExamModeLock in writing-environment.js (set on enter, clear on transition), and deployed to staging-lockdown Firebase project.
  </what-built>
  <how-to-verify>
    Open Chrome (or Edge) with the **2.7.0 leksihjelp extension installed**. Use a Norwegian student persona for student steps.

    **Teacher walkthrough:**
    1. Visit https://stb-lockdown.app and log in as a teacher account
    2. Create a new test (or open an existing one)
    3. In the resource-profile picker, confirm a new option "Eksamen med Leksihjelp" appears alongside Full pakke / Eksamen-modus / Kun Lexin / Ingen ressurser
    4. Select it and save the test
    5. Confirm: save succeeds without firestore permission errors (DevTools → Network → no 403 on the test doc write)

    **Student walkthrough (same browser, separate session or incognito):**
    6. Join the test as a student (use a test student account)
    7. Open the leksihjelp popup (extension icon)
    8. Verify the EKSAMENMODUS badge is visible
    9. Verify the exam-mode toggle is ON, disabled (greyed in ON state), with caption "Slått på av lærer"
    10. Verify the floating widget (when text is selected) gains an amber border tint
    11. In the editor, type a Norwegian word that would normally trigger word-prediction — confirm the dropdown does NOT open
    12. Type a token that fires a grammar-lookup rule (e.g. a DE preposition for de-prep-case if the student is writing German) — confirm grammar-lookup dots do NOT appear
    13. Type a typo (e.g. "skole" misspelled) — confirm typo dot DOES appear (typo rules are exam-safe)
    14. Trigger a dictionary lookup via the popup search — confirm it works (dictionary is exam-safe)
    15. DevTools → Console: no `__lexiExamRegistry undefined` errors

    **Transition-clear walkthrough:**
    16. As teacher, change the test profile from LEKSIHJELP_EXAM → Full pakke (or Ingen ressurser)
    17. As student (refresh the writing test page), confirm: toggle is no longer disabled, badge gone, amber border gone, prediction works again, grammar dots appear again
    18. DevTools → Application → Storage → confirm `examModeLocked` is `false` (or absent) and `examMode` is `false`

    Report any deviation. The 28-VERIFICATION.md staging-verification list documents the same observable surfaces; this is the production-path equivalent.
  </how-to-verify>
  <resume-signal>Reply "staging verified" if all 18 checks pass, or describe failures.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 28 'Simuler lærer-lås' dev button regression check</name>
  <action>Pause for human-driven regression check. Confirm the dev-only "Simuler lærer-lås" button in the leksihjelp popup still toggles the lock standalone and over a non-leksihjelp_exam lockdown context.</action>
  <what-built>
    Phase 28 shipped a dev-only "Simuler lærer-lås" button in the leksihjelp popup (commit 94d591b) that writes examModeLocked + examMode = true directly. Plan 02 added a parallel writer in lockdown's writing-environment.js. The dev path must still work standalone.
  </what-built>
  <how-to-verify>
    1. Open the leksihjelp extension popup directly (not via a lockdown test — just the standalone extension)
    2. Confirm the "Simuler lærer-lås (dev)" button is visible
    3. Click it
    4. Verify: toggle goes ON+disabled with "Slått på av lærer" caption, EKSAMENMODUS badge appears
    5. Click again to toggle off — verify lock clears (toggle returns to enabled, badge disappears, examMode flag cleared)
    6. Open https://stb-lockdown.app in the SAME browser, NOT logged into a leksihjelp_exam test (e.g. logged-out landing page or a Full pakke test)
    7. Click "Simuler lærer-lås" in the popup again — verify the lock applies on the lockdown context too (floating widget over a stb-lockdown.app text field shows amber border etc.)

    This proves the dev button path is independent of the new writing-environment.js writer. They cooperate (both write to the same chrome.storage.local keys) without conflict.
  </how-to-verify>
  <resume-signal>Reply "dev button verified" or describe failures.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Production deploy verification on papertek.app</name>
  <action>Pause for human-driven prod walkthrough on https://papertek.app. Repeat the staging checklist against production after the lockdown-stb deploy from 29-02 Task 3.</action>
  <what-built>
    Plan 02 Task 3 deployed firestore.rules + Cloud Functions to lockdown-stb (production). This task confirms prod behaves identically to staging.
  </what-built>
  <how-to-verify>
    Repeat the Task 1 walkthrough — but against https://papertek.app (the production domain) instead of stb-lockdown.app.

    Abbreviated checklist (the full 18-step list applies; key deltas):
    1. Teacher creates a test on papertek.app, picks "Eksamen med Leksihjelp", saves successfully
    2. Student joins, sees toggle ON+disabled, badge, amber border, prediction suppressed, grammar dots suppressed, typo dots + dictionary lookups remain
    3. Profile transition AWAY clears flags (refresh writing test, surfaces re-enable)
    4. DevTools console clean

    If prod fails but staging passed, suspect: prod Firebase deploy didn't complete, prod build hasn't picked up the lockdown-stb branch, browser cache holding stale JS bundle. Hard-reload (Cmd+Shift+R) before declaring failure.
  </how-to-verify>
  <resume-signal>Reply "prod verified" if all checks pass, or describe failures.</resume-signal>
</task>

</tasks>

<verification>
All three checkpoints passed (signed off by user). No automated verification — this plan is browser-driven UAT.
</verification>

<success_criteria>
- Staging walkthrough: 18/18 checks pass on stb-lockdown.app
- Dev-button regression: Phase 28's "Simuler lærer-lås" path still works standalone and over a lockdown context
- Production walkthrough: same behavior on papertek.app
- No regression in existing four resource profiles (Full pakke / Eksamen-modus / Kun Lexin / Ingen ressurser)
- EXAM-10 closed; v3.1 milestone audit can flip EXAM-10 status from Pending → Complete
</success_criteria>

<output>
After completion, create `.planning/phases/29-lockdown-teacher-lock-ux/29-03-SUMMARY.md` recording:
- staging walkthrough outcome (with any noted deviations)
- dev-button regression outcome
- prod walkthrough outcome
- screenshots / Loom links if user provided any
- recommendation to update REQUIREMENTS.md `EXAM-10 | Phase 29 | Pending` → `Complete` and tick the Phase 29 checkbox in ROADMAP.md
</output>
