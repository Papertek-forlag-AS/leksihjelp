---
phase: 29-lockdown-teacher-lock-ux
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - /Users/geirforbord/Papertek/lockdown/firestore.rules
  - /Users/geirforbord/Papertek/lockdown/functions/teacher/createTest.js
  - /Users/geirforbord/Papertek/lockdown/functions/teacher/toggleResourceAccess.js
  - /Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js
autonomous: false
requirements: [EXAM-10]

must_haves:
  truths:
    - "Backend enum (firestore.rules + Cloud Functions) accepts 'leksihjelp_exam' as a valid resourceProfile value"
    - "When the new profile is active, writing-environment.js writes chrome.storage.local.{examModeLocked: true, examMode: true}"
    - "On profile transition AWAY from leksihjelp_exam, writing-environment.js clears BOTH flags so the next non-exam test for the same student is not bricked"
    - "Firebase rules + functions are deployed to BOTH staging-lockdown and lockdown-stb projects"
  artifacts:
    - path: "/Users/geirforbord/Papertek/lockdown/firestore.rules"
      provides: "Updated enum allowlist on lines ~25 and ~30 (and any other hardcoded enum sites found by grep)"
      contains: "leksihjelp_exam"
    - path: "/Users/geirforbord/Papertek/lockdown/functions/teacher/createTest.js"
      provides: "ALLOWED array (around line 82) extended with 'leksihjelp_exam'"
      contains: "leksihjelp_exam"
    - path: "/Users/geirforbord/Papertek/lockdown/functions/teacher/toggleResourceAccess.js"
      provides: "Same enum extension"
      contains: "leksihjelp_exam"
    - path: "/Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js"
      provides: "applyExamModeLock helper called on profile change around line 953 + clear-on-transition path"
      contains: "examModeLocked"
  key_links:
    - from: "writing-environment.js (~line 953 BSPC-01 site)"
      to: "chrome.storage.local"
      via: "chrome.storage.local.set({ examModeLocked, examMode }) on profile===LEKSIHJELP_EXAM"
      pattern: "examModeLocked.*true"
    - from: "writing-environment.js profile transition"
      to: "chrome.storage.local"
      via: "chrome.storage.local.set({ examModeLocked: false, examMode: false }) when newProfile !== LEKSIHJELP_EXAM and previous was LEKSIHJELP_EXAM"
      pattern: "examModeLocked.*false"
    - from: "Firebase staging-lockdown project"
      to: "Firebase lockdown-stb project"
      via: "firebase deploy --only firestore:rules,functions executed against each --project alias"
      pattern: "deploy"
---

<objective>
Update lockdown's backend enum (firestore.rules + Cloud Functions) and student-side writing-environment.js so the new LEKSIHJELP_EXAM profile from Plan 01 actually sets the leksihjelp lock flags. Then deploy Firebase rules + functions to both staging and prod projects.

Purpose: Without the enum updates, firestore writes for tests with `resourceProfile: 'leksihjelp_exam'` are rejected. Without the writer, the profile is selectable but does nothing. Without the deploy, prod still rejects.

Output: Lock flags written + cleared correctly, deployed end-to-end.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/geirforbord/Papertek/leksihjelp/.planning/ROADMAP.md
@/Users/geirforbord/Papertek/leksihjelp/.planning/phases/28-lockdown-exam-mode-sync/28-VERIFICATION.md

# Cross-repo files (lockdown sibling):
@/Users/geirforbord/Papertek/lockdown/firestore.rules
@/Users/geirforbord/Papertek/lockdown/functions/teacher/createTest.js
@/Users/geirforbord/Papertek/lockdown/functions/teacher/toggleResourceAccess.js
@/Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js

<interfaces>
<!-- Confirmed enum sites (from grep): -->

firestore.rules line 25:
  return v in ['full', 'exam', 'lexin', 'none'];

firestore.rules line 30:
  return v == null || v in ['full', 'exam', 'lexin', 'none'];

functions/teacher/createTest.js line 82:
  const ALLOWED = ['full', 'exam', 'lexin', 'none'];

functions/teacher/createTest.js line 89 (error message):
  'resourceProfile must be one of: full, exam, lexin, none'

functions/teacher/toggleResourceAccess.js: similar enum (grep at execution time to confirm exact line)

writing-environment.js around line 953 (BSPC-01 site):
  if (newProfile === RESOURCE_PROFILES.EXAM && currentEngine === 'browser') {
      editor.spellcheck = true;
  }
  // ← LEKSIHJELP_EXAM hook goes here as a sibling branch.

writing-environment.js around line 31 imports:
  import { resolveResourceProfile, getSpellEngineEnvelope, RESOURCE_PROFILES } from '../shared/resource-profile.js';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backend enum (firestore.rules + Cloud Functions) for LEKSIHJELP_EXAM</name>
  <files>
    /Users/geirforbord/Papertek/lockdown/firestore.rules,
    /Users/geirforbord/Papertek/lockdown/functions/teacher/createTest.js,
    /Users/geirforbord/Papertek/lockdown/functions/teacher/toggleResourceAccess.js
  </files>
  <action>
    1. **firestore.rules:**
       - Edit line 25: `return v in ['full', 'exam', 'lexin', 'none', 'leksihjelp_exam'];`
       - Edit line 30: `return v == null || v in ['full', 'exam', 'lexin', 'none', 'leksihjelp_exam'];`
       - Grep the file for any OTHER hardcoded `'full', 'exam', 'lexin', 'none'` patterns (e.g. line 325 area mentions `resourceProfileOverride`) — extend if present. Run `grep -n "'full'.*'exam'\|'exam'.*'lexin'" firestore.rules` to find all sites
       - Update the comment block referencing "four named values" → "five named values"

    2. **functions/teacher/createTest.js:**
       - Line ~82: `const ALLOWED = ['full', 'exam', 'lexin', 'none', 'leksihjelp_exam'];`
       - Line ~89: update error message to list the five values
       - Run `grep -n "'full'.*'exam'\|four-value enum" functions/teacher/createTest.js` to catch the comment at line 248 and any peer enum sites; update consistently

    3. **functions/teacher/toggleResourceAccess.js:**
       - Run `grep -n "'full'.*'exam'\|ALLOWED" functions/teacher/toggleResourceAccess.js` to find the equivalent enum
       - Extend with 'leksihjelp_exam' identically

    4. **Run any function unit tests** that exist for these files (we already saw createTest-settings.test.js + createTest-oppgavesett.test.js). If any test asserts exact enum length or rejects on the new value, update those tests to accept the fifth value (mirror the resource-profile.test.js pattern from Plan 01).

    Keep edits minimal. Do NOT change behavior of existing four profiles.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/lockdown && grep -n "leksihjelp_exam" firestore.rules functions/teacher/createTest.js functions/teacher/toggleResourceAccess.js && (cd functions && npm test --silent 2>&1 | tail -20)</automated>
  </verify>
  <done>
    - `grep -c "leksihjelp_exam" firestore.rules` returns ≥ 2 (lines 25 and 30)
    - Same grep against createTest.js and toggleResourceAccess.js returns ≥ 1 each
    - `cd functions && npm test` exits 0 (or, if pre-existing failures unrelated, the createTest-settings + createTest-oppgavesett suites pass)
    - No edits to leksihjelp tree
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire applyExamModeLock + clear-on-transition in writing-environment.js</name>
  <files>/Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js</files>
  <action>
    Insert a sibling branch beside the existing BSPC-01 wiring at line 953.

    1. **Locate the block** (around lines 945-958): the BSPC-01 `if (newProfile === RESOURCE_PROFILES.EXAM && currentEngine === 'browser')` branch is inside the resource-profile change handler. The handler receives `newProfile` (the resolved next profile) and runs after `applyEnvelopeToDOM(envelope)`.

    2. **Add a helper** higher in the module:

       ```js
       /**
        * EXAM-10 (Phase 29): write or clear the leksihjelp teacher-lock storage flags
        * based on whether the current resource profile is LEKSIHJELP_EXAM.
        * Reads via chrome.runtime/storage shim (already used by Phase 28 plumbing).
        * Defensive: also called on first paint via the initial-profile resolution path.
        */
       function applyExamModeLock(newProfile, prevProfile) {
           if (typeof chrome === 'undefined' || !chrome?.storage?.local?.set) return;
           const isLeksiExam = newProfile === RESOURCE_PROFILES.LEKSIHJELP_EXAM;
           const wasLeksiExam = prevProfile === RESOURCE_PROFILES.LEKSIHJELP_EXAM;
           if (isLeksiExam) {
               chrome.storage.local.set({ examModeLocked: true, examMode: true });
           } else if (wasLeksiExam) {
               // Critical: clear BOTH flags on transition AWAY so the next
               // non-exam test for the same student is not bricked. Without
               // this, examModeLocked persists and the popup toggle stays
               // disabled forever. See Phase 29 ROADMAP success criterion 5.
               chrome.storage.local.set({ examModeLocked: false, examMode: false });
           }
       }
       ```

    3. **Call site 1** — inside the existing profile-change handler, immediately after the BSPC-01 branch (around line 956):

       ```js
       // EXAM-10: write/clear leksihjelp teacher-lock flags
       applyExamModeLock(newProfile, prevProfile);
       ```

       Note: the existing handler already has access to `newProfile`. Trace upward to find where `prevProfile` is known (likely captured before `applyEnvelopeToDOM`); if not, capture it explicitly: `const prevProfile = currentProfile;` before the envelope swap.

    4. **Call site 2** — initial paint path. Search the file for where `resolveResourceProfile` is first called on test load (likely an init path). Call `applyExamModeLock(initialProfile, null)` once on init so a student joining a leksihjelp_exam test gets the lock flag set without waiting for a profile change event.

    5. **Preserve "Simuler lærer-lås" dev path**: this writer is additive — the leksihjelp popup's dev button (commit 94d591b) still writes the flags directly via `chrome.storage.local`. The new writer only fires on profile transitions. Verify by grep: `grep -n "examModeLocked" /Users/geirforbord/Papertek/leksihjelp/extension/popup/popup.js` should still match (no edits to leksihjelp tree).

    DO NOT touch the existing EXAM branch — Phase 63 contract preserved.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/lockdown && grep -n "applyExamModeLock\|examModeLocked" public/js/writing-test/student/writing-environment.js && node --check public/js/writing-test/student/writing-environment.js</automated>
  </verify>
  <done>
    - `applyExamModeLock` defined and called at least twice (init + on-change)
    - `node --check writing-environment.js` confirms no syntax errors
    - On-transition path explicitly clears `examModeLocked: false, examMode: false` (grep proof)
    - leksihjelp popup.js unchanged
    - No regression in existing EXAM (Phase 63) branch — diff shows only additive lines
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Manual Firebase deploy to staging-lockdown then lockdown-stb</name>
  <action>Deploy firestore.rules + Cloud Functions to BOTH Firebase projects. This is human-driven because (a) DEPLOY_NOTES.md states rules are deployed manually, (b) it touches shared production infrastructure, (c) deploy credentials are user-specific and not appropriate for autonomous execution.</action>
  <instructions>
    From `/Users/geirforbord/Papertek/lockdown`:

    1. **Sanity check first** — confirm working tree clean except for Plan 02 changes:
       ```
       git status
       git diff firestore.rules functions/teacher/createTest.js functions/teacher/toggleResourceAccess.js
       ```

    2. **Deploy to staging-lockdown** (the staging project):
       ```
       firebase use staging-lockdown
       firebase deploy --only firestore:rules,functions
       ```
       Confirm: deploy completes without error; rules upload preview shows the new enum value.

    3. **Smoke-test on staging** — open https://stb-lockdown.app, log in as a teacher, create a new test, confirm the new "Eksamen med Leksihjelp" option appears in the resource-profile picker. Save the test (proves firestore.rules accepts the new enum value).

    4. **Deploy to lockdown-stb** (production):
       ```
       firebase use lockdown-stb
       firebase deploy --only firestore:rules,functions
       ```

    5. Report back with both deploy summaries (URL of preview / commit / function deploy logs).

    **Pre-deploy gate:** if any tests are failing in `cd functions && npm test`, do NOT deploy — fix tests first.
  </instructions>
  <resume-signal>Reply "deployed staging" after step 3, "deployed prod" after step 4, or describe failures.</resume-signal>
</task>

</tasks>

<verification>
- Backend enum: `grep -c leksihjelp_exam /Users/geirforbord/Papertek/lockdown/firestore.rules` ≥ 2
- Writer: `grep -n applyExamModeLock /Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js` returns matches in both definition + call sites
- Clear path: `grep -n "examModeLocked: false" /Users/geirforbord/Papertek/lockdown/public/js/writing-test/student/writing-environment.js` returns at least one match
- Deploy: human-confirmed deploys complete to both Firebase projects
- Regression: `git -C /Users/geirforbord/Papertek/leksihjelp status` clean
</verification>

<success_criteria>
- All five enum sites (rules ×2 + createTest + toggleResourceAccess + any peer) accept 'leksihjelp_exam'
- writing-environment.js sets examModeLocked+examMode on entering LEKSIHJELP_EXAM and clears both on leaving
- Firebase rules + functions deployed successfully to staging-lockdown AND lockdown-stb
- Existing four profiles unchanged
- Phase 28 dev-path "Simuler lærer-lås" still works (verified by Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/29-lockdown-teacher-lock-ux/29-02-SUMMARY.md` capturing: enum sites updated, writer call-site line numbers, both deploy outputs (or summaries), and any unexpected enum sites discovered by grep.
</output>
