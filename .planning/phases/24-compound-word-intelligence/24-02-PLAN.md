---
phase: 24-compound-word-intelligence
plan: 02
type: execute
wave: 2
depends_on:
  - "24-01"
files_modified:
  - extension/popup/popup.js
  - extension/i18n/strings.js
  - extension/styles/popup.css
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "Student types 'chefsstu' in popup search and sees 'Chefsstuhl' in results list"
    - "Compound card shows pedagogical note about last component determining gender"
    - "Compound card shows clickable link to last component's dictionary entry"
    - "After clicking component link, a 'Tilbake til [compound]' link appears"
    - "Clicking back-link returns to the compound card"
    - "Compound card shows translation guess labeled 'Kvalifisert gjetning'"
    - "Translation guess is assembled from component translations"
  artifacts:
    - path: "extension/popup/popup.js"
      provides: "Enhanced compound card with prediction, pedagogy, back-nav, translation guess"
      contains: "predictCompound"
    - path: "extension/i18n/strings.js"
      provides: "i18n keys for pedagogical note, back-nav, translation guess"
      contains: "compound_pedagogy"
    - path: "extension/styles/popup.css"
      provides: "Styles for new compound card elements"
      contains: "compound-pedagogy"
  key_links:
    - from: "extension/popup/popup.js"
      to: "vocab-seam-core.js predictCompound"
      via: "self.__lexiVocabCore.predictCompound or indexes.predictCompound"
      pattern: "predictCompound"
    - from: "extension/popup/popup.js performSearch"
      to: "renderCompoundCard"
      via: "prediction results fed to compound card renderer"
      pattern: "predictCompound.*renderCompound"
    - from: "extension/popup/popup.js renderCompoundCard"
      to: "performSearch with back-nav state"
      via: "compound navigation stack"
      pattern: "compoundNavStack\\|compoundBackLink"
---

<objective>
Wire compound prediction into popup search, enhance the compound card with pedagogical notes, back-navigation, and qualified translation guesses.

Purpose: Students discover compounds while typing (COMP-01 wiring), understand compound grammar via pedagogy (COMP-02), navigate between compound and component entries (COMP-03), and see translation guesses (COMP-04).
Output: Enhanced popup.js with prediction-driven compound suggestions, pedagogical compound card, back-navigation, and "Kvalifisert gjetning" translation display.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-compound-word-intelligence/24-01-SUMMARY.md
@extension/popup/popup.js
@extension/i18n/strings.js
@extension/styles/popup.css

<interfaces>
<!-- From Plan 01 output: predictCompound on buildIndexes return -->
```javascript
// On buildIndexes return object:
predictCompound: (partial) => predictCompound(partial, nounGenus, lang, decompFn)

// Returns: Array<{ word: string, decomposition: { parts: [{word, genus, linker}], gender, confidence } }>
// Max 10 results. Empty array if no matches.
```

<!-- Existing compound card rendering (popup.js lines 1465-1516) -->
```javascript
function renderCompoundCard(query, decomposition) {
  // decomposition = { parts: [{word, genus, linker}], gender, confidence }
  // Currently renders: badge + breakdown + clickable component buttons + gender
  // Component buttons trigger performSearch(word) on click
}
```

<!-- Existing getTranslation (popup.js line 29) -->
```javascript
function getTranslation(entry) {
  // Returns Norwegian translation for an entry respecting UI language (nb/nn)
}
```

<!-- Existing search flow -->
```javascript
// performSearch(query) in popup.js:
// 1. Direct word matches
// 2. Two-way linked lookup
// 3. Inflection matches
// 4. Sort + combine
// 5. Fallback reverse direction
// 6. Compound decomposition (last resort, only when combined.length === 0)
```

<!-- i18n pattern (strings.js) -->
```javascript
// NB strings at ~line 180, NN at ~line 400, EN at ~line 620
// Key pattern: compound_label, compound_pedagogy, etc.
```

<!-- allWords and nounGenusMap are module-level variables in popup.js -->
<!-- dictionary is the raw loaded dictionary object with bank structure -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire compound prediction into popup search and add compound suggestion rendering</name>
  <files>extension/popup/popup.js, extension/i18n/strings.js, extension/styles/popup.css</files>
  <action>
  Modify `performSearch` in popup.js to call `predictCompound` when the query has no direct results AND no decomposition of the exact query. Insert the prediction call between the fallback-direction search and the existing decomposition fallback (line ~1452).

  The flow becomes:
  1. (existing) Direct matches, inflection matches, fallback direction
  2. (existing) If combined.length === 0, try exact decomposition via tryDecomposeQuery
  3. (NEW) If still no results, call predictCompound for partial-input compound suggestions
  4. If predictions found, render them as a list of compound suggestion cards

  Implementation details:
  - Access predictCompound via the indexes object returned by buildIndexes (stored during dictionary load). Add a module-level variable `let currentIndexes = null;` and capture the indexes object at the same place nounGenusMap is captured (~line 530). Then call `currentIndexes.predictCompound(q)`.
  - Create `renderCompoundSuggestions(query, predictions)` that renders a list of compact compound cards. Each card shows: the full compound word, its breakdown (parts + fuge), and gender. Clicking a suggestion triggers `performSearch(suggestion.word)` which will then hit the exact decomposition path and show the full compound card.
  - Add a heading above the suggestions: use i18n key `compound_suggestions_heading` ("Mente du et sammensatt ord?" / "Meinte du eit samansett ord?" / "Did you mean a compound word?")
  - Style suggestion cards with `compound-suggestion` class: compact layout, subtle background, hover state. Reuse existing `.compound-card` styling patterns from popup.css.

  Also move the exact compound decomposition check BEFORE the fallback-direction search so that typing "chefsstuhl" (exact) shows the compound card immediately without trying the fallback direction first. Current order tries fallback first, which can return false matches on partial word overlaps.
  </action>
  <verify>
    <automated>node test/phase-24-unit.test.js</automated>
  </verify>
  <done>
  - Typing a partial compound in popup search shows compound suggestions when no direct results exist
  - Clicking a suggestion navigates to the full compound card
  - Exact compound input still shows the existing compound decomposition card
  - i18n strings added for NB/NN/EN
  - Suggestion cards have proper styling
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance compound card with pedagogy note, translation guess, and back-navigation</name>
  <files>extension/popup/popup.js, extension/i18n/strings.js, extension/styles/popup.css</files>
  <action>
  Enhance `renderCompoundCard` to add three new sections:

  **COMP-02 — Pedagogical note:**
  Below the compound breakdown, add a pedagogical note explaining that the last component determines the compound's gender and grammatical behavior. Use i18n key `compound_pedagogy` with interpolation for the last component word:
  - NB: "I sammensatte ord bestemmer det siste leddet kjonn og boying. Klikk {lastComponent} for a se oppslagsordet."
  - NN: "I samansette ord avgjer det siste leddet kjonn og boying. Klikk {lastComponent} for a sja oppslagsordet."
  - EN: "In compound words, the last component determines gender and inflection. Click {lastComponent} to see its dictionary entry."

  Render the last component word as a clickable link (styled distinctly from the existing component buttons) that triggers navigation to that component's dictionary entry.

  **COMP-04 — Translation guess ("Kvalifisert gjetning"):**
  For each part in the decomposition, look up the component word in allWords (or dictionary nounbank) and get its Norwegian translation via getTranslation. Concatenate them with " + " to form the guess. Display below the pedagogical note with the label from i18n key `compound_translation_guess`:
  - NB: "Kvalifisert gjetning av det sammensatte ordets betydning"
  - NN: "Kvalifisert gjetting av det samansette ordets tyding"
  - EN: "Qualified guess at the compound word's meaning"

  Show the assembled translation in a styled block: `<div class="compound-guess"><span class="compound-guess-label">{label}:</span> <span class="compound-guess-text">{translation1} + {translation2}</span></div>`

  If any component has no translation found, show it as the raw word in parentheses (e.g., "(chef) + stol").

  **COMP-03 — Back-navigation:**
  Add a module-level navigation stack: `let compoundNavStack = [];`

  When a user clicks a component button (existing behavior) or the pedagogical link (new), push `{ query, decomposition }` onto the stack before calling performSearch.

  In `renderCompoundCard` and `renderResults`, check if `compoundNavStack.length > 0`. If so, prepend a back-link at the top of the results container: `<a class="compound-back-link" href="#">Tilbake til "{compound}"</a>`. Clicking the back-link pops the stack and re-renders the compound card with the stored query and decomposition. Use i18n key `compound_back_link`:
  - NB: 'Tilbake til "{word}"'
  - NN: 'Tilbake til "{word}"'
  - EN: 'Back to "{word}"'

  Clear the stack when the user types a new query in the search input (in the input event handler, ~line 1164).

  Style the back-link with `compound-back-link` class: left arrow icon (CSS ::before with content "\\2190 " i.e. left arrow), link color matching the extension's purple accent, margin-bottom for spacing.
  </action>
  <verify>
    <automated>node test/phase-24-unit.test.js</automated>
  </verify>
  <done>
  - Compound card shows pedagogical note about last component determining gender
  - Last component is a clickable link that navigates to its dictionary entry
  - After navigating from compound card, "Tilbake til [compound]" link appears
  - Clicking back-link returns to the compound card with full state preserved
  - Back-link also appears when navigating from regular results back to compound
  - Compound card shows "Kvalifisert gjetning" with concatenated component translations
  - Missing component translations gracefully fall back to raw word in parentheses
  - Navigation stack clears on new search input
  - All i18n strings present for NB/NN/EN
  - All new elements properly styled
  </done>
</task>

</tasks>

<verification>
1. `node test/phase-24-unit.test.js` — all prediction unit tests pass
2. `npm run check-network-silence` — no forbidden network calls introduced
3. `npm run check-bundle-size` — extension stays under 20 MiB cap
4. Manual browser test: load extension, search "chefsstu" in German mode, verify suggestion appears, click it, see compound card with pedagogy + translation guess, click component, see back-link, click back-link to return
</verification>

<success_criteria>
- Typing partial compound in popup search shows compound word suggestions
- Compound card displays pedagogical note with clickable last-component link
- Back-navigation works bidirectionally between compound and component entries
- Translation guess assembled from component translations, labeled "Kvalifisert gjetning av det sammensatte ordets betydning"
- All i18n strings present for NB, NN, EN
- No regressions in existing search, compound decomposition, or release gates
</success_criteria>

<output>
After completion, create `.planning/phases/24-compound-word-intelligence/24-02-SUMMARY.md`
</output>
