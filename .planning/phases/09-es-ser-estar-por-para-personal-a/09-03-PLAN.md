---
phase: 09-es-ser-estar-por-para-personal-a
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - extension/content/spell-rules/es-por-para.js
  - extension/content/spell-rules/es-personal-a.js
  - extension/styles/content.css
  - extension/manifest.json
  - fixtures/es/por-para.jsonl
  - fixtures/es/personal-a.jsonl
autonomous: true
requirements: [ES-02, ES-03]

must_haves:
  truths:
    - "benchmark-texts/es.txt line 30 'por mi familia' flips from unflagged to flagged by es-por-para rule"
    - "benchmark-texts/es.txt line 37 'por leer un libro' flips from unflagged to flagged by es-por-para rule"
    - "benchmark-texts/es.txt line 45 'Veo Juan' flips from unflagged to flagged by es-personal-a rule"
    - "Correct por/para usage in acceptance fixtures does NOT flag"
    - "Correct use of personal 'a' (or non-human objects) in acceptance fixtures does NOT flag"
    - "The trigger-table data shape is consumed from grammar-tables.js, not re-implemented locally"
  artifacts:
    - path: "extension/content/spell-rules/es-por-para.js"
      provides: "ES-02 por/para preposition confusion rule"
      contains: "es-por-para"
    - path: "extension/content/spell-rules/es-personal-a.js"
      provides: "ES-03 personal 'a' missing-marker rule"
      contains: "es-personal-a"
    - path: "fixtures/es/por-para.jsonl"
      provides: "Regression fixtures for ES-02"
      min_lines: 45
    - path: "fixtures/es/personal-a.jsonl"
      provides: "Regression fixtures for ES-03"
      min_lines: 45
  key_links:
    - from: "extension/content/spell-rules/es-por-para.js"
      to: "self.__lexiGrammarTables"
      via: "lazy-init getTables()"
      pattern: "ES_POR_PARA_TRIGGERS"
    - from: "extension/content/spell-rules/es-personal-a.js"
      to: "self.__lexiGrammarTables"
      via: "lazy-init getTables()"
      pattern: "ES_HUMAN_NOUNS"
    - from: "extension/content/spell-rules/es-personal-a.js"
      to: "spell-check-core.js"
      via: "isLikelyProperNoun function"
      pattern: "isLikelyProperNoun"
---

<objective>
Ship ES-02 por/para preposition confusion rule and ES-03 personal "a" missing-marker rule. Both rules consume trigger data from grammar-tables.js.

Purpose: "por mi familia" (should be "para") and "Veo Juan" (missing "a") are high-frequency Norwegian-student Spanish errors. These two rules are independent of each other and can be built together since they share the same infrastructure contract.
Output: es-por-para.js + es-personal-a.js rule files, fixture suites, CSS bindings. Phase 11 stub shape-sanity assertion.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-es-ser-estar-por-para-personal-a/09-RESEARCH.md
@.planning/phases/09-es-ser-estar-por-para-personal-a/09-01-SUMMARY.md
@extension/content/spell-rules/grammar-tables.js
@extension/content/spell-rules/de-prep-case.js
@extension/content/spell-check-core.js
@extension/styles/content.css

<interfaces>
<!-- From grammar-tables.js (after Plan 01) — ES tables: -->
ES_POR_PARA_TRIGGERS: Array<{ id, wrongPrep, correctPrep, context, detect }>
ES_HUMAN_NOUNS: Set<string> (madre, padre, hermano, profesor, ...)
ES_COPULA_VERBS: Set<string> (ser, estar, parecer, ...)
ES_SER_FORMS: Set<string>
ES_ESTAR_FORMS: Set<string>

<!-- From spell-check-core.js — isLikelyProperNoun: -->
function isLikelyProperNoun(token, index, tokens, text)
// Returns true if token is capitalized mid-sentence (proper noun heuristic)

<!-- From spell-check-core.js — ctx shape: -->
ctx.lang, ctx.tokens, ctx.sentences, ctx.getTagged(i), ctx.vocab
ctx.getTagged(i).isFinite — true if token is a conjugated verb form
ctx.getTagged(i).pos — 'verb', 'noun', 'adj', 'pron', 'sub', 'other'

<!-- From spell-check-core.js — rule registration: -->
host.__lexiSpellRules.push({ id, languages, priority, check(ctx), explain(finding) })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create es-por-para.js rule + fixture suite</name>
  <files>extension/content/spell-rules/es-por-para.js, fixtures/es/por-para.jsonl, extension/styles/content.css, extension/manifest.json</files>
  <behavior>
  Positive cases (MUST flag):
  - "para comprar comida por mi familia" -> flag "por" before "mi familia", suggest "para" (beneficiary)
  - "por leer un libro" -> flag "por", suggest "para" (purpose + infinitive)
  - "Estudio espanol por mi trabajo" -> flag "por", suggest "para" (purpose/goal)
  - "por manana tengo que terminar" -> flag "por", suggest "para" (deadline)
  - "Trabajo por ganar dinero" -> flag "por", suggest "para" (purpose + infinitive)

  Acceptance cases (MUST NOT flag):
  - "Gracias por tu ayuda" (correct: cause/reason)
  - "por la manana" (correct: time expression)
  - "por ejemplo" (correct: fixed phrase)
  - "por favor" (correct: fixed phrase)
  - "para mi madre" (correct: beneficiary with para)
  - "para comer" (correct: purpose with para)
  - "por tres horas" (correct: duration)
  - "Caminamos por el parque" (correct: through/along)
  - "por eso" (correct: cause)

  Fixture ratio: at least 30 positive + 15 acceptance.
  </behavior>
  <action>
  **1. Create `fixtures/es/por-para.jsonl`** with the fixture suite. >= 30 positive + >= 15 acceptance fixtures.

  **2. Create `extension/content/spell-rules/es-por-para.js`** as an IIFE:

  - **Rule metadata:** `id: 'es-por-para'`, `languages: ['es']`, `priority: 50`.
  - **Lazy-init getTables():** Read `ES_POR_PARA_TRIGGERS`, `ES_HUMAN_NOUNS` from `host.__lexiGrammarTables`.
  - **check(ctx) logic:**
    1. Guard: `ctx.lang !== 'es'` -> return [].
    2. For each token, check if word is `por` or `para`.
    3. When `por` found, check trigger patterns in priority order:
       a. **Skip safe phrases:** `por favor`, `por ejemplo`, `por eso`, `por supuesto`, `por fin`, `por lo menos`, `por cierto`, `por lo tanto`. Build a SAFE_POR_PHRASES set and check if `por` + next token forms a safe phrase. Do NOT flag.
       b. **por + infinitive (purpose):** If next token is a verb infinitive (ends in -ar/-er/-ir and is in `ctx.vocab.verbInfinitive`), flag `por` -> `para`. This catches "por leer", "por comprar", "por ganar".
       c. **por + human noun/pronoun (beneficiary):** If next 1-2 tokens contain a word in `ES_HUMAN_NOUNS` or a known pronoun (`mi`, `tu`, `su`, `nuestro`, `vuestro` + familia forms), AND the preceding context includes a verb + object (purpose context), flag `por` -> `para`.
       d. **por + deadline marker:** If next token is a time word suggesting deadline (`manana`, `lunes`, `martes`, ..., `enero`, ..., `proximo`, `siguiente`), flag `por` -> `para`.
    4. When `para` found, check:
       a. **para + duration number:** If next token is a number or duration word (`dos`, `tres`, `cuatro`, `cinco`, `muchas`, `varias`) followed by a time unit (`horas`, `dias`, `semanas`, `meses`, `anos`), flag `para` -> `por`.
    5. All findings use warn severity (P2).

  - **explain function:** `explain: (finding) => ({ nb: '...', nn: '...', severity: 'warning' })`. Norwegian explanation should mention the specific pattern (e.g. "Bruk 'para' foran person som mottar noe" for beneficiary).

  **3. Add `.lh-spell-es-por-para` CSS binding** in content.css with P2 warn-tier dot colour.

  **4. Add `es-por-para.js` to manifest.json** content_scripts array after grammar-tables.js.

  No data component in papertek-vocabulary -- por/para trigger patterns are a closed pedagogical set.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp && npm run check-fixtures && npm run check-explain-contract && npm run check-rule-css-wiring && npm run check-network-silence</automated>
  </verify>
  <done>"por mi familia" and "por leer" flag with es-por-para suggesting "para"; safe phrases (por favor, por ejemplo) do not flag; all release gates pass; >= 30 positive + >= 15 acceptance fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create es-personal-a.js rule + fixture suite + Phase 11 stub shape-sanity test</name>
  <files>extension/content/spell-rules/es-personal-a.js, fixtures/es/personal-a.jsonl, extension/styles/content.css, extension/manifest.json</files>
  <behavior>
  Positive cases (MUST flag):
  - "Veo Juan todos los dias" -> flag "Juan", suggest "a Juan" (proper noun human DO)
  - "Conozco Maria" -> flag "Maria", suggest "a Maria"
  - "Ayudo mi madre" -> flag "mi madre", suggest "a mi madre" (known human noun)
  - "Busco mi hermano" -> flag "mi hermano", suggest "a mi hermano"
  - "Llamo mi profesor" -> flag "mi profesor", suggest "a mi profesor"

  Acceptance cases (MUST NOT flag):
  - "Veo a Juan" (personal "a" already present)
  - "Veo la casa" (non-human object)
  - "Compro comida" (non-human object)
  - "Soy profesor" (copula verb, not transitive)
  - "Visito Madrid" (place name -- conservative: do not flag place names)
  - "Es mi madre" (copula, not direct object)
  - "Hablo con Juan" (preposition present, not bare DO)
  - "Juan viene" (Juan is subject, not object)

  Fixture ratio: at least 30 positive + 15 acceptance.
  </behavior>
  <action>
  **1. Create `fixtures/es/personal-a.jsonl`** with the fixture suite. >= 30 positive + >= 15 acceptance.

  **2. Create `extension/content/spell-rules/es-personal-a.js`** as an IIFE:

  - **Rule metadata:** `id: 'es-personal-a'`, `languages: ['es']`, `priority: 55` (slightly after por/para to avoid interaction).
  - **Lazy-init getTables():** Read `ES_HUMAN_NOUNS`, `ES_COPULA_VERBS`, `ES_SER_FORMS`, `ES_ESTAR_FORMS` from `host.__lexiGrammarTables`.
  - **check(ctx) logic:**
    1. Guard: `ctx.lang !== 'es'` -> return [].
    2. For each token at position `i`:
       a. Check if token is a conjugated verb form using `ctx.getTagged(i).isFinite`.
       b. If not finite, skip.
       c. Check if the verb is a copula: if `ctx.tokens[i].word` is in `ES_SER_FORMS` or `ES_ESTAR_FORMS` or `ES_COPULA_VERBS`, skip (copulas don't take personal "a").
       d. Check if `ctx.tokens[i+1]` exists. If it equals `'a'`, skip (personal "a" already present).
       e. Check if `ctx.tokens[i+1]` is preceded by a preposition (`en`, `con`, `de`, `por`, `para`, `sin`, `sobre`, `entre`, `hacia`, `hasta`). If so, skip (not a direct object).
       f. Determine if `ctx.tokens[i+1]` is human:
          - If `ctx.tokens[i+1].word` is in `ES_HUMAN_NOUNS`, it is human.
          - If `isLikelyProperNoun(ctx.tokens[i+1], i+1, ctx.tokens, ctx.text)` returns true AND the word is NOT in a PLACE_NAMES blocklist (`madrid, barcelona, espana, mexico, colombia, argentina, peru, chile, francia, alemania, noruega, europa, america, africa, asia`), treat as human proper noun.
       g. If human, check for possessive determiner pattern: if `ctx.tokens[i+1].word` is a possessive (`mi`, `tu`, `su`, `nuestro`, `vuestro`, `mis`, `tus`, `sus`) AND `ctx.tokens[i+2]` is in `ES_HUMAN_NOUNS`, flag the possessive token position with fix `"a " + ctx.tokens[i+1].display + " " + ctx.tokens[i+2].display`. E.g. "Ayudo mi madre" -> suggest "a mi madre".
       h. If human (direct proper noun), flag `ctx.tokens[i+1]` with fix `"a " + ctx.tokens[i+1].display`.
    3. Severity: warning (P2).

  - **explain function:** `({ nb: 'Spansk bruker "a" foran menneskelige direkte objekter.', nn: 'Spansk brukar "a" framfor menneskelege direkte objekt.', severity: 'warning' })`.

  **3. Add `.lh-spell-es-personal-a` CSS binding** in content.css with P2 warn-tier dot colour.

  **4. Add `es-personal-a.js` to manifest.json** content_scripts array.

  **5. Phase 11 stub shape-sanity assertion:** Add a verification step (in the verify command below, not a separate file) that confirms the trigger-table shape from grammar-tables.js is consumable: read `host.__lexiGrammarTables.ES_COPULA_ADJ` and assert it is a non-empty object with string values. This proves Phase 11's `ES_SUBJUNCTIVE_TRIGGERS` can follow the same `{ triggerWord: requiredForm }` lookup pattern. Per success criterion 4 in the roadmap.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp && npm run check-fixtures && npm run check-explain-contract && npm run check-rule-css-wiring && npm run check-network-silence && npm run check-benchmark-coverage && node -e "
      // Phase 11 stub shape-sanity: confirm trigger-table shape is reusable
      const gt = require('./extension/content/spell-rules/grammar-tables.js');
      const assert = require('assert');
      assert(typeof gt.ES_COPULA_ADJ === 'object', 'trigger-table must be an object');
      const entries = Object.entries(gt.ES_COPULA_ADJ);
      assert(entries.length >= 20, 'trigger-table must have >= 20 entries');
      entries.forEach(([k, v]) => {
        assert(typeof k === 'string' && typeof v === 'string', 'all entries must be string->string');
        assert(['ser', 'estar', 'both'].includes(v), 'values must be ser/estar/both');
      });
      console.log('Phase 11 trigger-table shape-sanity: PASS (' + entries.length + ' entries)');
    "</automated>
  </verify>
  <done>"Veo Juan" flags with es-personal-a suggesting "a Juan"; "Veo a Juan" does not flag; place names do not flag; copula verbs do not flag; all release gates pass including check-benchmark-coverage; Phase 11 trigger-table shape-sanity assertion passes.</done>
</task>

</tasks>

<verification>
1. `npm run check-fixtures` exits 0 — por-para + personal-a fixtures pass
2. `npm run check-explain-contract` exits 0 — both rules have explain with nb, nn, severity
3. `npm run check-rule-css-wiring` exits 0 — .lh-spell-es-por-para and .lh-spell-es-personal-a CSS bindings exist
4. `npm run check-network-silence` exits 0 — no fetch/XHR in new rule files
5. `npm run check-benchmark-coverage` exits 0 — es.30, es.37, es.45, es.46 all flip
6. `npm run check-bundle-size` exits 0
7. Phase 11 stub shape-sanity assertion passes
</verification>

<success_criteria>
- es-por-para.js fires on "por mi familia" and "por leer" (benchmark lines 30, 37) with warn severity
- es-personal-a.js fires on "Veo Juan" (benchmark line 45) with warn severity
- Acceptance fixtures for correct usage pass
- Place names (Madrid, Barcelona) do NOT flag for personal "a"
- Trigger-table shape consumed from grammar-tables.js, not re-implemented
- Phase 11 stub asserts the trigger-table shape is reusable
- All release gates pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-es-ser-estar-por-para-personal-a/09-03-SUMMARY.md`
</output>
