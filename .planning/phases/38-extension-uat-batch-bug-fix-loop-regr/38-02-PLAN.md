---
phase: 38-extension-uat-batch-bug-fix-loop-regr
plan: 02
type: verify
wave: 1
depends_on: []
files_modified:
  - .planning/uat/UAT-EXT-04.md
  - .planning/uat/findings/
autonomous: false
verification_kind: human-browser-walk
requirements: [UAT-EXT-04]
must_haves:
  truths:
    - "Walker performs Phase 30-01 popup view 9-step browser walkthrough in real Chrome (canonical walk — second in sequence per STATE v3.2 entry decision)."
    - "`.planning/uat/UAT-EXT-04.md` exists, instantiated from `.planning/uat/TEMPLATE-walkthrough.md` (filename = walkthrough_id from REQUIREMENTS.md)."
    - "Pre-flight evidence block fully populated and `node scripts/check-vocab-deployment.js` exit-0 output captured (HYG-07 hard pre-condition)."
    - "All 9 canonical steps recorded with BOTH expected AND observed: load extension, search, lang switch, direction toggle, compound suggestion, Lær mer popover, settings, account section, pause, vocab-updates banner."
    - "Each ❌ step has a corresponding F38-N finding file with regression_fixture_id=TBD (HYG-02), severity, sync_status, walkthrough_id=UAT-EXT-04."
    - "Walker signs off with name + ISO-8601 date; findings list filled or marked `none`."
    - "Auto-mode hard-pauses on this plan (HYG-03 verification_kind: human-browser-walk)."
    - "Defect closure happens in decimal-insert fix plans (38-02.1, 38-02.2, …) — parallel siblings per RESEARCH Open Question 1 — appended only AFTER this walk completes. This plan does not contain fixes."
  artifacts:
    - path: ".planning/uat/UAT-EXT-04.md"
      provides: "Phase 30-01 popup view canonical 9-step walkthrough verification log"
      contains: "Walker signs off:"
  key_links:
    - from: ".planning/uat/UAT-EXT-04.md"
      to: ".planning/uat/findings/F38-N.md"
      via: "Defects observed bullet list"
      pattern: "F38-\\d+"
---

<objective>
Canonical walkthrough: walk all 9 popup view steps from Phase 30-01 (load extension, search, lang switch, direction toggle, compound suggestion, Lær mer popover, settings, account section, pause, vocab-updates banner) in real Chrome. Second in the locked sequence (warm-up → canonical → highest-stakes → final).

Purpose: Satisfy UAT-EXT-04. Re-confirm the Phase 30 view-module dep-injection refactor works end-to-end against real Chrome surfaces. Surface defects via F38-N findings for follow-up decimal-insert fix plans.
Output: `.planning/uat/UAT-EXT-04.md` walkthrough log + 0..N `.planning/uat/findings/F38-N.md` files.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/uat/TEMPLATE-walkthrough.md
@.planning/uat/TEMPLATE-finding.md
@.planning/phases/38-extension-uat-batch-bug-fix-loop-regr/38-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Instantiate UAT-EXT-04 walkthrough log from template</name>
  <files>.planning/uat/UAT-EXT-04.md</files>
  <action>
    Copy template to instance:

    ```bash
    cp .planning/uat/TEMPLATE-walkthrough.md .planning/uat/UAT-EXT-04.md
    ```

    Edit frontmatter:
    - `walkthrough_id: UAT-EXT-04`
    - `walker: Geir`
    - `date: <ISO-8601 today>`
    - `target_browsers: [chrome, edge, brave]`

    Title: `# Walkthrough: Phase 30-01 popup view 9-step (canonical)`

    Draft the 9 canonical Steps verbatim from REQUIREMENTS.md UAT-EXT-04:

    1. **Load extension** (chrome://extensions, install/reload, click toolbar icon) → expected: popup opens, no console errors, default view renders → observed: TBD → ✅/❌
    2. **Search** (type a word in search box) → expected: matching dictionary entries render via DictionaryView module → observed: TBD → ✅/❌
    3. **Lang switch** (popup language selector → switch to DE/ES/FR/EN/NB/NN) → expected: vocab swaps, search results re-render in new language, no stale results from prior language → observed: TBD → ✅/❌
    4. **Direction toggle** (toggle source/target language direction) → expected: dictionary direction inverts, results match new direction → observed: TBD → ✅/❌
    5. **Compound suggestion** (search a known compound, e.g. NB "språkundervisning") → expected: decomposition suggestion renders → observed: TBD → ✅/❌
    6. **Lær mer popover** (click "Lær mer" link on a pedagogy-enriched entry, e.g. DE preposition) → expected: pedagogy panel expands with examples + illustration → observed: TBD → ✅/❌
    7. **Settings** (popup settings view → toggle preset / grammar features / dark mode) → expected: settings persist across popup close+reopen (chrome.storage.local) → observed: TBD → ✅/❌
    8. **Account section** (popup account view) → expected: Vipps login state renders correctly (logged in shows email/quota; logged out shows login button) → observed: TBD → ✅/❌
    9. **Pause + vocab-updates banner** (toggle pause; trigger vocab-updates banner if available) → expected: pause toggle persists, banner renders if vocab-revision drift detected → observed: TBD → ✅/❌

    Walker fills `observed` during the walk.
  </action>
  <verify>
    <automated>test -f .planning/uat/UAT-EXT-04.md && node ~/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter get .planning/uat/UAT-EXT-04.md --field walkthrough_id | grep -q UAT-EXT-04</automated>
  </verify>
  <done>UAT-EXT-04.md exists; frontmatter walkthrough_id=UAT-EXT-04, walker=Geir, date=today; 9 canonical Steps drafted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walker performs the 9-step canonical walk</name>
  <files>.planning/uat/UAT-EXT-04.md, .planning/uat/findings/</files>
  <action>
    Walker performs the walk:

    1. **Pre-flight (HARD):**
       - `node scripts/check-vocab-deployment.js` exit 0 — paste output into pre-flight block.
       - Populate frontmatter: ext_version (from extension/manifest.json), idb_revision, preset_profile, browser_version, reload_ts.

    2. **Execute Steps 1-9.** For each step fill `observed:`, mark ✅/❌.

    3. **For every ❌, instantiate a finding** at `.planning/uat/findings/F38-<seq>.md` from TEMPLATE-finding.md. Fill: f_id, severity, sync_status (extension-only by default; needs-resync if touches synced surfaces — see CLAUDE.md downstream-consumers list), walkthrough_id=UAT-EXT-04, discovered=ISO-8601, status=open, regression_fixture_id=TBD (HARD field per HYG-02 — resolved by decimal-insert fix plan).

       View modules at `extension/popup/views/*.js` are a synced surface — defects here likely have sync_status=needs-resync.

    4. **Outcome:** tick all-pass OR list F38-N findings; sign off `Geir <ISO-8601>`.

    Defect closure NOT in this plan. Multiple defects → parallel sibling decimal-insert plans (38-02.1, 38-02.2, 38-02.3) per RESEARCH Open Question 1 recommendation.

    Inter-walk-blocking severity:blocker defects (e.g. popup unloads break exam-mode walk) → promote that fix plan to wave 1.5 with explicit depends_on=[38-02], sequenced before 38-03 starts (RESEARCH Open Question 3).
  </action>
  <verify>
    <automated>grep -q "Walker signs off:" .planning/uat/UAT-EXT-04.md && grep -q "Geir" .planning/uat/UAT-EXT-04.md && ! grep -q "observed: TBD" .planning/uat/UAT-EXT-04.md</automated>
  </verify>
  <done>Walker signed off; pre-flight evidence captured; all 9 steps recorded with observed; every ❌ has a F38-N finding with TBD regression_fixture_id.</done>
</task>

</tasks>

<verification>
- UAT-EXT-04.md complete: pre-flight populated, 9 steps walked, walker signed off.
- F38-N findings filed for each ❌ with HYG-02-compliant frontmatter.
- If defects surfaced, decimal-insert fix plans (38-02.1, 38-02.2, …) appended per RESEARCH Pattern 2. Each fix plan: extension fix → regression artifact (FIX-02 HARD) → 14 release gates green → 3-place version bump (HYG-04) → npm run package → [lockdown-resync-needed] commit marker if synced surface touched (HYG-06) → tag + GitHub Release asset.
- Auto-mode hard-pauses on verification_kind: human-browser-walk.
</verification>

<success_criteria>
UAT-EXT-04 9-step canonical walkthrough log committed; all defects captured as F38-N findings; canonical sequence position satisfied (next: 38-03 highest-stakes exam-mode walk).
</success_criteria>

<output>
After completion, create `.planning/phases/38-extension-uat-batch-bug-fix-loop-regr/38-02-SUMMARY.md` documenting walk outcome.
</output>
