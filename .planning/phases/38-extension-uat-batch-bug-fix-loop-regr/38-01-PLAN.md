---
phase: 38-extension-uat-batch-bug-fix-loop-regr
plan: 01
type: verify
wave: 1
depends_on: []
files_modified:
  - .planning/uat/UAT-EXT-01.md
  - .planning/uat/findings/
autonomous: false
verification_kind: human-browser-walk
requirements: [UAT-EXT-01]
must_haves:
  truths:
    - "Walker performs F36-1 fr-aspect-hint browser confirmation in real Chrome (warm-up walkthrough — first in sequence per STATE v3.2 entry decision)."
    - "`.planning/uat/UAT-EXT-01.md` exists, instantiated from `.planning/uat/TEMPLATE-walkthrough.md`."
    - "Pre-flight evidence block fully populated: ext_version (matches extension/manifest.json), idb_revision, preset_profile, browser_version, reload_ts, walker=Geir, date=ISO-8601 today."
    - "`node scripts/check-vocab-deployment.js` exit 0 captured in pre-flight (defends Pitfall 2 stale-vocab class)."
    - "Every step records BOTH expected AND observed (empty observed = walk-not-completed signal per HYG-01)."
    - "Each ❌ step has a corresponding `.planning/uat/findings/F38-N.md` instantiated from TEMPLATE-finding.md, with severity, sync_status, walkthrough_id=UAT-EXT-01, discovered=ISO-8601, regression_fixture_id=TBD (resolved by decimal-insert fix plan, not this plan)."
    - "If no defects, walkthrough Outcome section signed off (walker name + ISO-8601 date) and findings list = `none`."
    - "Auto-mode hard-pauses on this plan (HYG-03 verification_kind: human-browser-walk frontmatter is the trigger)."
  artifacts:
    - path: ".planning/uat/UAT-EXT-01.md"
      provides: "F36-1 fr-aspect-hint warm-up walkthrough verification log"
      contains: "Walker signs off:"
  key_links:
    - from: ".planning/uat/UAT-EXT-01.md"
      to: ".planning/uat/findings/F38-N.md"
      via: "Defects observed bullet list"
      pattern: "F38-\\d+"
---

<objective>
Warm-up walkthrough: confirm in real Chrome that the F36-1 fr-aspect-hint rule (passé-composé vs imparfait soft-hint) fires correctly AND its Lær mer panel renders. First in the locked sequence (warm-up → canonical → highest-stakes → final per STATE v3.2 entry decision).

Purpose: Satisfy UAT-EXT-01. Re-confirm the v2.9.15+ vocab-seam fix landed correctly in real-browser conditions. Surface any defects via F38-N findings for follow-up decimal-insert fix plans (38-01.1, 38-01.2, …).
Output: `.planning/uat/UAT-EXT-01.md` walkthrough log + 0..N `.planning/uat/findings/F38-N.md` files.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/uat/TEMPLATE-walkthrough.md
@.planning/uat/TEMPLATE-finding.md
@.planning/phases/38-extension-uat-batch-bug-fix-loop-regr/38-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Instantiate UAT-EXT-01 walkthrough log from template</name>
  <files>.planning/uat/UAT-EXT-01.md</files>
  <action>
    Copy `.planning/uat/TEMPLATE-walkthrough.md` to `.planning/uat/UAT-EXT-01.md`:

    ```bash
    cp .planning/uat/TEMPLATE-walkthrough.md .planning/uat/UAT-EXT-01.md
    ```

    Edit the new file's frontmatter:
    - `walkthrough_id: UAT-EXT-01`
    - `walker: Geir`
    - `date: <ISO-8601 today>`
    - Leave `ext_version`, `idb_revision`, `preset_profile`, `browser_version`, `reload_ts` as TBD — walker fills during pre-flight.

    Set the title `# Walkthrough: F36-1 fr-aspect-hint browser confirmation (warm-up)`.

    Draft the Steps section with concrete observables for fr-aspect-hint:
    1. Open a page with French text input. Type a sentence with passé-composé in narrative-imperfect context (e.g. "Pendant que je marchais, j'ai vu un chien.") → expected: soft-hint dot appears under `j'ai vu` OR `marchais` OR both per rule logic → observed: TBD → ✅/❌
    2. Hover the dot → expected: tooltip text matches rule's explain.nb message (passé-composé vs imparfait pedagogy phrasing) → observed: TBD → ✅/❌
    3. Click the dot to open the popover → expected: popover renders with "Lær mer" link visible → observed: TBD → ✅/❌
    4. Click "Lær mer" → expected: pedagogy panel expands with examples + illustration if present → observed: TBD → ✅/❌
    5. Switch to NN locale (popup settings → language → nynorsk) and reload → expected: popover/Lær mer text renders in NN register → observed: TBD → ✅/❌
    6. Switch to default preset and verify rule still fires (defends Phase 05.1 feature-gating regression class) → observed: TBD → ✅/❌

    Walker fills `observed` during the actual walk.
  </action>
  <verify>
    <automated>test -f .planning/uat/UAT-EXT-01.md && node ~/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter get .planning/uat/UAT-EXT-01.md --field walkthrough_id | grep -q UAT-EXT-01</automated>
  </verify>
  <done>UAT-EXT-01.md exists; frontmatter has walkthrough_id=UAT-EXT-01, walker=Geir, date=today; Steps section drafted with concrete fr-aspect-hint observables.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walker performs the walk in real Chrome</name>
  <files>.planning/uat/UAT-EXT-01.md, .planning/uat/findings/</files>
  <action>
    Walker performs the walk. Sequence:

    1. **Pre-flight (HARD — defends Pitfall 2 stale-vocab class):**
       - Run `node scripts/check-vocab-deployment.js` from repo root. Capture exit code (must be 0) and tail output. Paste both into the pre-flight code block in UAT-EXT-01.md.
       - Read `extension/manifest.json` `version` field. Paste into frontmatter `ext_version`.
       - Open chrome://extensions, reload Leksihjelp. Capture ISO-8601 timestamp; paste into `reload_ts`.
       - Open DevTools → Application → IndexedDB on a leksihjelp-active page. Capture revision; paste into `idb_revision`.
       - Capture browser+version (chrome://version); paste into `browser_version`.
       - Confirm popup preset profile matches frontmatter `preset_profile` (default unless explicitly testing variant).

    2. **Execute Steps 1-6** drafted in Task 1. For each step, fill `observed:` with what actually happened. Mark ✅ if observed matches expected, ❌ otherwise.

    3. **For every ❌**, instantiate a finding:
       ```bash
       mkdir -p .planning/uat/findings
       cp .planning/uat/TEMPLATE-finding.md .planning/uat/findings/F38-<seq>.md
       ```
       Fill frontmatter: `f_id: F38-<seq>`, `severity` (blocker|major|minor|trivial), `sync_status` (extension-only is the default for spell-rule defects; needs-resync if it touches a synced surface like styles/content.css), `walkthrough_id: UAT-EXT-01`, `discovered: <ISO-8601>`, `status: open`, `regression_fixture_id: TBD` (HARD-required field per HYG-02 — leave TBD; the decimal-insert fix plan will resolve it).
       Fill body: Reproduction (numbered repro steps), Observed output (verbatim DOM/console/screenshot), Expected output, Root cause hypothesis (file:line if known).

    4. **Outcome section in UAT-EXT-01.md:**
       - Tick "All steps pass" if no ❌; otherwise tick "Findings filed" and list F38-N IDs comma-separated.
       - Sign off: `Geir <ISO-8601>`.

    DO NOT fix any defects in this plan. Defect closure happens in decimal-insert plans (38-01.1, …) appended after this plan completes. This plan's job is to walk and document.

    DO NOT skip the pre-flight. A walk against stale vocab is a walk-not-completed (Pitfall 2 / STATE v3.2 entry decision).

    [lockdown-resync-needed] does NOT apply to this commit — UAT logs are not synced surfaces.
  </action>
  <verify>
    <automated>grep -q "Walker signs off:" .planning/uat/UAT-EXT-01.md && grep -q "Geir" .planning/uat/UAT-EXT-01.md && ! grep -q "observed: TBD" .planning/uat/UAT-EXT-01.md</automated>
  </verify>
  <done>Walker signed off with name + ISO-8601 date; no `observed: TBD` lines remain; every ❌ step has a corresponding F38-N finding file with regression_fixture_id=TBD; pre-flight evidence block fully populated with check-vocab-deployment exit-0 output.</done>
</task>

</tasks>

<verification>
- `.planning/uat/UAT-EXT-01.md` exists with all pre-flight fields populated, all steps recorded with both expected + observed, walker sign-off present.
- For every ❌ in Steps, a corresponding `.planning/uat/findings/F38-N.md` exists with full frontmatter (severity, sync_status, walkthrough_id, discovered, regression_fixture_id=TBD, status=open).
- If defects surfaced, append decimal-insert fix plans (38-01.1-PLAN.md, …) per the canonical 9-step Fix → Sync → Re-test loop documented in 38-RESEARCH.md "Code Examples → Canonical 9-step loop". Each fix plan must include FIX-02 regression artifact (fixture or benchmark expectation) — HARD criterion.
- Auto-mode hard-pauses on this plan (`gsd-tools.cjs frontmatter get … --field verification_kind` returns `human-browser-walk`).
</verification>

<success_criteria>
UAT-EXT-01 walkthrough log committed; if defects observed, F38-N findings filed with TBD regression_fixture_id; warm-up sequence position satisfied (next plan: 38-02 canonical popup view 9-step).
</success_criteria>

<output>
After completion, create `.planning/phases/38-extension-uat-batch-bug-fix-loop-regr/38-01-SUMMARY.md` documenting the walk outcome (defects count, finding IDs, any decimal-insert fix plans queued).
</output>
