---
phase: 38-extension-uat-batch-bug-fix-loop-regr
plan: 04
type: verify
wave: 1
depends_on: []
files_modified:
  - .planning/uat/UAT-EXT-02.md
  - .planning/uat/findings/
autonomous: false
verification_kind: human-browser-walk
requirements: [UAT-EXT-02]
must_haves:
  truths:
    - "Walker performs Phase 26 DE Lær mer browser walks in real Chrome (final walk in sequence — 4 default-locale walks + 2 NN/EN locale walks closing F7 from Phase 35)."
    - "`.planning/uat/UAT-EXT-02.md` exists, instantiated from `.planning/uat/TEMPLATE-walkthrough.md`."
    - "Pre-flight evidence captured including `node scripts/check-vocab-deployment.js` exit 0."
    - "All 6 sub-walks recorded with both expected AND observed: (1-4) DE de-prep-case + Wechselpräpositionen Lær mer in default (NB) locale; (5) NN locale Lær mer; (6) EN locale Lær mer — closes F7 carry-over."
    - "Each ❌ has a corresponding F38-N finding with regression_fixture_id=TBD (HYG-02)."
    - "Walker signs off; findings list filled or `none`."
    - "Auto-mode hard-pauses on this plan (HYG-03 verification_kind: human-browser-walk)."
    - "Defect closure happens in decimal-insert fix plans (38-04.1, …) post-walk. Pedagogy-data fixes likely cross repos — fix at papertek-vocabulary if root cause is data, then re-sync via `npm run sync-vocab` and bump version."
  artifacts:
    - path: ".planning/uat/UAT-EXT-02.md"
      provides: "Phase 26 DE Lær mer 4+2 walkthrough verification log (closes F7)"
      contains: "Walker signs off:"
  key_links:
    - from: ".planning/uat/UAT-EXT-02.md"
      to: ".planning/uat/findings/F38-N.md"
      via: "Defects observed bullet list"
      pattern: "F38-\\d+"
---

<objective>
Final walkthrough: walk Phase 26 DE Lær mer pedagogy UI in real Chrome — 4 default-locale (NB) walks covering de-prep-case + Wechselpräpositionen, plus 2 cross-locale walks (NN + EN) that close F7 carried over from Phase 35. Final position in the locked sequence.

Purpose: Satisfy UAT-EXT-02. Closes the Phase 35 F7 deferred finding (NN/EN locale Lær mer never walked in real browser). Surfaces any remaining DE pedagogy-data gaps for upstream fixing in papertek-vocabulary.
Output: `.planning/uat/UAT-EXT-02.md` walkthrough log + 0..N `.planning/uat/findings/F38-N.md` files.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/uat/TEMPLATE-walkthrough.md
@.planning/uat/TEMPLATE-finding.md
@.planning/phases/38-extension-uat-batch-bug-fix-loop-regr/38-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Instantiate UAT-EXT-02 walkthrough log from template</name>
  <files>.planning/uat/UAT-EXT-02.md</files>
  <action>
    Copy template:

    ```bash
    cp .planning/uat/TEMPLATE-walkthrough.md .planning/uat/UAT-EXT-02.md
    ```

    Edit frontmatter:
    - `walkthrough_id: UAT-EXT-02`
    - `walker: Geir`
    - `date: <ISO-8601 today>`

    Title: `# Walkthrough: Phase 26 DE Lær mer (4 default + 2 NN/EN locale, closes F7)`

    Draft the 6 sub-walks as Steps:

    **Default locale (NB) — 4 DE walks:**

    1. **DE de-prep-case (accusative trigger)** — type DE sentence with prep-case mistake, e.g. "Ich gehe in der Schule." → expected: dot fires on `der`; popover renders with "Lær mer"; click expands pedagogy panel showing accusative/dative table + examples → observed: TBD → ✅/❌
    2. **DE de-prep-case (dative trigger)** — type "Ich bin in den Schule." → expected: dot fires on `den`; Lær mer panel renders dative explanation → observed: TBD → ✅/❌
    3. **DE Wechselpräpositionen (movement vs. location)** — type sentence with Wechselpräp mistake (e.g. "Ich lege das Buch auf dem Tisch." for accusative-needed-here case) → expected: dot fires; Lær mer panel explains Wechselpräp + auf/an/in/etc. table with movement vs. location distinction → observed: TBD → ✅/❌
    4. **DE Wechselpräpositionen (full table render)** — open Lær mer on a Wechselpräp finding and verify the prepositions table renders fully with all 9 standard Wechselpräps (an, auf, hinter, in, neben, über, unter, vor, zwischen) → observed: TBD → ✅/❌

    **Cross-locale (closes F7):**

    5. **NN locale Lær mer** — switch popup language to nynorsk; trigger any DE pedagogy finding from steps 1-4; click Lær mer → expected: pedagogy text renders in NN register (not NB fallback) → observed: TBD → ✅/❌
    6. **EN locale Lær mer** — switch popup language to English (memory `project_pedagogy_followups.md` notes EN strings deferred — confirm whether EN strings now ship or fallback gracefully) → expected: either EN pedagogy text renders OR graceful NB fallback with no broken layout → observed: TBD → ✅/❌

    Walker fills `observed` during the walk.
  </action>
  <verify>
    <automated>test -f .planning/uat/UAT-EXT-02.md && node ~/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter get .planning/uat/UAT-EXT-02.md --field walkthrough_id | grep -q UAT-EXT-02</automated>
  </verify>
  <done>UAT-EXT-02.md exists; 6 sub-walks (4 default + 2 cross-locale) drafted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walker performs the 4+2 DE Lær mer walk</name>
  <files>.planning/uat/UAT-EXT-02.md, .planning/uat/findings/</files>
  <action>
    Walker performs the walk:

    1. **Pre-flight (HARD):**
       - `node scripts/check-vocab-deployment.js` exit 0 — paste output. Especially important here: pedagogy data lives in papertek-vocabulary; stale data here = false-defect (Pitfall 2).
       - Frontmatter populated.

    2. **Execute Steps 1-6.** For each sub-walk, fill `observed:`.

    3. **For every ❌, file F38-N finding.** For DE Lær mer defects, classify root-cause direction:
       - **Logic defect** (popover doesn't render, panel doesn't expand, layout broken) → fix in extension code, sync_status=extension-only.
       - **Data defect** (wrong example, missing pedagogy entry, NN/EN string missing) → root cause is in papertek-vocabulary, sync_status=needs-resync. Fix at the API source per CLAUDE.md "Data-Logic Separation"; then re-sync via `npm run sync-vocab` in extension.

       `regression_fixture_id: TBD` (HYG-02 HARD field — resolved in fix plan; data fixes verified via `npm run check-fixtures` after sync, OR via `npm run check-governance-data` if governance data shape is touched).

    4. **F7 closure**: if Steps 5+6 (NN/EN locale Lær mer) both pass, the F7 carry-over from Phase 35 is closed — note this explicitly in the Outcome section. If they fail, the F7 closure happens in the decimal-insert fix plans.

    5. **Outcome:** sign off `Geir <ISO-8601>`.

    Decimal-insert fix plans (38-04.1, …) follow canonical 9-step loop. For pedagogy-data fixes that touch papertek-vocabulary, the loop extends:
    - Fix data in papertek-vocabulary repo.
    - Run papertek-vocabulary's deployment, verify via `papertek-vocabulary.vercel.app/api/vocab/v1/revisions`.
    - In leksihjelp: `npm run sync-vocab` (or per-language variant).
    - Then version bump (HYG-04, HYG-05 — `extension/data/*.json` is a synced surface, no exclusion).
    - [lockdown-resync-needed] commit marker required (HYG-06).
  </action>
  <verify>
    <automated>grep -q "Walker signs off:" .planning/uat/UAT-EXT-02.md && grep -q "Geir" .planning/uat/UAT-EXT-02.md && ! grep -q "observed: TBD" .planning/uat/UAT-EXT-02.md</automated>
  </verify>
  <done>Walker signed off; pre-flight captured; 6 sub-walks recorded; F7 closure status documented in Outcome; F38-N findings filed for each ❌.</done>
</task>

</tasks>

<verification>
- UAT-EXT-02.md complete with pre-flight, 6 sub-walks, sign-off.
- F7 closure status explicitly noted in Outcome section.
- F38-N findings classify root-cause direction (logic vs data) for fix-plan routing.
- Decimal-insert fix plans handle FIX-01 + FIX-02 + (FIX-03 if buildIndexes touched). Data fixes route through papertek-vocabulary first.
- Auto-mode hard-pauses on verification_kind: human-browser-walk.
</verification>

<success_criteria>
UAT-EXT-02 4+2 DE Lær mer final walkthrough log committed; F7 carry-over from Phase 35 explicitly closed (or routed to fix plans); final sequence position satisfied — all 4 walkthroughs complete (next: 38-05 FIX-04 release-asset closure, plus any decimal-insert fixes still in flight).
</success_criteria>

<output>
After completion, create `.planning/phases/38-extension-uat-batch-bug-fix-loop-regr/38-04-SUMMARY.md` documenting walk outcome and F7 closure status.
</output>
