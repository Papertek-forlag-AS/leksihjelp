---
phase: 38-extension-uat-batch-bug-fix-loop-regr
plan: 03
type: verify
wave: 1
depends_on: []
files_modified:
  - .planning/uat/UAT-EXT-03.md
  - .planning/uat/findings/
autonomous: false
verification_kind: human-browser-walk
requirements: [UAT-EXT-03]
must_haves:
  truths:
    - "Walker performs Phase 27 exam-mode 9-step browser walkthrough in real Chrome (highest-stakes walk — third in sequence; school-deployment trust depends on this)."
    - "`.planning/uat/UAT-EXT-03.md` exists, instantiated from `.planning/uat/TEMPLATE-walkthrough.md`."
    - "Pre-flight evidence captured including `node scripts/check-vocab-deployment.js` exit 0."
    - "All 9 exam-mode canonical steps recorded with BOTH expected AND observed: toggle on, toggle off, EKSAMENMODUS badge, amber widget border, suppression behavior across rules (typos still surface; non-exam-safe rules suppressed), lockdown teacher-lock dual mode."
    - "Each ❌ step has a corresponding F38-N finding with regression_fixture_id=TBD (HYG-02). Severity for exam-mode defects is at least `major` by default — exam-mode is school-trust-critical."
    - "Walker signs off; findings list filled or `none`."
    - "Auto-mode hard-pauses on this plan (HYG-03 verification_kind: human-browser-walk)."
    - "Defect closure happens in decimal-insert fix plans (38-03.1, 38-03.2, …) appended after this walk. Fix plans must include FIX-02 regression artifact AND, if exam-marker shape changes, exam-marker validation."
  artifacts:
    - path: ".planning/uat/UAT-EXT-03.md"
      provides: "Phase 27 exam-mode 9-step highest-stakes walkthrough verification log"
      contains: "Walker signs off:"
  key_links:
    - from: ".planning/uat/UAT-EXT-03.md"
      to: ".planning/uat/findings/F38-N.md"
      via: "Defects observed bullet list"
      pattern: "F38-\\d+"
---

<objective>
Highest-stakes walkthrough: walk all 9 exam-mode steps from Phase 27 in real Chrome — toggle, EKSAMENMODUS badge, amber widget border, rule suppression, lockdown teacher-lock dual mode. Third in the locked sequence (after warm-up + canonical, before final).

Purpose: Satisfy UAT-EXT-03. Exam-mode trust is school-deployment-critical — teachers must be able to rely on the suppression boundary. Every defect here is at least `major` severity by default.
Output: `.planning/uat/UAT-EXT-03.md` walkthrough log + 0..N `.planning/uat/findings/F38-N.md` files.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/uat/TEMPLATE-walkthrough.md
@.planning/uat/TEMPLATE-finding.md
@.planning/phases/38-extension-uat-batch-bug-fix-loop-regr/38-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Instantiate UAT-EXT-03 walkthrough log from template</name>
  <files>.planning/uat/UAT-EXT-03.md</files>
  <action>
    Copy template:

    ```bash
    cp .planning/uat/TEMPLATE-walkthrough.md .planning/uat/UAT-EXT-03.md
    ```

    Edit frontmatter:
    - `walkthrough_id: UAT-EXT-03`
    - `walker: Geir`
    - `date: <ISO-8601 today>`

    Title: `# Walkthrough: Phase 27 exam-mode 9-step (highest-stakes)`

    Draft the 9 canonical steps (from REQUIREMENTS.md UAT-EXT-03 + Phase 27 SC):

    1. **Toggle exam-mode ON** in popup settings → expected: setting persists, popup re-renders showing exam-mode active state → observed: TBD → ✅/❌
    2. **EKSAMENMODUS badge visible** in popup header AND on the floating widget when active on a page → expected: badge renders with exam-mode styling → observed: TBD → ✅/❌
    3. **Amber widget border** on the spell-check widget → expected: widget has visible amber/exam-styled border distinct from normal mode → observed: TBD → ✅/❌
    4. **Suppression behavior — typos still surface** (type a misspelled word) → expected: nb-typo-curated / nb-typo-fuzzy still fire (exam-safe) → observed: TBD → ✅/❌
    5. **Suppression behavior — non-exam-safe rules suppressed** (type input that would normally trigger a rule with `exam.safe: false`, e.g. a pedagogy-only hint) → expected: dot does NOT render; popover does NOT appear → observed: TBD → ✅/❌
    6. **Toggle exam-mode OFF** → expected: previously-suppressed rules now fire on the same input; badge disappears; widget border returns to normal → observed: TBD → ✅/❌
    7. **Exam-registry coverage** — verify each registered exam-marker entry behaves per its `safe: true|false` declaration (sample 2-3 from `extension/exam-registry.js`) → observed: TBD → ✅/❌
    8. **Lockdown teacher-lock dual mode** (test in lockdown sidepanel — set teacher-lock to LEKSIHJELP_EXAM profile and verify exam-mode forced regardless of student toggle) → expected: student toggle is read-only / overridden → observed: TBD → ✅/❌
    9. **Persistence across reload** — exam-mode state persists across chrome://extensions reload AND across browser restart → observed: TBD → ✅/❌

    Walker fills `observed` during the walk.
  </action>
  <verify>
    <automated>test -f .planning/uat/UAT-EXT-03.md && node ~/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter get .planning/uat/UAT-EXT-03.md --field walkthrough_id | grep -q UAT-EXT-03</automated>
  </verify>
  <done>UAT-EXT-03.md exists; 9 exam-mode steps drafted; frontmatter complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walker performs the 9-step exam-mode walk</name>
  <files>.planning/uat/UAT-EXT-03.md, .planning/uat/findings/</files>
  <action>
    Walker performs the walk:

    1. **Pre-flight (HARD):**
       - `node scripts/check-vocab-deployment.js` exit 0 — paste output.
       - Frontmatter: ext_version, idb_revision, preset_profile, browser_version, reload_ts populated.

    2. **Execute Steps 1-9.** Fill `observed:` for each.

       For Step 8 (lockdown teacher-lock dual mode), this requires lockdown staging access. If lockdown staging is not yet set up for this walker, mark Step 8 with observed=`SKIPPED — lockdown staging not yet provisioned; covered by Phase 39 UAT-LOCK-02` and ✅. The dual-mode verification belongs to Phase 39 staging UAT proper; covering it inline only if lockdown is ready.

    3. **Findings:** every ❌ → `.planning/uat/findings/F38-<seq>.md`. Default `severity: major` for exam-mode defects (school-trust); use `blocker` if exam-mode toggle/suppression itself is broken. `sync_status: extension-only` for spell-rule defects; `needs-resync` if `extension/exam-registry.js` (synced surface) is touched. `regression_fixture_id: TBD` (HYG-02 HARD field).

    4. **Outcome:** sign off `Geir <ISO-8601>`; list findings or `none`.

    Defect closure → decimal-insert fix plans (38-03.1, …). Each fix plan must:
    - Include FIX-02 regression artifact (typos rule fix → fixture row; suppression-boundary fix → fixture or benchmark expectation).
    - If `extension/exam-registry.js` is touched, run `npm run check-exam-marker` AND its `:test` self-test (release-gate step 6).
    - Use [lockdown-resync-needed] commit marker if exam-registry.js or any other synced surface is modified (HYG-06).
  </action>
  <verify>
    <automated>grep -q "Walker signs off:" .planning/uat/UAT-EXT-03.md && grep -q "Geir" .planning/uat/UAT-EXT-03.md && ! grep -q "observed: TBD" .planning/uat/UAT-EXT-03.md</automated>
  </verify>
  <done>Walker signed off; pre-flight captured; 9 steps walked (or Step 8 explicitly deferred to Phase 39 with marker); F38-N findings filed for each ❌.</done>
</task>

</tasks>

<verification>
- UAT-EXT-03.md complete with pre-flight, 9 steps, sign-off.
- F38-N findings filed at `major`+ default severity.
- Decimal-insert fix plans cover FIX-01 + FIX-02 (HARD) + FIX-03 (if buildIndexes touched). exam-registry.js changes invoke check-exam-marker gate.
- Auto-mode hard-pauses on verification_kind: human-browser-walk.
</verification>

<success_criteria>
UAT-EXT-03 9-step exam-mode highest-stakes walkthrough log committed; all defects captured; sequence position 3 satisfied (next: 38-04 final DE Lær mer 4+2 walks).
</success_criteria>

<output>
After completion, create `.planning/phases/38-extension-uat-batch-bug-fix-loop-regr/38-03-SUMMARY.md` documenting walk outcome.
</output>
