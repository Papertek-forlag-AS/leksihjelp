---
phase: 02.1-close-sc-4-bundle-size-cap
plan: 01
subsystem: infra

tags: [release-gate, bundle-size, ceiling-bump, check-script, tdd-mirror]

# Dependency graph
requires:
  - phase: 02-data-layer-frequency-bigrams-typo-bank
    provides: "check-bundle-size release gate (Plan 02-04) + JSON minification pipeline + the 10.11 MiB post-minification zip that's now under the new 20 MiB cap"
provides:
  - "Bundle-size release gate at 20 MiB (up from 10 MiB) — SC-4 now GREEN with ~9.89 MiB headroom"
  - "Corrected gate framing — 20 MiB is an internal engineering guard, not a publicly-stated promise"
  - "Test-mirror CEILING_BYTES synchronized with script — behavioral contract (test name + asserted cap) now matches gate reality"
affects: [Phase 3 word-prediction (headroom for NB/NN bigram expansion), Phase 4 særskriving (fixture data additions), Phase 5 TTS (possible voice-pack additions), GitHub Releases workflow (gate now exits 0)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Release-gate constant + test-mirror lockstep — when a release gate uses a hard numeric threshold, both the gate and its behavior-test mirror must share the same value, and the test's human-readable name must describe the same number the assertion uses. Drifting either silently invalidates the test's intent."

key-files:
  created: []
  modified:
    - scripts/check-bundle-size.js
    - scripts/check-bundle-size.test.js

key-decisions:
  - "Raised CEILING_BYTES from 10 * 1024 * 1024 to 20 * 1024 * 1024 (20,971,520 bytes). Current post-minification zip is 10,599,772 bytes (10.11 MiB); new cap leaves 9.89 MiB headroom — enough runway for Phase 3-5 data/rule growth without re-litigating the ceiling decision mid-milestone."
  - "Dropped 'publicly-stated promise' framing from script header comment + failure-mode console output + test-name string. The 10 MiB number was never on the landing page (confirmed in RESEARCH.md via grep against backend/public/index.html). New framing: 'internal engineering guard against accidental bundle growth.' Chrome Web Store ceiling is 2 GB; this number is ours."
  - "Kept CEILING_BYTES declaration line as the single source of the threshold, with a one-line trailing rationale comment ('raised 2026-04-19, Phase 02.1'). The explicit '10 MiB' string is NOT retained in the trailing comment — the plan's hard verification check forbids it across the file, and the Phase/date pointer is enough for a future reader to locate the phase artifact that documents the raise."
  - "No edits outside the two scripts/check-bundle-size*.js files. Docs sweep (CLAUDE.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/research/*.md) is owned by sibling plan 02.1-02 running in parallel — coordination rule followed."

patterns-established:
  - "Release-gate ceiling change = threshold + surrounding prose move together atomically. Leaving a bumped constant next to prose that describes the old number (header comment, failure-mode console.log, test name) is worse than not bumping — it creates contradictory signal in a file that's human-read at release time."
  - "Parallel-plan file-boundary discipline — when two plans run in the same wave with no declared dependencies, each plan's file-modified list is the only surface it touches. 02.1-01 touched scripts/check-bundle-size*.js only, leaving STATE.md/ROADMAP.md/CLAUDE.md/PROJECT.md/research/*.md untouched for 02.1-02 to handle. Commits with tight file-lists (individual `git add <path>` per file) preserve atomicity for merge-level safety."

requirements-completed: [SC-4, DATA-03]

# Metrics
duration: ~2 min
completed: 2026-04-19
---

# Phase 02.1 Plan 01: Bundle-size ceiling bump 10 MiB → 20 MiB Summary

**CEILING_BYTES raised from 10 \* 1024 \* 1024 to 20 \* 1024 \* 1024 in both the `check-bundle-size` release gate and its behavior-test mirror, with header/failure-output/test-name prose refreshed to drop the incorrect "publicly-stated promise" framing — current 10.11 MiB zip now passes the gate cleanly (exit 0, 9.89 MiB headroom), closing Phase 2 SC-4.**

## Performance

- **Duration:** ~2 min (very light edit plan — 5 edits to script + 2 edits to test mirror + 2 verification runs)
- **Started:** 2026-04-19T07:59:00Z
- **Completed:** 2026-04-19T08:00:53Z
- **Tasks:** 2
- **Files modified:** 2 (scripts/check-bundle-size.js, scripts/check-bundle-size.test.js)

## Accomplishments

- `npm run check-bundle-size` now exits 0 — the first time since Plan 02-04 shipped the gate. GitHub Releases are unblocked at the Release Workflow step 2 gate.
- `node scripts/check-bundle-size.test.js` still exits 0 — all 10 behavior tests PASS, including the renamed Test 4/5 which now asserts against a cap that matches its name (fixing a silent-intent drift risk flagged in RESEARCH.md Pitfall 2).
- SC-4 (Packaged zip within the bundle-size ceiling) closes with 9.89 MiB (~10.4 MB) headroom on the new 20 MiB cap — comfortable runway for Phase 3-5 data/rule growth without a near-term ceiling re-decision.
- Script prose now accurately describes the gate's real purpose (regression detection vs. accidental bundle growth) and removes the inaccurate "publicly-stated promise" framing that was never backed by landing-page copy.

## Task Commits

Each task was committed atomically:

1. **Task 1: Bump CEILING_BYTES + rewrite script header/failure prose in scripts/check-bundle-size.js** — `bf0cf01` (chore)
2. **Task 2: Bump CEILING_BYTES + update test name in scripts/check-bundle-size.test.js, then run both verification commands** — `4e3210b` (test)

**Plan metadata:** (added in the final `docs(02.1-01)` commit that ships this SUMMARY.md alongside STATE.md + ROADMAP.md + REQUIREMENTS.md updates)

## Files Created/Modified

- `scripts/check-bundle-size.js` — 5 targeted edits: header comment lines 6-7 (20 MiB), header block lines 9-13 (new "internal engineering guard" framing), exit-codes block lines 19-20 (20 MiB), CEILING_BYTES declaration line 33 (`20 * 1024 * 1024`), failure-mode console.log lines ~150-155 (new framing). Architecture of the script (package → measure → breakdown → compare → exit 0/1) is unchanged.
- `scripts/check-bundle-size.test.js` — 2 targeted edits: CEILING_BYTES at line 27 (`20 * 1024 * 1024`), Test 4/5 test-name string at line 124 ("vs 20 MiB cap"). Test suite architecture unchanged.

## Decisions Made

- **Raise the ceiling, don't shrink the bundle.** This is the product decision locked in by 02.1-CONTEXT.md: Phase 02.1 closes SC-4 by bumping the cap, not by pursuing English-removal / audio-strip / conjugation-trim remediations. Evidence from 02-05-AUDIT.md (four runtime paths for en.json) + audio/de/*.mp3 offline-first-play pledge informed the choice.
- **20 MiB, not 12 MiB.** CONTEXT.md left the exact number as planner discretion; 02.1-01-PLAN.md locked in 20 MiB because it accommodates Phase 3-5 data growth without thrashing the decision. 12 MiB would have required another bump within the milestone.
- **Keep the gate as a hard exit-0/exit-1.** No soft "warning" mode. Purpose shifts from "enforce the 10 MB story" to "catch accidental bundle growth" (the common regression: a pretty-printed JSON committed unintentionally, or a large new asset added silently) — same mechanism, sharper purpose.
- **Date-stamped trailing comment on the CEILING_BYTES line.** Per RESEARCH.md Open Question 2 (Claude's Discretion) + CONTEXT.md "Claude's Discretion" — a one-line `(raised 2026-04-19, Phase 02.1)` rationale keeps the change discoverable at the obvious place (the constant itself) without polluting the header block. The explicit "10 MiB" string is NOT kept in this comment — the plan's overall verification forbids any "10 MiB"/"10 MB" hits in the file.
- **Respect the parallel-plan scope boundary.** 02.1-02 is running concurrently and owns all prose outside scripts/. This plan's `git add` lists were strictly limited to the two script files. No edits to CLAUDE.md, PROJECT.md, ROADMAP.md, REQUIREMENTS.md, or research/*.md, even though those files appeared in `git status` as modified by the sibling plan.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Dropped literal "10 MiB" from the CEILING_BYTES trailing comment**
- **Found during:** Task 1 (after the 5 edits were applied)
- **Issue:** The plan's Edit 4 instruction said to include a trailing rationale comment on the `CEILING_BYTES` line that mentioned the old value: `// 20 MiB = 20,971,520 (raised 2026-04-19, Phase 02.1; was 10 MiB)`. But the plan's own overall `<verification>` section (check #2) and Task 1's `<done>` criterion both required `grep for "10 MiB" returns zero hits in the file`. Keeping the literal string "10 MiB" in the trailing comment directly violated the hard verification gate — two plan instructions in direct contradiction.
- **Fix:** Kept the date-stamped rationale but dropped the explicit "was 10 MiB" fragment. Final comment: `// 20 MiB = 20,971,520 (raised 2026-04-19, Phase 02.1)`. Historical rationale is still one `git log` away; the phase/date pointer is enough.
- **Files modified:** scripts/check-bundle-size.js (line 33)
- **Verification:** `grep -n "10 MiB\|10 MB\|publicly-stated" scripts/check-bundle-size.js scripts/check-bundle-size.test.js` → zero hits; overall plan verification check #2 PASSES.
- **Committed in:** bf0cf01 (part of Task 1's atomic commit — caught and fixed before the commit was made)

---

**Total deviations:** 1 auto-fixed (1 bug — internal contradiction between two plan instructions)
**Impact on plan:** Zero scope impact. Resolution preserved both intent (rationale stays on the constant line) and the hard verification rule (no "10 MiB" anywhere in the file). The plan's <verify> automated grep and the Edit 4 instruction were genuinely contradictory — the resolution honors the stricter gate (verification) over the weaker-stated preference ("include" the trailing comment, with CONTEXT.md explicitly marking the exact wording as Claude's Discretion: "Either is fine").

## Issues Encountered

None — plan execution was clean. Both verification commands (`node scripts/check-bundle-size.test.js` and `npm run check-bundle-size`) exited 0 on the first post-edit run. No retries, no debugging, no rollbacks.

## User Setup Required

None — no external service configuration, no environment variables, no dashboard tweaks. This is a pure internal threshold change.

## Next Phase Readiness

- **SC-4 CLOSED.** Phase 02.1 is done once sibling plan 02.1-02 finishes the docs sweep. Phase 2 can be marked fully complete (SC-1/SC-2/SC-3 already VERIFIED; SC-4 now GREEN at 20 MiB).
- **Release Workflow unblocked.** The next GitHub Release attempt will pass the `check-bundle-size` step (exit 0, 10.11 MiB under 20 MiB cap). Release can ship whenever the version bump + zip upload + notes are ready.
- **Phase 3 word-prediction can proceed without bundle-size anxiety.** New bigram/frequency data from Phase 2 gave NB/NN word-prediction its ranking signal; Phase 3 will refine the ranker and may add small additional data files. 9.89 MiB headroom is more than ample.
- **Phase 4 særskriving** has plenty of headroom for expanded fixture sentences; no bundle constraint.
- **Deferred remediations** (English on-demand, audio stripping, conjugation trimming) remain available if Phase 3-5 data growth pushes past 20 MiB. They are preserved in 02-05-AUDIT.md § "What a CLEAN path would have required" and in .planning/STATE.md's Blocker entry.

---

## Self-Check: PASSED

Claims verified:
- `scripts/check-bundle-size.js` exists on disk with `CEILING_BYTES = 20 * 1024 * 1024` at line 33 (verified via Grep, zero other "10 MiB" hits) ✓
- `scripts/check-bundle-size.test.js` exists on disk with `CEILING_BYTES = 20 * 1024 * 1024` at line 27 and test-name "vs 20 MiB cap" at line 124 (verified via Grep) ✓
- Task 1 commit `bf0cf01` exists (verified via `git log --oneline | grep bf0cf01` during commit step) ✓
- Task 2 commit `4e3210b` exists (verified via `git log --oneline | grep 4e3210b` during commit step) ✓
- `node scripts/check-bundle-size.test.js` exited 0 with all 10 behavior tests PASS ✓
- `npm run check-bundle-size` exited 0 with PASS line "10.11 MiB (10,599,772 bytes) — under cap by 10,371,748 bytes (9.89 MiB headroom)" ✓

---
*Phase: 02.1-close-sc-4-bundle-size-cap*
*Completed: 2026-04-19*
