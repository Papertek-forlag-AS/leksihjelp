---
phase: 02.1-close-sc-4-bundle-size-cap
plan: 02
subsystem: docs
tags: [bundle-size, sc-4, data-03, ceiling, documentation-sweep, state-hygiene]

# Dependency graph
requires:
  - phase: 02.1-close-sc-4-bundle-size-cap plan 01
    provides: CEILING_BYTES raised 10 MiB → 20 MiB in scripts/check-bundle-size.js + scripts/check-bundle-size.test.js (script + test side of the atomic edit unit)
provides:
  - CLAUDE.md Release Workflow step 2 rewritten against 20 MiB internal engineering ceiling (no "publicly-stated promise" framing)
  - PROJECT.md Out-of-Scope + Constraints describe 20 MiB internal budget enforced by scripts/check-bundle-size.js
  - ROADMAP.md Phase 2 Goal reworded against 20 MiB budget
  - ROADMAP.md Phase 2 SC-4 reworded "stays under the internal engineering ceiling (20 MiB), verified by scripts/check-bundle-size.js exit 0"
  - REQUIREMENTS.md DATA-03 reworded "internal bundle-size budget (~20 MiB ceiling)"
  - .planning/research/FEATURES.md + ARCHITECTURE.md (2 sites) + STACK.md (2 sites) + PITFALLS.md (1 site) prose corrected to 20 MiB internal ceiling
  - STATE.md live SC-4 blocker replaced with "RESOLVED 2026-04-19 by Phase 02.1" marker (historical decision-log entries untouched)
affects:
  - Phase 3 code-switching (planners can scan REQUIREMENTS.md DATA-03 + ROADMAP.md SC-4 without inheriting the old "publicly-stated promise" framing)
  - Phase 4 særskriving (same: consistent ceiling framing across live docs)
  - Future release gates (CLAUDE.md Release Workflow step 2 is the canonical release contract; prose now matches the 20 MiB script constant)
  - Future audits/phases (.planning/research/*.md drops the 10 MB references so forward-looking design notes reference the correct ceiling)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Docs-sweep discipline: scope-exclusion list in the plan frontmatter (historical archive + STATE.md historical decision rows) prevents retrofitting history when correcting live framing"
    - "Atomic script+prose edit unit split across parallel plans in one wave: 02.1-01 owns the machine-readable constant, 02.1-02 owns the human-readable prose, both land in the same release"

key-files:
  created:
    - ".planning/phases/02.1-close-sc-4-bundle-size-cap/02.1-02-SUMMARY.md"
  modified:
    - "CLAUDE.md (Release Workflow step 2, line ~348-350)"
    - ".planning/PROJECT.md (Out-of-Scope line ~52, Constraints line ~79)"
    - ".planning/ROADMAP.md (Phase 2 Goal line ~43, SC-4 line ~50)"
    - ".planning/REQUIREMENTS.md (DATA-03 line ~23)"
    - ".planning/research/FEATURES.md (Works-offline table row line ~34)"
    - ".planning/research/ARCHITECTURE.md (2 sites: line ~383, line ~519)"
    - ".planning/research/STACK.md (2 sites: line ~112, line ~206)"
    - ".planning/research/PITFALLS.md (Pitfall 17 line ~397)"
    - ".planning/STATE.md (live SC-4 blocker at line 127 replaced with RESOLVED marker)"

key-decisions:
  - "Kept Phase 02.1 meta-descriptions in ROADMAP.md lines 20 + 61 intact — they describe WHAT Phase 02.1 did ('drop the false publicly-stated promise framing') and necessarily quote the obsolete phrasing. This is meta-commentary, not live ceiling framing."
  - "STATE.md historical decision-log rows (lines 100-111, including [Phase 02-04] Outcome B at line 103 and [Phase 02-05] rows at 108-111) preserved byte-identical per plan scope_exclusions — history must stay faithful to what was decided at the time."
  - "Historical archive .planning/phases/02-data-layer-frequency-bigrams-typo-bank/ untouched (git diff --stat returns empty)."
  - "Split the atomic edit across two parallel plans worked — 02.1-01 landed the machine-readable constant (CEILING_BYTES) first, 02.1-02 landed the human-readable prose in a second wave, no merge conflicts on shared state file."

patterns-established:
  - "Live-docs sweep with scope_exclusions: when prose spans many files, the plan frontmatter's scope_exclusions section lists files/directories that must NOT be touched (historical archive, historical decision rows) — prevents retrofitting history"
  - "Parallel-plan coordination via subject-matter split: one plan owns code, parallel plan owns prose; both can reference the same truths (the new constant) without stomping each other's files"
  - "STATE.md Blockers/Concerns entries are replaced (not deleted) when resolved — keeps the trail; historical decision-log rows under ## Accumulated Context > Decisions are append-only forever"

requirements-completed: [SC-4, DATA-03]

# Metrics
duration: 2m 20s
completed: 2026-04-19
---

# Phase 02.1 Plan 02: Live-docs sweep to 20 MiB internal ceiling Summary

**Nine live documentation files rewritten to honestly describe the 20 MiB internal engineering ceiling (no "publicly-stated promise" framing), and the live SC-4 blocker in STATE.md replaced with a "RESOLVED 2026-04-19 by Phase 02.1" marker — while the historical archive and STATE.md historical decision-log rows remain byte-identical to pre-edit.**

## Performance

- **Duration:** 2m 20s
- **Started:** 2026-04-19T08:00:08Z
- **Completed:** 2026-04-19T08:02:28Z
- **Tasks:** 3
- **Files modified:** 9 (CLAUDE.md, PROJECT.md, ROADMAP.md, REQUIREMENTS.md, FEATURES.md, ARCHITECTURE.md, STACK.md, PITFALLS.md, STATE.md)

## Accomplishments

- CLAUDE.md Release Workflow step 2 now describes the 20 MiB internal engineering ceiling without the false "publicly-stated promise" framing; the new prose explicitly notes that Chrome Web Store has no relevant size limit (accepts up to 2 GB) and that the 20 MiB number is our own regression-detection guard.
- PROJECT.md Out-of-Scope + Constraints, all four `.planning/research/*.md` files (FEATURES, ARCHITECTURE, STACK, PITFALLS), and REQUIREMENTS.md DATA-03 now consistently reference the 20 MiB internal ceiling.
- ROADMAP.md Phase 2 Goal + SC-4 now match — SC-4 is the authoritative statement: "Total packaged extension zip size from `npm run package` stays under the internal engineering ceiling (20 MiB), verified by `scripts/check-bundle-size.js` exit 0 in the release checklist."
- STATE.md's live SC-4 blocker (previously "Phase 2 bundle-size 10 MiB ceiling contingency — SC-4 STILL OPEN 2026-04-19" multi-paragraph entry) is replaced with a single-line RESOLVED marker that points at Phase 02.1, records the ceiling raise, and explicitly notes the remediation paths that Phase 02.1 chose NOT to pursue (English-removal / audio-strip / data-trim — all still documented in 02-05-AUDIT.md as future options).
- Historical archive `.planning/phases/02-data-layer-frequency-bigrams-typo-bank/` is untouched (`git diff --stat` returns empty for that directory across all 3 task commits).
- Historical decision-log rows in STATE.md `## Accumulated Context > Decisions` (lines 100-111, including the [Phase 02-04] "Outcome B" row at line 103 and all [Phase 02-05] rows) are byte-identical to pre-edit.

## Task Commits

1. **Task 1: Rewrite live ceiling prose (6 files)** — `7244e3a` (docs)
2. **Task 2: Reword SC-4 in ROADMAP + DATA-03 in REQUIREMENTS + Phase 2 goal** — `0a3b29f` (docs)
3. **Task 3: Resolve live SC-4 blocker in STATE.md** — `3477ef0` (docs)

**Plan metadata:** (pending final commit with SUMMARY + STATE + ROADMAP updates)

**Sibling plan:** 02.1-01 ran in parallel in the same wave and shipped the machine-readable side: `bf0cf01` (chore: raise CEILING_BYTES + refresh gate prose in scripts/check-bundle-size.js), `4e3210b` (test: align test CEILING_BYTES + test name). The two plans together form the atomic edit unit — script + prose now consistent at 20 MiB.

## Files Created/Modified

- `CLAUDE.md` — Release Workflow step 2 rewritten: 20 MiB internal engineering ceiling, no "publicly-stated promise" framing, notes Chrome Web Store has no size limit
- `.planning/PROJECT.md` — Out-of-Scope bullet (line ~52) + Constraints bullet (line ~79) both describe the 20 MiB internal budget enforced by `scripts/check-bundle-size.js`
- `.planning/ROADMAP.md` — Phase 2 Goal (line ~43) + SC-4 (line ~50) reworded; SC-4 now references the script gate explicitly for machine-readability
- `.planning/REQUIREMENTS.md` — DATA-03 (line ~23) reworded to "internal bundle-size budget (~20 MiB ceiling)"
- `.planning/research/FEATURES.md` — Works-offline table row (line ~34) reads "~20 MiB internal ceiling"
- `.planning/research/ARCHITECTURE.md` — Two sites (line ~383 freq-table size discussion + line ~519 memory-monitoring section) both refreshed to 20 MiB internal budget; line ~519 also swaps the abandoned `npm run size-report` idea for the shipped `npm run check-bundle-size` gate
- `.planning/research/STACK.md` — Two sites (line ~112 freq-table size + line ~206 bigram-growth contingency) refreshed to 20 MiB internal
- `.planning/research/PITFALLS.md` — Pitfall 17 body (line ~397) refreshed to "the 20 MiB internal ceiling"
- `.planning/STATE.md` — Live SC-4 blocker at line 127 replaced with RESOLVED marker; all other content (historical decisions, session continuity, other blockers) byte-identical to pre-edit

## Decisions Made

- **Kept the Phase 02.1 meta-description rows in ROADMAP.md (lines 20 + 61) as-is.** Those rows describe WHAT Phase 02.1 did — they necessarily quote the obsolete "publicly-stated promise" phrasing to explain that the phase's job is to drop it. This is meta-commentary on Phase 02.1's own deliverable, not live ceiling framing. Scrubbing the quote would make the phase's own narrative illegible.
- **Preserved STATE.md historical decision-log rows byte-identical** per the plan's scope_exclusions (lines ~100-111, including the [Phase 02-04] "Outcome B accepted over silent-bypass" row at line 103 that explicitly mentions "the publicly-stated 10 MB promise on the landing page" — that row documents what was thought at the time of Plan 02-04 and must remain faithful to that history).
- **Used `Edit` with a single multi-paragraph `old_string` to replace the live SC-4 blocker in one call** rather than line-by-line edits — the entire bullet was one logical unit (SC-4 STILL OPEN → SC-4 RESOLVED) and replacing the whole block in one atomic operation avoids the risk of partial-replacement drift.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 02.1 completed end-to-end (plans 01 + 02 both landed in the same wave).
- Phase 2 success criterion SC-4 is now closed; `scripts/check-bundle-size.js` exits 0 at the current zip size (10.11 MiB) with ~9.89 MiB headroom for Phase 3+ data additions.
- No blockers for Phase 3 planning. The bundle-size gate shifts semantics from "enforce 10 MiB promise" to "catch accidental bundle-growth regressions" — same exit-0/1 contract, same Release Workflow integration, so future phases inherit the same release discipline without the now-removed prose contradiction.
- Deferred items (NN phrase-infinitive triage, missing fin_adj entry, DATA-02 cross-app rollback) unchanged — still tracked in STATE.md Blockers/Concerns; out of scope for Phase 02.1.

## Verification Results

Final repo-wide greps (per plan's Verification block):

1. `grep -rn "publicly-stated promise" --include="*.md" --include="*.js" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=02-data-layer-frequency-bigrams-typo-bank .` → hits only in:
   - `.planning/STATE.md:127` (inside the new RESOLVED marker — describes what was dropped)
   - `.planning/ROADMAP.md:20, 61` (Phase 02.1 meta-description rows — describe what Phase 02.1 did)
   - `.planning/phases/02.1-close-sc-4-bundle-size-cap/*.md` (this phase's own plan/research/context files — allowed per plan)
   - Zero hits in any other live doc.
2. `grep -rn "10 MiB ceiling|10 MB ceiling|10\s*MB budget" ...` (same exclusions) → hits only inside `.planning/phases/02.1-close-sc-4-bundle-size-cap/*.md` (this phase's own files) — zero hits in any other live doc.
3. `grep -c "20 MiB" .planning/ROADMAP.md .planning/REQUIREMENTS.md CLAUDE.md .planning/PROJECT.md` → 10 hits across the 4 files (≥ 5 required).
4. `git diff --stat .planning/phases/02-data-layer-frequency-bigrams-typo-bank/` → empty (historical archive untouched).
5. `grep -c "RESOLVED 2026-04-19 by Phase 02.1" .planning/STATE.md` → 1 (exactly once, as required).
6. STATE.md historical decision rows (lines 100-111) byte-identical to pre-edit — manual Read comparison confirmed.

## Self-Check: PASSED

- All 9 target files present on disk with the expected edits.
- All 3 task commits present in git log (`7244e3a`, `0a3b29f`, `3477ef0`).
- Sibling plan 02.1-01 commits also present (`bf0cf01`, `4e3210b`) — atomic edit unit complete.
- Historical archive diff empty.
- Historical STATE.md decision rows unchanged.
- All verification greps returned allowed-only hits.

---
*Phase: 02.1-close-sc-4-bundle-size-cap*
*Completed: 2026-04-19*
