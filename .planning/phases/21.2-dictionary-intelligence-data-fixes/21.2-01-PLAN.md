---
phase: 21.2-dictionary-intelligence-data-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/data/nb.json
  - extension/data/de.json
  - extension/data/fr.json
autonomous: true
requirements: [FF-01, POLY-01, POLY-03, POLY-04]
gap_closure: true

must_haves:
  truths:
    - "NB fin_adj entry has linkedTo.fr pointing to fin_noun so reverse enrichment index maps FR fin to NB falseFriends warning"
    - "NB på_contr entry has linkedTo.de pointing to am_contr so reverse enrichment index maps DE am to NB senses"
    - "All 8 release gates pass after re-sync"
  artifacts:
    - path: "extension/data/nb.json"
      provides: "NB vocabulary with corrected linkedTo on fin_adj and på_contr"
      contains: "linkedTo"
    - path: "extension/data/de.json"
      provides: "DE vocabulary (re-synced, unchanged content)"
    - path: "extension/data/fr.json"
      provides: "FR vocabulary (re-synced, unchanged content)"
  key_links:
    - from: "vocabulary/lexicon/links/nb-fr/adjectivebank.json (Papertek)"
      to: "extension/data/nb.json (synced)"
      via: "npm run sync-vocab nb"
      pattern: "fin_adj.*linkedTo.*fr"
    - from: "vocabulary/lexicon/links/nb-de/generalbank.json (Papertek)"
      to: "extension/data/nb.json (synced)"
      via: "npm run sync-vocab nb"
      pattern: "på_contr.*linkedTo.*de"
---

<objective>
Fix two missing `linkedTo` entries in the Papertek Vocabulary API link data so that:
1. NB `fin_adj` links to FR `fin_noun` -- enabling the FR false-friend warning ("fin" != "fin/pen")
2. NB `på_contr` links to DE `am_contr` -- enabling DE sense-grouped translations (4 senses)

Then re-sync vocabulary to pull corrected data into the extension.

Purpose: Close the last 2 integration gaps from the v2.2 milestone audit -- without these links, the reverse enrichment index (built in Phase 21.1) cannot find the NB falseFriends/senses data for FR and DE lookups.
Output: Corrected link files in Papertek + re-synced extension/data/*.json
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21.1-dictionary-intelligence-pipeline-fix/21.1-01-SUMMARY.md

The Papertek Vocabulary project is at `/Users/geirforbord/papertek/papertek-vocabulary/`.

<interfaces>
Link file format (from existing nb-en/generalbank.json på_contr entry):
```json
{
  "på_contr": {
    "primary": "at_contr",
    "alternatives": ["on_contr"]
  }
}
```

Link files live at: `vocabulary/lexicon/links/{source}-{target}/{bankname}.json`
Each file is a flat object mapping source wordId to link data.

The v3 export API (`api/vocab/v3/export/[language].js`) reads these link files via `loadOutgoingLinks()` and merges them into each entry's `linkedTo` field during export. The `targetWordIndex` resolves word IDs across ALL banks, so cross-bank links (e.g. NB adj -> FR noun) work.

Verified target entries exist:
- FR `fin_noun` in `vocabulary/lexicon/fr/nounbank.json` (word: "fin", meaning "end/ending")
- DE `am_contr` in `vocabulary/lexicon/de/articlesbank.json` (word: "am", contraction of "an dem")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing linkedTo entries in Papertek Vocabulary link files</name>
  <files>
    /Users/geirforbord/papertek/papertek-vocabulary/vocabulary/lexicon/links/nb-fr/adjectivebank.json
    /Users/geirforbord/papertek/papertek-vocabulary/vocabulary/lexicon/links/nb-de/generalbank.json
  </files>
  <action>
    1. Read `/Users/geirforbord/papertek/papertek-vocabulary/vocabulary/lexicon/links/nb-fr/adjectivebank.json`
    2. Add entry: `"fin_adj": { "primary": "fin_noun" }` — this links NB fin (adjective, "nice/fine") to FR fin (noun, "end/ending") which is the false-friend target
    3. Read `/Users/geirforbord/papertek/papertek-vocabulary/vocabulary/lexicon/links/nb-de/generalbank.json`
    4. Add entry: `"på_contr": { "primary": "am_contr" }` — this links NB på (contraction) to DE am (contraction of "an dem") which is the primary sense-trigger for time expressions

    Note: These are link files in the PAPERTEK VOCABULARY project (sibling repo), not in leksihjelp. The sync script fetches from the deployed API, but we can also test locally.

    Do NOT add alternatives for DE — NB "på" maps to multiple DE words (an, auf, um) across different senses, but the linkedTo primary only needs to connect one entry for the reverse index to work. The senses data on the NB entry already contains all 4 DE translations.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('/Users/geirforbord/papertek/papertek-vocabulary/vocabulary/lexicon/links/nb-fr/adjectivebank.json') as f:
    d = json.load(f)
assert 'fin_adj' in d and d['fin_adj']['primary'] == 'fin_noun', 'fin_adj link missing'
with open('/Users/geirforbord/papertek/papertek-vocabulary/vocabulary/lexicon/links/nb-de/generalbank.json') as f:
    d = json.load(f)
assert 'på_contr' in d and d['på_contr']['primary'] == 'am_contr', 'på_contr link missing'
print('PASS: both links present')
"</automated>
  </verify>
  <done>Both link files contain the new entries; fin_adj -> fin_noun (FR) and på_contr -> am_contr (DE)</done>
</task>

<task type="auto">
  <name>Task 2: Deploy Papertek Vocabulary API + re-sync leksihjelp vocab data</name>
  <files>
    extension/data/nb.json
    extension/data/de.json
    extension/data/fr.json
  </files>
  <action>
    1. Commit the link file changes in the Papertek Vocabulary repo:
       - `cd /Users/geirforbord/papertek/papertek-vocabulary && git add vocabulary/lexicon/links/nb-fr/adjectivebank.json vocabulary/lexicon/links/nb-de/generalbank.json && git commit -m "fix(links): add nb-fr fin_adj and nb-de på_contr linkedTo entries"`

    2. Deploy the Papertek Vocabulary API so the export endpoint serves corrected data:
       - `cd /Users/geirforbord/papertek/papertek-vocabulary && vercel --prod`
       - Wait for deployment to complete

    3. Back in leksihjelp, re-sync vocabulary:
       - `cd /Users/geirforbord/Papertek/leksihjelp && npm run sync-vocab`
       - This fetches from the deployed API and writes corrected data to extension/data/

    4. Verify the synced data:
       - Check nb.json: `fin_adj.linkedTo.fr.primary` should be `"fin_noun"`
       - Check nb.json: `på_contr.linkedTo.de.primary` should be `"am_contr"`

    5. Run all 8 release gates:
       - `npm run check-fixtures && npm run check-explain-contract && npm run check-rule-css-wiring && npm run check-spellcheck-features && npm run check-network-silence && npm run check-bundle-size && npm run check-benchmark-coverage && npm run check-governance-data`
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp && python3 -c "
import json
with open('extension/data/nb.json') as f:
    data = json.load(f)
# Check fin_adj
fin = None
for bank in data:
    if isinstance(data[bank], dict) and 'fin_adj' in data[bank]:
        fin = data[bank]['fin_adj']
        break
assert fin and fin.get('linkedTo', {}).get('fr', {}).get('primary') == 'fin_noun', f'fin_adj linkedTo.fr missing, got: {fin.get(\"linkedTo\") if fin else \"NOT FOUND\"}'
# Check på_contr
pa = None
for bank in data:
    if isinstance(data[bank], dict) and 'på_contr' in data[bank]:
        pa = data[bank]['på_contr']
        break
assert pa and pa.get('linkedTo', {}).get('de', {}).get('primary') == 'am_contr', f'på_contr linkedTo.de missing, got: {pa.get(\"linkedTo\") if pa else \"NOT FOUND\"}'
print('PASS: both linkedTo entries present in synced nb.json')
" && npm run check-fixtures && npm run check-explain-contract && npm run check-rule-css-wiring && npm run check-spellcheck-features && npm run check-network-silence && npm run check-bundle-size && npm run check-benchmark-coverage && npm run check-governance-data</automated>
  </verify>
  <done>
    - nb.json fin_adj has linkedTo.fr.primary = "fin_noun"
    - nb.json på_contr has linkedTo.de.primary = "am_contr"
    - All 8 release gates pass
  </done>
</task>

</tasks>

<verification>
1. `fin_adj.linkedTo.fr.primary == "fin_noun"` in synced nb.json
2. `på_contr.linkedTo.de.primary == "am_contr"` in synced nb.json
3. All 8 release gates pass: `npm run check-fixtures && npm run check-explain-contract && npm run check-rule-css-wiring && npm run check-spellcheck-features && npm run check-network-silence && npm run check-bundle-size && npm run check-benchmark-coverage && npm run check-governance-data`
4. Browser verification (deferred): search FR word in popup with NB "fin" false-friend; search DE word in popup with NB "på" senses
</verification>

<success_criteria>
- NB `fin_adj` has `linkedTo.fr` so the reverse enrichment index (from Phase 21.1) connects FR lookups to the false-friend warning
- NB `på_contr` has `linkedTo.de` so the reverse enrichment index connects DE lookups to the 4-sense grouped translations
- All release gates green
</success_criteria>

<output>
After completion, create `.planning/phases/21.2-dictionary-intelligence-data-fixes/21.2-01-SUMMARY.md`
</output>
