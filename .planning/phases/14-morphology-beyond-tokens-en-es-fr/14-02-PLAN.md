---
phase: 14-morphology-beyond-tokens-en-es-fr
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - extension/content/spell-rules/en-morphology.js
  - extension/content/spell-rules/en-word-family.js
  - fixtures/en/morphology.jsonl
  - fixtures/en/word-family.jsonl
autonomous: true
requirements:
  - MORPH-01
  - MORPH-03

must_haves:
  truths:
    - "EN text containing 'childs' is flagged with suggestion 'children'"
    - "EN text containing 'eated' is flagged with suggestion 'ate'"
    - "EN text containing 'goed' is flagged with correct irregular past"
    - "EN text 'i have improve' is flagged with suggestion 'improved' (past participle after have)"
    - "Correct irregular forms (children, ate, went) do NOT flag"
    - "Regular verb past tenses (played, walked) do NOT flag"
    - "Correct POS usage (improve as verb, improvement as noun) does NOT flag"
  artifacts:
    - path: "extension/content/spell-rules/en-morphology.js"
      provides: "MORPH-01 EN irregular overgeneration rule"
      contains: "irregularForms"
    - path: "extension/content/spell-rules/en-word-family.js"
      provides: "MORPH-03 EN word-family POS-slot confusion rule"
      contains: "WORD_FAMILIES"
    - path: "fixtures/en/morphology.jsonl"
      provides: ">= 30 positive + >= 15 acceptance fixtures for MORPH-01"
    - path: "fixtures/en/word-family.jsonl"
      provides: ">= 30 positive + >= 15 acceptance fixtures for MORPH-03"
  key_links:
    - from: "extension/content/spell-rules/en-morphology.js"
      to: "extension/content/vocab-seam-core.js"
      via: "ctx.vocab.irregularForms Map lookup"
      pattern: "irregularForms\\.get"
    - from: "extension/content/spell-rules/en-word-family.js"
      to: "extension/content/spell-check-core.js"
      via: "ctx.getTagged for POS-slot detection"
      pattern: "getTagged"
---

<objective>
Implement MORPH-01 (EN irregular overgeneration) and MORPH-03 (EN word-family POS confusion) rules with full fixture suites.

Purpose: Flag common Norwegian-student EN errors: applying regular patterns to irregular verbs/nouns (childs, eated, goed) and using wrong word-family members in syntactic slots (have improve instead of have improved).
Output: Two new rule files with >= 30 positive + >= 15 acceptance fixtures each.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-morphology-beyond-tokens-en-es-fr/14-RESEARCH.md
@.planning/phases/14-morphology-beyond-tokens-en-es-fr/14-01-SUMMARY.md
@extension/content/spell-rules/en-grammar.js
@extension/content/vocab-seam-core.js
@extension/content/spell-check-core.js
@fixtures/en/grammar.jsonl
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MORPH-01 EN irregular overgeneration rule + fixtures</name>
  <files>extension/content/spell-rules/en-morphology.js, fixtures/en/morphology.jsonl</files>
  <behavior>
    - "childs" -> flagged, suggestion "children" (irregular noun plural)
    - "mouses" -> flagged, suggestion "mice"
    - "womans" -> flagged, suggestion "women"
    - "mans" -> flagged, suggestion "men"
    - "tooths" -> flagged, suggestion "teeth"
    - "foots" -> flagged, suggestion "feet"
    - "eated" -> flagged, suggestion "ate" (irregular verb past)
    - "goed" -> flagged, suggestion "went"
    - "runned" -> flagged, suggestion "ran"
    - "comed" -> flagged, suggestion "came"
    - "writed" -> flagged, suggestion "wrote"
    - "taked" -> flagged, suggestion "took"
    - "maked" -> flagged, suggestion "made"
    - "sayed" -> flagged, suggestion "said"
    - "doed" -> flagged, suggestion "did"
    - "haved" -> flagged, suggestion "had"
    - "children" -> NOT flagged (correct irregular plural)
    - "ate" -> NOT flagged (correct irregular past)
    - "played" -> NOT flagged (regular verb, correct form)
    - "houses" -> NOT flagged (regular noun plural)
    - "walked" -> NOT flagged (regular verb past)
    - "dogs" -> NOT flagged (regular noun plural)
    - "mice" -> NOT flagged (correct irregular plural)
    - "went" -> NOT flagged (correct irregular past)
    - Sentence context: "the childs were playing" -> flags "childs"
    - Sentence context: "we eated pizza" -> flags "eated"
    - Multi-word tokens like "be afraid" -> NOT processed (skipped at index build time)
  </behavior>
  <action>
Create `extension/content/spell-rules/en-morphology.js` using the IIFE self-registering pattern:

```javascript
(function () {
  'use strict';
  const host = typeof self !== 'undefined' ? self : globalThis;
  host.__lexiSpellRules = host.__lexiSpellRules || [];
  const { matchCase, escapeHtml } = host.__lexiSpellCore || {};
  // ...
```

Rule shape:
- id: `'en-morphology'`
- languages: `['en']`
- priority: 17 (after gender 15, before grammar 20)
- severity: `'warning'`

check(ctx) logic:
1. Get `irregularForms` from `ctx.vocab.irregularForms` (the Map built in Plan 01)
2. If Map is empty or undefined, return [] (defensive)
3. For each token in ctx.tokens:
   - Lowercase the token word
   - Look up in irregularForms Map
   - If found, create a finding with:
     - rule_id: 'en-morphology'
     - start/end from token position
     - original: the token as written
     - fix/suggestion: matchCase(entry.correct, token.word)
     - message: brief explanation based on entry.type ('plural'/'past')
4. Return findings array

explain(finding) returns `{nb, nn, severity}`:
- nb: "Uregelrett form — bruk <em>{fix}</em> i stedet for <em>{original}</em>."
- nn: "Uregelrett form — bruk <em>{fix}</em> i staden for <em>{original}</em>."
- severity: 'warning'

Create `fixtures/en/morphology.jsonl` with >= 30 positive cases (tokens that should flag) and >= 15 acceptance cases (tokens that should NOT flag). Follow existing JSONL format from fixtures/en/grammar.jsonl. Each case: `{"id": "en-morph-XXX", "text": "...", "expected": [...]}` where expected is empty array for acceptance cases.

Positive cases should cover: irregular verb past (eated, goed, runned, comed, writed, taked, maked, sayed, doed, haved, gived, thinked, finded, bringed), irregular noun plural (childs, mouses, womans, mans, tooths, foots, gooses, peoples). Include sentence-context cases too.

Acceptance cases: correct irregular forms (children, mice, ate, went, ran), regular verbs (played, walked, talked, worked), regular nouns (dogs, cats, houses, tables, friends), mixed sentences with correct usage.
  </action>
  <verify>
    <automated>npm run check-fixtures -- --filter=en 2>&1 | tail -10</automated>
  </verify>
  <done>en-morphology rule flags childs/eated/goed etc with correct suggestions. >= 30 positive + >= 15 acceptance fixtures all pass. No regressions on existing EN fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MORPH-03 EN word-family POS-slot confusion rule + fixtures</name>
  <files>extension/content/spell-rules/en-word-family.js, fixtures/en/word-family.jsonl</files>
  <behavior>
    - "i have improve" -> flagged, suggestion "improved" (have + base -> need participle)
    - "she has improve" -> flagged, suggestion "improved"
    - "we had create" -> flagged, suggestion "created"
    - "they have succeed" -> flagged, suggestion "succeeded"
    - "i have improve a lot" -> flagged on "improve"
    - "i want to improve" -> NOT flagged (infinitive after "to" is correct)
    - "they improve every day" -> NOT flagged (base form as present tense)
    - "the improvement was great" -> NOT flagged (correct noun usage)
    - "she is very creative" -> NOT flagged (correct adjective usage)
    - "he successfully completed it" -> NOT flagged (correct adverb usage)
    - "i have improved" -> NOT flagged (correct participle)
    - "she has created" -> NOT flagged (correct participle)
  </behavior>
  <action>
Create `extension/content/spell-rules/en-word-family.js` using the IIFE self-registering pattern.

Rule shape:
- id: `'en-word-family'`
- languages: `['en']`
- priority: 18 (after en-morphology 17, before grammar 20)
- severity: `'warning'`

Inline a closed WORD_FAMILIES Map (~30 families). Each entry maps every family member to the full family: `{ verb, noun, adj, adv, pp }`. The `pp` field is the past participle form.

Seed families (common Norwegian-student confusions):
- improve/improvement/improved/improved
- create/creation/creative/creatively/created
- succeed/success/successful/successfully/succeeded
- decide/decision/decisive/decisively/decided
- develop/development/developed/developed
- explain/explanation/explanatory/explained
- compete/competition/competitive/competitively/competed
- communicate/communication/communicative/communicated
- educate/education/educational/educated
- organize/organization/organizational/organized
- (and ~20 more common derivational families)

check(ctx) logic:
1. For each token, check if the previous token(s) form a "have/has/had" auxiliary pattern
2. If `have/has/had` + current token matches a word-family key AND the token is the base verb (not the pp form):
   - Flag with suggestion = the pp form from the family
3. Additional slot checks (stretch): "the + base-verb" suggesting noun form, "very + base-verb" suggesting adjective. Only implement if the ~30-family set provides high precision.
4. Use `ctx.getTagged(i)` for POS context if helpful for disambiguation

explain(finding):
- nb: "Feil ordform — bruk <em>{fix}</em> etter <em>have/has/had</em>."
- nn: "Feil ordform — bruk <em>{fix}</em> etter <em>have/has/had</em>."
- severity: 'warning'

Create `fixtures/en/word-family.jsonl` with >= 30 positive + >= 15 acceptance cases.

Positive: "i have improve", "she has create a plan", "we had succeed", "they have decide", "i have develop", "she has explain", "we have compete", "they had communicate", "i have educate", "she has organize" — plus sentence-context variations.

Acceptance: "i want to improve", "they improve daily", "the improvement was big", "she is creative", "he successfully finished", "i have improved", "she has created", "we had succeeded", "the decision was final", correct usage of each family member in its natural slot.

Important: ensure MORPH-03 does NOT double-flag tokens already caught by en-grammar or en-morphology. Check that the rule only fires for word-family members, not arbitrary verb forms. The priority ordering (17 for morphology, 18 for word-family, 20 for grammar) plus dedupeOverlapping in the runner should handle overlap.
  </action>
  <verify>
    <automated>npm run check-fixtures -- --filter=en 2>&1 | tail -10</automated>
  </verify>
  <done>en-word-family rule flags "have improve" pattern with correct participle suggestions. >= 30 positive + >= 15 acceptance fixtures pass. No regressions.</done>
</task>

</tasks>

<verification>
1. `npm run check-fixtures` exits 0 — all EN fixtures (existing + new morphology + word-family) pass
2. `npm run check-explain-contract` exits 0 — both new rules have valid explain contracts
3. `npm run check-network-silence` exits 0 — no fetch/XHR in new rule files
4. `npm run check-benchmark-coverage` — EN benchmark lines 34 (eated), 27 (childs), 39 (improve) now flip
</verification>

<success_criteria>
- en-morphology flags irregular overgeneration with correct suggestions (childs->children, eated->ate, goed->went)
- en-word-family flags have+base-form with participle suggestion (have improve -> have improved)
- >= 30 positive + >= 15 acceptance fixtures per rule, all green
- All release gates pass
- Benchmark EN lines flip as expected
</success_criteria>

<output>
After completion, create `.planning/phases/14-morphology-beyond-tokens-en-es-fr/14-02-SUMMARY.md`
</output>
