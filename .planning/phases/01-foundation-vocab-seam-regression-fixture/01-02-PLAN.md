---
phase: 01-foundation-vocab-seam-regression-fixture
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - extension/content/spell-check-core.js
  - extension/content/spell-check.js
  - extension/content/word-prediction.js
  - extension/manifest.json
requirements:
  - INFRA-01
  - INFRA-04
autonomous: true
must_haves:
  truths:
    - "spell-check-core.js exports a pure check(text, vocab, opts) function that returns findings with rule_id (not type) — runnable under Node via require()"
    - "spell-check.js delegates rule evaluation to __lexiSpellCore.check(), keeping only DOM markers, popover UI, and event listeners"
    - "spell-check.js no longer rebuilds its own nounGenus/verbInfinitive/validWords/typoFix/compoundNouns — it reads them from __lexiVocab getters"
    - "spell-check.js references zero internals of word-prediction.js (grep: no __lexiPrediction, no word-prediction imports/references, no premium/subscription state)"
    - "word-prediction.js reads wordList and bigrams from __lexiVocab instead of loading them itself"
    - "manifest.json content_scripts loads vocab-seam-core.js and vocab-seam.js before word-prediction.js and spell-check.js, with __lexiPrediction fully removed"
  artifacts:
    - path: extension/content/spell-check-core.js
      provides: "Pure check(text, vocab, opts) → findings[] with rule_id — dual-export (module.exports + self.__lexiSpellCore)"
      contains: "module.exports"
      min_lines: 300
    - path: extension/content/spell-check.js
      provides: "DOM adapter: markers, popover, event listeners; delegates to __lexiSpellCore"
      contains: "self.__lexiSpellCore"
      min_lines: 300
    - path: extension/content/word-prediction.js
      provides: "Prediction dropdown consuming __lexiVocab for wordList + bigrams"
      contains: "self.__lexiVocab"
    - path: extension/manifest.json
      provides: "Content script ordering puts vocab-seam-core.js + vocab-seam.js before consumers"
      contains: "vocab-seam.js"
  key_links:
    - from: extension/content/spell-check.js
      to: extension/content/spell-check-core.js
      via: "self.__lexiSpellCore.check(text, vocab, opts)"
      pattern: "__lexiSpellCore\\.check"
    - from: extension/content/spell-check.js
      to: extension/content/vocab-seam.js
      via: "self.__lexiVocab.getNounGenus/getVerbInfinitive/getValidWords/getTypoFix/getCompoundNouns/onReady"
      pattern: "__lexiVocab\\.(getNounGenus|getVerbInfinitive|getValidWords|getTypoFix|getCompoundNouns|onReady|getLanguage)"
    - from: extension/content/word-prediction.js
      to: extension/content/vocab-seam.js
      via: "self.__lexiVocab.getWordList() and self.__lexiVocab.getBigrams()"
      pattern: "__lexiVocab\\.(getWordList|getBigrams)"
    - from: extension/manifest.json
      to: "content script load order"
      via: "vocab-seam-core.js + vocab-seam.js listed before word-prediction.js + spell-check.js"
      pattern: "vocab-seam"
---

<objective>
Cutover: extract spell-check's pure rule logic into `spell-check-core.js`, refactor `spell-check.js` and `word-prediction.js` to consume `__lexiVocab` instead of loading vocab themselves, and delete `__lexiPrediction` outright. Update `manifest.json` to load `vocab-seam-core.js` and `vocab-seam.js` before both consumers.

Purpose: Complete INFRA-01 (shared seam consumed by both modules) and INFRA-04 (spell-check structurally separable — no imports from word-prediction internals, no premium coupling). Also seeds INFRA-03's Phase 3 rule-plugin refactor by delivering the pure-core split today.

Output: Updated `spell-check.js` + new `spell-check-core.js` + refactored `word-prediction.js` + reordered `manifest.json`. One cutover commit; `__lexiPrediction` disappears entirely. The fixture harness (Plan 03) depends on `spell-check-core.js` being Node-importable.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-vocab-seam-regression-fixture/01-CONTEXT.md
@.planning/phases/01-foundation-vocab-seam-regression-fixture/01-RESEARCH.md
@.planning/phases/01-foundation-vocab-seam-regression-fixture/01-01-SUMMARY.md

# Source files to move logic out of
@extension/content/spell-check.js
@extension/content/word-prediction.js
@extension/content/vocab-seam.js
@extension/content/vocab-seam-core.js
@extension/manifest.json

<interfaces>
<!-- Contracts created by Plan 01 that this plan consumes -->

// self.__lexiVocab (from extension/content/vocab-seam.js)
// Full surface: getWordList, getLanguage, isReady, isPaused, isTextInput, onReady,
//               getFrequency, getBigrams, getTypoBank, getNounGenus, getVerbInfinitive,
//               getValidWords, getTypoFix, getCompoundNouns
// (See 01-01-SUMMARY.md for exact return shapes.)

// self.__lexiVocabCore (from extension/content/vocab-seam-core.js)
// Pure API: buildIndexes({raw, bigrams, lang, isFeatureEnabled}) → indexes object.
// Needed in this plan only via the browser wrapper; not referenced directly in consumers.

<!-- Contracts created by this plan that Plan 03 consumes -->

// extension/content/spell-check-core.js (dual-export: self.__lexiSpellCore + module.exports)
//
// export function check(text, vocab, opts) → Finding[]
//   text: string (raw input text)
//   vocab: {
//     nounGenus: Map<string, string>,
//     verbInfinitive: Map<string, string>,
//     validWords: Set<string>,
//     typoFix: Map<string, string>,
//     compoundNouns: Set<string>,
//   }
//   opts: { cursorPos?: number | null, lang?: 'nb' | 'nn' }
//
// Finding shape (stable contract — fixture files will match this):
//   {
//     rule_id: 'gender' | 'modal_form' | 'sarskriving' | 'typo',
//     word: string,          // the original offending token text
//     start: number,         // inclusive offset into `text`
//     end: number,           // EXCLUSIVE offset (end = start + word.length)
//     fix: string,           // the suggested replacement (current field name in spell-check.js)
//     // plus any additional metadata currently emitted (kept byte-for-byte)
//   }
//
// IMPORTANT — naming convention: the core emits `rule_id`, not `type`.
// The DOM adapter (spell-check.js) can re-expose `type` to existing UI code via a post-process
// shim if needed; keep the adapter's internal field name whatever the current UI expects,
// but the CORE's output field is `rule_id`. See pitfall #10 in RESEARCH.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract spell-check-core.js — pure check() + helpers, dual-export footer, rule_id (not type)</name>
  <files>extension/content/spell-check-core.js</files>
  <action>
Create `extension/content/spell-check-core.js` as an IIFE-wrapped pure module holding the rule logic currently in `spell-check.js`. No DOM, no `chrome.*`, no timers, no `document` references.

Step 1 — Read `extension/content/spell-check.js` in full (lines 1–898) before writing anything. Identify and port:
  - `check()` function body (approximately lines 196–317): the rule-evaluation loop that produces findings.
  - All pure helpers it calls: `editDistance` (lines 337–367 — preserve the Damerau-Levenshtein with adjacent transposition; see "Don't Hand-Roll" — existing implementation beats `fast-levenshtein` because it handles `berde→bedre`), `findFuzzyNeighbor`, `matchCase`, `dedupeOverlapping` (lines 452–459), tokenization helpers, and the ARTICLE_GENUS / GENUS_ARTICLE / MODAL_VERBS constants (lines 42–54).

Step 2 — `check(text, vocab, opts)` signature:
  ```javascript
  function check(text, vocab, opts = {}) {
    const { cursorPos = null, lang = 'nb' } = opts;
    const { nounGenus, verbInfinitive, validWords, typoFix, compoundNouns } = vocab;
    // ...tokenize, run rules, collect findings, dedupe, return
    return findings;
  }
  ```
  Replace every `PREDICTION.getLanguage()` / `this.lang` / implicit `lang`-from-closure with the `lang` from `opts`.
  Replace every index access (`nounGenus.get(...)`, `verbInfinitive.get(...)`, etc.) with the destructured vocab arg.

Step 3 — Rename the output field `type` → `rule_id` at the core emission site.
  - Inside the ported `check()` body, wherever spell-check.js emits `type: 'gender' | 'modal_form' | 'sarskriving' | 'typo'`, change to `rule_id`. Keep the string values **exactly** (`"gender"`, `"modal_form"`, `"sarskriving"`, `"typo"`) per RESEARCH open-question #4.
  - See pitfall #10: fixture files use `rule_id`; a mismatch silently fails every case.

Step 4 — Preserve rule evaluation order and dedupe behavior:
  - The order in which rules are checked inside `check()` is load-bearing (pitfall #6). Copy the order verbatim from `spell-check.js`; do not reorganize for style.
  - `dedupeOverlapping` (spell-check.js:452–459): preserve the "earlier-listed wins" tie-breaker. Add a comment that this ordering is tested by the fixture suite in Plan 03.

Step 5 — `end` is exclusive (past-the-end):
  - spell-check.js uses `end = m.index + m[0].length` (line 188). Keep this. Add a file-header comment:
    `// Span convention: start is inclusive, end is exclusive (Python-style). end = start + word.length.`
  - This seeds pitfall #5 prevention; fixture authors will reference this comment.

Step 6 — Strip everything that isn't pure:
  - No `chrome.storage`, `chrome.runtime`, `chrome.tabs` references.
  - No DOM references (`document`, `activeEl`, markers, overlay, popover).
  - No timers (`setTimeout`, `debounceTimer`).
  - No `dismissed` Set — that's adapter-side state.
  - No imports from `__lexiPrediction` or `__lexiVocab` — the vocab arg is passed in.

Step 7 — Dual-export footer (same pattern as vocab-seam-core.js):
  ```javascript
  const api = { check, editDistance /* any helpers useful for tests */ };
  if (typeof module !== 'undefined' && module.exports) module.exports = api;
  if (typeof self !== 'undefined') self.__lexiSpellCore = api;
  ```

Avoid:
- Do NOT bring over the "acceptable = (lang === 'nb' && actual === 'f' && articleTok.word === 'en')" gender tolerance by rewriting it (spell-check.js:231). Copy verbatim — that branch is what makes "en bok" not flag, and fixtures rely on it.
- Do NOT split `check()` into smaller functions as a cleanup pass. First copy works, then if the function feels too long note it for Phase 3 (where the rule-plugin refactor splits each rule into its own file). Pitfall #2 logic: byte-for-byte moves first, cleanup later.
- Do NOT expose `self.__lexiPrediction` or any compatibility alias.
  </action>
  <verify>
    <automated>node -e "const c = require('/Users/geirforbord/Papertek/leksihjelp/extension/content/spell-check-core.js'); if (typeof c.check !== 'function') process.exit(1); const vc = require('/Users/geirforbord/Papertek/leksihjelp/extension/content/vocab-seam-core.js'); const raw = require('/Users/geirforbord/Papertek/leksihjelp/extension/data/nb.json'); const idx = vc.buildIndexes({ raw, bigrams: null, lang: 'nb' }); const findings = c.check('en hus', idx, { lang: 'nb' }); if (!Array.isArray(findings)) { console.error('check() must return an array'); process.exit(1); } const f = findings[0]; if (!f || f.rule_id !== 'gender') { console.error('expected gender finding, got '+JSON.stringify(findings)); process.exit(1); } if (typeof f.start !== 'number' || typeof f.end !== 'number' || f.end <= f.start) { console.error('invalid span: '+JSON.stringify(f)); process.exit(1); } const src = require('fs').readFileSync('/Users/geirforbord/Papertek/leksihjelp/extension/content/spell-check-core.js','utf8'); const forbidden = ['document.','chrome.storage','chrome.runtime','chrome.tabs','setTimeout','__lexiPrediction']; const leaks = forbidden.filter(k => src.includes(k)); if (leaks.length) { console.error('core has leaked adapter refs: '+leaks.join(',')); process.exit(1); } console.log('OK: core pure, rule_id='+f.rule_id+' span=['+f.start+','+f.end+']');"</automated>
  </verify>
  <done>
`require('./extension/content/spell-check-core.js').check('en hus', vocabIndexes, {lang:'nb'})` returns an array with at least one finding whose `rule_id === 'gender'` and `end > start`. File contains no references to `document.`, `chrome.storage`, `chrome.runtime`, `chrome.tabs`, `setTimeout`, or `__lexiPrediction`. Dual-export footer present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor spell-check.js (DOM adapter) + word-prediction.js (consumer) to read from __lexiVocab, delete __lexiPrediction</name>
  <files>extension/content/spell-check.js, extension/content/word-prediction.js</files>
  <action>
Both files touched together so the cutover is atomic (no intermediate state where `__lexiPrediction` exists AND `__lexiVocab` is consumed).

### Part A — Refactor `extension/content/spell-check.js`:

Step A1 — Replace the top-of-file seam bind:
  ```javascript
  // OLD (delete):
  //   const PREDICTION = self.__lexiPrediction;
  //   if (!PREDICTION) return;
  //
  // NEW:
  const VOCAB = self.__lexiVocab;
  const CORE  = self.__lexiSpellCore;
  if (!VOCAB || !CORE) return;
  ```

Step A2 — Delete the entire `rebuildIndexes` function and the module-level `nounGenus` / `verbInfinitive` / `validWords` / `typoFix` / `compoundNouns` declarations (spell-check.js:33–37 + 136–172). These now live in vocab-seam-core.js and are exposed via VOCAB getters.

Step A3 — Replace `runCheck()` (or the equivalent per-keystroke function) to call the core instead of local rule logic. The body becomes roughly:
  ```javascript
  function runCheck() {
    if (!activeEl || !enabled || paused) { hideOverlay(); return; }
    const { text, cursor } = readInput(activeEl);
    if (!text || text.length < 3) { hideOverlay(); return; }
    const lang = VOCAB.getLanguage();
    if (lang !== 'nb' && lang !== 'nn') { hideOverlay(); return; } // SC-06 v1 scope
    let findings = CORE.check(text, {
      nounGenus:      VOCAB.getNounGenus(),
      verbInfinitive: VOCAB.getVerbInfinitive(),
      validWords:     VOCAB.getValidWords(),
      typoFix:        VOCAB.getTypoFix(),
      compoundNouns:  VOCAB.getCompoundNouns(),
    }, { cursorPos: cursor, lang });
    findings = findings.filter(f => !isDismissed(f));    // existing dismissed-set logic stays
    // Optional compat: if existing UI still reads `f.type`, shim it here:
    //   findings.forEach(f => { f.type = f.rule_id; });
    renderMarkers(findings);
  }
  ```

Step A4 — Replace `PREDICTION.onReady(...)` calls (spell-check.js:94, 117) with `VOCAB.onReady(...)`. These become one-line renames.

Step A5 — Delete all rule-evaluation helpers that moved into the core (`editDistance`, `findFuzzyNeighbor`, `matchCase`, `dedupeOverlapping`, ARTICLE_GENUS/GENUS_ARTICLE/MODAL_VERBS constants, the `check()` body). Keep ONLY:
  - DOM marker rendering (overlay, dots)
  - Popover UI + dismiss actions
  - Event listeners (`focus`/`blur`/`input` on text inputs)
  - `isDismissed` helper + the dismissed Set
  - `chrome.runtime.onMessage` listener for `SPELL_CHECK_TOGGLED` + `LEXI_PAUSED` + `LANGUAGE_CHANGED` (the adapter still cares about these for its own policy)
  - `readInput` / `writeInput` helpers

Step A6 — Verify decoupling (INFRA-04):
  - No reference to `__lexiPrediction` anywhere (grep).
  - No reference to `word-prediction` (string) anywhere except inside comments.
  - No reference to subscription/premium/Vipps/Stripe/JWT state — this file must be free of policy coupling per INFRA-04 ("no premium/subscription state ... module could later be extracted to `skriv.papertek.app`").

### Part B — Refactor `extension/content/word-prediction.js`:

Step B1 — Delete the `self.__lexiPrediction = { ... }` export at the bottom of the file (lines 1829–1844). No alias. No replacement. `__lexiPrediction` disappears entirely from the extension.

Step B2 — Delete `loadWordList` (approximately lines 440–760) and `loadBigrams` (approximately lines 763–785) — their logic moved to vocab-seam-core.js in Plan 01. Replace the places they're called with reads from `__lexiVocab`:
  ```javascript
  const VOCAB = self.__lexiVocab;
  if (!VOCAB) { console.error('[word-prediction] vocab seam missing'); return; }

  // Wherever the code previously referenced a local `wordList` variable, use:
  //   const wordList = VOCAB.getWordList();
  // Wherever it referenced local bigrams, use:
  //   const bigrams = VOCAB.getBigrams();
  ```
  Small but load-bearing detail: word-prediction uses `wordList` in many functions. Introduce one top-of-function `const wordList = VOCAB.getWordList();` inside each function that needs it (rather than a single module-level `const` that captures the wordList at init time — the VOCAB state changes on LANGUAGE_CHANGED, so reading fresh per-call is safer).

Step B3 — Delete word-prediction's own `chrome.runtime.onMessage` branch for `LANGUAGE_CHANGED` that triggers a reload (vocab-seam now owns reload). Keep branches for consumer-local concerns: `PREDICTION_TOGGLED`, `GRAMMAR_FEATURES_CHANGED` (if the prediction UI needs to respond — but NOT to reload vocab; vocab-seam handles the rebuild).

Step B4 — Delete the old `isFeatureEnabled` function if it duplicates what the seam now uses — OR keep it as consumer-side post-filter if prediction applies extra filtering beyond the seam-level emission. Whichever, do not leave TWO sources of truth for grammar-feature toggling; pick one and document in the plan summary.

Step B5 — Keep the prediction-specific logic: the dropdown UI, the ranking/scoring, the keyboard handlers, the `isTextInput` check (or use `VOCAB.isTextInput`).

### Part C — Update `extension/manifest.json`:

Step C1 — Edit the `content_scripts[0].js` array. Final order:
  ```json
  "js": [
    "i18n/strings.js",
    "content/vocab-store.js",
    "content/vocab-seam-core.js",
    "content/vocab-seam.js",
    "content/floating-widget.js",
    "content/word-prediction.js",
    "content/spell-check.js"
  ]
  ```
  Why this order:
  - `vocab-store.js` first — provides `__lexiVocabStore` used by `vocab-seam.js`.
  - `vocab-seam-core.js` before `vocab-seam.js` — seam wrapper reads `self.__lexiVocabCore`.
  - Both seam scripts before `word-prediction.js` and `spell-check.js`.
  - `floating-widget.js` keeps its current relative slot (deferred — routing it through `__lexiVocab` is out of scope per CONTEXT).
  - `spell-check.js` last — depends on both `__lexiVocab` and `__lexiSpellCore`.

Step C2 — Add `content/spell-check-core.js` to the array, immediately before `content/spell-check.js`:
  ```json
  "js": [
    "i18n/strings.js",
    "content/vocab-store.js",
    "content/vocab-seam-core.js",
    "content/vocab-seam.js",
    "content/floating-widget.js",
    "content/word-prediction.js",
    "content/spell-check-core.js",
    "content/spell-check.js"
  ]
  ```

Step C3 — Bump `"version"` in manifest.json per the project's release workflow (CLAUDE.md: update extension/manifest.json + package.json + backend/public/index.html). From `2.2.9` → `2.3.0` (minor bump — structural refactor, no new user-visible features but not a patch either). Also bump `package.json` `"version"` and the landing-page display version in `backend/public/index.html`.

Avoid:
- Do NOT leave `__lexiPrediction` as an alias even "just in case." CONTEXT explicitly forbids it, and pitfall #4 in RESEARCH warns about hybrid states.
- Do NOT refactor word-prediction's ranking/scoring logic in this plan. That's Phase 3. This plan only re-routes data sources.
- Do NOT introduce a new global `wordList` variable at module scope in word-prediction.js. Read per-call from `VOCAB.getWordList()` — prevents stale captures across `LANGUAGE_CHANGED`.
- Do NOT touch `floating-widget.js` — routing it through `__lexiVocab` is explicitly deferred in CONTEXT.
- Do NOT remove or rename `chrome.runtime.onMessage` branches that `service-worker.js` dispatches to. Double-check `background/service-worker.js` still sends `LANGUAGE_CHANGED` etc. (no code change — just sanity).
  </action>
  <verify>
    <automated>bash -c "cd /Users/geirforbord/Papertek/leksihjelp && ! grep -q '__lexiPrediction' extension/content/spell-check.js extension/content/word-prediction.js extension/manifest.json && grep -q 'self.__lexiSpellCore' extension/content/spell-check.js && grep -q 'self.__lexiVocab\b' extension/content/spell-check.js && grep -q 'self.__lexiVocab\b' extension/content/word-prediction.js && node -e \"const m = require('./extension/manifest.json'); const js = m.content_scripts[0].js; const req = ['content/vocab-store.js','content/vocab-seam-core.js','content/vocab-seam.js','content/word-prediction.js','content/spell-check-core.js','content/spell-check.js']; for (const r of req) { if (!js.includes(r)) { console.error('missing '+r); process.exit(1); } } const wpIdx = js.indexOf('content/word-prediction.js'); const seamIdx = js.indexOf('content/vocab-seam.js'); const coreIdx = js.indexOf('content/vocab-seam-core.js'); const scIdx = js.indexOf('content/spell-check.js'); const scCoreIdx = js.indexOf('content/spell-check-core.js'); if (coreIdx > seamIdx) { console.error('core must load before seam'); process.exit(1); } if (seamIdx > wpIdx) { console.error('seam must load before word-prediction'); process.exit(1); } if (seamIdx > scIdx) { console.error('seam must load before spell-check'); process.exit(1); } if (scCoreIdx > scIdx) { console.error('spell-core must load before spell-check'); process.exit(1); } console.log('OK: manifest order valid'); const grep = require('fs').readFileSync('./extension/content/spell-check.js','utf8'); const coupled = ['subscription','premium','vipps','stripe','JWT','word-prediction'].filter(k => new RegExp(k,'i').test(grep)); const filtered = coupled.filter(k => { const lines = grep.split('\n').filter(l => new RegExp(k,'i').test(l)); return lines.some(l => !l.trim().startsWith('//') && !l.trim().startsWith('*')); }); if (filtered.length) { console.error('INFRA-04 violation: spell-check.js contains coupling: '+filtered.join(',')); process.exit(1); } console.log('OK: INFRA-04 decoupling verified');\""</automated>
  </verify>
  <done>
- `grep "__lexiPrediction" extension/content/*.js extension/manifest.json` returns zero matches.
- `spell-check.js` contains `self.__lexiVocab` and `self.__lexiSpellCore` and delegates rule evaluation to `CORE.check(...)`.
- `word-prediction.js` reads `VOCAB.getWordList()` and `VOCAB.getBigrams()`; no longer owns `loadWordList` or `loadBigrams`.
- `manifest.json` content_scripts order: `vocab-store.js` → `vocab-seam-core.js` → `vocab-seam.js` → `floating-widget.js` → `word-prediction.js` → `spell-check-core.js` → `spell-check.js`.
- `extension/manifest.json`, `package.json`, `backend/public/index.html` all have version bumped to `2.3.0`.
- `spell-check.js` contains zero non-comment references to `subscription`, `premium`, `vipps`, `stripe`, `JWT`, or `word-prediction` (INFRA-04 decoupling).
  </done>
</task>

</tasks>

<verification>
1. `grep -r "__lexiPrediction" extension/` returns zero matches (no alias, no adapter, per CONTEXT).
2. `grep -c "self.__lexiSpellCore" extension/content/spell-check.js` ≥ 1.
3. `grep -c "VOCAB.get" extension/content/spell-check.js` ≥ 5 (all five index getters referenced).
4. `grep -c "VOCAB.get" extension/content/word-prediction.js` ≥ 2 (at least getWordList + getBigrams).
5. Manifest content_scripts ordering: both vocab-seam scripts load before word-prediction.js and spell-check.js; spell-check-core.js loads before spell-check.js.
6. INFRA-04 check: `spell-check.js` contains no non-comment references to `subscription|premium|vipps|stripe|JWT|word-prediction`.
7. `node -e "const c = require('./extension/content/spell-check-core.js'); const v = require('./extension/content/vocab-seam-core.js'); const idx = v.buildIndexes({raw: require('./extension/data/nb.json'), bigrams: null, lang:'nb'}); const r = c.check('en hus', idx, {lang:'nb'}); if (r.length === 0 || r[0].rule_id !== 'gender') process.exit(1);"` passes.
8. Versions bumped consistently in `extension/manifest.json`, `package.json`, and `backend/public/index.html`.
</verification>

<success_criteria>
- Plan 01's `__lexiVocab` is the sole vocab source for both `spell-check.js` and `word-prediction.js`.
- `__lexiPrediction` is deleted everywhere (no alias).
- Pure rule logic lives in `spell-check-core.js` with `rule_id` field (not `type`); it is `require()`-able from Node.
- `spell-check.js` is pure UI + wiring; any grep for word-prediction internals or premium/subscription state comes up empty.
- Manifest content_scripts ordering is: vocab-store → vocab-seam-core → vocab-seam → floating-widget → word-prediction → spell-check-core → spell-check.
- Extension version bumped to 2.3.0 in all three locations per CLAUDE.md release workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-vocab-seam-regression-fixture/01-02-SUMMARY.md` covering:
- Final `check()` signature and `Finding` shape (locked here, consumed by Plan 03 fixtures)
- Any field-name renames beyond `type → rule_id` discovered during the port
- Whether the adapter shim re-exposes `f.type = f.rule_id` for legacy UI code or whether UI code reads `rule_id` directly
- Final decision on grammar-feature filtering (seam-level only, consumer-level post-filter, or both)
- Manifest ordering finalized + version bumped to 2.3.0
- Confirmation that a manual smoke test on a `<textarea>` still flags "en hus" and suggests "et hus" (load the unpacked extension in Chrome and type the text)
</output>
