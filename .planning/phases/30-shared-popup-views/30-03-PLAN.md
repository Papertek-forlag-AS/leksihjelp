---
phase: 30-shared-popup-views
plan: 03
type: execute
wave: 3
depends_on:
  - 30-01
  - 30-02
files_modified:
  - .planning/phases/30-shared-popup-views/30-03-UAT.md
autonomous: false
requirements:
  - PHASE-30-G6  # Rolled-up E2E UAT (Phase 29 lock + Phase 30 dictionary parity + Phase 28 regression)
must_haves:
  truths:
    - "Teacher creating a LEKSIHJELP_EXAM test on staging-lockdown produces a working exam-locked student environment with a real (non-stub) leksihjelp dictionary panel"
    - "Student sees EKSAMENMODUS badge, leksihjelp toggle ON + disabled + 'Slått på av lærer' caption (Phase 29 lock surface)"
    - "Floating widget renders amber border in exam mode (Phase 27 visual cue)"
    - "Word-prediction dropdown does not open in exam mode (Phase 29 envelope wordPrediction:false)"
    - "Grammar-lookup dots are suppressed; typo dots remain visible; dictionary lookups still work (Phase 27 exam-marker filtering)"
    - "Dictionary sidepanel renders full conjugations, declensions, language switcher works, direction toggle works (Phase 30 parity)"
    - "NO audio play buttons render anywhere in the lockdown sidepanel (Phase 30 audioEnabled:false suppression)"
    - "Profile transition LEKSIHJELP_EXAM → FULL clears examModeLocked + examMode flags; surfaces re-enable; FULL profile shows audio buttons in extension popup but lockdown stays no-audio"
    - "Phase 28's dev-only 'Simuler lærer-lås' button still works in extension popup (no regression)"
    - "Production deploy to lockdown-stb (papertek.app) is NOT executed during this plan — explicitly deferred to user-driven follow-up"
  artifacts:
    - path: .planning/phases/30-shared-popup-views/30-03-UAT.md
      provides: Pass/fail walkthrough record with timestamps + screenshots/notes per step
      contains: "PASS\\|FAIL"
  key_links:
    - from: teacher action (create LEKSIHJELP_EXAM test)
      to: chrome.storage.local.examModeLocked = true at student session
      via: writing-environment.js applyExamModeLock() (Phase 29-02)
      pattern: "examModeLocked"
    - from: student opens test
      to: leksihjelp-sidepanel-host mounts dictionary view
      via: bootLeksihjelp().then(mountLeksihjelpSidepanel) (Phase 30-02)
      pattern: "__leksihjelpSidepanelHost\\.mount"
    - from: dictionary view in lockdown
      to: NO audio buttons in DOM
      via: deps.audioEnabled === false short-circuits playAudio render
      pattern: "audioEnabled.*false"
---

<objective>
Run the rolled-up end-to-end UAT covering Phase 29 (teacher-lock surface) + Phase 30 (dictionary parity + audio suppression) + Phase 28 (regression check). This was originally Phase 29-03's deferred verification; deferred because the lockdown leksihjelp panel was a stub. Now that Plan 30-02 ships a real dictionary surface, the full surface is testable.

**Production deploy is explicitly OUT OF SCOPE per user instruction.** All verification happens against `staging-lockdown.web.app`. After staging UAT passes, production deploy to `lockdown-stb` (`papertek.app`) is a separate user-driven action.

Purpose: Confirm v3.3 Exam Mode milestone delivers a coherent classroom-ready surface — lock works, dictionary works, audio is suppressed, regression-safe. Without this rollup, Phase 29 + Phase 30 ship blind.

Output: `30-03-UAT.md` with a pass/fail line per checklist item, screenshots for visual checks, and a clear go/no-go for un-deferring the lockdown-stb production deploy.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-shared-popup-views/30-CONTEXT.md
@.planning/phases/30-shared-popup-views/30-01-PLAN.md
@.planning/phases/30-shared-popup-views/30-02-PLAN.md
@.planning/phases/29-lockdown-teacher-lock-ux/29-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-UAT smoke-checks + create UAT.md scaffold</name>
  <files>.planning/phases/30-shared-popup-views/30-03-UAT.md</files>
  <action>
**A. Confirm staging is ready:**

```bash
# 1. Lockdown staging branch deployed
gh run list -R papertek/lockdown -L 3      # most recent run on staging branch should be green
# OR check Firebase Hosting console for staging-lockdown.web.app

# 2. Lockdown staging contains the new sidepanel host
curl -s https://staging-lockdown.web.app/js/writing-test/student/leksihjelp-sidepanel-host.js | head -5
# Should return JS, not 404. (If the staging app gates static assets behind auth, skip the curl and rely on a logged-in browser walkthrough.)

# 3. Lockdown staging serves the synced popup views
curl -s -o /dev/null -w "%{http_code}\n" https://staging-lockdown.web.app/leksihjelp/popup/views/dictionary-view.js
# Should return 200.
```

If any of those fail: stop and fix the staging deploy before running the human walkthrough.

**B. Create `.planning/phases/30-shared-popup-views/30-03-UAT.md`** with the walkthrough scaffold (the human verifier fills in PASS/FAIL + notes during Task 2). Use this template:

```markdown
---
phase: 30-shared-popup-views
plan: 03
type: uat
status: pending  # pending | in-progress | passed | failed | diagnosed
verifier: <user>
staging_url: https://staging-lockdown.web.app
extension_version: <from manifest.json>
lockdown_version: v4.10.0
production_deploy: NOT_RUN (deferred per user instruction)
---

# Phase 30 Rolled-up UAT — Phase 29 Lock + Phase 30 Dictionary Parity + Phase 28 Regression

## Setup
- [ ] Browser: Chrome with leksihjelp extension installed (version <X>)
- [ ] Staging URL accessible: https://staging-lockdown.web.app
- [ ] Two accounts ready: 1 teacher, 1 student (or same account toggled if staging supports it)
- [ ] DevTools console open during student session for error inspection

## Section A — Teacher creates LEKSIHJELP_EXAM test
- [ ] A1. Teacher logs in to staging-lockdown
- [ ] A2. Teacher opens new test creation
- [ ] A3. Resource-profile picker shows "Eksamen med Leksihjelp" option (label correct: nb/nn/en variants if testable)
- [ ] A4. Teacher selects LEKSIHJELP_EXAM → saves test
- [ ] A5. Test code captured: __________

## Section B — Student joins, lock surface visible
- [ ] B1. Student opens test code → enters writing environment
- [ ] B2. EKSAMENMODUS badge visible in extension popup header
- [ ] B3. Open extension popup → exam-mode toggle is ON, disabled, with "Slått på av lærer" caption
- [ ] B4. Floating widget on selection: amber border visible
- [ ] B5. Word-prediction dropdown: type 4 chars in editor → no dropdown opens
- [ ] B6. Grammar-lookup dots: select a noun → no grammar dot, typo dots still visible if word misspelled
- [ ] B7. DevTools console: no errors during lock setup

## Section C — Dictionary parity (Phase 30 NEW scope)
- [ ] C1. In writing environment, click resource panel → switch to "Leksihjelp ordbok" tab
- [ ] C2. Sidepanel renders: search input + result area (NOT the old stub `<input>+<div>`)
- [ ] C3. Type "schule" (or current bundled DE noun) → results render with article "die", plural form, conjugations table for verbs
- [ ] C4. Language switcher chips at top: switch DE → NB → EN → DE — each switches lookup language
- [ ] C5. Direction toggle (NO→Target / Target→NO) works
- [ ] C6. Compound query (e.g., "chefsstu") shows compound suggestion card with translation guess
- [ ] C7. **NO audio play buttons render** anywhere in the result list (Phase 30 audio suppression)
- [ ] C8. Click "Lær mer" pedagogy popover (if available on a test entry) → popover opens with examples, no audio inside
- [ ] C9. Open settings (collapsible details under dictionary): grammar preset selector + dark mode toggle visible; language picker NOT visible; account/payments NOT visible; exam-toggle NOT visible

## Section D — Profile transition clears the lock
- [ ] D1. Teacher (in second tab) changes test resource-profile FULL on the same student
- [ ] D2. Student session: EKSAMENMODUS badge clears, exam-toggle becomes interactive again
- [ ] D3. Floating widget: amber border clears
- [ ] D4. Word prediction: now opens after 4 chars
- [ ] D5. Sidepanel dictionary still renders (FULL profile keeps leksihjelp visible)
- [ ] D6. **In lockdown sidepanel: STILL no audio buttons** even on FULL (Phase 30 audioEnabled:false is per-host, not per-profile)
- [ ] D7. Open extension popup directly (chrome://extensions popup, not lockdown sidepanel) → audio buttons DO render in extension's own dictionary view (positive control: audio works in extension, suppressed only in lockdown)

## Section E — Phase 28 regression check
- [ ] E1. Open extension popup, with student NOT in any lockdown test
- [ ] E2. Click "Simuler lærer-lås (dev)" button in settings
- [ ] E3. Exam-toggle becomes ON + disabled + "Slått på av lærer" caption (Phase 28 dev path still works without lockdown teacher)
- [ ] E4. Click again → toggle clears

## Outcome

**Result:** PASS / FAIL / DIAGNOSED-WITH-GAPS

**Production deploy decision:**
- [ ] If PASS: production deploy of Phase 29 + Phase 30 to lockdown-stb (papertek.app) is now eligible. **User runs the deploy manually** — NOT this plan.
- [ ] If FAIL: log gaps in the section below; route to `/gsd:plan-phase 30 --gaps` for closure.

## Gaps (if any)
<!-- Per-failure: { id, section, truth-failed, reason, artifacts-affected, missing } -->
```

Commit the scaffold to leksihjelp repo: `git add .planning/phases/30-shared-popup-views/30-03-UAT.md && git commit -m "docs(30-03): UAT walkthrough scaffold"`.
  </action>
  <verify>
    <automated>test -f .planning/phases/30-shared-popup-views/30-03-UAT.md && grep -q "Phase 30 Rolled-up UAT" .planning/phases/30-shared-popup-views/30-03-UAT.md && grep -q "audio play buttons" .planning/phases/30-shared-popup-views/30-03-UAT.md && grep -q "production_deploy: NOT_RUN" .planning/phases/30-shared-popup-views/30-03-UAT.md</automated>
  </verify>
  <done>
- UAT.md scaffold exists with Sections A-E covering teacher setup, student lock visibility, dictionary parity, profile transition, Phase 28 regression.
- Scaffold explicitly notes production_deploy: NOT_RUN.
- Pre-UAT smoke-checks confirm staging serves new sidepanel host file + synced views.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human walkthrough — execute UAT against staging-lockdown</name>
  <what-built>
Phase 29 + Phase 30 staged on staging-lockdown.web.app. Teacher-lock writer wired (Phase 29-02), real dictionary sidepanel mounting synced view modules with audioEnabled:false (Phase 30-02), Phase 28 dev-only Simuler lærer-lås button preserved.
  </what-built>
  <how-to-verify>
Open `.planning/phases/30-shared-popup-views/30-03-UAT.md` in the editor. Walk through Sections A through E in order. For each checkbox:

- Mark `[x]` if the step passes.
- If a step fails, mark `[FAIL]` and add a one-line note in the same bullet describing what was observed vs. expected. Take a screenshot of any unexpected UI state and reference it in the note (saved to `.planning/phases/30-shared-popup-views/screenshots/<step>.png`).

**Tools you'll need:**
- Two browser windows (or two profiles): one for teacher, one for student
- DevTools open in the student window throughout (watch for console errors)
- Latest extension installed locally (loaded unpacked from /Users/geirforbord/Papertek/leksihjelp/extension/) so the popup-side checks (B2, B3, D7, E1-E4) work

**Special attention to:**
- C7 (NO audio buttons in lockdown sidepanel) — open DevTools, search the rendered DOM for `audio-btn` class. Should return zero matches.
- D7 (audio DOES render in extension popup) — positive control, confirms the suppression is per-host, not a regression.
- B6 (grammar-lookup dots suppressed but typo dots remain) — write a sentence with both a misspelled word AND a correctly-spelled noun. Misspelled word should get a typo dot; noun should NOT get a grammar-lookup dot.

**At the end:**
- If ALL boxes checked: set `status: passed` in frontmatter and `Result: PASS` in Outcome section. Commit UAT.md.
- If any FAIL: set `status: diagnosed`, fill in the Gaps section with structured entries (`{ id, section, truth-failed, reason, artifacts-affected, missing }`) so a future `/gsd:plan-phase 30 --gaps` can plan closure. Commit UAT.md.

**Production deploy:** Even on PASS, do NOT run `firebase deploy --only firestore:rules,functions --project lockdown-stb`. Mark the production_deploy line as eligible-pending-user-action. The user runs the prod deploy themselves after reviewing the UAT.
  </how-to-verify>
  <resume-signal>Type "passed", "failed: <one-line summary>", or "diagnosed" with a count of gaps logged.</resume-signal>

  <files>(human verification — no files modified by this task)</files>
  <action>HUMAN CHECKPOINT — do NOT execute autonomously. Pause execution and present the &lt;how-to-verify&gt; walkthrough above to the user. Wait for resume-signal before continuing.</action>
  <verify>Human resume-signal received per &lt;resume-signal&gt; clause above.</verify>
  <done>User has typed the resume-signal token and any failures have been logged for follow-up.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize UAT outcome + commit + close phase</name>
  <files>.planning/phases/30-shared-popup-views/30-03-UAT.md, .planning/STATE.md</files>
  <action>
Based on Task 2's resume-signal:

**If PASSED:**
1. Confirm UAT.md frontmatter `status: passed` and `Outcome: PASS`.
2. Commit: `git commit -am "docs(30-03): UAT passed — Phase 29 lock + Phase 30 dictionary parity verified on staging"`.
3. Update `.planning/STATE.md`:
   - Set `position.plan: 3 of 3 complete`.
   - Append to Decisions: `[Phase 30-03]: Rolled-up UAT PASSED on staging-lockdown <date>; production deploy to lockdown-stb deferred per user instruction (eligible to run).`
   - Append to Pending Todos: `Production deploy to lockdown-stb still outstanding — user runs firebase deploy --only firestore:rules,functions + a lockdown main-branch merge for the sidepanel host change after reviewing UAT.md.`
4. Print a clear handoff message:
   ```
   Phase 30 complete. Staging UAT PASSED. Production deploy is YOUR call:
     1. cd /Users/geirforbord/Papertek/lockdown
     2. git checkout main && git merge staging
     3. git push origin main          # triggers papertek.app deploy
     4. firebase deploy --only firestore:rules,functions --project lockdown-stb
   ```

**If FAILED / DIAGNOSED:**
1. Confirm UAT.md frontmatter `status: diagnosed` and Gaps section is filled in.
2. Commit: `git commit -am "docs(30-03): UAT diagnosed — <N> gaps logged for closure"`.
3. Update `.planning/STATE.md`:
   - Append to Blockers/Concerns: `Phase 30-03 UAT failed at <sections>; <N> gaps in 30-03-UAT.md awaiting closure plan.`
4. Print handoff:
   ```
   Phase 30 UAT diagnosed <N> gaps. Next:
     /gsd:plan-phase 30 --gaps    # creates 30-04 closure plan from UAT.md gaps
   ```

**In all cases:** create the phase summary `30-03-SUMMARY.md` capturing:
- UAT outcome (passed/failed/diagnosed)
- Each section's pass/fail count
- Production deploy state: NOT_RUN, eligible / blocked-pending-gap-closure
- Cross-references: link to 29-03-SUMMARY.md (deferred predecessor) showing the rollup is now closed
  </action>
  <verify>
    <automated>grep -E "status: (passed|diagnosed|failed)" .planning/phases/30-shared-popup-views/30-03-UAT.md && grep -q "production_deploy: NOT_RUN" .planning/phases/30-shared-popup-views/30-03-UAT.md && test -f .planning/phases/30-shared-popup-views/30-03-SUMMARY.md && grep -qi "lockdown-stb\|production deploy" .planning/STATE.md</automated>
  </verify>
  <done>
- UAT.md has terminal status (passed / diagnosed / failed) and is committed.
- 30-03-SUMMARY.md exists capturing outcome + production-deploy state.
- STATE.md updated with the right pending todo (deploy outstanding) or blocker (gaps logged).
- User has a clear next-step instruction printed.
- Production deploy NOT executed by Claude.
  </done>
</task>

</tasks>

<verification>
- UAT.md walks all 5 sections (A-E) and has a terminal status.
- Audio-suppression assertion (C7) explicitly verified by inspecting DOM in DevTools.
- Production deploy line in UAT.md frontmatter remains `NOT_RUN` regardless of pass/fail.
- STATE.md reflects the new state.
</verification>

<success_criteria>
- Rolled-up UAT executed on staging-lockdown.web.app; outcome documented in UAT.md.
- Phase 29 lock surface verified (badge, disabled toggle, amber border, prediction off, grammar dots suppressed).
- Phase 30 dictionary parity verified (real sidepanel, conjugations, language switcher, direction toggle, compound suggestions).
- Audio suppression in lockdown verified by absence of `audio-btn` in rendered DOM.
- Phase 28 dev-button regression verified.
- Production deploy to lockdown-stb (papertek.app) is **explicitly NOT executed** in this plan — user-driven follow-up only.
</success_criteria>

<output>
After completion, create `.planning/phases/30-shared-popup-views/30-03-SUMMARY.md` with:
- UAT outcome (PASS / FAIL / DIAGNOSED) with section-by-section breakdown
- Screenshot references (if any failures)
- List of gaps (if diagnosed) ready for `/gsd:plan-phase 30 --gaps` consumption
- Production deploy state: explicitly NOT_RUN, with eligibility note
- Link back to 29-03-SUMMARY.md confirming the deferred predecessor scope is now closed
</output>
