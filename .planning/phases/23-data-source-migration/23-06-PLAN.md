---
phase: 23-data-source-migration
plan: 06
type: execute
wave: 2
depends_on: ["23-02"]
files_modified:
  - scripts/check-network-silence.js
  - scripts/check-baseline-bundle-size.js
  - scripts/check-baseline-bundle-size.test.js
  - package.json
  - CLAUDE.md
autonomous: true
requirements: [GATES-01, GATES-02]

must_haves:
  truths:
    - "check-network-silence excludes background/vocab-bootstrap.js, background/vocab-updater.js, content/vocab-store.js (the sanctioned bootstrap path) but still scans spell-check + word-prediction"
    - "check-baseline-bundle-size enforces extension/data/nb-baseline.json ≤ 200 KB"
    - "check-baseline-bundle-size:test plants an oversized baseline → gate fires; restores well-formed baseline → gate passes"
    - "CLAUDE.md Release Workflow lists the new gate alongside existing ones"
  artifacts:
    - path: "scripts/check-baseline-bundle-size.js"
      provides: "Baseline size gate"
      contains: "200"
    - path: "scripts/check-baseline-bundle-size.test.js"
      provides: "Paired self-test"
    - path: "scripts/check-network-silence.js"
      provides: "Updated allowlist for sanctioned bootstrap path"
      contains: "vocab-bootstrap"
  key_links:
    - from: "scripts/check-network-silence.js"
      to: "background/vocab-bootstrap.js + background/vocab-updater.js + content/vocab-store.js"
      via: "explicit allowlist"
      pattern: "vocab-bootstrap"
    - from: "package.json scripts"
      to: "scripts/check-baseline-bundle-size.js"
      via: "npm run check-baseline-bundle-size"
      pattern: "check-baseline-bundle-size"
---

<objective>
Two release-gate changes:
1. **SC-06 carve-out:** update `check-network-silence` so it explicitly allows fetch/network in `extension/background/vocab-bootstrap.js`, `extension/background/vocab-updater.js`, and `extension/content/vocab-store.js` (the sanctioned bootstrap/cache path) — but continues to scan and forbid network calls in `extension/content/spell-check*.js`, `extension/content/spell-rules/**`, and `extension/content/word-prediction.js`.
2. **New gate `check-baseline-bundle-size`:** enforces `extension/data/nb-baseline.json` ≤ 200 KB. Paired self-test plants an oversized file and asserts the gate fires; restores and asserts it passes.

Plus: update CLAUDE.md Release Workflow to list the new gate.

Purpose: Locks in the architectural decision that the bootstrap is the only allowed network site, and prevents accidental baseline bloat.
Output: Updated gate + new gate + tests + docs.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/23-data-source-migration/23-02-PLAN.md

@scripts/check-network-silence.js
@scripts/check-network-silence.test.js
@scripts/check-bundle-size.js
@scripts/check-bundle-size.test.js
@scripts/check-explain-contract.test.js

<interfaces>
Existing pattern for self-tested gates (mirror `check-explain-contract:test` and `check-rule-css-wiring:test`):
- gate script in `scripts/check-NAME.js` exits 0 on pass, 1 on fail with file:line pointer.
- self-test in `scripts/check-NAME.test.js`:
  1. Plant a broken state.
  2. Run the gate.
  3. Assert exit code 1.
  4. Restore.
  5. Plant a well-formed state.
  6. Run the gate.
  7. Assert exit code 0.
- package.json scripts: `check-NAME` and `check-NAME:test`.

This plan must NOT depend on plans 03/05 having shipped — the carve-out adjusts the allowlist proactively. The gate file `scripts/check-baseline-bundle-size.js` will exist and pass even before plan 03 ships (it can be lenient: if the baseline file doesn't exist yet, exit 0 with a "skipped — baseline not yet built" message, and the gate becomes meaningful once plan 03 lands). This makes plan 06 safely runnable in wave 2 alongside plan 02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SC-06 carve-out to check-network-silence + update its self-test</name>
  <files>scripts/check-network-silence.js, scripts/check-network-silence.test.js</files>
  <action>Edit `scripts/check-network-silence.js`:
- Locate the file-walking logic. Today it scans `extension/content/spell-check*.js`, `extension/content/spell-rules/**`, and `extension/content/word-prediction.js`.
- Add an explicit ALLOWLIST_FILES set: `['extension/background/vocab-bootstrap.js', 'extension/background/vocab-updater.js', 'extension/content/vocab-store.js']`. The gate already scopes its scan to the spell-check/prediction files, so these don't need to be excluded — they're outside the scan set. BUT, to make the carve-out explicit and self-documenting, add a top-of-file comment:
    ```
    // SC-06 sanctioned bootstrap path (do NOT add to scan set):
    //   - extension/background/vocab-bootstrap.js
    //   - extension/background/vocab-updater.js
    //   - extension/content/vocab-store.js
    // These files are the only places allowed to issue fetch() against the Papertek vocabulary API.
    // Spell-check + word-prediction must remain network-silent — that contract is unchanged.
    ```
- Verify the scan set still includes spell-check + spell-rules + word-prediction by reading the existing globs.
- If the existing scan logic walks `extension/content/**.js` broadly, narrow it: explicitly exclude `vocab-store.js` and `vocab-seam.js` (vocab-seam may legitimately fetch the bundled baseline via chrome.runtime.getURL — already whitelisted, but make it clear).

Update `scripts/check-network-silence.test.js`:
- Add a third self-test scenario: plant a fetch() call in `extension/background/vocab-bootstrap.js` (creating a stub if file doesn't exist yet). Run the gate. Assert exit 0 (because vocab-bootstrap is the sanctioned exception, NOT scanned). Restore.
- This proves the carve-out is real — the gate doesn't just happen to ignore that path; it explicitly does not scan it.</action>
  <verify>
    <automated>npm run check-network-silence && npm run check-network-silence:test</automated>
  </verify>
  <done>check-network-silence has explicit SC-06 carve-out comment + self-test verifies that planting a fetch in vocab-bootstrap does not fire the gate, while planting it in spell-check.js does.</done>
</task>

<task type="auto">
  <name>Task 2: Create check-baseline-bundle-size gate + paired self-test + CLAUDE.md update</name>
  <files>scripts/check-baseline-bundle-size.js, scripts/check-baseline-bundle-size.test.js, package.json, CLAUDE.md</files>
  <action>Three things:

(a) `scripts/check-baseline-bundle-size.js`:
- Define `BASELINE_PATH = 'extension/data/nb-baseline.json'` and `CEILING_BYTES = 200 * 1024`.
- If file does not exist: print "Baseline not built yet — gate is informational. Run npm run build-nb-baseline." and exit 0. (This keeps the gate runnable before plan 03 lands.)
- Otherwise: stat the file, print `Baseline size: <bytes> / <CEILING_BYTES>`. If over, print `FAIL: nb-baseline.json exceeds 200 KB cap. Trim with scripts/build-nb-baseline.js (raise the Zipf threshold or drop bigrams).` and exit 1. Else print `OK` and exit 0.
- Note: the gate measures the source file (pretty-printed) for early warning. The packaged zip is measured separately by `check-bundle-size`.

(b) `scripts/check-baseline-bundle-size.test.js`:
- Mirror the structure of `scripts/check-explain-contract.test.js` (paired self-test with restore-on-failure).
- Phase A: plant an oversized baseline. If `extension/data/nb-baseline.json` exists, back it up, then write a 250 KB file at that path. Run `node scripts/check-baseline-bundle-size.js`. Assert non-zero exit. Restore.
- Phase B: write a small (5 KB) well-formed JSON to the path (back up first if real one exists). Run gate. Assert exit 0. Restore.
- Phase C (informational baseline): if the baseline file doesn't exist, run gate, assert exit 0 (the "skipped" branch).
- Use try/finally to guarantee restoration so an aborted test doesn't leave the repo in a broken state.

(c) `package.json` scripts: add
  `"check-baseline-bundle-size": "node scripts/check-baseline-bundle-size.js"`
  `"check-baseline-bundle-size:test": "node scripts/check-baseline-bundle-size.test.js"`

(d) `CLAUDE.md` Release Workflow section: insert a new numbered step right after step 6 (`check-bundle-size`) introducing the new gate. Wording mirrors the existing entries — gate purpose, paired self-test command, and rationale (catches accidental baseline bloat as the bundled-data-removal landed in MIGRATE-01).</action>
  <verify>
    <automated>npm run check-baseline-bundle-size && npm run check-baseline-bundle-size:test</automated>
  </verify>
  <done>New gate + self-test + package.json scripts + CLAUDE.md updated; both `check-baseline-bundle-size` and `check-baseline-bundle-size:test` exit 0.</done>
</task>

</tasks>

<verification>
- `npm run check-network-silence` exits 0.
- `npm run check-network-silence:test` exits 0 and includes the new vocab-bootstrap carve-out scenario.
- `npm run check-baseline-bundle-size` exits 0 (informational while baseline absent; passes once plan 03 builds it ≤ 200 KB).
- `npm run check-baseline-bundle-size:test` exits 0 with both planted-fail and planted-pass scenarios.
- CLAUDE.md Release Workflow lists the new gate.
</verification>

<success_criteria>
- GATES-01, GATES-02 satisfied.
- Phase 23 gate inventory complete; subsequent phases inherit the new gate.
</success_criteria>

<output>
After completion, create `.planning/phases/23-data-source-migration/23-06-SUMMARY.md`.
</output>
