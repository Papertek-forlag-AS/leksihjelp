---
phase: 33-v3.1-cleanup
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified:
  - extension/content/spell-rules/de-gender.js
  - extension/content/spell-rules/de-grammar.js
  - extension/content/spell-rules/de-modal-verb.js
  - extension/content/spell-rules/de-prep-case.js
  - extension/content/spell-rules/de-separable-verb.js
  - extension/content/spell-rules/de-perfekt-aux.js
  - extension/content/spell-rules/de-v2.js
  - extension/content/spell-rules/de-verb-final.js
  - extension/content/spell-rules/de-compound-gender.js
  - extension/content/spell-rules/doc-drift-de-address.js
  - extension/content/spell-rules/doc-drift-fr-address.js
  - extension/content/spell-rules/doc-drift-nb-passiv-overuse.js
  - extension/content/spell-rules/doc-drift-nb-register.js
  - extension/content/spell-rules/doc-drift-nn-infinitive.js
  - extension/content/spell-rules/en-grammar.js
  - extension/content/spell-rules/en-homophones.js
  - extension/content/spell-rules/en-morphology.js
  - extension/content/spell-rules/en-word-family.js
  - extension/content/spell-rules/es-coordination.js
  - extension/content/spell-rules/es-fr-gender.js
  - extension/content/spell-rules/es-fr-modal-verb.js
  - extension/content/spell-rules/es-grammar.js
  - extension/content/spell-rules/es-gustar.js
  - extension/content/spell-rules/es-imperfecto-hint.js
  - extension/content/spell-rules/es-personal-a.js
  - extension/content/spell-rules/es-por-para.js
  - extension/content/spell-rules/es-pro-drop.js
  - extension/content/spell-rules/es-ser-estar.js
  - extension/content/spell-rules/es-subjuntivo.js
  - extension/content/spell-rules/fr-adj-gender.js
  - extension/content/spell-rules/fr-aspect-hint.js
  - extension/content/spell-rules/fr-bags.js
  - extension/content/spell-rules/fr-clitic-order.js
  - extension/content/spell-rules/fr-contraction.js
  - extension/content/spell-rules/fr-elision.js
  - extension/content/spell-rules/fr-etre-avoir.js
  - extension/content/spell-rules/fr-grammar.js
  - extension/content/spell-rules/fr-pp-agreement.js
  - extension/content/spell-rules/fr-preposition.js
  - extension/content/spell-rules/fr-subjonctif.js
  - extension/content/spell-rules/nb-aa-og.js
  - extension/content/spell-rules/nb-compound-gender.js
  - extension/content/spell-rules/nb-demonstrative-gender.js
  - extension/content/spell-rules/nb-gender.js
  - extension/content/spell-rules/nb-modal-verb.js
  - extension/content/spell-rules/nb-nn-passiv-s.js
  - extension/content/spell-rules/nb-sarskriving.js
  - extension/content/spell-rules/nb-v2.js
  - extension/content/spell-rules/universal-agreement.js
  - extension/manifest.json
  - package.json
  - backend/public/index.html
autonomous: false
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Every spell-rule with exam.safe=false has been individually re-evaluated; rules whose work is purely lookup-shaped (no algorithmic generation beyond Chrome native spellcheck parity) are flipped to safe=true with updated reason"
    - "Rules that legitimately exceed Chrome native parity (Lær mer pedagogy popovers, multi-token grammar generation, doc-drift detection) stay at safe=false with a one-line code-comment justifying why — so a future audit doesn't re-question them"
    - "check-exam-marker exits 0 after the flips"
    - "Version is aligned across extension/manifest.json + package.json + backend/public/index.html (single bumped value, no skew)"
    - "All 15 release-workflow gates exit 0"
    - "Cross-repo commits land: leksihjelp commit covers spell-rule flips + version bump + 33-01 popup refactor; lockdown commit covers the 33-02 sync refresh + dict-state-builder.js + sidepanel host fixes"
    - "Phase 33 SUMMARY documents which rules flipped to safe=true (count + per-rule rationale), which stayed safe=false (count + justification), and the new version number"
  artifacts:
    - path: ".planning/phases/33-v3.1-cleanup/33-03-SUMMARY.md"
      provides: "Phase 33 audit results + flip rationale + version bump record"
      contains: "exam.safe audit"
    - path: "extension/manifest.json"
      provides: "Bumped version aligned with package.json + backend/public/index.html"
    - path: "package.json"
      provides: "Bumped version"
    - path: "backend/public/index.html"
      provides: "Bumped landing-page display version"
  key_links:
    - from: "Every safe:false rule"
      to: "exam.safe field"
      via: "audit decision (flip to true with new reason, OR stay false with justification comment)"
      pattern: "exam:.*safe:"
    - from: "version field"
      to: "all three version locations"
      via: "manual edit"
      pattern: "\"version\":"
---

<objective>
Audit every spell-rule currently classified `exam: { safe: false, ... }` and flip those whose work is genuinely lookup-shaped (no work beyond what Chrome's native browser spellcheck already does). For each rule that stays at safe=false, add a one-line justification code-comment so the next audit doesn't re-question it. Then ship Phase 33: bump version, run all 15 release gates, commit + push.

Purpose: Phase 27-01 made a default-conservative call ("safe=false pending browser-baseline research") on 49 rules. The STATE.md pending-todos list flags this for revisit. This plan does the revisit. The category that legitimately exceeds Chrome parity (Lær mer pedagogy panel, multi-token generation, doc-drift) stays conservative; everything else can flip.

Output:
  - Each `exam.safe=false` rule is re-classified
  - Version bumped to 2.9.12 (or whatever next minor — see Task 4)
  - All 15 release gates green
  - 33-03-SUMMARY.md authored
  - Cross-repo commits: leksihjelp + lockdown
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v3.1-MILESTONE-AUDIT.md
@.planning/phases/27-exam-mode/27-01-PLAN.md
@.planning/phases/27-exam-mode/27-01-SUMMARY.md
@.planning/phases/33-v3.1-cleanup/33-01-SUMMARY.md
@.planning/phases/33-v3.1-cleanup/33-02-SUMMARY.md
@CLAUDE.md
@extension/exam-registry.js
@scripts/check-exam-marker.js

<interfaces>
<!-- Decision rubric for the safe=true vs safe=false flip:

FLIP TO safe=true when ALL of these are true:
  - The rule's matcher is pure lookup against a static word/form list (e.g. genus map, irregular-verb form list, fixed list of capitalized common nouns)
  - The "fix" suggestion is a single token replacement (no multi-token generation, no reorder)
  - The popover does NOT include a Lær mer pedagogy panel
  - Chrome's native browser spellcheck would flag the same surface as a misspelling (or it's a typo-style surface that browsers ALSO flag)
  - The rule does NOT consume sentence-level context (single-token decision)

STAY at safe=false when ANY of these are true:
  - Multi-token generation (de-v2 word-order rewrite, fr-clitic-order reorder, de-separable-verb reattach)
  - Doc-drift detection (looks at >1 sentence — doc-drift-* family)
  - Lær mer pedagogy panel (de-prep-case, fr-aspect-hint, es-por-para, es-gustar — these surface explanatory content beyond Chrome parity)
  - Stateful / cross-token reasoning (es-pro-drop, fr-pp-agreement)
  - Rule that "teaches" rather than "corrects" (es-imperfecto-hint, fr-aspect-hint)

Apply this rubric per-rule. The safe ones to flip are mostly the simple lookup rules:
  - de-gender.js (lookup against gender map — Chrome doesn't flag wrong articles, but the FIX is single-token; if the rule's POPOVER is plain "use 'der' not 'das'" with no pedagogy, it's lookup-shaped)
  - nb-gender.js, nb-modal-verb.js, nb-sarskriving.js, nb-aa-og.js, nb-compound-gender.js, nb-demonstrative-gender.js (NB lookup rules)
  - en-grammar.js, en-homophones.js, en-word-family.js, en-morphology.js (EN lookup rules)
  - es-fr-gender.js, fr-adj-gender.js, fr-elision.js, fr-contraction.js (single-token agreement/elision rules)
  - es-coordination.js (y/e o/u coordinator swap — single-token lookup)
  - universal-agreement.js (verify shape)

The rules that stay safe=false are the pedagogy-bearing + multi-token + doc-drift ones:
  - de-prep-case.js (Lær mer pedagogy)
  - de-v2.js, de-verb-final.js, de-separable-verb.js (multi-token word-order)
  - de-modal-verb.js, es-fr-modal-verb.js (modal-verb rewrite if multi-token)
  - de-perfekt-aux.js, fr-etre-avoir.js (auxiliary selection — context-dependent)
  - es-por-para.js, es-gustar.js (Lær mer pedagogy)
  - es-imperfecto-hint.js, fr-aspect-hint.js (pedagogy hints, teaching not correcting)
  - es-pro-drop.js, fr-clitic-order.js, fr-pp-agreement.js (multi-token / agreement reasoning)
  - es-personal-a.js (insertion of "a" — multi-token edit)
  - es-ser-estar.js, es-subjuntivo.js, fr-subjonctif.js (mood/aspect — context-dependent)
  - fr-bags.js (BAGS adjective placement — multi-token)
  - nb-nn-passiv-s.js (passive-s detection — multi-token reasoning if v3-style)
  - nb-v2.js (V2 word order — multi-token)
  - All doc-drift-*.js (cross-sentence reasoning by definition)

The executor's job: walk each rule in the files_modified list, apply the rubric, document the per-rule decision in 33-03-SUMMARY.md.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit every safe:false spell-rule + apply flip rubric</name>
  <files>extension/content/spell-rules/*.js (49 files with exam.safe=false)</files>
  <action>
    1. List all rules currently at exam.safe=false:
       ```
       grep -l "safe: false\|safe:false" extension/content/spell-rules/*.js
       ```
    2. For each rule, open the file and read enough to classify per the rubric in &lt;interfaces&gt; above:
       - Read the matcher (find/check function) to determine if it's lookup-shaped or generative
       - Read the suggest/replace logic to determine if the fix is single-token or multi-token
       - Check whether the rule has a `pedagogy` field returned from explain() — if yes, stays safe=false
       - Check whether the rule's category is "doc-drift" — if yes, stays safe=false
    3. For rules that meet the FLIP rubric:
       - Change `safe: false` → `safe: true`
       - Update the `reason` string to describe browser-parity argument:
         - Old: `'Lookup-shaped grammar rule (de-gender); pending browser-baseline research per CONTEXT.md'`
         - New: `'Lookup-shaped grammar rule (de-gender) — Chrome native parity confirmed in 33-03 audit (single-token suggestion, no pedagogy popover, no cross-sentence context)'`
    4. For rules that STAY at safe=false, add a one-line code-comment ABOVE the exam: block (so a future audit knows it was considered):
       ```javascript
       // exam-audit 33-03: stays safe=false — &lt;reason: Lær mer pedagogy / multi-token / doc-drift&gt;
       exam: {
         safe: false,
         ...
       }
       ```
    5. Track the flip count + stay count in a running list to feed Task 5's SUMMARY.
    6. CONSTRAINT: The reason string must remain a single line, max ~120 chars. The category field must NOT change (rules stay in their original closed-set category).
    7. CONSTRAINT: Do NOT touch rules already at safe=true. Do NOT touch exam-registry.js entries (those are non-rule UI surfaces; out of scope for this audit).
  </action>
  <verify>
    <automated>npm run check-exam-marker</automated>
  </verify>
  <done>Every audited rule has an updated reason string (if flipped) or a one-line code-comment (if staying); check-exam-marker exits 0; flip count + stay count recorded for SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Verify check-exam-marker:test paired self-test still passes after flips</name>
  <files>(no file edits — verification only)</files>
  <action>
    Run `npm run check-exam-marker:test` (the paired self-test that plants malformed/missing/invalid-category scratch rules and confirms the gate fires on each + a well-formed scratch rule confirms the gate passes). The flips in Task 1 should not affect this gate's behaviour, but verify the self-test still passes — if it fails, something in Task 1 broke the rule loader.
  </action>
  <verify>
    <automated>npm run check-exam-marker:test</automated>
  </verify>
  <done>check-exam-marker:test exits 0 (gate still fires correctly on planted scratch rules).</done>
</task>

<task type="auto">
  <name>Task 3: Run all 15 release-workflow gates</name>
  <files>(no file edits — verification only)</files>
  <action>
    Run each gate from CLAUDE.md Release Workflow steps 1-11. Every one must exit 0:

    1. `npm run check-fixtures`
    2. `npm run check-explain-contract` AND `npm run check-explain-contract:test`
    3. `npm run check-rule-css-wiring` AND `npm run check-rule-css-wiring:test`
    4. `npm run check-spellcheck-features`
    5. `npm run check-network-silence` AND `npm run check-network-silence:test`
    6. `npm run check-exam-marker` AND `npm run check-exam-marker:test`
    7. `npm run check-popup-deps` AND `npm run check-popup-deps:test`
    8. `npm run check-bundle-size` (runs `npm run package` internally)
    9. `npm run check-baseline-bundle-size` AND `npm run check-baseline-bundle-size:test`
    10. `npm run check-benchmark-coverage` AND `npm run check-benchmark-coverage:test`
    11. `npm run check-governance-data` AND `npm run check-governance-data:test`

    Plus the additional gates from package.json that aren't in the Release Workflow yet but should be green:
    - `npm run check-pedagogy-shape` AND `npm run check-pedagogy-shape:test`
    - `npm run check-stateful-rule-invalidation` AND `npm run check-stateful-rule-invalidation:test`
    - `npm run test:vocab-store`
    - `npm run test:vocab-seam`
    - `npm run test:popup-views`
    - `npm run test:dict-state-builder` (added in Plan 33-01)

    If any gate fails: STOP. Investigate. Do not proceed to version bump. The flips in Task 1 should not affect any gate other than check-exam-marker — if a different gate breaks, it's a real regression to fix before shipping.
  </action>
  <verify>
    <automated>npm run check-fixtures &amp;&amp; npm run check-explain-contract &amp;&amp; npm run check-explain-contract:test &amp;&amp; npm run check-rule-css-wiring &amp;&amp; npm run check-rule-css-wiring:test &amp;&amp; npm run check-spellcheck-features &amp;&amp; npm run check-network-silence &amp;&amp; npm run check-network-silence:test &amp;&amp; npm run check-exam-marker &amp;&amp; npm run check-popup-deps &amp;&amp; npm run check-popup-deps:test &amp;&amp; npm run check-baseline-bundle-size &amp;&amp; npm run check-baseline-bundle-size:test &amp;&amp; npm run check-benchmark-coverage &amp;&amp; npm run check-benchmark-coverage:test &amp;&amp; npm run check-governance-data &amp;&amp; npm run check-governance-data:test &amp;&amp; npm run check-pedagogy-shape &amp;&amp; npm run check-pedagogy-shape:test &amp;&amp; npm run check-stateful-rule-invalidation &amp;&amp; npm run check-stateful-rule-invalidation:test &amp;&amp; npm run test:vocab-store &amp;&amp; npm run test:vocab-seam &amp;&amp; npm run test:popup-views &amp;&amp; npm run test:dict-state-builder &amp;&amp; npm run check-bundle-size</automated>
  </verify>
  <done>All gates exit 0. Bundle stays under 20 MiB cap. NB baseline stays under 200 KB cap.</done>
</task>

<task type="auto">
  <name>Task 4: Bump version across all three locations</name>
  <files>extension/manifest.json, package.json, backend/public/index.html</files>
  <action>
    1. Read current package.json version (will be 2.9.11 per STATE.md).
    2. Choose next version: 2.9.12 (patch — gap closure, no new features). Document rationale in SUMMARY.
    3. Update all three files to the new version. CLAUDE.md Release Workflow step 12 requires alignment:
       - extension/manifest.json — `"version": "2.9.12"` (top-level)
       - package.json — `"version": "2.9.12"` (top-level)
       - backend/public/index.html — find the version display string (likely something like `&lt;span class="version"&gt;v2.9.11&lt;/span&gt;` or a JS const) and update.
    4. Re-run `npm run check-bundle-size` after the bumps to ensure the version change in manifest.json gets picked up by the package step (the check internally runs `npm run package`).
  </action>
  <verify>
    <automated>node -e "const m=require('./extension/manifest.json'),p=require('./package.json');console.log(m.version===p.version?'aligned':'SKEW: '+m.version+' vs '+p.version);process.exit(m.version===p.version?0:1)" &amp;&amp; grep -E "2\.9\.12|version" backend/public/index.html | head -5</automated>
  </verify>
  <done>All three files show the same new version (2.9.12); check-bundle-size still passes after the bump.</done>
</task>

<task type="auto">
  <name>Task 5: Author 33-03-SUMMARY.md + Phase 33 close-out</name>
  <files>.planning/phases/33-v3.1-cleanup/33-03-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    1. Author 33-03-SUMMARY.md documenting:
       - exam.safe audit results: per-rule decision table (rule, decision: flip / stay, rationale: 1 line)
       - Total flip count + total stay count
       - Version bumped: 2.9.11 → 2.9.12
       - All 15 release gates exit 0 (with timing/output evidence)
       - Cross-references to 33-01-SUMMARY.md (dict-state-builder + sidepanel host) + 33-02-SUMMARY.md (lockdown sync)
    2. Update .planning/STATE.md:
       - Position phase: 33 → complete
       - Last activity entry for 33-03 with version + flip count
       - Remove from Pending Todos: "Browser-baseline research: revisit lookup-shaped grammar rules currently classified exam.safe=false"
       - Remove from Pending Todos: "Lockdown sync needed: run `node scripts/sync-leksihjelp.js`"
       - Update progress counters
    3. Update .planning/ROADMAP.md:
       - Mark Phase 33 plans `[x]`
       - Update phase header status to complete
  </action>
  <verify>
    <automated>test -f .planning/phases/33-v3.1-cleanup/33-03-SUMMARY.md &amp;&amp; grep -q "exam.safe audit" .planning/phases/33-v3.1-cleanup/33-03-SUMMARY.md &amp;&amp; grep -q "Phase 33" .planning/STATE.md</automated>
  </verify>
  <done>SUMMARY authored with audit table; STATE.md + ROADMAP.md updated to mark Phase 33 complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Final review + cross-repo commits + GitHub Release</name>
  <what-built>
    - Plan 33-01 dict-state-builder lift (popup.js + lockdown sidepanel host) — staged
    - Plan 33-02 lockdown sync refresh — staged in lockdown repo
    - Plan 33-03 spell-rule exam.safe audit + version bump — staged in leksihjelp repo
    - All 15 release gates green
    - SUMMARY + STATE + ROADMAP updated
  </what-built>
  <how-to-verify>
    1. Review the leksihjelp `git status` and `git diff` — confirm:
       - extension/popup/dict-state-builder.js (extended)
       - extension/popup/dict-state-builder.test.js (new)
       - extension/popup/popup.js (refactored, dead-code removed)
       - extension/content/spell-rules/*.js (audit flips)
       - extension/manifest.json + package.json + backend/public/index.html (version bumped)
       - .planning/phases/33-v3.1-cleanup/33-01,02,03-SUMMARY.md (new)
       - .planning/STATE.md + .planning/ROADMAP.md (updated)
    2. Review the lockdown `git status` and `git diff` (cd /Users/geirforbord/Papertek/lockdown) — confirm:
       - public/leksihjelp/* (sync refresh)
       - public/js/writing-test/student/leksihjelp-sidepanel-host.js (Plan 33-01 task 3 fixes)
       - scripts/sync-leksihjelp.js (if Plan 33-02 Task 1 extended it)
       - public/js/leksihjelp-loader.js (if LEKSI_BUNDLE updated)
    3. If everything looks good: approve the commit. Two commits land in parallel:
       - leksihjelp: `feat(phase-33): close v3.1 cleanup — dict-state-builder lift + exam.safe audit + version 2.9.12`
       - lockdown: `chore(leksihjelp-sync): refresh synced surface + sidepanel host integration (leksihjelp 2.9.12)`
    4. After commits land, run `npm run package` from leksihjelp to produce the zip, then upload to GitHub Releases as v2.9.12 (Release Workflow step 14).
    5. Production deploy of lockdown is OUT OF SCOPE for this phase (user holds prod-deploy decisions per STATE.md). Document staging-only.
  </how-to-verify>
  <resume-signal>
    Type "approved" to ship. If issues: describe and we'll fix before commit.
  </resume-signal>
  <files>(checkpoint task — manual UAT, no files modified by Claude)</files>
  <action>Pause for human verification per the &lt;how-to-verify&gt; steps above. Resume on approval signal.</action>
  <verify>
    <automated>echo "Manual verification — see how-to-verify"</automated>
  </verify>
  <done>User types approved signal after walking the verification steps.</done>
</task>

</tasks>

<verification>
  - All 15 release gates pass after Task 1 audit (Task 3)
  - Version aligned across 3 files (Task 4)
  - check-exam-marker passes after flips
  - SUMMARY documents per-rule decisions
  - STATE.md + ROADMAP.md mark Phase 33 complete
  - Cross-repo commits reviewed + landed (Task 6)
  - GitHub Release zip uploaded (Task 6)
</verification>

<success_criteria>
  - Every safe:false rule audited; flip count + stay count documented
  - check-exam-marker exits 0
  - Version bumped 2.9.11 → 2.9.12 (aligned)
  - All 15 release gates green
  - SUMMARY authored
  - STATE + ROADMAP updated
  - Two cross-repo commits landed (leksihjelp + lockdown)
  - Phase 33 closes the v3.1 audit's three open items
</success_criteria>

<output>
.planning/phases/33-v3.1-cleanup/33-03-SUMMARY.md
</output>
