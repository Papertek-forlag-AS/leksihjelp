---
phase: 33-v3.1-cleanup
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified: []
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Lockdown's public/leksihjelp/ tree mirrors the current extension/ tree for all synced surfaces (spell-check.js, content.css, i18n/strings.js, exam-registry.js, popup/views/, popup/dict-state-builder.js, popup/grammar-features-section.js)"
    - "Any lockdown-side divergences from upstream that the sync overwrites are documented (so we know whether to port back upstream)"
    - "If lockdown's sync-leksihjelp.js doesn't already pick up dict-state-builder.js, it does after this plan"
    - "Phase 26 + Phase 27 changes (Lær mer popover, exam markers) are reflected in lockdown's synced tree"
  artifacts:
    - path: "/Users/geirforbord/Papertek/lockdown/public/leksihjelp/"
      provides: "Refreshed synced surface (spell-check.js, content.css, i18n/strings.js, exam-registry.js, popup/views/, popup/dict-state-builder.js, popup/grammar-features-section.js)"
    - path: ".planning/phases/33-v3.1-cleanup/33-02-SUMMARY.md"
      provides: "Documentation of any lockdown-side diffs that should round-trip upstream + record of which surfaces were refreshed"
  key_links:
    - from: "extension/popup/dict-state-builder.js"
      to: "lockdown/public/leksihjelp/popup/dict-state-builder.js"
      via: "lockdown/scripts/sync-leksihjelp.js"
      pattern: "popup/dict-state-builder"
    - from: "extension/exam-registry.js"
      to: "lockdown/public/leksihjelp/exam-registry.js"
      via: "lockdown/scripts/sync-leksihjelp.js (line ~136 per audit)"
      pattern: "exam-registry"
---

<objective>
Mechanically re-run lockdown's sync-leksihjelp.js to mirror the upstream surfaces refreshed across Phases 26, 27, 30, 32, and 33-01. The lockdown sync went stale — per STATE.md "Lockdown sync needed: run `node scripts/sync-leksihjelp.js`". This plan owns the re-sync + diff inspection + documentation.

Purpose: Close the cross-phase tech-debt item. Without this, lockdown's deployed leksihjelp tree is missing Phase 26 Lær mer + Phase 27 exam-markers + the Plan 33-01 dict-state-builder extensions, even though the upstream code shipped weeks ago.

Output:
  - Refreshed lockdown/public/leksihjelp/ tree
  - 33-02-SUMMARY.md documenting which files changed during the sync, any divergences flagged (lockdown-side fixes that should round-trip upstream)
  - If sync script missed dict-state-builder.js: an extension to lockdown/scripts/sync-leksihjelp.js (cross-repo edit, no commit yet — Plan 33-03 ships)

NOTE TO EXECUTOR: This plan does NOT modify any leksihjelp source files. All work happens inside the lockdown sibling repo at /Users/geirforbord/Papertek/lockdown via its own sync script. Use absolute paths. Do NOT cd between repos. The leksihjelp-side commit happens in Plan 33-03 along with the version bump.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v3.1-MILESTONE-AUDIT.md
@/Users/geirforbord/Papertek/lockdown/scripts/sync-leksihjelp.js
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect lockdown sync script + verify dict-state-builder coverage</name>
  <files>/Users/geirforbord/Papertek/lockdown/scripts/sync-leksihjelp.js</files>
  <action>
    1. Read /Users/geirforbord/Papertek/lockdown/scripts/sync-leksihjelp.js end-to-end.
    2. Confirm the script copies (or globs) ALL of:
       - extension/content/ → public/leksihjelp/
       - extension/styles/content.css → public/leksihjelp/styles/leksihjelp.css
       - extension/styles/popup-views.css → public/leksihjelp/styles/popup-views.css
       - extension/i18n/ → public/leksihjelp/i18n/
       - extension/data/ → public/leksihjelp/data/ (already excluded per design — language data fetched at runtime)
       - extension/exam-registry.js → public/leksihjelp/exam-registry.js
       - extension/popup/views/ → public/leksihjelp/popup/views/
       - extension/popup/dict-state-builder.js → public/leksihjelp/popup/dict-state-builder.js  ← ADDED IN PLAN 33-01 IF MISSING
       - extension/popup/grammar-features-section.js → public/leksihjelp/popup/grammar-features-section.js
    3. If dict-state-builder.js is NOT in the script's file list, add it. Mirror the existing pattern (likely a single-file copy or a `popup/*.js` glob). Save the script.
    4. Same check for grammar-features-section.js (added in Phase 30-04 follow-up).
    5. Read lockdown/public/js/leksihjelp-loader.js LEKSI_BUNDLE array — confirm it includes 'leksihjelp/popup/dict-state-builder.js' BEFORE 'leksihjelp/popup/views/dictionary-view.js'. If missing, add it. The order matters: dict-state-builder must be loaded before the views since the host calls __lexiDictStateBuilder.buildDictState during view mount.
  </action>
  <verify>
    <automated>grep -E "dict-state-builder|grammar-features-section" /Users/geirforbord/Papertek/lockdown/scripts/sync-leksihjelp.js</automated>
  </verify>
  <done>Sync script lists dict-state-builder.js and grammar-features-section.js in its copy plan; LEKSI_BUNDLE loads dict-state-builder before view modules.</done>
</task>

<task type="auto">
  <name>Task 2: Run the sync + capture the diff</name>
  <files>/Users/geirforbord/Papertek/lockdown/public/leksihjelp/ (cross-repo, multiple files)</files>
  <action>
    1. Run the sync from the LOCKDOWN repo:
       ```
       cd /Users/geirforbord/Papertek/lockdown &amp;&amp; node scripts/sync-leksihjelp.js
       ```
       (Use absolute path in Bash command; do NOT change leksihjelp's cwd.)
    2. Inspect the lockdown-side git status:
       ```
       cd /Users/geirforbord/Papertek/lockdown &amp;&amp; git status
       ```
    3. For each modified file in lockdown/public/leksihjelp/, run `git diff` and categorise:
       - "expected refresh" — straightforward upstream change (Phase 26 Lær mer, Phase 27 exam markers, Plan 33-01 dict-state-builder)
       - "potential lockdown-side divergence" — content that looks like a lockdown-only fix that the sync just overwrote (e.g. someone hand-edited a synced file in lockdown). For these, capture the original lockdown content via `git show HEAD:public/leksihjelp/{path}` and document in the SUMMARY so the user can decide whether to round-trip upstream.
    4. Pay special attention to:
       - public/leksihjelp/spell-check.js — Phase 26 + 27 + 32 changes should appear (look for examMode references, fr-aspect-hint imports, pedagogy popover fields)
       - public/leksihjelp/content.css — Phase 26 Lær mer panel styles, Phase 27 amber widget border
       - public/leksihjelp/i18n/strings.js — Phase 26 new keys (laer_mer, etc.), Phase 27 examModeBadge, Phase 32 FR/ES pedagogy strings
       - public/leksihjelp/exam-registry.js — exists at all (should after Phase 28 closure)
       - public/leksihjelp/popup/dict-state-builder.js — newly added if it wasn't there
    5. Do NOT commit yet. Plan 33-03 owns the version-bump-coordinated commit.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/lockdown &amp;&amp; git status --short public/leksihjelp/ | wc -l</automated>
  </verify>
  <done>Sync completes without errors; git status shows the expected refresh in lockdown's leksihjelp tree; any potential divergences captured for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Author 33-02-SUMMARY.md with diff inventory</name>
  <files>.planning/phases/33-v3.1-cleanup/33-02-SUMMARY.md</files>
  <action>
    Create the SUMMARY documenting:
    1. Sync command run + result (clean / errors / warnings)
    2. Files refreshed (table with: file, change-category, summary-of-change, source-phase)
    3. Any lockdown-side divergences captured — for each, the original content and a recommendation (port upstream / accept overwrite / further investigation)
    4. Confirmation that LEKSI_BUNDLE order is correct (dict-state-builder before views)
    5. Outstanding: Plan 33-03 will (a) bump version triggering the downstream re-pin signal, (b) commit the lockdown-side refresh as a coordinated cross-repo PR

    NOTE: The leksihjelp-side commit happens in Plan 33-03. This plan stages the lockdown-side changes and documents them; the executor must NOT run `git commit` on either repo.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-v3.1-cleanup/33-02-SUMMARY.md &amp;&amp; grep -q "Files refreshed" .planning/phases/33-v3.1-cleanup/33-02-SUMMARY.md</automated>
  </verify>
  <done>33-02-SUMMARY.md exists with file-by-file change inventory and any divergence flags.</done>
</task>

</tasks>

<verification>
  - Sync script (cross-repo) lists dict-state-builder.js + grammar-features-section.js
  - `node scripts/sync-leksihjelp.js` completes cleanly from /Users/geirforbord/Papertek/lockdown
  - lockdown public/leksihjelp/ tree refreshed (Phase 26/27/32/33-01 surfaces present)
  - 33-02-SUMMARY.md authored
  - No git commits made yet (Plan 33-03 owns the commit)
</verification>

<success_criteria>
  - All synced surfaces refreshed in lockdown
  - dict-state-builder.js present in lockdown/public/leksihjelp/popup/
  - LEKSI_BUNDLE order correct
  - SUMMARY documents any divergences for upstream review
</success_criteria>

<output>
.planning/phases/33-v3.1-cleanup/33-02-SUMMARY.md
</output>
