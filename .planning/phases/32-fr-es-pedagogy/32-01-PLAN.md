---
phase: 32-fr-es-pedagogy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/content/spell-rules/fr-aspect-hint.js
  - extension/content/spell-check.js
  - fixtures/fr/aspect-hint.jsonl
  - extension/data/fr.json
  - scripts/check-explain-contract.js
  - scripts/check-explain-contract.test.js
  - extension/manifest.json
  - package.json
  - backend/public/index.html
  - extension/styles/content.css
autonomous: true
requirements:
  - PHASE-32-A
must_haves:
  truths:
    - "Student writing French with a passé-composé temporal adverb (e.g. 'hier', 'soudain') paired with an imparfait finite verb sees a soft-hint dot from fr-aspect-hint."
    - "Student writing French with an imparfait temporal adverb (e.g. 'souvent', 'tous les jours') paired with a passé-composé finite verb sees a soft-hint dot."
    - "Clicking 'Lær mer' on the fr-aspect-hint popover expands a panel with summary + explanation + example pair, sourced from papertek-vocabulary pedagogy data."
    - "Existing French rule fixtures (adj-gender, bags, clitic-order, elision, etre-avoir, pp-agreement, subjonctif, grammar) remain green — no regression."
    - "check-explain-contract passes when a rule's explain() returns optional pedagogy block; fails when pedagogy block is malformed."
  artifacts:
    - path: "extension/content/spell-rules/fr-aspect-hint.js"
      provides: "FR passé-composé vs imparfait soft-hint rule (P3)"
      contains: "module.exports"
    - path: "fixtures/fr/aspect-hint.jsonl"
      provides: "≥40 positive + ≥40 negative fixture lines"
    - path: "extension/data/fr.json"
      provides: "aspect_passe_compose_adverbs + aspect_imparfait_adverbs banks AND pedagogy.aspect_choice block (synced from papertek-vocabulary)"
      contains: "aspect_passe_compose_adverbs"
    - path: "scripts/check-explain-contract.js"
      provides: "Optional pedagogy-shape validation branch"
      contains: "pedagogy"
  key_links:
    - from: "extension/content/spell-rules/fr-aspect-hint.js"
      to: "extension/data/fr.json (aspect_*_adverbs banks + pedagogy.aspect_choice)"
      via: "vocab-seam-core lookup of generalbank/pedagogy entries"
      pattern: "aspect_(passe_compose|imparfait)_adverbs|pedagogy\\.aspect_choice"
    - from: "extension/content/spell-rules/fr-aspect-hint.js"
      to: "extension/exam-registry.js pedagogyPanel surface"
      via: "explain() returning { nb, nn, pedagogy }"
      pattern: "pedagogy:"
    - from: "scripts/check-explain-contract.js"
      to: "fr-aspect-hint.js (and any pedagogy-bearing rule)"
      via: "additive validate-pedagogy-shape branch"
      pattern: "validatePedagogy|pedagogy\\.summary"
---

<objective>
Ship a brand-new FR rule (`fr-aspect-hint`) that gives Norwegian students a soft hint when they pick the wrong aspect (passé composé vs imparfait), and ship the first FR pedagogy block — pulling structured `summary` / `explanation` / `examples` from papertek-vocabulary so "Lær mer" expands a real lesson.

This plan also extends `check-explain-contract` with an additive `pedagogy`-shape branch so every future pedagogy-bearing rule (32-02, 32-03, future FR/EN/NB) gets validated by the same gate. The gate extension lives here because 32-01 ships first AND is the canonical positive case the gate's :test scratch rules need to mirror.

Purpose: First FR pedagogy block. Establishes the cross-repo data-led pattern (data in papertek-vocabulary, function in leksihjelp) for every subsequent FR rule. Closes Phase 32 acceptance criterion A.
Output: New rule file, new fixture, refreshed fr.json, extended explain-contract gate (+ paired :test), version bump.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-fr-es-pedagogy/32-CONTEXT.md
@./CLAUDE.md

# Sibling rule to clone (structurally identical: temporal-adverb + finite verb pattern)
@extension/content/spell-rules/es-imperfecto-hint.js

# Canonical pedagogy-bearing rule (data-shape reference for the JSON contract)
@extension/content/spell-rules/de-prep-case.js

# Gate to extend (additive pedagogy branch + paired :test scratch rules)
@scripts/check-explain-contract.js
@scripts/check-explain-contract.test.js

# Surface registry (verify pedagogyPanel entry — should require no change)
@extension/exam-registry.js

# Cross-repo: data lives here
# /Users/geirforbord/Papertek/papertek-vocabulary/data/fr/generalbank.json (or wherever the FR data files live)

<interfaces>
<!-- es-imperfecto-hint.js export shape — fr-aspect-hint.js MUST mirror this contract -->

From extension/content/spell-rules/es-imperfecto-hint.js (clone target):
```js
module.exports = {
  id: 'es-imperfecto-hint',
  language: 'es',
  priority: 'P3',                      // soft hint, not correction
  exam: { safe: false, reason: '...', category: 'grammar-aspect' },
  detect(tokens, context) { /* returns array of findings */ },
  explain(finding) {
    return { nb: '...', nn: '...' };   // existing contract — strings only
  }
};
```

After this plan, fr-aspect-hint.js explain() returns:
```js
{
  nb: '...',
  nn: '...',
  pedagogy: {                          // NEW additive field
    summary:     { nb, nn, en },
    explanation: { nb, nn, en },
    examples:    [ { sentence, translation, note } ],
    common_error:{ wrong, correct, explanation: { nb, en } },
    context_hint: { nb, en },
    semantic_category: 'aspect-marker',
    related_phenomena: []
  }
}
```

From extension/data/de.json (canonical pedagogy data shape — exact fields):
```json
{
  "generalbank": {
    "an_prep": {
      "pedagogy": {
        "summary":     { "nb": "...", "nn": "...", "en": "..." },
        "explanation": { "nb": "...", "nn": "...", "en": "..." },
        "examples": [
          { "sentence": "...", "translation": { "nb": "...", "en": "..." }, "note": { "nb": "...", "en": "..." } }
        ],
        "common_error": { "wrong": "...", "correct": "...", "explanation": { "nb": "...", "en": "..." } },
        "context_hint": { "nb": "...", "en": "..." },
        "semantic_category": "preposition",
        "related_phenomena": []
      }
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FR aspect data + pedagogy block in papertek-vocabulary, sync into leksihjelp</name>
  <files>
    /Users/geirforbord/Papertek/papertek-vocabulary/data/fr/generalbank.json (or equivalent FR generalbank file — verify path on read),
    extension/data/fr.json,
    scripts/sync-vocab.js (read-only — verify it picks up new banks)
  </files>
  <action>
    Cross-repo step 1 of 4 for this plan.

    (a) In `/Users/geirforbord/Papertek/papertek-vocabulary/`, locate the FR generalbank source file (likely `data/fr/generalbank.json` or under a build pipeline at `src/`). Read its existing structure first — DO NOT assume layout.

    (b) Add two new banks under generalbank:
        - `aspect_passe_compose_adverbs`: array of strings — `hier`, `soudain`, `tout à coup`, `pendant deux ans`, `il y a trois jours`, `la semaine dernière`, `un jour`, `tout d'un coup`, `puis`, `ensuite`, `enfin`, `d'abord`, `après`, `avant-hier`, `lundi dernier`. Aim for ≥15 entries.
        - `aspect_imparfait_adverbs`: array of strings — `souvent`, `tous les jours`, `pendant que`, `quand j'étais`, `chaque été`, `toujours`, `parfois`, `de temps en temps`, `d'habitude`, `chaque jour`, `chaque matin`, `tous les soirs`, `à l'époque`, `autrefois`, `tandis que`. Aim for ≥15 entries.

    (c) Add `pedagogy.aspect_choice` shared entry (sibling of generalbank if convention puts shared pedagogy at root, OR under generalbank if that's how DE does it — match whatever de.json uses). Use the JSON shape documented in 32-CONTEXT.md §"JSON shape" and the de.json `an_prep.pedagogy` example as the canonical reference. Required fields:
        - `summary` { nb, nn, en } — one-sentence "imparfait = ongoing/habitual; passé composé = completed event"
        - `explanation` { nb, nn, en } — paragraph
        - `examples`: ≥3 entries, each with `sentence` (FR), `translation` { nb, en }, `note` { nb, en }
        - `common_error`: `wrong`/`correct` FR strings + `explanation` { nb, en }
        - `context_hint` { nb, en } — when does this matter
        - `semantic_category`: "aspect-marker"
        - `related_phenomena`: []

    (d) Commit in papertek-vocabulary with message `feat(fr): add aspect-choice pedagogy + temporal-adverb banks (leksihjelp 32-01)`.

    (e) Back in leksihjelp, run `npm run sync-vocab:fr` (or `npm run sync-vocab` if no per-language script). Verify `extension/data/fr.json` now contains both new banks AND the pedagogy block.

    (f) Inspect the synced fr.json with `grep -c aspect_passe_compose_adverbs extension/data/fr.json` and `grep -c '"summary"' extension/data/fr.json` — both should be ≥1.

    Why cross-repo first: data must exist in fr.json before fr-aspect-hint.js can read it; otherwise the rule would no-op when loaded by check-fixtures.
  </action>
  <verify>
    <automated>node -e "const d=require('./extension/data/fr.json'); if (!d.generalbank.aspect_passe_compose_adverbs || d.generalbank.aspect_passe_compose_adverbs.length < 15) throw new Error('passe_compose adverbs missing'); if (!d.generalbank.aspect_imparfait_adverbs || d.generalbank.aspect_imparfait_adverbs.length < 15) throw new Error('imparfait adverbs missing'); const ped = d.pedagogy?.aspect_choice || d.generalbank?.pedagogy?.aspect_choice; if (!ped || !ped.summary?.nb || !ped.summary?.en || !ped.explanation?.nb) throw new Error('pedagogy.aspect_choice malformed'); console.log('OK')"</automated>
  </verify>
  <done>
    fr.json contains both temporal-adverb banks (≥15 entries each) + pedagogy.aspect_choice block with non-empty nb/nn/en summary, nb/nn/en explanation, ≥3 examples, common_error, context_hint, semantic_category. Cross-repo commit landed in papertek-vocabulary.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create fr-aspect-hint rule + fixtures, register in spell-check.js</name>
  <files>
    extension/content/spell-rules/fr-aspect-hint.js,
    fixtures/fr/aspect-hint.jsonl,
    extension/content/spell-check.js,
    extension/styles/content.css
  </files>
  <behavior>
    - Test 1 (positive): "Hier je mangeais une pomme" → fires on `mangeais` (imparfait paired with passé-composé adverb `hier`).
    - Test 2 (positive): "Tous les jours j'ai mangé une pomme" → fires on `j'ai mangé` (passé composé paired with imparfait adverb `tous les jours`).
    - Test 3 (positive): "Souvent il a oublié son livre" → fires on `a oublié`.
    - Test 4 (negative): "Hier j'ai mangé une pomme" → NO finding (correct pairing).
    - Test 5 (negative): "Tous les jours je mangeais une pomme" → NO finding (correct pairing).
    - Test 6 (negative — token without aspect adverb): "Je mange une pomme" → NO finding.
    - Test 7 (explain contract): rule.explain(finding) returns { nb: <non-empty>, nn: <non-empty>, pedagogy: { summary: {nb, en}, explanation: {nb}, ... } }.
    - Test 8 (priority): finding.priority === 'P3' (soft hint).
    - Test 9 (exam marker): rule.exam = { safe: false, reason: <string>, category: 'grammar-aspect' }.
    - Test 10 (no false fire on quoted text): "« Hier je mangeais »" inside quotation → suppressed per the existing P2/P3 quotation-suppression infrastructure (see check-spellcheck-features for context).
  </behavior>
  <action>
    (a) Read `extension/content/spell-rules/es-imperfecto-hint.js` end-to-end. It is the structural template — same temporal-adverb + finite-verb pattern, same P3, same explain() shape. Clone its skeleton verbatim, then swap:
        - `id`: `'fr-aspect-hint'`
        - `language`: `'fr'`
        - Tense detection: replace ES presente/imperfecto check with FR passé-composé / imparfait check. FR passé composé = aux (avoir|être conjugated in present) + past participle. FR imparfait = stem + (-ais, -ais, -ait, -ions, -iez, -aient). Use simple suffix heuristics — this is a P3 hint, not a correction; perfect recall not required.
        - Adverb lists: read `vocab.generalbank.aspect_passe_compose_adverbs` and `vocab.generalbank.aspect_imparfait_adverbs` from the loaded FR vocab (NOT hardcoded — match the data-logic separation principle).
        - exam marker: `{ safe: false, reason: 'aspect-hint surfaces a P3 grammar nudge that browser native spellcheck does not provide; suppress under exam mode', category: 'grammar-aspect' }`.

    (b) Update explain() to additionally return the `pedagogy` block. Read it from the loaded vocab on rule construction (cache in module closure — DO NOT re-read per call). Path: `vocab.pedagogy?.aspect_choice` (or wherever Task 1 placed it — match exactly).

    (c) Add the rule import + registration to `extension/content/spell-check.js`. Find the existing FR-rule registration block (search for `fr-subjonctif` or `fr-bags` to locate it), add `fr-aspect-hint` alongside in the same pattern.

    (d) Add CSS dot-color binding in `extension/styles/content.css`: `.lh-spell-fr-aspect-hint { background: <color from existing P3 hint palette — copy from .lh-spell-es-imperfecto-hint>; }` — required by `check-rule-css-wiring`.

    (e) Create `fixtures/fr/aspect-hint.jsonl` with ≥40 positive + ≥40 negative cases. Use the existing fr/*.jsonl files as the format reference (one JSON object per line: `{ "text": "...", "expected": [{ "rule": "fr-aspect-hint", "span": [start, end] }] }` — match exact field names from a sibling fixture). Include the 10 test cases above plus enough variety: different verbs (manger/aller/parler/finir/voir/faire/avoir/être/dormir), different subjects (je/tu/il/nous/vous/ils), all 15 adverbs from each bank.

    (f) Run `npm run check-fixtures -- fr --rule=fr-aspect-hint --verbose` until ≥80% recall on positives, ≤2% false positives on negatives. Tune the suffix heuristics if needed but DO NOT inflate the rule's complexity past P3-hint territory.

    (g) Run full `npm run check-fixtures` — ALL existing rule fixtures must remain green (no regression in other FR rules, NB, etc.).
  </action>
  <verify>
    <automated>npm run check-fixtures -- fr --rule=fr-aspect-hint && npm run check-fixtures && npm run check-rule-css-wiring && npm run check-exam-marker</automated>
  </verify>
  <done>
    fr-aspect-hint.js exists, registered in spell-check.js, has CSS dot binding, fires on ≥80% of fixture positives with ≤2% FP on negatives, returns the additive pedagogy block in explain(). All other rule fixtures still green. check-rule-css-wiring + check-exam-marker pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend check-explain-contract with optional pedagogy-shape branch + paired :test scratch rules + version bump</name>
  <files>
    scripts/check-explain-contract.js,
    scripts/check-explain-contract.test.js,
    extension/manifest.json,
    package.json,
    backend/public/index.html
  </files>
  <behavior>
    - Test 1 (gate, positive — pedagogy absent): existing rule returning only { nb, nn } → gate passes (backward compat).
    - Test 2 (gate, positive — pedagogy well-formed): rule returning { nb, nn, pedagogy: { summary: {nb,en}, explanation: {nb}, ... } } → gate passes.
    - Test 3 (gate, negative — pedagogy malformed: missing summary.nb): gate fails with file:line pointer to the offending rule and message naming `pedagogy.summary.nb`.
    - Test 4 (gate, negative — pedagogy malformed: summary.nb empty string): gate fails.
    - Test 5 (gate, negative — pedagogy malformed: examples present but missing sentence): gate fails.
    - Test 6 (gate :test self-test): scratch positive rule (well-formed pedagogy) → gate passes. Scratch negative rule (malformed pedagogy) → gate fires. Both scratch rules cleaned up in finally{}.
  </behavior>
  <action>
    (a) Read `scripts/check-explain-contract.js` end-to-end. Identify the loop that loads each TARGETS rule and validates `rule.explain` returns `{ nb: string, nn: string }` with non-empty values.

    (b) Add an additive branch INSIDE that loop, AFTER the existing nb/nn check passes: if the explain() return value has a `pedagogy` key, validate the shape with a new `validatePedagogy(rule, pedagogy)` helper. Required:
        - `pedagogy.summary.nb` non-empty string
        - `pedagogy.summary.en` non-empty string (per CONTEXT §JSON shape: "non-empty `summary` for at least `nb` and `en`")
        - `pedagogy.explanation.nb` non-empty string
        - If `pedagogy.examples` present: must be array, each entry must have non-empty `sentence` string
        - If `pedagogy.common_error` present: must have non-empty `wrong` AND `correct` strings
    On any failure, exit 1 with a message naming the rule file AND the missing/malformed field path (e.g. `[fr-aspect-hint] pedagogy.summary.en is empty`).

    (c) Add `fr-aspect-hint` to the TARGETS list in check-explain-contract.js (alongside existing nb-gender, nb-modal-verb, nb-sarskriving, nb-typo-curated, nb-typo-fuzzy, plus de-prep-case which already has pedagogy — verify its current TARGETS membership). After this plan, fr-aspect-hint should be the second pedagogy-bearing rule the gate validates.

    (d) Extend `scripts/check-explain-contract.test.js` with two new scratch-rule scenarios (mirror the existing plant/cleanup pattern in try/finally):
        - **Positive scratch**: a rule returning { nb, nn, pedagogy: <well-formed block> } → assert gate exits 0.
        - **Negative scratch — empty summary.en**: rule returning { nb, nn, pedagogy: { summary: { nb: "x", en: "" }, explanation: { nb: "y" } } } → assert gate exits 1 with message containing `pedagogy.summary.en`.
        - **Negative scratch — example missing sentence**: rule with pedagogy.examples = [{ translation: { nb: "x" } }] (no `sentence` field) → assert gate exits 1.

    (e) Run `npm run check-explain-contract` — must exit 0 with fr-aspect-hint included.
    Run `npm run check-explain-contract:test` — must exit 0 (all scratch scenarios pass).

    (f) Run the full Release Workflow gate sweep per CLAUDE.md (steps 1-11):
        - `npm run check-fixtures` (already passed in Task 2)
        - `npm run check-explain-contract` + `:test`
        - `npm run check-rule-css-wiring` + `:test`
        - `npm run check-spellcheck-features`
        - `npm run check-network-silence`
        - `npm run check-exam-marker` + `:test`
        - `npm run check-popup-deps` + `:test` (untouched, should still pass)
        - `npm run check-bundle-size`
        - `npm run check-baseline-bundle-size`
        - `npm run check-benchmark-coverage`
        - `npm run check-governance-data`
        - `npm run check-pedagogy-shape` (already passing post-Phase 26; should still pass)

    (g) Bump version in three places (per CLAUDE.md Release Workflow step 12):
        - `extension/manifest.json` — bump patch (e.g. 2.9.9 → 2.9.10 or whatever current is — read first)
        - `package.json` — match
        - `backend/public/index.html` — match the displayed version number
    Why bump: signals lockdown + skriveokt-zero downstream consumers to re-pin per CLAUDE.md "Bumping `package.json` version signals a downstream sync is needed" rule.

    (h) Run `npm run package` — produces the updated zip (rebuild step, no upload here).
  </action>
  <verify>
    <automated>npm run check-explain-contract && npm run check-explain-contract:test && npm run check-fixtures && npm run check-rule-css-wiring && npm run check-rule-css-wiring:test && npm run check-spellcheck-features && npm run check-network-silence && npm run check-exam-marker && npm run check-exam-marker:test && npm run check-popup-deps && npm run check-popup-deps:test && npm run check-bundle-size && npm run check-baseline-bundle-size && npm run check-benchmark-coverage && npm run check-governance-data && npm run check-pedagogy-shape && node -e "const m=require('./extension/manifest.json'); const p=require('./package.json'); if (m.version !== p.version) throw new Error('version skew: manifest=' + m.version + ' package=' + p.version); console.log('versions aligned at ' + m.version)"</automated>
  </verify>
  <done>
    check-explain-contract validates pedagogy shape additively (passes with old-shape rules + new pedagogy-bearing rules; fails on malformed pedagogy). :test gate covers positive + negative scratch scenarios. All 15 release-gate scripts exit 0. Version aligned across manifest/package/landing-page. Zip rebuilt.
  </done>
</task>

</tasks>

<verification>
- `npm run check-fixtures` exits 0 (fr-aspect-hint ≥80% positive recall, ≤2% FP)
- `npm run check-explain-contract` and `:test` exit 0
- All 15 Release-Workflow gates exit 0
- Version bumped + aligned in manifest.json / package.json / backend/public/index.html
- Cross-repo PR landed in papertek-vocabulary with the FR generalbank/pedagogy data
</verification>

<success_criteria>
- New rule `fr-aspect-hint` ships, registered in spell-check.js, with CSS dot binding and exam marker
- ≥40 positive + ≥40 negative fixture lines in `fixtures/fr/aspect-hint.jsonl`
- `extension/data/fr.json` carries both temporal-adverb banks AND `pedagogy.aspect_choice` block, sourced from papertek-vocabulary
- `check-explain-contract` extended with optional pedagogy-shape branch (additive — no regression on existing rules)
- `check-explain-contract:test` covers positive + negative pedagogy scratch rules
- All 15 release gates pass
- Version bumped + aligned
</success_criteria>

<output>
After completion, create `.planning/phases/32-fr-es-pedagogy/32-01-SUMMARY.md` per the standard template, including:
- Cross-repo commit SHAs (papertek-vocabulary + leksihjelp)
- Final fixture counts and P/R/F1 numbers for fr-aspect-hint
- Version bump path (X.Y.Z → X.Y.Z+1)
- Confirmation that 32-02 and 32-03 can now consume the same `pedagogy` validation branch in check-explain-contract
</output>
