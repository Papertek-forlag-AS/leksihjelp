---
phase: 36-v3.1-uat-sweep-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-vocab-seam-coverage.js
  - scripts/check-vocab-seam-coverage.test.js
  - package.json
  - CLAUDE.md
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "F36-6: `npm run check-vocab-seam-coverage` exits 0 against current head; exits 1 with file:line on a planted index gap"
    - "Paired `:test` self-test exits 0; covers both gate-fires and gate-passes scenarios"
    - "Both gates registered in package.json; CLAUDE.md release workflow lists them as gate #12 with subsequent steps renumbered"
  artifacts:
    - path: "scripts/check-vocab-seam-coverage.js"
      provides: "Parses vocab-seam-core.buildIndexes return shape; asserts every key has a getX() in vocab-seam.js AND an entry in spell-check.js vocab construction"
      min_lines: 80
    - path: "scripts/check-vocab-seam-coverage.test.js"
      provides: "Plants a scratch index → asserts gate fires; removes it → asserts gate passes"
      min_lines: 60
    - path: "package.json"
      provides: "scripts.check-vocab-seam-coverage + scripts['check-vocab-seam-coverage:test']"
    - path: "CLAUDE.md"
      provides: "Release workflow section updated with gate #12 + paired :test pattern; subsequent gates renumbered"
  key_links:
    - from: "vocab-seam-core.js buildIndexes return object"
      to: "vocab-seam.js getX() getters AND spell-check.js vocab object"
      via: "static AST/regex parse"
      pattern: "return\\s*\\{[^}]+\\}"
---

<objective>
Ship the long-term fix for the bug class that broke Phase 35 verification: a release gate that asserts every index produced by `vocab-seam-core.buildIndexes` is surfaced through both `vocab-seam.js` (browser seam) AND `spell-check.js` (consumer composition).

Background: Phase 26-01, 32-01, and 32-03 each added a new index to `buildIndexes` (prepPedagogy, frAspectPedagogy, gustarPedagogy) without wiring the corresponding seam getter or spell-check.js entry. All 11 gates stayed green because the Node fixture-runner passes the raw indexes object directly into `core.check()`, bypassing the browser seam. Browser users got empty Maps/Sets and silent rules. The seam fix shipped in v2.9.15 (commit 7f55b1c). This gate prevents the next instance.

Purpose: Enforce the seam contract automatically.
Output: New gate + paired self-test + package.json + CLAUDE.md update.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@extension/content/vocab-seam.js
@extension/content/vocab-seam-core.js
@extension/content/spell-check.js
@scripts/check-explain-contract.js
@scripts/check-explain-contract.test.js
@scripts/check-popup-deps.js
@scripts/check-popup-deps.test.js

# Existing gate patterns to follow
- `scripts/check-explain-contract.js` + `.test.js` — TARGETS array + per-target validation; paired :test plants scratch files in try/finally.
- `scripts/check-popup-deps.js` + `.test.js` — regex-driven file scan with whitelist; paired :test plants a scratch fixture.

# Source-of-truth file regions
- `vocab-seam-core.js` lines ~1610-1675: `buildIndexes` return object (the canonical key list)
- `vocab-seam.js` lines ~380-455: `self.__lexiVocab = { ... }` getter object
- `spell-check.js` lines ~297-346: `const vocab = { … VOCAB.getX(), … }` consumer composition

# Naming convention
Each index key in core's return object maps to a getter named `get<PascalCase>`:
- `nounGenus` → `getNounGenus`
- `prepPedagogy` → `getPrepPedagogy`
- `gustarClassVerbs` → `getGustarClassVerbs`
- Function-shaped indexes also follow this (e.g. `decomposeCompound` → `getDecomposeCompound`).
- Special: `_sourceTag`, `bigrams`, `pitfalls`, `typoBank` are seam-internal/diagnostic and should be on a small EXEMPT list.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write check-vocab-seam-coverage.js (the gate)</name>
  <files>scripts/check-vocab-seam-coverage.js, package.json</files>
  <action>
Create `scripts/check-vocab-seam-coverage.js`. CommonJS, zero deps, Node 18+, mirrors the style of `scripts/check-explain-contract.js`.

ALGORITHM:

1. Read `extension/content/vocab-seam-core.js` as text.
2. Locate the `buildIndexes` function. Find the LAST `return {` block inside it (or use a more reliable anchor: the `// Phase 16: compound decomposition` comment is the tail of the return). Capture from `return {` to its matching `};`.
3. Parse the keys of that object literal. Both shorthand (`nounGenus,`) and `key: expression` forms must be captured. Spread (`...moodIndexes`) requires special handling:
   - Find the source declaration (e.g., `const moodIndexes = { ... }`) and recurse to capture its keys.
4. Build the canonical key set. Apply EXEMPT list:
   ```js
   const EXEMPT = new Set([
     '_sourceTag',     // diagnostic only
     'bigrams',        // accessed via getBigrams (verify it IS in seam, but skip naming check)
     'pitfalls',       // hydrated post-build, not from raw
     'typoBank',       // alias of typoFix — both must be in seam
   ]);
   ```
   (Decide which are truly exempt by inspecting current vocab-seam.js — anything that already has `getBigrams`, `getPitfalls`, etc. should not be exempt; only exempt diagnostic-only keys like `_sourceTag`.)

5. For each non-exempt key, derive the expected getter name:
   ```js
   function toGetter(key) {
     return 'get' + key[0].toUpperCase() + key.slice(1);
   }
   ```

6. Read `extension/content/vocab-seam.js`. Assert each expected getter name appears as a property in the `self.__lexiVocab = { ... }` object literal. If missing, record a violation with the canonical fix line:
   ```
   getPrepPedagogy: () => (state && state.prepPedagogy) ? state.prepPedagogy : new Map(),
   ```

7. Read `extension/content/spell-check.js`. Locate the `const vocab = {` block (the one inside `runCheck()` near line 297). Assert each canonical key (or its consumed alias) appears as a property in that literal, set from `VOCAB.<getter>()`. If missing, record a violation.

8. On any violation, print per-violation diagnostic:
   ```
   FAIL extension/content/vocab-seam.js: missing getter for index `prepPedagogy`
     Add this line near the existing getters:
       getPrepPedagogy: () => (state && state.prepPedagogy) ? state.prepPedagogy : new Map(),

   FAIL extension/content/spell-check.js: missing entry for index `prepPedagogy`
     Add this line inside the vocab = { ... } literal:
       prepPedagogy: VOCAB.getPrepPedagogy(),
   ```
   `process.exit(1)`.

9. On clean pass, print:
   ```
   check-vocab-seam-coverage: PASS — N indexes, all surfaced through seam + consumer
   ```
   `process.exit(0)`.

10. Add to `package.json` scripts:
    ```json
    "check-vocab-seam-coverage": "node scripts/check-vocab-seam-coverage.js",
    "check-vocab-seam-coverage:test": "node scripts/check-vocab-seam-coverage.test.js"
    ```

VERIFY THE GATE PASSES AT HEAD: post-Phase-35 v2.9.15 fix means all 5 previously-missing indexes are now wired. The gate must exit 0 immediately.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp &amp;&amp; npm run check-vocab-seam-coverage</automated>
  </verify>
  <done>Gate exits 0 at current head; npm script registered; per-violation diagnostics print on synthetic gap.</done>
</task>

<task type="auto">
  <name>Task 2: Write check-vocab-seam-coverage.test.js (paired self-test)</name>
  <files>scripts/check-vocab-seam-coverage.test.js</files>
  <action>
Create `scripts/check-vocab-seam-coverage.test.js`. Mirrors `scripts/check-explain-contract.test.js` structure.

The self-test must (using try/finally with backup/restore):

1. **Scenario A — gate fires on planted gap:**
   - Read current `vocab-seam-core.js` and `vocab-seam.js`. Backup both file contents in memory.
   - Inject a scratch key into the buildIndexes return object: edit `vocab-seam-core.js` to add `__scratchSeamCoverageProbe: new Map(),` inside the return literal (just before the closing `};`).
   - Do NOT add the matching getter to `vocab-seam.js`.
   - Run the gate: `child_process.spawnSync('node', ['scripts/check-vocab-seam-coverage.js'], { encoding: 'utf8' })`.
   - Assert: `result.status === 1` AND stderr/stdout includes `__scratchSeamCoverageProbe` AND mentions both `vocab-seam.js` and `spell-check.js`.
   - Restore both files in `finally`.

2. **Scenario B — gate passes after gap closed:**
   - Same plant in `vocab-seam-core.js`, but ALSO inject the matching `getScratchSeamCoverageProbe: () => new Map(),` into `vocab-seam.js`'s public surface AND inject `__scratchSeamCoverageProbe: VOCAB.getScratchSeamCoverageProbe(),` into `spell-check.js`'s vocab object.
   - Run the gate. Assert: `result.status === 0`.
   - Restore all three files.

3. **Scenario C — gate passes at clean head:**
   - No mutation. Run the gate. Assert: `result.status === 0`.

On any assertion failure, print the failing scenario name + observed status/output and `process.exit(1)`. On all pass: print `check-vocab-seam-coverage:test: PASS — 3/3 scenarios green` and `process.exit(0)`.

CRITICAL: All file mutations MUST be guarded by try/finally with the original content restored even on uncaught exception. Otherwise a crashed self-test corrupts the source tree.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp &amp;&amp; npm run check-vocab-seam-coverage:test</automated>
  </verify>
  <done>Self-test exits 0 on clean tree; 3 scenarios green; source tree byte-identical before/after.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md release workflow with gate #12</name>
  <files>CLAUDE.md</files>
  <action>
Edit the "Release Workflow" section of CLAUDE.md.

1. After existing gate #11 (`check-governance-data`), insert a new gate #12 block following the exact pattern of #11:

   ```markdown
   12. Validate vocab-seam coverage (INFRA-10):
       - `npm run check-vocab-seam-coverage` — must exit 0. Parses `extension/content/vocab-seam-core.js`'s `buildIndexes` return object, enumerates every key, and asserts each has a matching `get<PascalCase>` getter in `extension/content/vocab-seam.js` AND a matching entry in `extension/content/spell-check.js`'s `vocab = { ... }` consumer composition (around line 297). Exits 1 with file:line + a copy-paste fix line on any deviation.
       - Paired self-test: `npm run check-vocab-seam-coverage:test` — plants a scratch index → confirms gate fires; closes the gap → confirms gate passes; runs against clean head → confirms gate passes.
       - Why this gate exists: Phase 26-01, 32-01, and 32-03 each added a new index to `buildIndexes` without surfacing it through `vocab-seam.js` or wiring `spell-check.js`. All 11 prior gates stayed green because the Node fixture-runner passes raw `buildIndexes` output directly to `core.check()`, bypassing the browser seam. Browser users got empty Maps/Sets and silent rules — the de-prep-case Lær mer button never rendered, es-gustar was silent on extended verbs, fr-aspect-hint was silent on canonical triggers. The seam fix shipped in v2.9.15 (commit 7f55b1c). This gate prevents the next instance.
   ```

2. Renumber subsequent steps (currently 12 = bump version, 13 = rebuild zip, 14 = upload release) → 13, 14, 15.

3. Verify the renumber didn't break any internal cross-references in CLAUDE.md (search for `step 12`, `step 13`, `step 14` in the file body — if any prose refers to them, update accordingly).
  </action>
  <verify>
    <automated>grep -n "check-vocab-seam-coverage" /Users/geirforbord/Papertek/leksihjelp/CLAUDE.md | head -5</automated>
  </verify>
  <done>CLAUDE.md release workflow lists 15 numbered steps total (gates 1-12 + version-bump + rebuild + upload); new gate #12 follows the established pattern.</done>
</task>

</tasks>

<verification>
- `npm run check-vocab-seam-coverage` exits 0 at current head
- `npm run check-vocab-seam-coverage:test` exits 0 (3 scenarios)
- Both scripts registered in package.json
- CLAUDE.md release workflow has 15 numbered steps including the new gate #12
- Source tree byte-identical before/after self-test (no leaked scratch content)
</verification>

<success_criteria>
- INFRA-10 gate ships and exits 0 at head
- Paired self-test ships and exits 0
- CLAUDE.md updated; subsequent steps renumbered
- Plan 36-01's release-gate sweep can include the new gate without modification
</success_criteria>

<output>
After completion, create `.planning/phases/36-v3.1-uat-sweep-2/36-02-SUMMARY.md`
</output>
