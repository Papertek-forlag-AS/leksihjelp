---
created: 2026-05-02
status: pending
trigger: chrome-web-store v2.9.27 approval
---

# Follow-up: ship v2.9.28 to Chrome Web Store + tighten personvern

After Chrome Web Store approves and publishes v2.9.27:

1. Upload `backend/public/lexi-extension.zip` (v2.9.28) to the dashboard as
   a new version. Should auto-accept since 2.9.28 > 2.9.27.
2. The listing description, screenshots, privacy URL, etc. don't need to
   change — Chrome Web Store carries them over.
3. After Google approves and publishes v2.9.28:
   a. Edit `backend/public/personvern.html` — remove the bullet starting
      with "*Frem til og med versjon 2.9.27:*" and the transition note.
      The URL line is no longer collected, so the disclosure can be tight.
   b. Commit + push (Vercel redeploys leksihjelp.no/personvern).
4. Resync lockdown: `cd /Users/geirforbord/Papertek/lockdown && node scripts/sync-leksihjelp.js`
   then commit + `firebase deploy --only hosting` (since v2.9.28 has
   a synced-surface change in `extension/content/spell-check.js`).

**Why this exists:** v2.9.27 was submitted before the URL-removal patch
landed. Personvern temporarily re-discloses URL collection with a "to be
removed in v2.9.28+" note to keep code/policy aligned during the v2.9.27
review window.

**Code commit with the URL-removal:** 0fe8df8.
**Personvern transition commit:** 8d1a428.
