---
gsd_state_version: 1.0
milestone: v2.0
milestone_name: Depth of Coverage — Grammar Governance Beyond Tokens
status: unknown
last_updated: "2026-04-25T01:44:44.356Z"
progress:
  total_phases: 3
  completed_phases: 2
  total_plans: 10
  completed_plans: 8
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-21 after v1.0 milestone)

**Core value:** Norwegian students write foreign languages better — with correct words, correct form, and confidence in pronunciation — without leaving the page they're working on.
**Current focus:** v2.0 Depth of Coverage — Grammar Governance Beyond Tokens. ROADMAP.md complete (Phases 6–15, 42/42 requirements mapped). Ready for `/gsd:plan-phase 6`.

## Current Position

Phase: 8 — DE Case/Agreement Governance
Plan: 1 of 3 complete (08-01 done)
Status: In Progress
Last activity: 2026-04-25 — Completed 08-01-PLAN.md (Shared grammar tables + participleToAux + benchmark)

Progress: [████░░░░░░] 20%

## Performance Metrics

**Velocity:**
- Total plans completed: 7
- Average duration: 21m 22s
- Total execution time: 2h 13m 24s

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| Phase 01 | 3 | 33m 36s | 11m 12s |
| Phase 02 | 3 | 1h 39m 48s | 33m 16s |

**Recent Trend:**
- Last 5 plans: 13m 47s, 15m 25s, 12m 08s, 34m 00s, 1h 20m
- Trend: Plan 02-03 was the heaviest in Phase 2 — cross-repo work in `papertek-vocabulary` (rule library expansion + dedupe script + defect fix), plus a mid-execution seam-bug auto-fix (Rule-1) in the vocab-seam, plus two iterations of `sync-vocab` while Vercel's API redeployed. The actual rule-library design + expansion was quick; most of the time went to post-sync debugging where the seam bug surfaced.

*Updated after each plan completion*

| Plan | Duration | Tasks | Files |
|------|----------|-------|-------|
| Phase 01 P01 | 4m 24s | 2 tasks | 2 files |
| Phase 01 P02 | 13m 47s | 2 tasks | 6 files |
| Phase 01-foundation-vocab-seam-regression-fixture P03 | 15m 25s | 3 tasks | 14 files |
| Phase 02-data-layer-frequency-bigrams-typo-bank P01 | 12m 8s | 2 tasks | 6 files |
| Phase 02-data-layer-frequency-bigrams-typo-bank P02 | 34 min | 1 tasks | 3 files |
| Phase 02 P03 | 1h 20m | 3 tasks | 14 files |
| Phase 02-data-layer-frequency-bigrams-typo-bank P04 | 5 min | 2 tasks | 7 files |
| Phase 02-data-layer-frequency-bigrams-typo-bank P05 | 21 min | 1 task ran + 1 halted at checkpoint (Task 3 intentionally not executed) | 1 audit artifact created, 0 shipped files touched |
| Phase 02.1 P01 | 2 min | 2 tasks | 2 files |
| Phase 02.1 P02 | 2 min | 3 tasks | 9 files |
| Phase 03-rule-architecture-ranking-quality P01 | 3 min | 3 tasks | 3 files |
| Phase 03-rule-architecture-ranking-quality P05 | 4 min | 2 tasks | 5 files |
| Phase 03-rule-architecture-ranking-quality P02 | 5 min | 2 tasks | 9 files |
| Phase 03 P03 | 12 min | 2 tasks | 2 files |
| Phase 03-rule-architecture-ranking-quality P04 | 1h 30m | 3 tasks | 2 files |
| Phase 03.1-close-sc-01-browser-wiring P01 | 1 min | 2 tasks | 3 files |
| Phase 04-false-positive-reduction-nb-nn P01 | 4min | 2 tasks | 4 files |
| Phase 04 P02 | 6m 37s | 3 tasks | 8 files |
| Phase 04-false-positive-reduction-nb-nn P03 | 6 min | 3 tasks | 11 files |
| Phase 05-student-experience-polish P01 | 7 min | 3 tasks tasks | 8 files files |
| Phase 05 P04 | 3 min | 2 tasks | 2 files |
| Phase 05-student-experience-polish P03 | 6 min | 2 tasks | 2 files |
| Phase 05-student-experience-polish P02 | 6m 23s | 3 tasks | 6 files |
| Phase 05-student-experience-polish P05 | 6 min | 2 auto + 1 deferred checkpoint | 4 files |
| Phase 05.1 P02 | 4m 17s | 3 tasks | 9 files |
| Phase 05.1 P03 | 5m | 3 tasks | 4 files |
| Phase 05.1 P04 | 75min | 3 tasks | 7 files |
| Phase 05.1 P01 | 19m52s | 3 tasks | 7 files |
| Phase 05.1 P05 | ~90min (incl. ~15min inline bug-fix loop during human-verify) | 4 tasks (2 auto + 1 checkpoint:human-verify + 1 auto close) | 16 files |
| Phase 06 P02 | 4min | 2 tasks | 7 files |
| Phase 06 P01 | 8min | 2 tasks | 35 files |
| Phase 06 P03 | ~35min | 3 tasks (2 auto + 1 checkpoint) | 18 files |
| Phase 07 P01 | 2m | 2 tasks | 3 files |
| Phase 07 P03 | 16m | 2 tasks | 11 files |
| Phase 07 P02 | 20min | 2 tasks | 11 files |
| Phase 07 P04 | 16min | 2 tasks | 5 files |
| Phase 08-de-case-agreement-governance P01 | 4m 13s | 2 tasks | 7 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Project init: Spell-check stays free forever (landing-page promise)
- Project init: Heuristics only, no ML, no paid APIs
- Project init: Iterative releases — each phase independently shippable
- Phase 07-03: Modal verbs (kann/muss/soll) treated as true finite verb in subordinate clause verb-final checking
- Phase 07-03: Capitalized noun/verb homographs resolved via DE noun capitalization convention
- Phase 07-03: FR verb/noun homographs only treated as noun when preceded by article
- Phase 07-04: Question-initial guard restricted to sentences ending with "?" to avoid suppressing fronted-adverb V2 violations
- Phase 07-04: Complement clause "det er/var" guard uses broad verb detection (isFinite + verbForms + verbInfinitive + MODAL_VERBS) to catch cross-dialect verbs
- Phase 07-04: Added "fraa" to NN subordinator list; preposition-object guard prevents false positives on "Utan dei ville..."
- Project init: NB first, NN second for spell-check; all 6 languages for word-prediction
- Project init: Regression fixture is the quality-gate tool for this milestone
- [Phase 01]: vocab-seam surface: getBigrams() returns null (not empty object) when file missing — matches existing consumer null-handling
- [Phase 01]: vocab-seam default grammar predicate when enabledGrammarFeatures storage key missing: () => true (emit superset; consumers filter further)
- [Phase 01]: typoBank is a Map reference-alias of typoFix (same Map, zero memory cost)
- [Phase 01]: Finding contract locked — core emits `rule_id` (not `type`); DOM adapter shims `f.type = f.rule_id` post-call so legacy UI code works unchanged (consumed by Plan 03 fixture harness)
- [Phase 01]: Grammar-feature filtering is seam-level only — word-prediction consumes pre-filtered wordList as-is, single source of truth
- [Phase 01]: Consumer-local derived state (prefixIndex, tense sets) rebuilt via VOCAB.onReady(cb) re-registration on LANGUAGE_CHANGED / GRAMMAR_FEATURES_CHANGED — avoids stale captures without changing the seam API
- [Phase 01]: Fixture cases are hand-authored ground truth, not snapshots of current tool output (pitfall #4) — a case that fails today because of an imperfect rule stays in the fixture; fixing the rule is what flips the exit code back to 0
- [Phase 01]: Fixture exit-code semantics — non-zero on ANY hard mismatch (missing expected OR unexpected flag); P/R/F1 printed but not thresholded in Phase 1 (thresholds deferred to Phase 4 SC-05 per CONTEXT)
- [Phase 01]: Span convention in fixtures is end-EXCLUSIVE (end = start + word.length) and fixture filenames are ASCII-only (`saerskriving.jsonl`, not `særskriving.jsonl`)
- [Phase 01]: Live Chrome smoke test for Phase 1 deferred by user on 2026-04-18 ("I can't test now, but we shouldn't let that block progress — continue and we make the tests later") — to be picked up at release time or during `/gsd:verify-work`
- [Phase 01]: Three pre-existing data-source issues surfaced during fixture authoring (NN verb-infinitive pollution `lese → lese høyt`; NB noun `brev` tagged `m` instead of `n`; typos-in-validWords bypassing curated branch) — NOT rule bugs, data quality issues in `papertek-vocabulary`; natural fit for Phase 2 DATA-02
- [Phase 02-01]: NB N-gram 2021 digibok unigram CSV is **comma-separated** with schema `first,lang,freq,json` (not tab as the plan assumed). Rows where the first cell is CSV-quoted (starts with `"`) are always punctuation-only — fast-reject them. The 4th column is a quoted per-year JSON blob, parsed on first 3 comma positions only.
- [Phase 02-01]: Zipf floor of 3.0 (plan estimate) filters too aggressively for actual NB N-gram 2021 ↔ validWords overlap (~6K entries). Ship with Zipf floor 0.0 as the default — budget enforcer raises floor only if output exceeds 200 KB gzipped. Actual overlap is 13,132 NB / 11,013 NN entries at 61 KB / 52 KB gzipped (69% / 74% headroom).
- [Phase 02-01]: MIN_ENTRIES fail-loud floors are reality-based corruption guards, not ship-quality gates. ~4K of shipped validWords are multi-word phrases (cannot match unigram corpus); another ~2K are deliberate typos. Natural overlap is ~50% of validWords size. Set floors at 5K / 2K to catch true corruption (empty download, wrong lang) without false-alarming on real distribution.
- [Phase 02-01]: `corpus/` gitignore double-safety pattern — root `.gitignore` uses `corpus/*` + `!corpus/.gitignore` (negation so inner ignore tracks itself); inner `corpus/.gitignore` uses `*` + `!.gitignore` (self-exception). Together they guarantee the 1 GB corpus file stays out of git even if the inner file is ever deleted.
- [Phase 02-02]: NB N-gram 2021 digibok-BIGRAM CSV has DIFFERENT schema from the unigram file — columns are `first,second,lang,freq,json` with word cells DOUBLE-QUOTED (`"Råholt"`) and symbol cells bare (`!`, `$`, `-`). Parser must (a) indexOf-locate first 4 commas to avoid blowing up on the quoted-json fifth column, and (b) strip surrounding `"` from word cells with `unquote()` helper. Missing either step silently yields zero rows kept.
- [Phase 02-02]: 7 GB+ HTTP downloads from nb.no require Range-based resume — the server drops the connection mid-stream roughly every 1 GB. `ensureCorpus()` now does HEAD first to learn Content-Length, then a retry loop with `Range: bytes=N-` requests, a no-progress streak counter (10 consecutive retries that don't advance the on-disk byte count → abort), and 1-30s exponential backoff between attempts. Completed 7.15 GB download in 5 attempts with no user intervention.
- [Phase 02-02]: Bigram weight buckets use concentration ratio `pairFreq / firstTotal` (how much of `first`'s continuations this `next` takes), NOT true Pointwise Mutual Information. Thresholds 0.05 / 0.015 / else → {3, 2, 1}. Simpler to compute in one stream pass, reproducible, and Phase 3 WP-02 ranker is free to retune without a schema change.
- [Phase 02-02]: Pitfall-7 max-merge guarantee — `/tmp/02-02-snapshot/bigrams-{nb,nn}.pre.json` baseline captured BEFORE the script runs; the plan's verify block iterates every (prev, next) triple independently of any internal script assertion. Defence in depth: 314 hand-authored pairs verified preserved across NB+NN with zero downgrades. Script's own `assertPreserved()` is the first gate, external full-sweep verify is the second.
- [Phase 02-02]: `assertPreserved()` 10× growth floor only fires when `preHeads ≤ 100` (hand-authored scale) — re-runs of an already-enriched file would otherwise fail non-idempotently. Pitfall-7 sweep runs unconditionally and is the real correctness check.
- [Phase 02-02]: Downloaders NOT factored into a shared helper between `build-frequencies.js` (Plan 02-01) and `build-bigrams.js` (Plan 02-02). Research Open Question 4 closed in favour of cheap duplication — the error-retry surfaces differ enough that a shared helper would force both scripts into a lowest-common-denominator interface. Re-evaluate when a third corpus downloader lands.
- [Phase 02-02 coordination note]: Plans 02-01 and 02-02 ran in parallel in wave 1 with no declared dependencies; both edited `package.json`. The 02-01 commit landed both npm scripts (`build-frequencies` AND `build-bigrams`) because 02-01 committed slightly later while 02-02's edits were still unstaged. No harm done, but future multi-plan waves should be aware that shared-config edits need coordination or one plan will "absorb" the other's changes.
- [Phase 02-03]: Option B selected at checkpoint — expand the typo-bank rule library rather than ship a narrow defect-only fix. PR #5 in `papertek-vocabulary` had already saturated the existing rule library (topup dry-run: 0 additions). Added six new patterns (firstPairTranspose, allAdjacentTranspose, letterRepeat, consonantDouble, letterDropAnywhere, qwertyNeighborSub) — achieved +62.7% combined growth (NB +35.5%, NN +104.5%).
- [Phase 02-03]: Defect 1 (~214 NN phrase-infinitive entries in sibling repo verbbank.json) DEFERRED to a future plan / sibling-repo PR. Many are legitimate reflexive/phrasal verbs (`anstrenge seg`, `bli med`) — bulk normalization would discard real NN grammar. Triage requires human-in-the-loop classification.
- [Phase 02-03]: Vocab-seam `buildLookupIndexes()` had a latent bug where every wordList entry's `word` was added to `validWords`, including `type="typo"` entries. This silently disabled the curated-typo branch in `spell-check-core.js` (which skips any token present in validWords). The bug was masked in Phase 1 because baseline fixtures used typos absent from the bank; Phase 2 DATA-02's expansion surfaced it. Fix: one-line guard `if (entry.type !== 'typo')` around the `validWords.add(w)` call. Rule-1 auto-fix per deviation policy.
- [Phase 02-03]: SC-2 recall-delta seeded via 6 NB typo fixture cases chosen as position-0/1 transpositions (different first char from fix word) — guaranteed to bypass fuzzy matcher's `first-char-must-match` rule, so ONLY the curated-typo branch can resolve them. Pre-sync FAIL, post-sync PASS — operational signal rather than hand-waved F1=1.000.
- [Phase 02-03]: NN `finst_verb.typos` had `fint` and `fints` registered as typos, but both are valid neuter forms of the common adjective `fin`. The same-lang dedupe script didn't catch them because NN lexicon LACKS `fin_adj` entirely — cross-lang / missing-entry collision, not same-lang overlap. Removed `fint`/`fints` from typos; fixture `nn-clean-003` text swapped from `fint` to `stort` to avoid the underlying data gap. Gap tracked as a new STATE.md blocker.
- [Phase 02-03]: Vercel API redeploy lag is real — first `npm run sync-vocab` after sibling-repo push pulled pre-commit data. Waiting ~60s and re-syncing fixes it. Documented in Task 3 Rollback Protocol for future cross-repo plan executors.
- [Phase 02-04]: Outcome B accepted over silent-bypass — the 114 KB overage after minification could be hidden by bumping `CEILING_BYTES` in the script, but that violates the plan's explicit fail-loud policy AND the publicly-stated 10 MB promise on the landing page. Gate ships and correctly reports FAIL; Phase 2.1 does the product work.
- [Phase 02-04]: Staging-dir pattern (`.package-staging/` gitignored copy of `extension/`) chosen over in-tree minify-then-revert — lower risk of transient source-tree damage if the process is killed mid-run, zero risk of a minified JSON being committed by accident.
- [Phase 02-04]: JSON minification alone saves 13.25 MiB of whitespace pre-zip but only 114 KB short of closing the cap — the structural size driver is `data/` (20.84 MiB uncompressed) + `audio/` (7.67 MiB uncompressed), so Phase 2.1 remediation must target one of those directories (audio stripping is cheapest by impact/effort ratio).
- [Phase 02-04]: `unzip -l` date format is platform-dependent — macOS emits `MM-DD-YYYY`, Linux distributions often emit `YYYY-MM-DD`. Release-tooling regex must accept both (`\d{2,4}[-/]\d{2}[-/]\d{2,4}`) to work on dev + CI. Generic principle: cross-platform tool parsers should anchor on structure (column widths, separators) rather than specific numeric layouts.
- [Phase 02-04]: Release-gate pattern locked in — scripts named `scripts/check-*.js` run with zero args, produce a diagnostic + PASS/FAIL line, exit 0/1 against a hard numeric threshold, and register in CLAUDE.md's Release Workflow as must-exit-0 steps. Both `check-fixtures` and `check-bundle-size` follow this pattern; future release gates (e.g. a bundle-reproducibility check) should adopt it.
- [Phase 02-05]: Audit verdict = BLOCKED — `extension/data/en.json` is NOT dead weight. English is a first-class bundled vocabulary language with 4 independent runtime paths (popup.js first-run fallback + language-delete fallback + settings-UI picker; content-script init defaults across vocab-seam.js, word-prediction.js, floating-widget.js) plus a sync-vocab.js regeneration path at line 414. Silent deletion would cause user-visible 404s on fresh installs, cleared storage, language deletes, and explicit settings picks. Per 02-CONTEXT.md's hard requirement, the plan halted at the Task 2 checkpoint; Task 3 (deletion) was intentionally NOT executed. SC-4 remains OPEN.
- [Phase 02-05]: linkedTo.en entries in extension/data/{de,es,fr}.json (10,020 total) are DORMANT DATA — the runtime consumes linkedTo.nb and linkedTo.nn only; no code path dereferences linkedTo.en. These entries impose no runtime dependency on en.json existing and can remain untouched in any follow-up English-removal refactor. Preserving this finding so the follow-up plan doesn't re-audit.
- [Phase 02-05]: BLOCKED-by-design outcome pattern — a plan whose primary deliverable is authoritative audit evidence can terminate at a blocking checkpoint without being a "plan failure." The plan produced what it was supposed to produce (evidence + decision gate); the gate's `blocked` resolution halts the plan cleanly and hands the product decision back to the user. Count it as completed-end-to-end on its own terms; track SC-4 separately as the unmet roadmap criterion.
- [Phase 02-05]: Audit scope-distinction discipline locked in — future audits of `extension/data/{lang}.json` must explicitly triage every `'{lang}'` hit into {data-vocab, i18n-ui, html-lang, comment/doc, preposition/article-word-collision} classifications before assigning runtime-reference status. E.g., `'en'` in extension code overwhelmingly hits the Norwegian article word `en`, Spanish/French preposition `en`, or UI i18n `data-ui-lang` attributes — not the vocab language code. Without this triage, an audit can't distinguish false positives from real runtime refs.
- [Phase 02.1]: [Phase 02.1-01]: SC-4 closed by ceiling bump — CEILING_BYTES raised 10 * 1024 * 1024 → 20 * 1024 * 1024 in scripts/check-bundle-size.js + scripts/check-bundle-size.test.js (same value declared independently in each file, both lockstep to 20,971,520 bytes = 20 MiB). Current post-minification zip 10,599,772 bytes (10.11 MiB) passes with 9.89 MiB headroom. Gate ships exit 0. Chrome Web Store ceiling is 2 GB — the 20 MiB number is an internal engineering guard against accidental bundle growth (pretty-printed JSON checked in by mistake, large asset added silently), not a publicly-stated promise. — Product decision locked in 02.1-CONTEXT.md: raise the cap rather than pursue English-removal / audio-strip / conjugation-trim remediations. Evidence from 02-05-AUDIT.md (four runtime paths for en.json) + the offline-first-play pledge for audio/de informed the choice. 20 MiB (not 12 MiB) picked for headroom across Phase 3-5 without thrashing the decision mid-milestone.
- [Phase 02.1]: [Phase 02.1-01]: Release-gate constant + test-mirror lockstep pattern — when a release gate uses a hard numeric threshold, both the gate and its behavior-test mirror must share the same value, AND the test's human-readable name must describe the same number the assertion uses. Drifting either silently invalidates the test's intent. Applied in this plan: CEILING_BYTES bumped in BOTH scripts/check-bundle-size.js (line 33) and scripts/check-bundle-size.test.js (line 27); Test 4/5 name updated 'vs 10 MiB cap' → 'vs 20 MiB cap' so behavioral contract matches gate reality. — RESEARCH.md Pitfall 2: if the test file's CEILING_BYTES drifts from the script's, the test still passes against the current 10.11 MiB zip (under both caps) so the test appears green but its intent is silently broken. Codifying the lockstep rule + the test-name contract prevents this failure mode for any future release gate.
- [Phase 02.1]: [Phase 02.1-02]: Live-docs sweep completed — 9 files rewritten to describe the 20 MiB internal engineering ceiling; no "publicly-stated promise" framing remains in CLAUDE.md, PROJECT.md, ROADMAP.md, REQUIREMENTS.md, or any .planning/research/*.md. STATE.md live SC-4 blocker REPLACED with "RESOLVED 2026-04-19 by Phase 02.1" marker; historical decision-log rows (## Accumulated Context > Decisions, lines ~100-112) byte-identical to pre-edit; historical archive .planning/phases/02-data-layer-frequency-bigrams-typo-bank/ untouched (git diff --stat empty). Atomic 2-plan wave: 02.1-01 owned the machine-readable constant (CEILING_BYTES in scripts/check-bundle-size.js + test), 02.1-02 owned the human-readable prose in 9 live docs.
- [Phase 02.1]: [Phase 02.1-02]: Scope-exclusion discipline locked in — when correcting framing across many files, the plan frontmatter's scope_exclusions section MUST list files/directories that remain faithful to history (historical archive, historical decision rows). Applied here: STATE.md lines ~100-112 are byte-identical pre- and post-edit even though they contain the old "publicly-stated 10 MB promise" phrase (they document what was thought during Plan 02-04, not current state). Without this discipline, a live-docs sweep retrofits history. Pattern reusable: future framing corrections should split decision-log entries (historical, append-only) from Blockers/Concerns entries (live, replaceable).
- [Phase 02.1]: [Phase 02.1-02]: ROADMAP.md meta-description quoting pattern — the Phase 02.1 entry in ROADMAP.md (lines 20 + 61) necessarily quotes the obsolete "publicly-stated promise" phrase to describe what the phase fixed. This is meta-commentary on the phase's own deliverable, not live ceiling framing. Kept intact; verification grep treats these hits as allowed. General rule: when a phase's job is to remove a phrase, the phase's own ROADMAP narrative remains the one place where the phrase legitimately appears as a description of past state.
- [Phase 03]: [Phase 03-01]: Freq sidecar wired through vocab seam end-to-end via additive Promise.all parallel fetch (bigrams + freq) — VOCAB.getFrequency('bedre') NB returns 5.58 (was null); 'og' NN returns 7.59; 'hallo' EN returns null (no sidecar). State.freq Map populated 13,132 NB / 11,013 NN entries. Zero schema changes, zero new npm deps, no public API change (getFrequency null-return contract preserved). 138/138 fixtures still pass. SC-01 (fuzzy Zipf tiebreaker) and WP-01 (word-prediction frequency signal) now unblocked for Plans 03-02 and 03-04.
- [Phase 03]: [Phase 03-01]: Fail-loud guard in scripts/check-fixtures.js throws Error if NB or NN freq Map is empty post-buildIndexes — defends against silent freq-data loss for languages we contractually shipped a sidecar for in Phase 2 (Pitfall 2 from 03-RESEARCH.md). Seam itself stays silent on missing sidecar (correct production behavior for de/es/fr/en); the runner is the right place to assert the NB/NN contract.
- [Phase 03]: [Phase 03-05]: WP-02 closed for EN — extension/data/bigrams-en.json hand-authored with 51 head-words / 202 (prev,next) pairs covering A1/A2 learner-core categories (greetings, pronoun+be, articles, question words, courtesy responses, common verb frames). Schema mirrors bigrams-{de,es,fr,nb,nn}.json including the `_metadata` header. Weights 1-3 (3=rock-solid like "thank you"; 2=high-value default; 1=helpful-but-not-essential). Word-prediction.js:1007 multiplies weight × 40 → 40/80/120 score boost for matched candidates. 86% of next-words exist in en.json wordList; the rest (is/am/can/was/has/were etc.) are short copulas not in en.json's content-vocab focus — bigram signal still ships, applies whenever a matching candidate exists. EN now has bigrams parity with all other languages.
- [Phase 03]: [Phase 03-05]: SC-06 enforced via scripts/check-network-silence.js + paired self-test scripts/check-network-silence.test.js. Gate scans extension/content/spell-check-core.js, spell-check.js, word-prediction.js, and (when present) extension/content/spell-rules/*.js for `fetch(`, `XMLHttpRequest`, `sendBeacon`, and `http(s)://` URL literals. Whitelists `chrome.runtime.getURL` + `chrome-extension://` (local-resource access is fine) and JSDoc/// comment lines. Self-test plants `fetch('https://example.com/api')` in a scratch rule file, asserts gate fires (exit 1), removes scratch, asserts gate passes (exit 0) — guards against regex rot silently making the gate permissive. Pattern: zero deps, exit 0/1, PASS/FAIL diagnostic line, registered in CLAUDE.md Release Workflow as must-exit-0 step 2 (between check-fixtures and check-bundle-size). The "free + offline forever" promise is now CI-enforced on the extension side.
- [Phase 03]: [Phase 03-05]: Self-tested release-gate pattern locked in — every release gate that uses regex/heuristic detection should have a paired .test.js that proves the gate actually fires on a planted positive. Mirrors check-bundle-size.test.js. Without the self-test, a regex typo could silently make the gate permissive (passes on broken code, looks green in CI). Codifying the requirement: future gates added to scripts/check-*.js MUST ship with scripts/check-*.test.js.
- [Phase 03]: [Phase 03-05]: Optional-target collectFiles pattern in check-network-silence.js — gate scans both fixed SCAN_TARGETS (must exist, fail if missing) and SCAN_DIRS (walked recursively if present, silently skipped if absent). Lets the gate ship before all directories it scans exist (extension/content/spell-rules/ doesn't exist yet — Plans 03-02 / 03-03 / 03-04 will create it later in Wave 2). When those plans land, the gate auto-discovers their rule files without re-edit.
- [Phase 03]: [Phase 03-05]: Bigrams-file `_metadata` blindspot in plan verify command — Plan 03-05's quoted Node verify iterated `Object.keys(b)` without filtering, then asserted every value is `{next: int1-3}`. The `_metadata` key in all bigrams files holds `{language, description}` strings → verify exits 1. Affects ALL existing bigrams files identically, not just our new one. Resolution: skip keys starting with `_` in verify; keep `_metadata` in artifact for peer-file consistency (the `<interfaces>` block explicitly says "must match exactly. Peek at bigrams-de.json to mirror"). Pattern reusable: any future "schema check" against the bigrams family must filter `_metadata` first.
- [Phase 03-rule-architecture-ranking-quality]: [Phase 03-02]: INFRA-03 plugin registry shipped — five inline rules in spell-check-core.js extracted into self-registering IIFE files under extension/content/spell-rules/ (nb-gender, nb-modal-verb, nb-sarskriving, nb-typo-curated, nb-typo-fuzzy). Each pushes {id, languages, priority, check, explain} onto self.__lexiSpellRules; runner in core iterates filtered + priority-sorted. Constants ARTICLE_GENUS / GENUS_ARTICLE / MODAL_VERBS / SARSKRIVING_BLOCKLIST removed from core (zero hits). Shared helpers (isLikelyProperNoun, scoreCandidate, findFuzzyNeighbor, sharedPrefixLen, sharedSuffixLen, isAdjacentTransposition) exposed on self.__lexiSpellCore so rule files can reuse without redeclaring. 138/138 fixtures still pass — zero semantic regression.
- [Phase 03-rule-architecture-ranking-quality]: [Phase 03-02]: Dual-load guard pattern locked in — every rule file's IIFE preamble starts with 'const host = typeof self !== undefined ? self : globalThis; host.__lexiSpellRules = host.__lexiSpellRules || [];'. Required because the same file loads in MV3 content-script context (self === global) AND Node fixture harness (self undefined → falls back to globalThis). Forgetting silently breaks Node-side tests with vacuous F1=1.000 results. Belt-and-braces: spell-check-core.js footer ALSO does host.__lexiSpellRules = host.__lexiSpellRules || [] so rule files loading either before or after core both work.
- [Phase 03-rule-architecture-ranking-quality]: [Phase 03-02]: Empty-registry runtime guard in scripts/check-fixtures.js throws BEFORE any case runs if self.__lexiSpellRules is empty post-load — defends against the worst silent failure mode (rules don't register → runner reports vacuous green tests). Combined with the optional-target collectFiles auto-discovery in check-network-silence.js (Plan 03-05), adding a new rule under extension/content/spell-rules/ requires zero edits to either gate. Reusable for any future plugin-registry pattern with a paired test runner.
- [Phase 03-rule-architecture-ranking-quality]: [Phase 03-02]: Cursor-position skip preserved in each extracted rule file (Rule-2 auto-fix). The pre-refactor inline check() body had a single skip near the top of the per-token loop that protected ALL five rules from flagging incomplete words mid-typing. Plan template only showed the skip in the gender rule example; without re-applying it to the other four extracted rules, the live editor would have started flagging mid-thought tokens — a regression the fixture suite cannot detect (fixtures have no cursorPos). Each rule now performs the same skip inside its own per-token loop. Pattern: when extracting shared loop-level concerns into per-rule code, audit cursor/cancellation/skip predicates explicitly.
- [Phase 03]: [Plan 03-03] SC-01 Zipf tiebreaker landed inside extension/content/spell-rules/nb-typo-fuzzy.js — local scoreCandidate adds 'vocab.freq.get(cand) * ZIPF_MULT' (multiplier=10, max boost ~70 pts << 100-pt distance penalty). Two new fixture cases (nb-typo-zipf-001 hagde→hadde, nb-typo-zipf-002 hatde→hadde) load-bearing exercise the new path. 140/140 fixtures pass. Zero edits to spell-check-core.js — INFRA-03 contract held. Plan first proposed multiplier=15 but that flipped nb-typo-likr-001 (likr→liker) to 'like' via a 0.77 Zipf gap × 15 = 11.55 pts overshoot of the 10-pt length-penalty gap; lowered to 10 inline. Pitfall 4 surfaced exactly as RESEARCH.md predicted, just on a different anchor case.
- [Phase 03]: [Plan 03-03] Defence-in-depth fixture verification pattern locked in: when a new fixture targets a new code path, prove load-bearing by temporarily zeroing the new term and confirming the case fails with the EXACT pre-change diagnostic. Implemented inline as Node child_process patch+restore. Mirrors check-bundle-size.test.js / check-network-silence.test.js philosophy (a paired self-test guards against the gate going silently permissive). Reusable for any future fixture-targets-new-heuristic plan.
- [Phase 03]: [Plan 03-03] scripts/find-zipf-tiebreak-candidate.js — dev-only finder pattern. Iterates validWords producing single-character mutations at internal positions (skip [0] for first-char-match constraint, skip last for inflection noise), finds d=1 neighbors, emits TRIPLE lines where today ties or picks the lower-Zipf candidate. Surfaced 40,012 raw triples / 4,612 clean ones (both Zipf >= 3.0, gap >= 1.5) on current NB corpus — plenty of headroom for Phase 4 SC-01 fixture growth. NOT shipped (not in npm scripts, not in CLAUDE.md Release Workflow); lives in scripts/ purely for reproducibility when retuning the rule.
- [Phase 03-rule-architecture-ranking-quality]: [Plan 03-04] WP-01 + WP-03 + WP-04 shipped: word-prediction ranker now reads a universal frequency signal (Zipf sidecar for NB/NN, seam-normalized log10(freq+1) for DE/ES/FR/EN), with deterministic tiebreakers (score → eff-freq → length-match → suffix-match → alpha) and low-frequency demotion (-80 when 0 < zipf < 1.5). entry.zipf attached to every wordList push inside buildWordList (all 6 languages). freqSignal multiplier 20 → top score ~140, scale-comparable to the +120 bigram cap. applyBoosts refactored to workingEntry / numbered-signal pattern; caller sites unchanged. Pitfall 7 handled at normalization time, not at ranking time — ranker reads entry.zipf only, never raw entry.frequency.
- [Phase 03-rule-architecture-ranking-quality]: [Plan 03-04] Latent Phase 1 language-switch bug repaired: switchPredictionLang (inline flag-switcher click handler) was calling loadGrammarFeatures(lang) + loadWordList(lang), both deleted during Plan 01-02 seam cutover. loadGrammarFeatures threw ReferenceError on every click, aborting the handler before the LANGUAGE_CHANGED broadcast could fire → seam stayed pointed at the previously-loaded language → ranker served DE wordList when user was in NB/NN/EN. Flagged at the time in 01-VERIFICATION.md line 121 as a known issue (narrow code path; popup-driven switch bypasses this helper so it kept working) but never fixed because the inline switcher was rarely exercised. Fix: trim switchPredictionLang to persist + broadcast only; the service worker re-broadcasts LANGUAGE_CHANGED to all tabs including the sender, re-entering the existing onMessage handlers which do the actual reload work. Also removed the post-switch schedulePrediction call (racing the in-flight seam reload).
- [Phase 03-rule-architecture-ranking-quality]: [Plan 03-04] Effective-frequency abstraction locked in: getEffectiveFreq(entry) checks VOCAB.getFrequency(entry.word) first (sidecar, authoritative for NB/NN), falls back to entry.zipf (seam-normalized), returns 0 if neither. Every frequency-sensitive code path in the ranker reads through this helper, never raw entry.frequency — Pitfall 7 guard at the API boundary rather than per-call-site. Same pattern reusable for any future ranker signal that needs scale-uniform per-entry frequency.
- [Phase 03-rule-architecture-ranking-quality]: [Plan 03-04] Deterministic ranker tiebreaker chain: score → effective-freq → length-match-to-query → longer-shared-suffix → localeCompare (alphabetical). Last-rung alphabetical gives dev-readable + reproducible output when earlier signals tie, which matters for debugging tie-sensitive rankings and for snapshot-style tests. Pattern reusable for any future multi-signal ranker where ties are expected.
- [Phase 03-rule-architecture-ranking-quality]: [Plan 03-04] lowFreqDemotion floor Zipf 1.5 and -80 demotion chosen so learner-core vocab never triggers it (Zipf 1.5 ≈ occurs <30 times per million). Entries with zipf === 0 are LEFT ALONE — 'no sidecar data' ≠ 'rare word' (proper nouns + niche learner-core entries often lack corpus presence without being rare). Demoting them would punish legitimate entries. Reusable policy: corpus-based rarity penalties should only fire when corpus DATA exists and agrees, never on missing-data.
- [Phase 03-rule-architecture-ranking-quality]: [Plan 03-04] English data-shape caveat documented (not fixed in this plan): extension/data/en.json is structured as "Norwegian headwords with English translations" rather than "English headwords with Norwegian translations". Typing 'hel' in EN surfaces 'heltid', 'helg', 'hel', 'helt' — Norwegian headwords. Ranker is applying its contract correctly on the en.json wordList as-given; the fix is a data-source restructure in papertek-vocabulary, not a ranker change. User accepted + saved memory entry; explicitly out-of-scope for Plan 03-04. Contract verification for WP-01/03/04 comfortably met on 5/6 languages (NB/NN/DE/ES/FR); the plan's must_have was "3+ languages".
- [Phase 03-rule-architecture-ranking-quality]: [Plan 03-04] Out-of-plan orchestrator fix (commit 7150ad0): MV3 service workers resolve importScripts paths relative to the extension root, not the script's own location. Pre-existing latent bug from commit b3857af (leading '../' in a content-loader importScripts path) rejected by Chrome 130+, killing the service worker on startup. Surfaced during Plan 03-04 verification when the orchestrator investigated console output. Fixed separately by the orchestrator as 'fix: use absolute path for service-worker importScripts' — not attributable to Plan 03-04 task work but load-bearing to verification.
- [Phase 03.1-close-sc-01-browser-wiring]: [Phase 03.1-01] SC-01 browser-wiring closed end-to-end — VOCAB.getFreq() getter added to extension/content/vocab-seam.js (empty-Map default matching getValidWords/getNounGenus precedent); freq: VOCAB.getFreq() added as 6th field of runCheck vocab literal in extension/content/spell-check.js. Plan 03-03's Zipf tiebreaker in nb-typo-fuzzy.js:scoreCandidate is now active in the browser runtime, not just in the fixture-runner path.
- [Phase 03.1-close-sc-01-browser-wiring]: [Phase 03.1-01] Adapter-contract regression guard pattern: when a fixture runner and a browser-runtime adapter diverge in how they assemble the shared core's input, a source-level regex assertion at the top of the runner's main() catches adapter re-regressions that the fixture suite itself cannot detect (because the fixture path bypasses the adapter entirely). Applied here: scripts/check-fixtures.js asserts 'freq:\\s*VOCAB\\.getFreq\\(\\)' present in extension/content/spell-check.js, fails loud with explicit 'SC-01 browser-wiring regression' message on miss. Defence-in-depth test proved load-bearing (fires on regression, restores adapter byte-identical). Reusable for any future fixture-runner / browser-adapter split.
- [Phase 03.1-close-sc-01-browser-wiring]: [Phase 03.1-01] Empty-container-default vs null-default getter style codified: pre-built lookup indexes return empty Map/Set (getValidWords/getNounGenus/getVerbInfinitive/getTypoFix/getCompoundNouns/new getFreq); optional side-channels return null (getBigrams). Rule decides by the consumer's guard semantics — if the guard uses truthiness (if (vocab && vocab.freq) style), empty-container keeps the consumer path straight through with no special-casing (undefined lookups handled by the inner typeof check). If the consumer explicitly tests for null, use null-default. Pitfall 1 from 03.1-RESEARCH.md.
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-01] sisterValidWords seam rail shipped: buildIndexes accepts sisterRaw → derives Set (Pitfall-1 typo-filtered, lowercased); NB session=14,866 NN lemmas, NN session=14,773 NB lemmas, de/es/fr/en=empty Set. Zero typo leakage (0/14,754 pure-typo NN rows). Same-language-parity collision policy preserved: words that are simultaneously typos + valid lemmas stay in sisterValidWords via the valid-lemma path (103 tokens NN). Empty-Set default on getSisterValidWords() matches getValidWords/getNounGenus precedent. Zero runtime effect: no rule reads the new Set yet (Plans 04-02 + 04-03 will) — 140/140 fixtures still green.
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-01] SC-03 adapter-contract regression guard shipped in scripts/check-fixtures.js — paired below the SC-01 freq guard, shares adapterSrc read, throws 'SC-03 browser-wiring regression' with pointer to .planning/phases/04-false-positive-reduction-nb-nn/ if spell-check.js:runCheck ever drops the sisterValidWords field. Defence-in-depth probe verified load-bearing: removed field → exit 1 + explicit SC-03 message → adapter restored byte-identical (wc -c 20,684/20,684, git diff empty).
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-01] Paired adapter-contract guards pattern locked in: each SC requirement that depends on a specific adapter-literal field (SC-01 freq, now SC-03 sisterValidWords) gets its own named regex guard in scripts/check-fixtures.js main(), all sharing a single adapterSrc read. Fail-loud error messages carry the SC identifier + pointer to originating phase directory. Future SC requirements with the same adapter-vs-fixture-runner split should add a third guard below these two.
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-02] MIN_TOKENS=8 on codeswitch rule guards against Pitfall-8 over-suppression on short mixed greetings; fixture-driven retuning belongs to Plan 04-03 if real passages surface false-negatives
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-02] Density-window isUnknown excludes isLikelyProperNoun tokens (Pitfall 2) — side-effect: German snippets with mid-sentence-cap tokens like 'Ich' don't count toward density, protecting Norwegian-name-rich sentences from over-suppression; single-cap tokens handled by propernoun-guard layer
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-02] LOAN_WORDS authoring audit: 80 candidates probed against NB+NN validWords; 13 conflicts rejected (stream, gamer, online, offline, pasta, pizza, hashtag, emoji, selfie, post, skill, meme, like); 70 final entries shipped zero-conflict. Audit script reproducible from file header comment.
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-03] SC-05 saerskriving thresholds locked at P>=0.92, R>=0.95 for both NB and NN (option-a at Plan 04-03 checkpoint). Derived from Pitfall-4 `observed - 0.05` on the pre-fix corpus (NB observed P=0.974, NN P=0.968). NN observed P was the binding number; both languages rounded to the same 0.92 floor. Paper floor from 04-RESEARCH was P>=0.90, R>=0.60 — locked values comfortably exceed it. After Task 3's `stor` blocklist fix, observed P/R moved to 1.000/1.000 both langs — gate clears with 0.08 P-margin headroom.
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-03] THRESHOLDS key-vs-rule_id distinction codified: the fixture runner buckets results by `path.basename(file, '.jsonl')`, so THRESHOLDS table key uses filename basename (`saerskriving`, ASCII-safe per fixture-naming convention) NOT the finding-side rule_id (`sarskriving`, emitted by spell-check-core.check()). A mismatch silently no-ops the gate. Documented both in the THRESHOLDS comment block in scripts/check-fixtures.js AND in fixtures/README.md so future editors don't repeat the trap.
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-03] SARSKRIVING_BLOCKLIST two-class structure locked in: class 1 (function words: prepositions, articles, pronouns, conjunctions) — grammar should never glue these to the next noun. Class 2 (adjective collisions: currently only `stor`) — common adjectives whose concatenation with a following noun ALSO happens to exist in compoundNouns (`stor+by=storby` is a real compound, but "stor by" is an adjective phrase). Comment block documents the two-class pattern so future adjective FPs have an obvious home. Probe-verified `stor` is the only adjective in the expanded A1-level acceptance fixture corpus that currently collides.
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-03] Defence-in-depth probe validated SC-05 threshold gate: temporarily mutated THRESHOLDS to 0.99/0.99 PRE-fix, ran check-fixtures, observed `[nb/saerskriving] THRESHOLD FAIL: P=0.974 < 0.99` + `[nn/saerskriving] THRESHOLD FAIL: P=0.968 < 0.99` diagnostics + exit 1. Restored to locked 0.92/0.95 byte-identical. Matches Phase 03.1 + Plan 04-01 adapter-contract probe pattern.
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-03] Honest-ground-truth policy stayed honest: every new acceptance case in the Plan 04-03 expansion was hand-authored against plan ground-truth design (SC-02 loan-word cases from Plan 04-02's LOAN_WORDS; SC-03 dialect-tolerance from validWords semantics; SC-04 density from >=3-non-Norwegian-contiguous-tokens; SC-05 positives compoundNoun-probe-verified). The single case that failed the rule at Task 1 commit time (`stor by` FP) was left as the hard-failure signal per Phase 1 policy; Task 3 fixed the rule inline rather than back-fitting the expected to tool output.
- [Phase 04-false-positive-reduction-nb-nn]: [Phase 04-03] Pre-plan Rule-1 auto-fixes (prior session): 383552c closed SC-03 gap in nb-typo-curated.js by adding the sister-validWords early-exit that nb-typo-fuzzy.js already had from Plan 04-02 — Task 1 fixture authoring surfaced that NN-valid words were leaking through the curated-typo branch in NB documents (plan 04-02 had covered the fuzzy side of the rule but not curated). 8ea74c5 fixed the check-fixtures runner to load sister-dialect raw vocab and pass it into buildIndexes — without this, sisterValidWords was empty Set in the Node runner and all SC-03 fixture cases would either pass via other means OR fail loudly with false-positives; the browser adapter was correctly loading sisterRaw but the fixture path was bypassing it entirely.
- [Phase 05]: Phase 5 contract-gate-before-upgrade pattern — Plan 05-01 lands escapeHtml + check-explain-contract gate + self-test + i18n seeds BEFORE any rule-file edit. Wave-2 plans (05-02/03/04/05) then land concurrently with commit-time contract enforcement. Gate today exits 1 (rules still string explain); Plan 05-02 flips to 0.
- [Phase 05]: Dual-scenario self-test pattern — check-explain-contract.test.js plants broken scratch THEN well-formed scratch; both assertions required. Stronger than check-network-silence.test.js's plant-broken-only because the explain-contract gate CANNOT pass baseline today (5 rules still string explain pre-Plan-05-02). Source-injection via node -e isolates scratch-file validation without touching the gate file.
- [Phase 05]: [Plan 05-04] Storage-subscribe over runtime-message pattern — alternates toggle writes chrome.storage.local only; consumer uses chrome.storage.onChanged. Fires in all contexts including originating tab, no message-type string contract to maintain. Precedent split from setting-prediction (which ships sendMessage).
- [Phase 05]: [Plan 05-04] Default OFF preserves today's low-noise popover — first-time + never-touch-settings users see single-suggestion layout; multi-suggest is opt-in. Power-user power, dyslexia-persona safety. Opposite of prediction's default-true.
- [Phase 05]: [Plan 05-04] Concurrent Wave-2 execution discipline — stash-and-verify-baseline pattern when check-fixtures fails mid-run: git stash push -m 'plan-parallel-wave-stash' <unrelated-files>, rerun gate, restore. Proves scope-boundary: the failure is NOT caused by this plan. Dropped stash when a sibling plan landed a commit that superseded stashed content.
- [Phase 05-student-experience-polish]: [Phase 05]: [Plan 05-02] All 5 popover-surfacing rules now export explain: (finding) => ({nb, nn}) callable with XSS-safe <em>-wrapped student-word templates (Plan 05-01's escapeHtml on __lexiSpellCore consumed per rule). Voice split: 2 assertive (typo-curated + modal-verb) / 3 hedged (typo-fuzzy + gender + sarskriving). Copy adopted verbatim from the plan's interfaces block — no authoring-time wording adjustments; all 5 rows fit the ≤15-word + du-voice + no-jargon + hedging-tier criteria. sarskriving has a defensive fallback path for missing finding.original/fix — returns bare-concept strings without <em>, belt-and-braces against future probe-driven partial-finding construction.
- [Phase 05-student-experience-polish]: [Phase 05]: [Plan 05-02] Every emitted finding across all 5 popover-surfacing rules now carries priority: rule.priority as an object key at emit site (rather than a Plan-03-side registry lookup). Pitfall-1 curated-vs-fuzzy disambiguation is now renderer-friendly: shared rule_id='typo' findings split cleanly by priority (40 = curated/assertive copy, 50 = fuzzy/hedged copy). Closure captures the const-rule-object literal cleanly.
- [Phase 05-student-experience-polish]: [Phase 05]: [Plan 05-02] nb-typo-fuzzy.js findFuzzyNeighbor → findFuzzyNeighbors (plural) refactor — single-best tracking replaced with scored-array sort-desc + slice(0, 8). Filter gates (len-diff ≤ k, first-char match, edit-distance bound) and scoreCandidate formula byte-preserved. finding.suggestions: string[] emitted case-matched via matchCase; finding.fix === suggestions[0] holds for every existing fixture case (confirmed via 280/280 post-refactor + ZIPF_MULT mutation probe restoring byte-identical). Top-K cap 8 matches UX-02 'Vis flere alternativer' reveal max from Plan 05-04.
- [Phase 05-student-experience-polish]: [Phase 05]: [Plan 05-02] check-explain-contract gate flipped exit 1 → exit 0 this plan — 5/5 popover-surfacing rules satisfy the callable {nb, nn} contract. Acceptance signal for the Phase 5 Plan 05-01 foundation landed. Suppression rules (nb-codeswitch, nb-propernoun-guard) stay on string explain per plan scope — they never surface to the popover. git diff --stat on both files empty.
- [Phase 05-student-experience-polish]: [Phase 05]: [Plan 05-02] Defence-in-depth probe pattern extended: Zipf-tiebreaker mutation probe (ZIPF_MULT=10→0→10 byte-identical) confirmed that top-K refactor didn't silently bypass the Zipf code path. With ZIPF_MULT=0, nb/typo cases hagde→hagle and hatde→hate (expected hadde) both fail per check-fixtures exit 1 at P=0.900; restore to ZIPF_MULT=10 byte-identical returned 280/280. Mirrors Plan 03-03's load-bearing-test pattern + Plan 04-03's THRESHOLDS-mutation probe.
- [Phase 05-student-experience-polish]: [Phase 05]: [Plan 05-02] Top-K scored-sort-slice refactor-with-invariant pattern locked in: when expanding single-best helper to multi-suggest, invariant 'old winner === new[0]' is auto-guaranteed by sort-desc + slice(0, K). Add/change bookkeeping only; never change filter/score functions. Mutate-and-restore probe the downstream dependency to confirm the refactor didn't silently bypass a tiebreaker code path.
- [Phase 05-student-experience-polish]: [Phase 05]: [Plan 05-03] Thin-wrapper + render-helper split pattern locked in — showDropdown became a 6-line wrapper that (captures suggestions, resets expanded, delegates render, positions, makes visible). renderDropdownBody owns innerHTML assembly + reads module-scoped expanded flag. attachDropdownHandlers owns event wiring (language-switcher + pause button + item-click + new Vis-flere reveal). Reveal click path calls renderDropdownBody DIRECTLY so the expanded flag persists; if reveal click re-entered showDropdown, expanded would reset to false and the click would have no visible effect. Reusable for any UI where mutations need in-place re-render without resetting session state.
- [Phase 05-student-experience-polish]: [Phase 05]: [Plan 05-03] Session-reset pattern for Pitfall 5 — per-session state flags (here: expanded) MUST reset at the top of the session-entry function (here: showDropdown), BEFORE any render work reads them. Without this, typing 'häl' → expanded=true → typing 'hält' (new session, should start collapsed) would render with expanded=true leaked from the previous session. Applied on line 3 of showDropdown: `expanded = false;` BEFORE `renderDropdownBody(el)`.
- [Phase 05-student-experience-polish]: [Phase 05]: [Plan 05-03] Cap-invariant pattern for Pitfall 6 — irreversible operations (splice, truncate) must use the MAX-reveal cap (VISIBLE_EXPANDED=8), not the visible-default cap (VISIBLE_DEFAULT=3). If suggestions.splice used the default cap, 1-4 compound-type candidates prepended via unshift would knock regular top-3 ranker candidates OUT OF THE ARRAY before the view-layer slice could protect them. Raised splice(5) → splice(8) to match VISIBLE_EXPANDED. Reusable for any future multi-phase filtering where the UI has a visible cap + an expanded cap.
- [Phase 05-student-experience-polish]: [Phase 05]: [Plan 05-03] ArrowDown auto-reveal keyboard pattern — detect `selectedIndex === items.length - 1 && !expanded && lastSuggestions.length > items.length`, flip expanded, re-render, advance `selectedIndex = Math.min(items.length, newItems.length - 1)` onto the first newly-revealed row (index 3 when VISIBLE_DEFAULT=3). Zero extra keystrokes for keyboard users. Math.min clamp is defensive — if renderDropdownBody yields fewer items than expected the selection won't go out of bounds. Reusable for any progressive-disclosure UI with keyboard navigation.
- [Phase 05-student-experience-polish]: [Phase 05]: [Plan 05-03] Edit-tool silent-revert wrinkle during Wave-2 parallel execution — first Edit-tool pass across 5 edits on word-prediction.js reported success on all 5 but a subsequent Grep/Read confirmed file was at HEAD-state. Suspected race: sibling Wave-2 plan's working-tree churn invalidated the Edit-tool's stale cache. Re-ran all 5 edits after re-reading from disk; second pass persisted. Pattern to note: in Wave-2 parallel plan execution, Grep-verify first-edit-landed-on-disk before continuing to subsequent edits, especially on files the executor hasn't recently touched.
- [Phase 05.1]: Plan 05.1-03: Option A (emit finding fields + getString helper) chosen for gender-rule three-beat explain; preserves {nb, nn} contract + fixture runner tolerates extra fields.
- [Phase 05.1]: [Plan 05.1-02]: TYPE_TO_BANK keys match word-ID suffix (lang/nat), NOT the type string (language/nationality) — because wordIdToBank() in papertek-vocabulary splits on _ and looks up the LAST segment of the word ID. Plan's 'language'/'nationality' keys would have silently no-op'd fast-path routing. Fix requires 3-line inline comment in api/vocab/v3/lookup/[language]/[wordId].js documenting the suffix-key convention.
- [Phase 05.1]: [Plan 05.1-02]: Typed-bank-shield pattern — new bank files (languagesbank, nationalitiesbank) carry custom type values ('language', 'nationality') that are invisible to downstream consumers' pre-built indexes keyed on specific type literals (e.g. Leksihjelp vocab-seam-core's nounGenus pipe only pulls bank='nounbank' || type='nounform'|'plural'). Reusable for any future semantic-category bank where entries share noun-morphology but need to NOT populate the default noun-genus index. Applied: nationalitiesbank has full genus/plural/definite data but stays out of nounGenus.
- [Phase 05.1]: [Plan 05.1-02]: Plan-spec-vs-code-reality arbitration — when plan wording (e.g. manifest shape { entries: N } object) contradicts the actual code (integer counts), honor code reality not plan wording. Three such corrections this plan: TYPE_TO_BANK keys, manifest shape, and implicit search-index regeneration. All auto-fixed under Rule 3 blocking (silent-no-op prevention).
- [Phase 05.1]: [Plan 05.1-04]: nb-dialect-mix at priority 35 (not CONTEXT.md's 30) — sarskriving already owns 30, so 35 slots between sarskriving and typo-curated (40). dedupeOverlapping at 35 < 40/50 keeps dialect-mix on overlap with typo rules but avoids tying with sarskriving at 30.
- [Phase 05.1]: [Plan 05.1-04]: CROSS_DIALECT_MAP as rule-fire gate (executor refinement, Rule 4 inline-resolved). Full `sisterValidWords \ validWords` is ~6.6K words/direction including morphologically shared forms with data gaps (klokka, havet, kaldt, liker, tavla). Firing on the superset produced a false-positive storm on clean/codeswitch corpora. Restricted to curated map (34 NN→NB + 32 NB→NN). No-fix fallback branch in explain() now unreachable but retained for contract shape. Papertek-vocabulary promotion of the map is deferred.
- [Phase 05.1]: [Plan 05.1-04]: Dual-guard coexistence pattern — dialect-mix owns its own `sister-valid AND NOT current-valid AND in CROSS_DIALECT_MAP` guard; typo-curated + typo-fuzzy retain Phase 4's sisterValidWords early-exit as a data-gap shield (bucket B). Both guards consume the same seam-level Set but for different purposes: flag curated vs silently tolerate everything else. Required after Task 2's full-removal attempt regressed nn-typo-dialect-bare-001 (kaldt→kalde fuzzy false positive). Reusable pattern: when a narrow high-confidence rule claims a subset of tokens via dedupeOverlapping, broader rules MAY still need their own guard on the non-claimed remainder.
- [Phase 05.1]: [Plan 05.1-04]: Vocab-seam translation-entry path surface — buildWordList emits `type='translation'` entries from every bank row's `translation` field, seeding cross-dialect equivalents into BOTH dialects' validWords. Consequence: NB tokens like `jeg`/`ikke`/`bare`/`noen`/`hjem` show as valid NN words (because NN's `eg_pron.translation='jeg'` seeds `jeg` into NN validWords). Plan 05.1-04's central assumption that these tokens would flag as dialect-mix in NN docs was wrong; the 5 NN Phase-4 dialect fixtures retained as expected:[]. Regression-test value preserved: if the translation-entry path changes, those fixtures surface it.
- [Phase 05.1]: [Plan 05.1-04]: Explain signature resolution (RESEARCH.md open question #3) — Option A chosen: `lang` emitted on finding, explain() reads `finding.lang` internally. Preserves the (finding)-only contract-gate signature across all 6 popover-surfacing rules. Copy is single-string-returned-for-both-keys (the message describes the student's document, not popover locale); both keys non-empty satisfies the contract gate.
- [Phase 05.1]: [Plan 05.1-01]: NB blå_adj defect closed at papertek-vocabulary source — blåt → blått + 5 sibling mono-vowel roots (bra/fri/grå/ny/rå) fixed in both NB + NN adjectivebanks. Cross-repo commits in papertek-vocabulary (4307937c, c734f97e, dbdb8b12) pushed to origin/main. Leksihjelp pulls via sync-vocab in Plan 05.1-05.
- [Phase 05.1]: [Plan 05.1-01]: Conservative-predictor validator pattern — scripts/validate-adj-declensions.js emits predictions only for the monosyllabic-pure-vowel class; other classes (-ig/-leg/short-d/diphthong-y/loan-word-invariability/past-participle-a/double-cons-neuter) return null from predictNeuter and are skipped. Exits 0 post-fix (12 → 0 defects). Narrowing from broad-predictor cut false positives from 171 → 12 with zero IRREGULAR whitelist maintenance.
- [Phase 05.1]: [Plan 05.1-01]: fin_adj authored in both NB + NN adjectivebanks closing Plan 02-03 STATE.md deferred blocker. NN schema verified verbatim against existing NN entries (god_adj, pen_adj) — maskulin/feminin/noytrum/flertall/bestemt, no silent NB fallback. NN uses -are/-ast comparison (finare/finast/finaste), NB uses -ere/-est (finere/finest/fineste). Post-write inline Node verification confirmed noytrum==='fint' in NN. nn-clean-003 fixture revert (stort → fint) unblocked for Plan 05.1-05.
- [Phase 05.1]: [Plan 05.1-01]: Hand-edit-over-regenerate-all policy for source-data fixes — running npm run enrich:nb to apply the generator fix also regenerates 384+ lines across 443 adjectives, exposing PRE-EXISTING generator defects (barnevennlig → barnevennligt, blågrønt → blågrønn via wrong -nn double-cons rule, himmelblått → himmelblåt under default-t before compound allowlist added, glohett → glohet under endsWith-t branch). Generator fix still lands (prevents future regression) but surgical hand-edits applied to the 6 target entries; broader audit deferred.
- [Phase 05.1]: [Plan 05.1-01]: Diphthong-guard regex refinement (Rule-1 deviation from plan). Plan committed to `w.length <= 3 && /[aeiouyæøå]$/.test(w)` verbatim but that regresses høy / gøy → høytt / gøytt (both real NB adjectives). Final form adds a second clause `!/[aeiouyæøå][aeiouyæøå]$/.test(w)` rejecting final vowel+vowel pairs (diphthongs). Applied symmetrically in enrich-nb-lexicon.js + generate-nn-lexicon.js.
- [Phase 05.1]: [Plan 05.1-01]: Compound allowlist pattern — `/(blå|grå|rå|ny|fri|bra|tro|sky)$/` over general compound regex. A general cons+vowel$ pattern over-doubles invariable loan-word adjectives (rosa / lilla / ekstra / sjalu / sporty / moro). Explicit allowlist covers the 3 known compounds in data (himmelblå / mørkeblå / lyseblå) without collateral; future mono-vowel compounds add a single token.
- [Phase 05.1]: [Plan 05.1-05]: Node runner vs browser adapter blindspot closed by new check-rule-css-wiring release gate. Bug 4 surfaced during Chrome smoke test (dialect-mix dot CSS missing from extension/styles/content.css — rule fired correctly per fixture runner but no visible underline rendered in browser). Root cause: check-fixtures runs in Node where CSS is never parsed; the rule fired, DOM got the class, but .lh-spell-dialect-mix selector didn't exist. New gate scripts/check-rule-css-wiring.js scans every extension/content/spell-rules/*.js file for rule_id and asserts matching `.lh-spell-{rule_id}` selector exists in content.css. Paired self-test scripts/check-rule-css-wiring.test.js plants broken scratch rule (no CSS selector), confirms gate exits 1; removes scratch, confirms gate exits 0. Mirrors SC-01 (Phase 03.1) + SC-03 (Plan 04-01) adapter-contract guard pattern. Release workflow now 8 gates (was 6).
- [Phase 05.1]: [Plan 05.1-05]: Feature-gated lookup indexes decouple (Bug 2 fix ce343d9). Modal-verb went silent on `kan gikk` in Chrome because seam-level lookup indexes (BARE_INFINITIVE set, modal set) were being built from grammar-feature-toggled filtered wordList rather than raw wordList. Users with certain grammar features disabled had empty lookup sets → rule silently no-op'd. Fix: decoupled — seam builds lookup indexes from raw unfiltered wordList; feature gating moved to explain-render layer. Pattern: seam-level data structures must not be filtered by UI state; only render-time presentation may be gated.
- [Phase 05.1]: [Plan 05.1-05]: Dialect-mix CROSS_DIALECT_MAP as authoritative fire-gate (Bug 4 fix a90fcc0). Plan 05.1-04's original guard was `sisterValidWords.has(token) && map.has(token)`. Tokens present in map but data-gap absent from sister-lang validWords were silently skipped. Reversed to map-first: `if CROSS_DIALECT_MAP.has(token) fire regardless of sisterValidWords presence`. sisterValidWords no longer consulted in dialect-mix; typo-curated + typo-fuzzy retain their sisterValidWords shield for a different purpose (data-gap tolerance for non-curated tokens). Pattern: when two guards consume the same seam-level Set for different rule-behavior intents, each rule owns its decision — don't let shared state mask narrow-intent fires.
- [Phase 05.1]: [Plan 05.1-05]: Modal-verb bare-infinitive check inversion (Bug 3 fix 5aab727). `kan skrive` was flagging (false positive) because the rule fired when the following token was NOT in the BARE_INFINITIVE set; but `skrive` IS a bare infinitive — an empty/inconsistent set is NOT the same thing. Inverted logic: fire when the following token is a finite verb (in CONJUGATED_FORMS set) AND not itself a modal (preserving modal-modal chains like `kan må gå`). Pattern: explicit allowlist > implicit not-in-set when set membership is data-quality-sensitive.
- [Phase 05.1]: [Plan 05.1-05]: Register badge in popover header (Bug 1 fix a2d34e4). Smoke test revealed users couldn't tell which register (NB vs NN) a rule was evaluating against. Added small `Bokmål`/`Nynorsk` badge in popover header via `vocab.getLanguage()` read at render. Pattern: explicit register-causality indicators in spell-check UI are essential for dialect-mix rules whose flag direction depends on document register (flagging `ikkje` in NB doc because NB is the register, not because `ikkje` is NN).
- [Phase 05.1]: [Plan 05.1-05]: Inline bug-fix-during-human-verify is healthy pattern, not scope creep. Four bugs surfaced + resolved + retested + user-approved within a ~15 min debug loop inside the checkpoint. Without human-verify gate, all four would have shipped. Counts as plan scope: each fix was directly responsive to a smoke-test scenario the plan explicitly called for. Parent pattern: treat human-verify as a required stage, not a formality; allocate time budget for inline iteration.
- [Phase 05.1]: [Plan 05.1-05]: Phase 05.1 close-out — all four UX-01 gaps (A/B/C/D) landed + Chrome smoke test PASS 11/11 scenarios + UX-01 marked complete in REQUIREMENTS.md with dual-phase cross-link (Phase 5 + Phase 05.1). Release 2.3.1 packaged; zip + GitHub release publish owned by user. Project has no further inserted phases planned on current ROADMAP.md; natural next step is `/gsd:verify-work 05.1` then optional Phase 06 add.
- [Phase 06]: Empty expectations.json passes benchmark gate (ship-before-populate strategy)
- [Phase 06]: Governance gate pass-through when no banks exist (pre-data-sync tolerance)
- [Phase 06 P03]: Temporary seed data in rule files allows testing before papertek-vocabulary banks land; marked TEMPORARY for removal after data sync
- [Phase 06 P03]: Governance banks (registerbank/collocationbank/phrasebank) extracted separately from word-prediction BANKS array to avoid polluting autocomplete
- [Phase 06 P03]: REG-01 register rule defaults OFF via grammar_register toggle — opt-in to avoid false positives in informal student writing contexts
- [Phase 07]: classifyPOS receives pre-lowercased input; acceptance ratio counted per rule-specific fixture file
- [Phase 07]: Flag only subject pronoun span to avoid deduplication conflicts with other rules
- [Phase 07]: Use direct knownPresens/knownPreteritum lookup instead of isFinite to avoid multi-word stem false positives
- [Phase 08]: Built participleToAux from raw verbbank data (not wordList) since wordList entries lack conjugation details
- [Phase 08]: SEPARABLE_PREFIXES canonical source moved to grammar-tables.js; de-v2.js reads from shared global with local fallback

### Roadmap Evolution

- Phase 02.1 inserted after Phase 2: Close SC-4 bundle-size cap (URGENT)
- Phase 05.1 inserted after Phase 5: Close UX-01 gaps from Phase 5 smoke test (URGENT)

### Pending Todos

- ~~Live Chrome smoke test for Phase 1 (deferred 2026-04-18)~~ **CLOSED 2026-04-21 incidentally** by Phase 5 + Phase 05.1 Chrome sweeps (11/11 scenarios PASS across popover render + 558-word NB article paste; covers the Phase-1 `en hus` case via gender-rule coverage).
- ~~Live Chrome smoke test for Phase 4 (deferred 2026-04-20)~~ **CLOSED 2026-04-21 incidentally** by Phase 05.1 Chrome sweep (500-word NB article pasted into `<textarea>`, zero red underlines on proper nouns / loan words; SC-02 live confirmation captured).
- **[Plan 05.1-05 deferred 2026-04-21] papertek-vocabulary: add -s passiv conjugations.** `markere` verb lacks `markeres` passive form (and similar for other -ere verbs). Data gap surfaced in Chrome smoke test. Cross-repo data-authoring PR needed in `papertek-vocabulary` to extend verb conjugations with `-s passiv` forms. Not blocking.
- **[Plan 05.1-05 deferred 2026-04-21] papertek-vocabulary: ensure `setningen` alongside `setninga` for NB `setning`.** NB-bestemt-form coverage gap — `setning_noun` in NB lemma has `setninga` but not `setningen`. Cross-repo data-authoring PR needed. Not blocking.
- **[Plan 05.1-05 Phase 06 candidate] Demonstrative-mismatch rule.** `Det boka`, `Den huset` — extends nb-gender beyond en/ei/et indefinite-article scope to demonstrative-pronoun agreement. Smoke-test user surfaced the gap but accepted deferral. Different rule class from current nb-gender; different training required. Candidate for Phase 06 planning.
- **[Plan 05.1-05 Phase 06 candidate] Expand fuzzy-distance budget for triple-letter keystrokes.** `tykkkjer` → `tykkjer` currently not caught with best candidate — needs frequency-weighted tiebreak or budget increase from 2 to 3 for one specific keystroke class (triple-letter repetition). May risk false-positives; needs fixture calibration before landing. Candidate for Phase 06 planning.
- ~~Live bug: `ikkje` flags in NB documents despite fixture coverage saying it should not~~ **RESOLVED 2026-04-21 by Phase 05.1 Gap D policy reversal.** The "dialect tolerance" framing was itself the bug — per user domain policy (memory `project_nb_nn_no_mixing.md`), cross-standard tokens ARE student errors and SHOULD flag. New `nb-dialect-mix.js` rule (priority 35) now flags `ikkje` in NB documents with a register-aware suggestion (`ikke`). Typo rules no longer early-exit into `sisterValidWords`; `CROSS_DIALECT_MAP` is the authoritative fire-gate. 11 Phase-4 dialect-tolerance fixtures inverted. Chrome smoke-test 2026-04-21 PASS across `Jeg/Hun/Eg/Hva/vet/ikke` cross-dialect sweep.

### Blockers/Concerns

- Phase 2 DATA-02 has cross-app blast radius — schema changes in `papertek-vocabulary` affect `papertek-webapps` and `papertek-nativeapps`; coordinate before landing. *Resolved 2026-04-18 in Plan 02-03: zero schema changes shipped (entry.typos stays string[]); only additive typo growth + one genus correction + two removed erroneous typos. Safe for sibling consumers.*
- **Phase 2 DATA-02 cross-app rollback protocol (added 2026-04-18 revision 1, executed through 02-03):** if sibling-repo commits `0533e28d` (rule-library expansion) or `c6965c00` (fint/fints removal) cause a regression in `papertek-webapps` or `papertek-nativeapps` downstream consumers, the rollback sequence is documented inside Plan 02-03 Task 3 ("Rollback Protocol" subsection) and in the 02-03-SUMMARY.md Cross-App Impact section: `cd /Users/geirforbord/Papertek/papertek-vocabulary && git revert c6965c00 0533e28d --no-edit && git push origin main`, wait for API redeploy, then `cd /Users/geirforbord/Papertek/leksihjelp && npm run sync-vocab && git add extension/data/*.json && git commit -m "revert(02-03): roll back DATA-02 after sibling-repo regression"`. Leksihjelp's seam fix (vocab-seam-core.js) stays landed — it's Leksihjelp-specific.
- **[Plan 02-03 deferred] NN phrase-infinitive triage (~214 entries):** NN `verbbank.json` has ~214 entries where `word` contains a space. Some are legitimate reflexive/phrasal verbs (`anstrenge seg`, `bli med`); others are gloss residue (`bo, å leve`). Needs a sibling-repo PR with human-in-the-loop classification before bulk normalization. Out of scope for Phase 2; candidate for Phase 2.1 or a standalone sibling-repo data cleanup PR. Leksihjelp-side impact: modal-verb rule accuracy on NN phrasal-verb sentences may be marginally off until this lands. Not a hard blocker.
- **[Plan 02-03 deferred] Missing `fin_adj` entry in NB and NN adjective banks:** *RESOLVED 2026-04-21 by Phase 05.1 Plan 05.1-01 Task 3.* `fin_adj` authored in both NB and NN adjectivebanks in papertek-vocabulary (commit dbdb8b12, pushed to origin/main). NN schema grep-verified against existing NN adjectives (god_adj, pen_adj) before writing — no silent NB fallback. `fin_adj.declension.positiv.noytrum === "fint"` confirmed in both banks via inline Node verification. Once leksihjelp `npm run sync-vocab` pulls the new data (in Plan 05.1-05), the `nn-clean-003` fixture can revert text from `"Det er eit stort hus."` back to the original `"Det er eit fint hus."` — underlying data gap is now closed.
- **Phase 2 bundle-size SC-4 — RESOLVED 2026-04-19 by Phase 02.1.** The internal engineering ceiling was raised from 10 MiB to 20 MiB in `scripts/check-bundle-size.js` and `scripts/check-bundle-size.test.js`; ROADMAP SC-4 + REQUIREMENTS DATA-03 reworded; CLAUDE.md Release Workflow + PROJECT.md + .planning/research/*.md prose corrected to drop the false "publicly-stated promise" framing. The current zip (10.11 MiB / 10,599,772 bytes) now passes the gate with ~9.89 MiB headroom. The gate's purpose shifts from "enforce 10 MiB promise" to "catch accidental bundle-growth regressions" — same exit-0/1 semantics, same Release Workflow integration, same script architecture. Phase 02.1 explicitly chose NOT to pursue the English-removal / audio-strip / data-trim remediation paths from 02-05-AUDIT.md; those remain documented in the audit as future options if bundle size ever becomes a real constraint again.
- Phase 3 code-switching detection needs empirical calibration for Norwegian vs. close Germanic neighbors (Swedish, Danish, German) — research flag from SUMMARY.md.
- Phase 4 særskriving precision/recall thresholds depend on fixture sentences authored in Phase 1 — do not set thresholds until the fixture is in place.

## Session Continuity

Last session: 2026-04-20 — Phase 04 Plan 04-03 (fixtures + SC-05 threshold gate) completed across two sessions; this session's resumed portion took 6 min. Six atomic commits spanning the full plan: 383552c (fix: close SC-03 gap in nb-typo-curated.js with sister-validWords early-exit — Rule-1 auto-fix surfaced by Task 1 fixture authoring when NN-valid words leaked through curated-typo branch), 8ea74c5 (fix: scripts/check-fixtures.js now loads sister-dialect raw vocab and passes it to buildIndexes — without this the Node fixture path had empty sisterValidWords Set and SC-03 fixture cases were fixture-invisible. Rule-1 auto-fix bringing the runner in line with the browser adapter), 5023c97 (test: fixture expansion for SC-02/03/04/05 — 2 new codeswitch.jsonl files + 6 existing files expanded; observed pre-fix saerskriving P/R NB 0.974/1.000 and NN 0.968/1.000 recorded in commit body for Task-2 checkpoint consumption), a9d1314 (fix: add `stor` to SARSKRIVING_BLOCKLIST — Task 3 verification surfaced that Task 1's adjective+noun acceptance case `Hun bor i en stor by`/`Ho bur i ein stor by` FP'd because storby IS a real compound noun; probed all 16 other adjective+noun acceptance pairs, only `stor` collides with compoundNouns, so kept fix surgical; observed saerskriving P/R moves to 1.000/1.000 both langs post-fix), 21201c7 (feat: THRESHOLDS table + per-rule gate in scripts/check-fixtures.js — locked saerskriving at P>=0.92, R>=0.95 both languages per user's option-a checkpoint decision; key uses `saerskriving` filename basename NOT `sarskriving` finding-rule_id — documented distinction in both the comment block and fixtures/README.md; defence-in-depth probe validated gate is load-bearing at 0.99/0.99 mutation with correct diagnostic messages), 55eb757 (docs: fixtures/README.md gains Code-switching corpus section + P/R threshold gate section documenting the Phase 4 convention for future fixture authors). Release gates all PASS: check-fixtures 280/280 cases green across all 12 fixture files (saerskriving P/R=1.000 both langs clears locked 0.92/0.95 gate with 0.08 P-margin + 0.05 R-margin headroom), check-network-silence PASS (SC-06 preserved), check-bundle-size 10.13 MiB / 20 MiB cap PASS with 9.87 MiB headroom. 1 deviation this session (Rule-1 Bug — `stor by` FP, auto-fixed surgically); 2 deviations in prior session (both Rule-1, also auto-fixed). Honest-ground-truth policy held — no fixture `expected` back-fit from tool output. NO preemptive `gsd-tools requirements mark-complete` — per prompt instruction, phase-close requirement marking belongs to `/gsd:verify-work 04`.

Previous session: 2026-04-20 — Phase 04 Plan 04-02 (SC-02 + SC-04 architecture + SC-03 rule-layer close) shipped in 6m 37s. Three atomic commits 95f66ad, 65cf68a, be225d5 landed nb-codeswitch.js + nb-propernoun-guard.js + ctx.suppressed opt-ins on typo-curated/typo-fuzzy/sarskriving + nb-typo-fuzzy direct sisterValidWords early-exit. See 04-02-SUMMARY.md for full detail.

Earlier this session: 2026-04-20 — Phase 05 Plan 05-01 (foundation: explain contract + infrastructure) completed in 7 min. Three atomic commits 1328317, 9a23d09, 0f033f2 landed (a) escapeHtml on __lexiSpellCore dual-export api + spell-rules/README.md contract section, (b) scripts/check-explain-contract.js release gate + scripts/check-explain-contract.test.js paired self-test using dual-scenario plant-broken-then-plant-fixed pattern + package.json + CLAUDE.md Release Workflow step 2, (c) 10 new i18n entries (3 settings + 2 pred × 2 locales) + .planning/phases/05-student-experience-polish/COPY-REVIEW.md scaffold. Zero deviations. All 3 pre-existing release gates still pass (check-fixtures 280/280, check-network-silence PASS, check-bundle-size unchanged); new check-explain-contract gate exits 1 by design (acceptance signal for Plan 05-02 flip to 0). Phase 5 Wave 2 is fully unblocked: Plans 05-02/03/04/05 can land concurrently with commit-time contract enforcement.

Earlier this session (Wave 2): 2026-04-20 — Phase 05 Plan 05-03 (word-prediction top-3 reveal) completed in 6 min, running concurrently with Plans 05-02 and 05-04. One atomic commit 181f34b landed: (feat(05-03): UX-02 top-3 reveal in word-prediction dropdown — VISIBLE_DEFAULT=3 / VISIBLE_EXPANDED=8 constants; showDropdown refactored into thin-wrapper + renderDropdownBody + attachDropdownHandlers helpers; ArrowDown auto-reveal branch in handleKeydown; Vis-flere mousedown handler with preventDefault + stopPropagation; findSuggestions 5→8 + splice 5→8 compound-clip guard; .lh-pred-vis-flere CSS in light + dark modes). Task 2 was verification-only (release-gate sweep + 4-invariant source audit) with no code changes. Zero deviations. All 4 release gates PASS: check-fixtures 280/280, check-network-silence PASS, check-bundle-size 10.13 MiB / 20 MiB cap with 9.87 MiB headroom, check-explain-contract 5/5 PASS (flipped exit 1 → exit 0 during 05-03 window — Plan 05-02 substantially landed). Files modified: extension/content/word-prediction.js (+104/-9), extension/styles/content.css (+33). Minor note: first Edit-tool pass silently reverted on word-prediction.js (suspected Wave-2 parallel race); re-ran all 5 edits after re-read, second pass persisted — tracked in SUMMARY Issues Encountered.

This session (Wave 2 close): 2026-04-20 — Phase 05 Plan 05-02 (rule-side UX-01 explain callable + UX-02 fuzzy top-K) completed in 6m 23s, running concurrently with Plans 05-03 and 05-04. Three atomic commits landed: 62e51a8 (feat Task 1 — upgrade 3 static-template rules nb-gender + nb-modal-verb + nb-typo-curated; explain: (finding) => ({nb, nn}) callables with escapeHtml-wrapped <em> student-word templates; priority: rule.priority added to each emit block; COPY-REVIEW.md 3/5 cells populated), df3a002 (feat Task 2 — upgrade nb-typo-fuzzy.js findFuzzyNeighbor → findFuzzyNeighbors with scored-array sort-desc + slice(0, 8); rule emits suggestions: string[] case-matched via matchCase + priority; fix === suggestions[0] back-compat invariant preserved; upgrade nb-sarskriving.js explain with defensive fallback for missing original/fix; COPY-REVIEW.md 5/5 cells populated), f2eef3a (feat Task 3 — defence-in-depth probe: priority + suggestions flow confirmed end-to-end through core.check() dedupe on real nb.json vocab; Zipf mutation ZIPF_MULT=10 → 0 fires nb-typo-zipf cases (hagde→hagle, hatde→hate both fail, nb/typo F1 drops 1.000 → 0.900, exit 1); restore ZIPF_MULT=10 byte-identical returns 280/280 green; Probe log section appended to COPY-REVIEW.md). Zero deviations — plan landed exactly as written. All 4 release gates PASS: check-fixtures 280/280, check-network-silence PASS, check-bundle-size 10.13 MiB / 20 MiB cap with 9.87 MiB headroom, check-explain-contract **5/5 PASS (flipped from exit 1 → exit 0 this plan — acceptance signal landed for the Phase 5 Plan 05-01 foundation)**. Files modified: extension/content/spell-rules/nb-gender.js, nb-modal-verb.js, nb-typo-curated.js, nb-typo-fuzzy.js, nb-sarskriving.js, .planning/phases/05-student-experience-polish/COPY-REVIEW.md. Suppression rules (nb-codeswitch.js, nb-propernoun-guard.js) untouched per plan scope — git diff --stat empty. Minor note: mid-Task-2 Edit-tool revert on nb-typo-fuzzy.js + nb-sarskriving.js (suspected Wave-2 parallel race, same pattern as 05-03); re-applied byte-identical, second pass persisted — tracked in SUMMARY Issues Encountered. NO preemptive `gsd-tools requirements mark-complete` — per prompt instruction, phase-close requirement marking belongs to `/gsd:verify-work 05`.

Last session: 2026-04-21 — Phase 05.1 Plan 05.1-01 (Gap A — NB blå_adj + adjective-declension audit, cross-repo) completed in 19m52s. Three atomic commits in papertek-vocabulary: 4307937c (feat Task 1 — scripts/validate-adj-declensions.js + validate:adj-declensions npm script; conservative-predictor design flags only the monosyllabic-pure-vowel target class; initial run surfaced 12 defects across NB + NN), c734f97e (fix Task 2 — NB blå_adj fix blåt→blått + 5 sibling mono-vowel roots bra/fri/grå/ny/rå corrected to -tt doubled forms via surgical hand-edits; enrich-nb-lexicon.js gains monosyllabic-vowel branch with diphthong-guard and compound allowlist — prevents future regeneration of the defect), dbdb8b12 (fix Task 3 — NN 6 mono-vowel surgical fixes mirroring Task 2 + generate-nn-lexicon.js matching branches + fin_adj entry authored in both NB and NN adjectivebanks with NN-verified schema, closing Plan 02-03 STATE.md deferred blocker; manifest counts incremented 451→452). All three pushed to papertek-vocabulary origin/main (bf1bd07b..dbdb8b12). Validator post-fix: node scripts/validate-adj-declensions.js exits 0. Four Rule-1/3 deviations documented: (1) diphthong-guard regex refinement (plan's verbatim form regresses høy→høytt), (2) compound allowlist over general regex (general form over-doubles invariable loan-word adjectives rosa/lilla/ekstra), (3) validator predictor scope narrowing (broad predictor surfaced 171 false-positive-heavy "defects"; narrowed to target class only → 12 clean), (4) import-mistake recovery after accidental enrich:nb main-run regenerated 384 unrelated lines; reverted via git checkout + hand-applied target fixes. No changes to Leksihjelp repo in this plan (data flows via sync-vocab in Plan 05.1-05).

Previous session: 2026-04-20 — Phase 04 Plan 04-03 (fixtures + SC-05 threshold gate) completed across two sessions; this session's resumed portion took 6 min. Six atomic commits spanning the full plan: 383552c, 8ea74c5, 5023c97, a9d1314, 21201c7, 55eb757. Release gates all PASS. See 04-03-SUMMARY.md for full detail.

Earlier sessions (2026-04-20): Phase 05 Plans 05-01 through 05-05 shipped (foundation + explain callable + word-prediction top-3 reveal + settings toggle + popover render). Phase 5 Wave 2 closed 2026-04-20 with Plan 05-02 commit f2eef3a flipping check-explain-contract exit 1→0. Plan 05-05 popover render shipped 2026-04-20 with commits 87e0812, c35ab70, 10b5488; Task 3 checkpoint:human-verify 7-step Chrome smoke test deferred to `/gsd:verify-work 05`.

Last session: 2026-04-21 — Phase 05.1 Plan 05.1-05 (Integration close) completed in ~90 min across two focus windows (Task 1 + Task 2 fast + Task 3 human-verify with ~15 min inline bug-fix-and-retest loop + Task 4 docs-close). Nine atomic leksihjelp commits landed: 6877ec8 (Task 1 — sync-vocab pull-through + 4 registries (sync-vocab BANKS + vocab-seam-core BANKS + popup BANK_TO_POS + floating-widget BANK_TO_POS_KEY) + pos_language/pos_nationality i18n keys in nb/nn/en + nn-clean-003 fixture revert stort→fint after pre-revert gate PASSED), c6b64d0 (Task 2 — version 2.3.0→2.3.1 in manifest.json + package.json + backend/public/index.html + zip packaged under 20 MiB cap), a2d34e4 (Task 3 inline fix Bug 1 — register badge in popover header showing Bokmål/Nynorsk causality), ce343d9 (Task 3 inline fix Bug 2 — decouple seam-level lookup indexes from grammar-feature gating; fixed modal-verb silent on `kan gikk` with some feature toggles off), 5aab727 (Task 3 inline fix Bug 3 — modal-verb false-pos on bare infinitive `skrive` by inverting check from 'not-in-BARE_INFINITIVE-set' to 'token-is-CONJUGATED-not-modal'), a90fcc0 (Task 3 inline fix Bug 4a — dialect-mix CROSS_DIALECT_MAP as authoritative fire-gate, reversed sisterValidWords masking on map-present tokens), 5718b39 (Task 3 inline fix Bug 4b — .lh-spell-dialect-mix CSS added to content.css so sky-blue underline renders in browser), c592127 (Task 3 inline fix Bug 4c — NEW scripts/check-rule-css-wiring.js release gate + scripts/check-rule-css-wiring.test.js paired self-test closes Node-runner-vs-browser-adapter blindspot at release-workflow time), 54c9137 (Task 3 close — CLAUDE.md release-workflow docs updated to register the new check-rule-css-wiring gate). Cross-repo papertek-vocabulary commits already pushed: 4307937c/c734f97e/dbdb8b12 (Plan 05.1-01 Gap A) + bf1bd07b/ad210a53 (Plan 05.1-02 Gap B + search-index rebuild). Four Rule-1 Bug deviations, all caught by Task 3 human-verify gate, all inline-resolved + retested + user-approved within ~15 min. Release gates all PASS (now 8 total — the original 6 plus the new check-rule-css-wiring pair). Chrome smoke test PASS on 11/11 scenarios (5 Phase-5 re-run + 6 new Phase 05.1 gap scenarios). UX-01 marked complete in REQUIREMENTS.md with dual-phase cross-link (Phase 5 + Phase 05.1) and 8 key commit hashes. ROADMAP.md Phase 05.1 now reads 5/5 plans complete 2026-04-21. Release 2.3.1 packaged; GitHub release publish + `git push origin main` remain user-owned (per prompt, not executed by this plan).

Previous session: 2026-04-21 — Phase 05.1 Plan 05.1-01 (Gap A — NB blå_adj + adjective-declension audit, cross-repo) completed in 19m52s. Three atomic commits in papertek-vocabulary: 4307937c, c734f97e, dbdb8b12. See 05.1-01-SUMMARY.md for detail.

Stopped at: **Completed Phase 06 Plan 06-03 (register, collocation, redundancy rules with vocab-seam pipeline). Phase 06 COMPLETE — 3 of 3 plans shipped 2026-04-24. SUMMARY at `.planning/phases/06-structural-infrastructure-register-stylistic-polish/06-03-SUMMARY.md`.**

Previous stopped-at: **Completed Phase 05.1 Plan 05.1-05 (Integration close — sync-vocab pulled Plans 05.1-01 + 05.1-02 cross-repo data into extension/data/{nb,nn,en}.json; 4 registries + i18n wired for new languagesbank + nationalitiesbank; nn-clean-003 fixture reverted stort→fint after pre-revert gate PASS; version bumped 2.3.0→2.3.1; zip packaged; 4 browser-only bugs inline-fixed during Chrome smoke test; NEW check-rule-css-wiring release gate shipped to close the Node-runner-vs-browser-adapter blindspot; all 8 release gates PASS; 11/11 Chrome smoke-test scenarios PASS; UX-01 marked complete in REQUIREMENTS.md). SUMMARY at `.planning/phases/05.1-close-ux-01-gaps-from-phase-5-smoke-test/05.1-05-SUMMARY.md`.**
Resume: **Phase 05.1 COMPLETE — 5 of 5 plans shipped 2026-04-21.** Project now has no in-progress or queued phases on current ROADMAP.md. Suggested next steps (user-owned): (1) `git push origin main` to publish the 9 leksihjelp commits + the final docs-close commit to origin; (2) optional `gh release create v2.3.1 leksihjelp-2.3.1.zip` or manual GitHub Release publish; (3) `/gsd:verify-work 04` and `/gsd:verify-work 05.1` to audit SC-02/03/04/05 + UX-01 requirement marks; (4) `/gsd:add-phase` to pick up Phase 06 candidates (demonstrative-mismatch rule, triple-letter fuzzy budget) or data-authoring follow-ups (s-passiv conjugations, setningen NB bestemt-form). Deferred items unchanged except fin_adj RESOLVED (Plan 05.1-01) and two new data follow-ups + two new Phase 06 candidates added above.
