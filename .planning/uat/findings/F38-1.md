---
f_id: F38-1
severity: blocker
sync_status: extension-only
regression_fixture_id: fixtures/fr/aspect-hint.jsonl + scripts/check-fr-bundle-completeness.js
walkthrough_id: UAT-EXT-01
discovered: 2026-05-01
status: closed
status_history: closed-2026-05-01-then-reopened-2026-05-01-on-rewalk-regression-then-closed-v2-2026-05-01
---

# F38-1: fr-aspect-hint silent in real Chrome — French input scored against NB dictionary, FR sidecars 404

## Reproduction

1. Set popup foreign-language to French. Set NB locale.
2. Reload extension at `chrome://extensions` (reload_ts 2026-05-01T17:00:00+02:00; ext_version 2.9.18; Chrome 147.0.7727.117 arm64).
3. In a French-marked text input on any page, type: `Pendant que je marchais, j'ai vu un chien.`

**Observed output:**

```
- No spell-check dot under `marchais` (fr-aspect-hint never fires).
- No "Lær mer" panel (Step 4 not reachable since Step 1 ❌).
- `j'ai` is flagged by NB spell-check: popover reads
    "Jai står ikke i ordboken, kanskje du mente j'aime?"
  → engine is treating French tokens as Norwegian.
- Console:
    [lexi-spell] markers rendered Object
    chrome-extension://kciankappkjgeijgopddopaepjfgfknc/data/freq-fr.json:1
        Failed to load resource: net::ERR_FILE_NOT_FOUND
    chrome-extension://kciankappkjgeijgopddopaepjfgfknc/data/pitfalls-fr.json:1
        Failed to load resource: net::ERR_FILE_NOT_FOUND
    chrome-extension://kciankappkjgeijgopddopaepjfgfknc/data/bigrams-fr.json:1
        Failed to load resource: net::ERR_FILE_NOT_FOUND
```

**Expected output:**

```
- Soft-hint dot under `marchais` (or `j'ai vu`) per fr-aspect-hint rule.
- Click dot → popover with "Lær mer".
- "Lær mer" → pedagogy panel renders examples + illustration.
- French tokens treated as French; NB spell-check does NOT flag `j'ai`.
```

## Root cause hypothesis

Two-part defect, almost certainly the same root cause:

1. **Missing FR sidecar files.** `extension/data/` ships only `fr.json` + `grammarfeatures-fr.json` — `freq-fr.json`, `pitfalls-fr.json`, `bigrams-fr.json` don't exist in the bundle. `extension/content/vocab-seam.js:144-157` handles a 404 gracefully (returns `null`), so the missing files are not the proximate cause of "rule silent" — but they are evidence that FR data hydration is incomplete.

2. **Probable proximate cause: language detection.** The console fires `[lexi-spell] markers rendered`, NB spell-check fires on `j'ai` ("Jai står ikke i ordboken"), and fr-aspect-hint stays silent. This pattern matches: the engine ran spell-check against the input but treated it as Norwegian, so French rules never had a chance to evaluate. Either the per-input language plumbing isn't propagating the popup's foreign-language selection to the spell-check seam, OR the spell-check seam ignores `lang` and runs all enabled language rules (with NB winning by ordering / priority).

**Why slipped past gates:** Every release gate runs in Node against the fixture runner with `() => true` for feature predicates and synthetic input. None of the 14 gates exercise the browser's per-input language detection path. `check-vocab-seam-coverage` (Phase 38 FIX-03 anchor) defends `buildIndexes` shape, not language routing. This is a Node-vs-browser divergence, exactly the class STATE v3.2 entry called out as Pitfall 2.

Also: bigrams/freq/pitfalls sidecars for FR (and DE/ES) appear to never have been built — this is a separate but related data-pipeline gap. `npm run sync-vocab` likely doesn't generate them.

**Confirmed in Step 6 walk:** `pitfalls-nb.json` is ALSO missing (`vocab-seam.js:147 GET chrome-extension://.../data/pitfalls-nb.json net::ERR_FILE_NOT_FOUND`). NB still works because `loadBundledSidecar` returns `null` on 404 and the rule path doesn't depend on it for the gates the walker tested. So the pipeline gap is broader than FR — it's universal. The fix should regenerate sidecars for all languages, not just FR.

## Fix tracking

- [x] Extension fix landed (commit: see Resolution below)
- [x] Regression artifact added (`fixtures/fr/aspect-hint.jsonl` — pos-f38-1-canonical row asserts fr-aspect-hint fires on `j'ai vu` AND nb-typo-fuzzy does NOT false-fire on `j'ai`)
- [x] All release gates green (per Plan 38-01.1 Task 3)
- [x] Version bumped in 3 places (`extension/manifest.json`, `package.json`, `backend/public/index.html`)
- [x] `npm run package` rebuild + zip uploaded as GitHub Release asset
- [x] Lockdown re-sync — N/A (extension-only) but `[lockdown-resync-needed]` marker carried since `extension/content/spell-rules/nb-typo-fuzzy.js` is in the synced surface set

## Resolution

**Proximate cause (Branch C — multi-language typo-fuzzy F36-1 guard, data-side miss):**
The `nb-typo-fuzzy` rule (multi-language: `nb, nn, en, de, es, fr`) emits findings
on FR tokens when `vocab.validWords` does not contain them. Phase 36-02 added a
cross-language verb-form guard (`tokenIsForeignVerbForm`) to suppress this on
known FR aux/imparfait/passé-composé verb forms. The guard checked
`vocab.frAuxPresensForms.has(t.word)` against the literal token — but the FR
tokenizer emits elided tokens whole (`j'ai`, `n'a`, `qu'as`), while
`frAuxPresensForms` contains only unelided bases (`ai`, `a`, `as`). Result:
the guard slipped on every elided auxiliary, fuzzy fired, and `j'ai` was
mis-suggested as `j'aime`.

**Why all 12 release gates stayed green pre-fix:** No fixture exercised an
elided-auxiliary FR token in a context where `nb-typo-fuzzy`'s guard was
relied on. The Phase 36-02 INFRA-10 gate (`check-vocab-seam-coverage`) defends
seam-shape, not token-shape. The new fixture row `fr-aspect-pos-f38-1-canonical`
plugs this gap.

**Fix (file:line):**
- `extension/content/spell-rules/nb-typo-fuzzy.js:162-185` — added
  `ELISION_RE = /^(?:j|n|s|c|d|l|m|t|qu)'(.+)$/i` and `stripElision()` helper;
  `tokenIsForeignVerbForm()` now checks both the literal token AND the
  elision-stripped form against the foreign-verb sets.
- `extension/popup/popup.js:1289-1296` — companion preventative fix: moved the
  `chrome.runtime.sendMessage({type:'LANGUAGE_CHANGED'})` broadcast OUT of the
  try/catch that wraps `loadDictionary` + `loadGrammarFeatures`. Previously a
  throw from those (e.g. FR vocab not yet downloaded) silently swallowed the
  broadcast. This was a separate latent bug not on the F38-1 critical path
  but bundled here since both touch the same plumbing surface.

**Sidecar-pipeline gap (freq-fr.json, pitfalls-fr.json, bigrams-fr.json,
pitfalls-nb.json all 404) is OUT OF SCOPE** for this plan. `loadBundledSidecar`
returns null gracefully on 404, and the absence is not the proximate blocker
for F38-1. Sidecar generation is upstream Papertek-API/sync-vocab territory
per data-logic separation philosophy. Queued as 38-01.2 candidate follow-up.

**STATE/UAT-EXT-01 paired update:** Step 1 status remains ❌ in the original
walk log (frozen as historical evidence); the re-walk evidence will be appended
under `## Re-walk after F38-1 fix (v<new>)` per Task 4.

## Re-walk regression (2026-05-01, v2.9.19)

Walker (Geir) re-walked UAT-EXT-01 Steps 1–4 against v2.9.19 (sideloaded from `extension/` on Mac).

**Result:**
- ✅ NB false-fire on `j'ai` is GONE — Branch C fix (`nb-typo-fuzzy` elision strip) works as designed; partial closure stays in v2.9.19.
- ❌ `fr-aspect-hint` STILL silent on `Pendant que je marchais, j'ai vu un chien.` — no dot anywhere. The proximate cause was misdiagnosed. Branch C addressed a real-but-symptomatic bug (NB rule false-firing on `j'ai`), not the actual reason `fr-aspect-hint` doesn't fire on this sentence.

**New evidence:**
- Console shows `[lexi-spell] init {lang: 'fr', ...}` and `[lexi-spell] vocab ready {validWords: 1416, ...}` (NB-baseline first), then later FR vocab loads (`vocabSize: 18917` reported on a different input). So the language routing IS reaching `lang: 'fr'` and FR vocab IS hydrating — but the rule still doesn't fire on the test sentence.
- The regression fixture `fixtures/fr/aspect-hint.jsonl#fr-aspect-pos-f38-1-canonical` PASSED in `npm run check-fixtures` (89/89), proving fr-aspect-hint fires correctly in **Node** on the canonical sentence. Yet the rule doesn't fire in **browser** on the same sentence.
- This is a classic Node-vs-browser divergence. Hypothesis space (orchestrator triage, not yet investigated):
  1. Vocab-seam hot-swap race: rule runs against an empty/partial `vocab.frAspectAdverbs` before `buildAndApply` finishes, even though `[lexi-spell] check` reports a non-zero `vocabSize`.
  2. Per-input language detection runs the rule with `lang: 'fr'` but a different `ctx.vocab` snapshot — perhaps the wrong language's vocab if the fix's `LANGUAGE_CHANGED` broadcast hasn't propagated to the active content-script seam yet (the popup.js fix moves the broadcast out of try/catch but doesn't change downstream handlers).
  3. The rule's adverb-phrase matcher (multi-word `pendant que`) has a normalisation bug — accent strip / case / punctuation handling — that's masked in Node (where the fixture runner may pre-normalise) but bites in browser.
  4. `frAspectAdverbs.imparfait` is empty in browser despite the source data being in `extension/data/fr.json:187319`. Could be that vocab-seam-core's `collectAdverbs` reads a different `raw` snapshot in browser vs Node.

**Pre-flight evidence (re-walk):**
- ext_version: 2.9.19 (sideloaded from `/Users/geirforbord/Papertek/leksihjelp/extension/` on Mac)
- Same Chrome 147.0.7727.117 arm64
- Reload at ~17:55 2026-05-01

**Status:** REOPENED. Branch C fix stays in tree (genuine partial improvement); follow-up plan 38-01.2 needs to diagnose actual fr-aspect-hint silence path.

**Decision points for next plan (38-01.2):**
- (a) Add browser-side instrumentation to `fr-aspect-hint.js` (log `vocab.frAspectAdverbs.imparfait.phrases.length`, log adverb hits, log token stream — under a `__lexiDebug` flag) and ask walker to re-run with DevTools open. This is the diagnostic-first approach the original plan called for.
- (b) Add a test that exercises the seam end-to-end in jsdom rather than Node — would have caught this gap.
- (c) Do not promote the v2.9.19 GitHub Release from Draft to Latest — the regression in fr-aspect-hint was the *primary* F38-1 closure check.

## Resolution v2 (2026-05-01, v2.9.20 — Plan 38-01.2)

**Actual proximate cause (vs Plan 38-01.1's BRANCH C fix which was real-but-symptomatic):**

The Papertek API at `https://papertek-vocabulary.vercel.app/api/vocab/v1/bundle/fr`
serves an FR `generalbank` with 865 keys MISSING the 3 aspect meta entries:
- `aspect_passe_compose_adverbs`
- `aspect_imparfait_adverbs`
- `aspect_choice_pedagogy`

Root cause upstream: papertek-vocabulary's `lib/_bundle.js#resolveLangPath`
reads from `vocabulary/lexicon/fr/generalbank.json` (which lacks aspects)
instead of `vocabulary/core/fr/generalbank.json` (which has them).

The bundled `extension/data/fr.json` HAS all 866 entries (sourced from
`core/fr/`), but vocab-seam's `BUNDLED_LANGS = ['nb']` forces FR through the
API path, so the bundled file was never read for FR. Without these entries,
`buildIndexes` produces empty `frAspectAdverbs.imparfait.phrases` and the
fr-aspect-hint rule silently fell back to "no triggers found" on every FR
input — including the canonical UAT-EXT-01 sentence
`Pendant que je marchais, j'ai vu un chien.`.

The 38-01.1 Branch C fix (nb-typo-fuzzy elision strip) was a real-but-
symptomatic improvement: it correctly stopped NB-typo from false-firing on
elided FR auxiliaries like `j'ai`. But it did NOT address why fr-aspect-hint
itself stayed silent. That required this data-load fix.

**Fix (file:line):**
- `extension/content/vocab-seam.js:122-138` — added `loadFrBundledOnce()`
  cached helper (single-fetch promise per page lifetime).
- `extension/content/vocab-seam.js:185-216` — added defensive backfill block
  at the top of `buildAndApply`. When `lang === 'fr'` AND the cached/fetched
  payload's `generalbank` is missing one or more of the 3 aspect meta
  entries, overlay them from bundled `extension/data/fr.json` BEFORE calling
  `core.buildIndexes`. Narrow and surgical — does NOT broadly merge bundled
  data over API data; only the specific known-stripped meta entries.

**Why upstream papertek-vocabulary fix was NOT chosen:**
- Cross-app blast radius: lockdown webapp + skriveokt-zero are also consumers.
- User explicitly constrained Plan 38-01.2 to leksihjelp-only (live staging
  testing on stb-lockdown.app for Saturday).
- Defensive seam-side fix is the right tradeoff: contained, reversible,
  doesn't invalidate sibling-repo deployments.

**Regression artifact:** `scripts/check-fr-bundle-completeness.js` exercises
the seam buildIndexes contract end-to-end against BOTH bundled fr.json AND
the live API response (with simulated backfill applied to the API path,
mirroring what `vocab-seam.js#buildAndApply` does in browser). The existing
`check-fixtures` runner bypasses the seam (passes raw indexes directly to
`core.check`) so it could not catch this class — same Node-vs-browser
divergence root cause as Phase 36-02 INFRA-10.

**Browser re-walk DEFERRED:** walker is on stb-lockdown.app for Saturday
testing; v2.9.20 sideload would invalidate that environment. Re-walk happens
post-Saturday-staging per user discretion. v2.9.20 GitHub Release ships as
DRAFT pending re-walk.
