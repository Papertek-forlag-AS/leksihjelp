---
phase: 02.1-close-sc-4-bundle-size-cap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-bundle-size.js
  - scripts/check-bundle-size.test.js
autonomous: true
requirements: [SC-4, DATA-03]
must_haves:
  truths:
    - "`npm run check-bundle-size` exits 0 against the current ~10.11 MiB zip"
    - "`node scripts/check-bundle-size.test.js` exits 0 — all behavior tests still pass"
    - "Script and test agree on `CEILING_BYTES` value (both 20 * 1024 * 1024)"
    - "No 'publicly-stated promise' framing remains in script header comment, failure-mode console output, or test name"
  artifacts:
    - path: "scripts/check-bundle-size.js"
      provides: "Bundle-size release gate at 20 MiB ceiling"
      contains: "CEILING_BYTES = 20 * 1024 * 1024"
    - path: "scripts/check-bundle-size.test.js"
      provides: "Behavior test mirror of the gate's ceiling"
      contains: "CEILING_BYTES = 20 * 1024 * 1024"
  key_links:
    - from: "scripts/check-bundle-size.test.js"
      to: "scripts/check-bundle-size.js"
      via: "shared CEILING_BYTES value (independently declared in each file, but must match)"
      pattern: "CEILING_BYTES = 20 \\* 1024 \\* 1024"
    - from: "scripts/check-bundle-size.js"
      to: "backend/public/lexi-extension.zip"
      via: "fs.statSync zip size compared to CEILING_BYTES"
      pattern: "zipSize <= CEILING_BYTES"
---

<objective>
Bump the bundle-size release gate's `CEILING_BYTES` constant from 10 MiB to 20 MiB in both `scripts/check-bundle-size.js` and its test mirror `scripts/check-bundle-size.test.js`, and update the surrounding script prose (header comment block, failure-mode console output) plus the test name to reflect the new number and drop the "publicly-stated promise" framing.

Purpose: Close Phase 2 SC-4 by raising the gate to a number the current zip already passes (10.11 MiB current zip vs new 20 MiB cap = ~9.89 MiB headroom). Preserves the gate's purpose as a regression-detection guard against accidental bundle growth (pretty-printed JSON checked in, large asset added silently).

Output: Modified script + test file, both passing. `npm run check-bundle-size` exits 0; `node scripts/check-bundle-size.test.js` exits 0.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-close-sc-4-bundle-size-cap/02.1-CONTEXT.md
@.planning/phases/02.1-close-sc-4-bundle-size-cap/02.1-RESEARCH.md
@scripts/check-bundle-size.js
@scripts/check-bundle-size.test.js

<scope_exclusions>
DO NOT touch any of the following — they are out of scope:
- `extension/data/*.json` — no data file edits
- `scripts/package-extension.js` — packaging pipeline unchanged
- `scripts/sync-vocab.js` — sync pipeline unchanged
- `popup.js`, `vocab-seam.js`, `word-prediction.js`, `floating-widget.js` — no extension source edits
- `package.json` — no script entry changes
- Any file under `.planning/phases/02-data-layer-frequency-bigrams-typo-bank/` — historical archive, untouched
- `.planning/STATE.md` historical decision-log entries (lines ~103, 127 historical context) — historical record
- Any docs (CLAUDE.md, PROJECT.md, ROADMAP.md, REQUIREMENTS.md, .planning/research/*.md) — handled by 02.1-02-PLAN.md in parallel

This plan ONLY touches the two `scripts/check-bundle-size*.js` files.
</scope_exclusions>

<interfaces>
<!-- Current state of the gate (from RESEARCH.md verified call-site map). -->
<!-- The architecture is preserved — only the threshold number and the surrounding prose move. -->

scripts/check-bundle-size.js (162 lines):
- Line 7: header comment "exits 0 if the zip is ≤ 10 MiB or 1 if it exceeds it"
- Line 9: header comment "The 10 MiB ceiling is ROADMAP success criterion #4 and a publicly-stated promise."
- Line 17: header comment "0 — zip ≤ 10 MiB (PASS)"
- Line 18: header comment "1 — zip > 10 MiB OR packaging itself failed (FAIL)"
- Line 31: const CEILING_BYTES = 10 * 1024 * 1024; // 10 MiB = 10,485,760
- Lines 149-154: failure-mode console.log with "publicly-stated promise" framing

scripts/check-bundle-size.test.js (151 lines):
- Line 27: const CEILING_BYTES = 10 * 1024 * 1024;
- Line 124: test('Test 4/5 (behavior): exit code matches reported bytes vs 10 MiB cap', () => {
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump CEILING_BYTES + rewrite script header/failure prose in scripts/check-bundle-size.js</name>
  <files>scripts/check-bundle-size.js</files>
  <action>
Edit `scripts/check-bundle-size.js` in five places. Use the Edit tool, NOT a global find-and-replace, because each location has slightly different surrounding context.

**Edit 1 — Header comment block (lines 6-7):**
Change:
```
 * Runs `npm run package`, measures the resulting zip, prints a per-directory
 * byte breakdown, and exits 0 if the zip is ≤ 10 MiB or 1 if it exceeds it.
```
To:
```
 * Runs `npm run package`, measures the resulting zip, prints a per-directory
 * byte breakdown, and exits 0 if the zip is ≤ 20 MiB or 1 if it exceeds it.
```

**Edit 2 — Header comment block (lines 9-11):**
Change:
```
 * The 10 MiB ceiling is ROADMAP success criterion #4 and a publicly-stated
 * promise. The script does NOT bypass the cap — a FAIL + breakdown is the
 * correct signal that bundle-size remediation is needed.
```
To:
```
 * The 20 MiB ceiling is an internal engineering guard against accidental
 * bundle growth (e.g., a pretty-printed JSON checked in by mistake, a large
 * asset added silently). Chrome Web Store accepts up to 2 GB; this number is
 * ours, not the store's. A FAIL + breakdown is the correct signal that
 * something has changed and warrants investigation before release.
```

**Edit 3 — Header comment exit-codes block (lines 17-18):**
Change:
```
 *   0 — zip ≤ 10 MiB (PASS)
 *   1 — zip > 10 MiB OR packaging itself failed (FAIL)
```
To:
```
 *   0 — zip ≤ 20 MiB (PASS)
 *   1 — zip > 20 MiB OR packaging itself failed (FAIL)
```

**Edit 4 — Constant declaration (line 31):**
Change:
```
const CEILING_BYTES = 10 * 1024 * 1024; // 10 MiB = 10,485,760
```
To:
```
const CEILING_BYTES = 20 * 1024 * 1024; // 20 MiB = 20,971,520 (raised 2026-04-19, Phase 02.1; was 10 MiB)
```
The trailing date-stamped comment is per Claude's Discretion in CONTEXT.md (Open Question 2 in RESEARCH.md). Include it — the script is human-read at release time and the rationale is one search away.

**Edit 5 — Failure-mode console output (lines 149-154):**
Change:
```js
    console.log(
      '\nThe 10 MiB ceiling is ROADMAP success criterion #4 and a publicly-stated promise.\n' +
      'Do NOT bypass by silently editing CEILING_BYTES. Investigate the breakdown above\n' +
      'and remediate (e.g., strip bundled audio + fetch on first use; trim rarely-used vocab;\n' +
      'split the lexicon; or open a new phase to make the product decision with user sign-off).'
    );
```
To:
```js
    console.log(
      '\nThe 20 MiB ceiling is an internal engineering guard, not a Chrome Web Store limit.\n' +
      'A FAIL means the zip grew unexpectedly — investigate the breakdown above. The fix is\n' +
      'usually a regression (e.g., a pretty-printed JSON checked in, a large new asset\n' +
      'committed unintentionally). If the growth is intentional and the new size is\n' +
      'justified, raise CEILING_BYTES in a new phase with explicit sign-off.'
    );
```

**Why these specific edits (not a sed/global-replace):** RESEARCH.md Pitfall 3 + Common Pitfall #1 both warn that updating the constant without updating the surrounding prose leaves contradictory numbers a few lines away. All five edits move together as one atomic unit.
  </action>
  <verify>
    <automated>grep -n "CEILING_BYTES = 20 \* 1024 \* 1024" scripts/check-bundle-size.js && ! grep -n "10 MiB" scripts/check-bundle-size.js && ! grep -n "publicly-stated" scripts/check-bundle-size.js</automated>
  </verify>
  <done>scripts/check-bundle-size.js has CEILING_BYTES = 20 * 1024 * 1024 at line ~31; header comment block describes a 20 MiB internal engineering ceiling (no "publicly-stated promise" framing); failure-mode console.log describes the 20 MiB internal guard; grep for "10 MiB" returns zero hits in the file; grep for "publicly-stated" returns zero hits in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Bump CEILING_BYTES + update test name in scripts/check-bundle-size.test.js, then run both verification commands</name>
  <files>scripts/check-bundle-size.test.js</files>
  <action>
Edit `scripts/check-bundle-size.test.js` in two places, then run both verifications.

**Edit 1 — Constant declaration (line 27):**
Change:
```js
const CEILING_BYTES = 10 * 1024 * 1024;
```
To:
```js
const CEILING_BYTES = 20 * 1024 * 1024;
```

**Edit 2 — Test name string (line 124):**
Change:
```js
test('Test 4/5 (behavior): exit code matches reported bytes vs 10 MiB cap', () => {
```
To:
```js
test('Test 4/5 (behavior): exit code matches reported bytes vs 20 MiB cap', () => {
```

**Why both edits matter:** RESEARCH.md Pitfall 2 — if the test file's CEILING_BYTES drifts from the script's, the test still passes against the current 10.11 MiB zip (it's under both old and new caps), so the test appears green but its *intent* (assert gate matches the cap) is silently broken. The test name is a behavioral contract — it must describe the cap it actually asserts.

**After editing, run BOTH verifications in this exact order:**

1. `node scripts/check-bundle-size.test.js`
   - Expected: All behavior tests PASS, exit 0.
   - The test that previously asserted "10 MiB cap" now asserts "20 MiB cap" with the bumped CEILING_BYTES — the actual logic at lines 127-132 (`if (size <= CEILING_BYTES && result.status !== 0)` etc.) re-runs the gate via spawnSync and confirms exit code matches the new comparison.

2. `npm run check-bundle-size`
   - Expected: PASS, exit 0. The current zip is 10,599,772 bytes (10.11 MiB), well under the new 20 MiB cap. Headroom should report ~10,371,748 bytes (~9.89 MiB).
   - This re-runs `npm run package` which takes ~30-60 seconds (it minifies all 15 data/*.json files into the staging dir then zips). Be patient.
   - Expected output line: "PASS: 10.11 MiB (10,599,772 bytes) — under cap by 10,371,748 bytes (9.89 MiB headroom)" (numbers approximate; actual may differ if data has changed since 2026-04-19 verification).

**If `npm run check-bundle-size` exits 1:** something is wrong with the constant edit. Re-check both files have `CEILING_BYTES = 20 * 1024 * 1024`. Don't proceed until both verifications pass.

**Do NOT** edit any other file in this task. The CLAUDE.md / PROJECT.md / ROADMAP.md / REQUIREMENTS.md / research/ doc sweep is handled by 02.1-02-PLAN.md running in parallel.
  </action>
  <verify>
    <automated>node scripts/check-bundle-size.test.js && npm run check-bundle-size</automated>
  </verify>
  <done>scripts/check-bundle-size.test.js has CEILING_BYTES = 20 * 1024 * 1024 at line ~27; test name at line ~124 says "20 MiB cap"; `node scripts/check-bundle-size.test.js` exits 0 with all behavior tests reporting PASS; `npm run check-bundle-size` exits 0 with a PASS line showing the current zip (~10.11 MiB) under the new 20 MiB cap with ~9.89 MiB headroom.</done>
</task>

</tasks>

<verification>
After both tasks complete, all of the following must hold:

1. `grep -n "CEILING_BYTES = " scripts/check-bundle-size.js scripts/check-bundle-size.test.js` shows BOTH files at `20 * 1024 * 1024`.
2. `grep -n "10 MiB\|10 MB\|publicly-stated" scripts/check-bundle-size.js scripts/check-bundle-size.test.js` returns zero hits.
3. `node scripts/check-bundle-size.test.js` exits 0.
4. `npm run check-bundle-size` exits 0 (PASS line printed; headroom shown).

If any of these fail, the plan is not done.
</verification>

<success_criteria>
- [ ] scripts/check-bundle-size.js: CEILING_BYTES = 20 * 1024 * 1024 (line ~31), header block describes 20 MiB internal engineering guard, failure-mode console.log refreshed, no "publicly-stated promise" or "10 MiB"/"10 MB" hits remain in the file
- [ ] scripts/check-bundle-size.test.js: CEILING_BYTES = 20 * 1024 * 1024 (line ~27), Test 4/5 name string updated to "20 MiB cap"
- [ ] `node scripts/check-bundle-size.test.js` exits 0
- [ ] `npm run check-bundle-size` exits 0 with PASS message and headroom report
- [ ] No edits to extension/, package.json, package-extension.js, sync-vocab.js, .planning/research/, .planning/STATE.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, CLAUDE.md, or `.planning/phases/02-data-layer-frequency-bigrams-typo-bank/`
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-close-sc-4-bundle-size-cap/02.1-01-SUMMARY.md` documenting:
- Final CEILING_BYTES value (20 * 1024 * 1024 = 20,971,520 bytes)
- Test suite green (all behavior tests pass)
- Bundle gate green (`npm run check-bundle-size` exit 0; report current zip MiB and headroom)
- Files touched (exactly two: scripts/check-bundle-size.js + scripts/check-bundle-size.test.js)
- Sibling plan 02.1-02 status (running parallel; doc sweep handles all prose outside scripts/)
</output>
