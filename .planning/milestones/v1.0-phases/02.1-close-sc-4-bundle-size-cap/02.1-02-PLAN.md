---
phase: 02.1-close-sc-4-bundle-size-cap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/research/FEATURES.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/STACK.md
  - .planning/research/PITFALLS.md
  - .planning/STATE.md
autonomous: true
requirements: [SC-4, DATA-03]
must_haves:
  truths:
    - "All live (non-archive) docs reference a 20 MiB internal engineering ceiling, not a 10 MiB promise"
    - "ROADMAP.md SC-4 (Phase 2 success criterion #4) reads against the 20 MiB ceiling and verifies via `scripts/check-bundle-size.js` exit 0"
    - "REQUIREMENTS.md DATA-03 reads against the ~20 MiB internal bundle-size budget"
    - "CLAUDE.md Release Workflow step 2 describes the 20 MiB internal engineering ceiling (no 'publicly-stated promise' framing)"
    - "PROJECT.md Out-of-Scope and Constraints sections describe the 20 MiB internal budget"
    - ".planning/research/{FEATURES,ARCHITECTURE,STACK,PITFALLS}.md reference the 20 MiB ceiling, not 10 MB"
    - "STATE.md live SC-4 blocker entry is marked resolved (closed by Phase 02.1)"
    - "Historical archive under .planning/phases/02-data-layer-frequency-bigrams-typo-bank/ remains untouched"
    - "STATE.md historical decision-log entries (lines ~103, ~127 historical context) remain untouched"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project Release Workflow with corrected ceiling framing"
      contains: "20 MiB internal engineering ceiling"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 SC-4 reworded against new ceiling"
      contains: "stays under the internal engineering ceiling (20 MiB)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DATA-03 reworded against new ceiling"
      contains: "internal bundle-size budget (~20 MiB ceiling)"
    - path: ".planning/PROJECT.md"
      provides: "Out-of-Scope + Constraints describing internal budget"
      contains: "20 MiB"
    - path: ".planning/STATE.md"
      provides: "Live SC-4 blocker resolved"
      contains: "RESOLVED"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "scripts/check-bundle-size.js"
      via: "SC-4 references the script as the gate"
      pattern: "scripts/check-bundle-size\\.js"
    - from: "CLAUDE.md Release Workflow step 2"
      to: "scripts/check-bundle-size.js"
      via: "Release-time exit-0 contract"
      pattern: "npm run check-bundle-size"
    - from: ".planning/REQUIREMENTS.md DATA-03"
      to: ".planning/ROADMAP.md SC-4"
      via: "Both describe the same ceiling — number must match"
      pattern: "20 MiB|20\\s*MB"
---

<objective>
Sweep all live (non-archive) documentation that references the 10 MiB / 10 MB / "publicly-stated promise" bundle-size framing and rewrite it to honestly describe the new 20 MiB internal engineering ceiling. This includes the project README (CLAUDE.md), the project context (PROJECT.md), the active milestone roadmap (ROADMAP.md), requirements (REQUIREMENTS.md), forward-looking research notes (.planning/research/*.md), and the live SC-4 blocker entry in STATE.md.

Purpose: After Plan 02.1-01 bumps the gate constant, the surrounding prose must reflect the new number — otherwise future readers see contradictory ceilings (script says 20 MiB, docs say 10 MiB) and don't know which is authoritative. This is the prose half of the atomic edit unit.

Output: 9 files updated. Grep for "publicly-stated promise" and "10 MiB ceiling"/"10 MB ceiling" in live docs returns zero hits (excluding the historical archive and STATE.md historical decision-log entries). SC-4 in ROADMAP and DATA-03 in REQUIREMENTS describe the same ceiling number consistently.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-close-sc-4-bundle-size-cap/02.1-CONTEXT.md
@.planning/phases/02.1-close-sc-4-bundle-size-cap/02.1-RESEARCH.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<scope_exclusions>
DO NOT touch any of the following — they are out of scope for this plan and explicitly excluded by CONTEXT.md + RESEARCH.md:

**Out of scope (handled by 02.1-01-PLAN.md in parallel):**
- `scripts/check-bundle-size.js` — script edits (constant + header + failure prose)
- `scripts/check-bundle-size.test.js` — test edits (constant + test name)

**Out of scope (HISTORICAL ARCHIVE — must remain faithful to what was decided at the time):**
- ANY file under `.planning/phases/02-data-layer-frequency-bigrams-typo-bank/` (PLAN, SUMMARY, RESEARCH, CONTEXT, AUDIT, VERIFICATION — all of it)
- This is a HARD exclusion. Per RESEARCH.md Pitfall 3: editing the archive retrofits a different past. NO edits there.

**Out of scope inside .planning/STATE.md (CRITICAL DISCIPLINE):**
- Lines ~103 and the historical "Outcome B accepted over silent-bypass" decision-log entry under "## Accumulated Context > Decisions" — historical record of what was thought during Plan 02-04. LEAVE UNTOUCHED.
- Per RESEARCH.md Pitfall 4: STATE.md mixes historical log + current state. The two need different treatment.
- The historical Phase 02-05 decision rows that mention "publicly-stated 10 MB promise" are also untouched (they document what was thought at that time).
- ONLY the live `Blockers/Concerns` SC-4 OPEN entry (current line ~127, the "Phase 2 bundle-size 10 MiB ceiling contingency — SC-4 STILL OPEN 2026-04-19" entry) is in scope — to be marked RESOLVED, not deleted.

**Out of scope (extension source — explicitly forbidden by CONTEXT.md):**
- Any file under `extension/`
- `scripts/sync-vocab.js`, `scripts/package-extension.js`
- `package.json`, `manifest.json`
- `backend/public/index.html` (the landing page; verified by RESEARCH.md to make NO size promise — no edit needed)
- Any other backend file
</scope_exclusions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite live ceiling prose in CLAUDE.md + PROJECT.md + .planning/research/{FEATURES,ARCHITECTURE,STACK,PITFALLS}.md</name>
  <files>CLAUDE.md, .planning/PROJECT.md, .planning/research/FEATURES.md, .planning/research/ARCHITECTURE.md, .planning/research/STACK.md, .planning/research/PITFALLS.md</files>
  <action>
Edit each of the six files using the Edit tool — one targeted edit per call site. NO global find-and-replace; each location has different context and the prose-rewrite is bespoke per RESEARCH.md Examples 4-9.

**Per RESEARCH.md Open Question 1:** the four `.planning/research/*.md` files ARE in scope (forward-looking reference material; CONTEXT.md authorizes the rewrite via the user's CONTEXT-resolution decision noted in the planning prompt — "rewrite .planning/research/*.md too: YES, include them — forward-looking reference material, not historical archive").

---

**File 1: CLAUDE.md (lines 348-350) — Release Workflow note**

Change the entire two-line block:
```
2. Verify the packaged extension stays under the 10 MiB ceiling:
   - `npm run check-bundle-size` — must exit 0. The script runs `npm run package` (which minifies `data/*.json` on the way into the zip), measures the resulting zip, and prints a per-directory byte breakdown.
   - If it exits 1 (zip over cap), stop and investigate the breakdown. The fix is almost always a data-file growth regression; do NOT bypass the cap without user sign-off — the 10 MB ceiling is a ROADMAP success criterion and a publicly-stated promise.
```

To:
```
2. Verify the packaged extension stays under the 20 MiB internal engineering ceiling:
   - `npm run check-bundle-size` — must exit 0. The script runs `npm run package` (which minifies `data/*.json` on the way into the zip), measures the resulting zip, and prints a per-directory byte breakdown.
   - If it exits 1 (zip over cap), stop and investigate the breakdown. The fix is almost always a data-file growth regression; do NOT bypass the cap by silently editing `CEILING_BYTES`. The 20 MiB number is our own (not Chrome Web Store's — they accept up to 2 GB) and exists to catch accidental growth. If the growth is intentional, raise the cap in a new phase with explicit sign-off.
```

---

**File 2: .planning/PROJECT.md (line 52) — Out of Scope bullet**

Change:
```
- **Bundle-size growth beyond current ~10MB** — bigrams and frequency tables should stay within the existing budget; any growth must be justified.
```

To:
```
- **Bundle-size growth beyond current internal budget (~20 MiB)** — bigrams, frequency tables, and any added assets should stay within the engineering ceiling enforced by `scripts/check-bundle-size.js`; any planned growth that approaches the cap must be justified.
```

---

**File 3: .planning/PROJECT.md (line 79) — Constraints bullet**

Change:
```
- **Bundle size**: Current ~10 MB. Data additions (frequency tables, bigrams, typo expansions) must stay within that budget.
```

To:
```
- **Bundle size**: Current ~10.1 MiB; internal engineering ceiling 20 MiB enforced by `scripts/check-bundle-size.js`. Data additions (frequency tables, bigrams, typo expansions) must stay within that budget.
```

---

**File 4: .planning/research/FEATURES.md (line 34) — Works-offline table row**

Change the trailing column:
```
... | Bundle-size constraint (~10MB) flows from this |
```

To:
```
... | Bundle-size constraint (~20 MiB internal ceiling) flows from this |
```

---

**File 5: .planning/research/ARCHITECTURE.md — TWO edits (lines 383 + 519)**

Line 383 — change:
```
- ✓ Size: top-10k NB words at 4-byte ranks ≈ 100–150 KB gzipped. Well inside the 10 MB budget.
```
To:
```
- ✓ Size: top-10k NB words at 4-byte ranks ≈ 100–150 KB gzipped. Well inside the 20 MiB internal budget.
```

Line 519 — change:
```
4. **Fourth (not in this milestone, monitor only): memory.** Bundle currently ~10 MB; bigram/freq/rule additions must stay inside the budget. Add a `npm run size-report` step that prints per-file sizes — early warning beats reactive trimming.
```
To:
```
4. **Fourth (not in this milestone, monitor only): memory.** Bundle currently ~10.1 MiB; internal engineering ceiling 20 MiB enforced by `scripts/check-bundle-size.js`. Bigram/freq/rule additions must stay inside the budget. The `npm run check-bundle-size` gate (Plan 02-04) prints per-directory sizes — early warning beats reactive trimming.
```

---

**File 6: .planning/research/STACK.md — TWO edits (lines 112 + 206)**

Line 112 — change:
```
- One float per word × 25k words ≈ 200 KB uncompressed, ~60 KB gzipped. Well within the 10 MB bundle budget.
```
To:
```
- One float per word × 25k words ≈ 200 KB uncompressed, ~60 KB gzipped. Well within the 20 MiB internal bundle budget.
```

Line 206 — change:
```
**If** bundle size grows beyond the 10 MB ceiling because of bigrams:
```
To:
```
**If** bundle size grows beyond the 20 MiB internal ceiling because of bigrams:
```

---

**File 7: .planning/research/PITFALLS.md (line 397) — Pitfall body**

Change:
```
**What goes wrong:** Adding a frequency dictionary for ranking (see Pitfall 1) pushes bundle past 10 MB. PROJECT.md constraint explicitly caps bundle growth.
```

To:
```
**What goes wrong:** Adding a frequency dictionary for ranking (see Pitfall 1) pushes bundle past the 20 MiB internal ceiling. PROJECT.md constraint explicitly caps bundle growth.
```

---

**Verify after these 6 files:** Run the grep below. Expected: zero hits in any of the six edited files.

```bash
grep -n "publicly-stated\|publicly stated\|10 MiB ceiling\|10 MB ceiling\|10\s*MB budget\|10 MB budget\|10MB\|~10 MB\|~10MB" \
  CLAUDE.md \
  .planning/PROJECT.md \
  .planning/research/FEATURES.md \
  .planning/research/ARCHITECTURE.md \
  .planning/research/STACK.md \
  .planning/research/PITFALLS.md
```
  </action>
  <verify>
    <automated>! grep -E "publicly-stated|publicly stated|10 MiB ceiling|10 MB ceiling|10\s*MB budget|10MB|~10\s*MB" CLAUDE.md .planning/PROJECT.md .planning/research/FEATURES.md .planning/research/ARCHITECTURE.md .planning/research/STACK.md .planning/research/PITFALLS.md</automated>
  </verify>
  <done>All six files edited. grep for "publicly-stated"/"10 MiB ceiling"/"10 MB ceiling"/"10MB"/"10 MB budget"/"~10 MB"/"~10MB" returns zero hits across these six files. CLAUDE.md Release Workflow step 2 describes 20 MiB internal engineering ceiling. PROJECT.md Out-of-Scope + Constraints reference 20 MiB internal budget. All four research files reference 20 MiB internal ceiling.</done>
</task>

<task type="auto">
  <name>Task 2: Reword SC-4 in ROADMAP.md + DATA-03 in REQUIREMENTS.md + Phase 2 goal</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Edit ROADMAP.md in TWO places (Phase 2 Goal at line 42, SC-4 at line 49) and REQUIREMENTS.md in ONE place (DATA-03 at line 23). The number must match across both files (RESEARCH.md Pitfall 5).

---

**File 1: .planning/ROADMAP.md (line 42) — Phase 2 Goal**

Change:
```markdown
**Goal**: The data foundations that pay twice — frequency tables and expanded bigrams for NB/NN plus additional typo-bank coverage in `papertek-vocabulary` — are bundled and synced, keeping the extension under the 10 MB budget.
```

To:
```markdown
**Goal**: The data foundations that pay twice — frequency tables and expanded bigrams for NB/NN plus additional typo-bank coverage in `papertek-vocabulary` — are bundled and synced, keeping the extension within the internal bundle-size budget (20 MiB).
```

---

**File 2: .planning/ROADMAP.md (line 49) — SC-4**

Change:
```markdown
  4. Total packaged extension zip size from `npm run package` stays within the 10 MB ceiling, verified in the release checklist
```

To:
```markdown
  4. Total packaged extension zip size from `npm run package` stays under the internal engineering ceiling (20 MiB), verified by `scripts/check-bundle-size.js` exit 0 in the release checklist
```

---

**File 3: .planning/REQUIREMENTS.md (line 23) — DATA-03**

Change:
```markdown
- [x] **DATA-03**: Extend bundled bigram data for NB and NN with high-frequency pairs derived from NB N-gram 2021; same schema as the existing `bigrams-{lang}.json` files; respect the bundle-size budget (~10 MB ceiling)
```

To:
```markdown
- [x] **DATA-03**: Extend bundled bigram data for NB and NN with high-frequency pairs derived from NB N-gram 2021; same schema as the existing `bigrams-{lang}.json` files; respect the internal bundle-size budget (~20 MiB ceiling)
```

---

**Number-consistency check after editing both files:** the number `20 MiB` must appear in all three rewritten lines, AND the old `10 MB` / `10 MiB ceiling` must not appear. Verify with:

```bash
grep -n "10 MB\|10 MiB ceiling\|10 MB ceiling\|10\s*MB budget" .planning/ROADMAP.md .planning/REQUIREMENTS.md
```
Expected: zero hits.

```bash
grep -n "20 MiB" .planning/ROADMAP.md .planning/REQUIREMENTS.md
```
Expected: at least 3 hits (the three edits above).

**Do NOT** touch the Phase 02.1 stub at ROADMAP.md lines 58-66 — `update_roadmap` step in the planner workflow handles that separately. This task only updates Phase 2 Goal + SC-4 + DATA-03.
  </action>
  <verify>
    <automated>! grep -E "10 MB ceiling|10 MiB ceiling|10\s*MB budget|under the 10 MB" .planning/ROADMAP.md .planning/REQUIREMENTS.md && grep -c "20 MiB" .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>ROADMAP.md Phase 2 Goal (line ~42) reads "internal bundle-size budget (20 MiB)"; SC-4 (line ~49) reads "stays under the internal engineering ceiling (20 MiB), verified by `scripts/check-bundle-size.js` exit 0"; REQUIREMENTS.md DATA-03 (line ~23) reads "internal bundle-size budget (~20 MiB ceiling)". grep for "10 MB ceiling"/"10 MiB ceiling"/"10 MB budget" in both files returns zero hits. grep for "20 MiB" in both files returns >= 3 hits.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve live SC-4 blocker in STATE.md (preserve historical decision rows untouched) + run final repo-wide verification grep</name>
  <files>.planning/STATE.md</files>
  <action>
**CRITICAL DISCIPLINE (per RESEARCH.md Pitfall 4):** STATE.md mixes historical decision-log entries (which document what was thought at the time and MUST stay faithful to history) with the live `## Blockers/Concerns` section (which describes current state).

This task makes ONE edit to ONE entry — the live SC-4 OPEN blocker — to mark it RESOLVED by Phase 02.1.

---

**Step 1: Read the current STATE.md Blockers/Concerns section.**

Use the Read tool to load `.planning/STATE.md` lines 120-140 (the Blockers/Concerns section containing the live SC-4 entry).

The live entry starts with:
```
- **Phase 2 bundle-size 10 MiB ceiling contingency — SC-4 STILL OPEN 2026-04-19:** Plan 02-04 shipped the `check-bundle-size` gate ...
```

This entry is the LIVE blocker — currently around line 127. It is multi-paragraph and contains BOTH historical context (Plan 02-04 outcome, Plan 02-05 audit findings) AND the current "remaining remediation candidates" list.

---

**Step 2: REPLACE the entire live SC-4 blocker entry** with a short RESOLVED marker pointing to Phase 02.1.

Use the Edit tool with `old_string` matching the entire multi-paragraph blocker bullet (start at "- **Phase 2 bundle-size 10 MiB ceiling contingency — SC-4 STILL OPEN 2026-04-19:**", end at the final period of the paragraph that ends "... that's the intended signal, not a bug.").

Replace with:
```
- **Phase 2 bundle-size SC-4 — RESOLVED 2026-04-19 by Phase 02.1.** The internal engineering ceiling was raised from 10 MiB to 20 MiB in `scripts/check-bundle-size.js` and `scripts/check-bundle-size.test.js`; ROADMAP SC-4 + REQUIREMENTS DATA-03 reworded; CLAUDE.md Release Workflow + PROJECT.md + .planning/research/*.md prose corrected to drop the false "publicly-stated promise" framing. The current zip (10.11 MiB / 10,599,772 bytes) now passes the gate with ~9.89 MiB headroom. The gate's purpose shifts from "enforce 10 MiB promise" to "catch accidental bundle-growth regressions" — same exit-0/1 semantics, same Release Workflow integration, same script architecture. Phase 02.1 explicitly chose NOT to pursue the English-removal / audio-strip / data-trim remediation paths from 02-05-AUDIT.md; those remain documented in the audit as future options if bundle size ever becomes a real constraint again.
```

---

**Step 3: DO NOT TOUCH any other STATE.md content.** Specifically:

- The `## Accumulated Context > Decisions` section (lines ~66-112): contains historical decision rows for Phase 01, Phase 02-01..05. **LEAVE UNTOUCHED.** Specifically the rows at line ~103 ("Outcome B accepted over silent-bypass") and the [Phase 02-05] rows around line ~108-111 stay verbatim — they document what was thought at the time.
- The `## Roadmap Evolution` line ("Phase 02.1 inserted after Phase 2: Close SC-4 bundle-size cap (URGENT)" at ~line 115): leave as-is. The "completed" marker is added separately by `/gsd:resume-work` or `/gsd:verify-work` tooling at phase finalization.
- `## Current Position`, `## Performance Metrics`, `## Session Continuity`, `## Pending Todos`: out of scope for this task. The plan-phase orchestrator + execute-phase + finalization workflow handle those.
- The OTHER blocker entries in `## Blockers/Concerns` (DATA-02 cross-app rollback, NN phrase-infinitive triage, missing fin_adj entry, Phase 3 code-switching calibration, Phase 4 særskriving thresholds): out of scope. Touch only the SC-4 blocker.

---

**Step 4: After the STATE.md edit, run the final repo-wide verification grep** (RESEARCH.md Verification Commands #3 + #4):

```bash
# Should return ZERO hits in live docs (excluding historical archive)
grep -rn "publicly-stated promise\|publicly stated promise" \
  --include="*.md" --include="*.js" \
  --exclude-dir=node_modules \
  --exclude-dir=".git" \
  --exclude-dir="02-data-layer-frequency-bigrams-typo-bank" \
  .
```

Expected: zero hits OR only hits inside `.planning/phases/02.1-close-sc-4-bundle-size-cap/02.1-RESEARCH.md` and `02.1-CONTEXT.md` (research files quote the old phrasing as context — ALLOWED) and `02.1-01-PLAN.md` and `02.1-02-PLAN.md` (this plan and the sibling plan quote the old phrasing in `<action>` blocks for executors — ALLOWED).

```bash
# Should return ZERO hits in live docs
grep -rn "10\s*MiB ceiling\|10\s*MB ceiling\|10\s*MB budget" \
  --include="*.md" --include="*.js" \
  --exclude-dir=node_modules \
  --exclude-dir=".git" \
  --exclude-dir="02-data-layer-frequency-bigrams-typo-bank" \
  .
```

Expected: zero hits OR only hits inside `.planning/phases/02.1-close-sc-4-bundle-size-cap/*.md` (this phase's own research/context/plan files, which legitimately quote the old phrasing for context).

If either grep returns hits OUTSIDE the allowed locations (this phase's own files + the excluded historical archive), there is drift somewhere — re-check Tasks 1, 2, and the script edits in plan 02.1-01.
  </action>
  <verify>
    <automated>grep -c "RESOLVED 2026-04-19 by Phase 02.1" .planning/STATE.md && ! grep -E "(SC-4 STILL OPEN|Phase 2 bundle-size 10 MiB ceiling contingency)" .planning/STATE.md | grep -v "^$"</automated>
  </verify>
  <done>STATE.md live SC-4 blocker is replaced with a single short "RESOLVED 2026-04-19 by Phase 02.1" entry; the historical decision-log rows under `## Accumulated Context > Decisions` are unchanged; `grep -rn "publicly-stated promise"` across live docs (excluding `.planning/phases/02-data-layer-frequency-bigrams-typo-bank/`) returns zero hits or only hits inside `.planning/phases/02.1-close-sc-4-bundle-size-cap/`; `grep -rn "10 MiB ceiling\|10 MB ceiling"` across live docs (with same exclusion) returns zero hits or only Phase 02.1's own files.</done>
</task>

</tasks>

<verification>
After all three tasks complete, all of the following must hold:

1. `grep -rn "publicly-stated promise" --include="*.md" --include="*.js" --exclude-dir=node_modules --exclude-dir=".git" --exclude-dir="02-data-layer-frequency-bigrams-typo-bank" .` returns ZERO hits OR only hits inside `.planning/phases/02.1-close-sc-4-bundle-size-cap/` (this phase's own research/context/plan files quoting the old phrasing for context).

2. `grep -rn "10 MiB ceiling\|10 MB ceiling\|10\s*MB budget" --include="*.md" --include="*.js" --exclude-dir=node_modules --exclude-dir=".git" --exclude-dir="02-data-layer-frequency-bigrams-typo-bank" .` returns ZERO hits OR only hits inside `.planning/phases/02.1-close-sc-4-bundle-size-cap/`.

3. `grep -n "20 MiB" .planning/ROADMAP.md .planning/REQUIREMENTS.md CLAUDE.md .planning/PROJECT.md` returns at least 5 hits (Phase 2 Goal + SC-4 + DATA-03 + CLAUDE.md step 2 + PROJECT.md Constraints, plus possibly the Out-of-Scope bullet).

4. `git diff --stat .planning/phases/02-data-layer-frequency-bigrams-typo-bank/` shows ZERO changes (historical archive untouched).

5. `.planning/STATE.md` contains "RESOLVED 2026-04-19 by Phase 02.1" exactly once.

6. `.planning/STATE.md` historical decision rows ("Outcome B accepted over silent-bypass" line ~103, [Phase 02-05] rows lines ~108-111) are byte-identical to pre-edit.

If any of the above fails, the plan is not done.
</verification>

<success_criteria>
- [ ] CLAUDE.md line ~348-350 (Release Workflow step 2): describes 20 MiB internal engineering ceiling, no "publicly-stated promise" framing
- [ ] .planning/PROJECT.md line ~52 (Out of Scope): describes 20 MiB internal budget
- [ ] .planning/PROJECT.md line ~79 (Constraints): describes 20 MiB internal engineering ceiling
- [ ] .planning/research/FEATURES.md line ~34: 20 MiB
- [ ] .planning/research/ARCHITECTURE.md lines ~383, ~519: 20 MiB
- [ ] .planning/research/STACK.md lines ~112, ~206: 20 MiB
- [ ] .planning/research/PITFALLS.md line ~397: 20 MiB
- [ ] .planning/ROADMAP.md line ~42 (Phase 2 Goal): 20 MiB
- [ ] .planning/ROADMAP.md line ~49 (SC-4): 20 MiB + references `scripts/check-bundle-size.js` exit 0
- [ ] .planning/REQUIREMENTS.md line ~23 (DATA-03): ~20 MiB
- [ ] .planning/STATE.md: live SC-4 blocker REPLACED with "RESOLVED 2026-04-19 by Phase 02.1" marker
- [ ] .planning/STATE.md: historical decision-log rows (Decisions section, Phase 02-04/02-05 rows) UNCHANGED
- [ ] `.planning/phases/02-data-layer-frequency-bigrams-typo-bank/` directory: ZERO changes
- [ ] No edits to scripts/check-bundle-size.js, scripts/check-bundle-size.test.js, package.json, extension/, backend/, scripts/sync-vocab.js, scripts/package-extension.js
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-close-sc-4-bundle-size-cap/02.1-02-SUMMARY.md` documenting:
- 9 files edited (list them with line numbers)
- Final repo-wide grep results (publicly-stated promise: 0 hits in live docs; 10 MiB ceiling: 0 hits in live docs)
- STATE.md SC-4 blocker resolution marker added
- Historical archive untouched (verified by `git diff --stat .planning/phases/02-data-layer-frequency-bigrams-typo-bank/`)
- Sibling plan 02.1-01 status (running parallel; script + test handle the constant + script-internal prose)
</output>
