---
phase: 02-data-layer-frequency-bigrams-typo-bank
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/nb/typo.jsonl
  - fixtures/nn/typo.jsonl
  - extension/data/nb.json
  - extension/data/nn.json
autonomous: false
requirements:
  - DATA-02

must_haves:
  truths:
    - "Before the sync, 4-6 NEW hand-authored NB typo fixture cases are seeded in fixtures/nb/typo.jsonl that use typos currently ABSENT from the Phase-1 typo bank (confirmed by a pre-sync fixture run: each new case FAILS with a 'missing expected finding' for rule_id=typo) — this makes the post-sync recall delta operationally measurable (addresses checker warning on SC-2 measurability against F1=1.000 Phase-1 baseline)"
    - "After npm run sync-vocab, the same 4-6 new NB typo fixture cases all PASS (F1 on [nb/typo] remains ≥ 1.000 but over a LARGER corpus — recall grew from 11 cases to 15-17 cases, all passing)"
    - "After npm run sync-vocab, extension/data/nb.json has visibly more typo strings than before (success criterion #2: 'visibly larger typo bank') — target ≥ +30% NB typo strings"
    - "extension/data/nn.json also grows in typo coverage (target ≥ +30% NN typo strings)"
    - "npm run check-fixtures exits 0 on the EXPANDED fixture corpus. ZERO regressions on nn/clean.jsonl and nb/clean.jsonl (no new false positives on NN dialect words — Pitfall 4). [nb/typo] F1 ≥ 1.000 on the grown 15-17-case corpus AND [nn/typo] F1 ≥ 1.000 on its 10-case corpus (NN typo coverage must not regress even if no NN cases were added)"
    - "The three known-baseline data defects from Phase 1's Plan 01-03 SUMMARY are fixed at the papertek-vocabulary source: (a) NN verb-infinitive pollution (`lese høyt` → `lese`); (b) NB noun `brev` wrongly tagged genus:'m' → corrected to 'n'; (c) `entry.typos` strings that collide with `validWords` are deduped at sync time"
    - "papertek-vocabulary commits land on the sibling repo's main branch with a clear message describing the Leksihjelp Phase 2 DATA-02 scope, so papertek-webapps and papertek-nativeapps maintainers can see the change in their git log"
    - "A Rollback Protocol is documented in Task 3's push step — if a sibling-repo push regresses papertek-webapps or papertek-nativeapps downstream, the revert sequence is concrete git commands, not hand-waving (STATE.md Blockers/Concerns references this protocol)"
    - "No schema changes to entry.typos (stays string[]) — additive growth only, cross-app impact is zero-surface"
  artifacts:
    - path: "fixtures/nb/typo.jsonl"
      provides: "Expanded NB typo fixture corpus — 4-6 new cases that FAIL pre-sync and PASS post-sync, making the recall delta explicit and gate-able"
    - path: "fixtures/nn/typo.jsonl"
      provides: "NN typo fixture corpus unchanged in this plan; the explicit F1≥1.000 post-sync assertion guarantees the existing 10 cases still pass (no NN regression from Pitfall 4 collisions)"
    - path: "extension/data/nb.json"
      provides: "NB vocabulary with expanded entry.typos arrays (pulled from papertek-vocabulary via sync-vocab; additive, no schema change)"
    - path: "extension/data/nn.json"
      provides: "NN vocabulary with expanded entry.typos arrays"
    - path: "sibling-repo commits in /Users/geirforbord/Papertek/papertek-vocabulary"
      provides: "Upstream typo-bank growth (the single source of truth, per CLAUDE.md Papertek Vocabulary rule)"
  key_links:
    - from: "fixtures/nb/typo.jsonl (seeded with 4-6 pre-sync-failing cases)"
      to: "npm run check-fixtures [nb/typo] recall delta"
      via: "operationally verifiable measurement: pre-sync FAIL count > 0 → post-sync FAIL count == 0 makes success criterion #2 gate-able (not hand-waved against F1=1.000 baseline)"
      pattern: "check-fixtures.*nb.*typo"
    - from: "papertek-vocabulary/scripts/expand-nb-nn-typos.js"
      to: "papertek-vocabulary/vocabulary/lexicon/{nb,nn}/*bank.json"
      via: "--mode=topup run after dry-run preview; enforces global no-collision invariant across all six languages"
      pattern: "expand-nb-nn-typos"
    - from: "papertek-vocabulary API (Vercel)"
      to: "leksihjelp/extension/data/{nb,nn}.json via scripts/sync-vocab.js"
      via: "fetch → write entry.typos arrays verbatim; NO client-side transformation"
      pattern: "sync-vocab"
    - from: "leksihjelp fixture suite (npm run check-fixtures)"
      to: "data change validation"
      via: "must exit 0 after sync; higher typo recall on the 4-6 newly-seeded NB cases, [nn/typo] F1 ≥ 1.000, zero new clean-corpus regressions"
      pattern: "check-fixtures"
---

<objective>
Grow the Norwegian typo bank that powers the curated-typo rule by coordinating an additive expansion in the upstream `papertek-vocabulary` repository (a sibling repo at `/Users/geirforbord/Papertek/papertek-vocabulary`), then syncing the result into `extension/data/nb.json` and `extension/data/nn.json` via the existing `npm run sync-vocab`. This plan also fixes three known-baseline data-quality defects surfaced during Phase 1's fixture authoring — they are upstream data bugs, not Leksihjelp rule bugs, and Phase 2 DATA-02 is the natural home for their fix per the STATE.md handoff note.

**SC-2 measurability note (addresses checker warning):** Phase 1 closed with `[nb/typo] F1=1.000` on 11 cases. If DATA-02 only grows the typo bank without growing the fixture corpus, the recall delta from success criterion #2 ("higher recall on nb/typo") cannot be measured — the fixture would still show F1=1.000 before and after, with no operational signal of improvement. To fix this, Task 1 of this plan FIRST seeds 4-6 new NB typo fixture cases targeting typos that are KNOWN to be absent from today's `extension/data/nb.json` typo arrays (so they FAIL pre-sync with a clear 'missing expected finding for rule_id=typo'). The post-sync success criterion then becomes concrete: those same 4-6 cases must PASS after the sync pulls the expanded typos. Recall delta = number of newly-passing cases, measurable and gate-able, not hand-waved.

Per CLAUDE.md's Papertek Vocabulary rule: data corrections MUST happen at the source, not as client-side workarounds in Leksihjelp. All three fixes land in `papertek-vocabulary`, get validated there (`npm run validate:lexicon`), committed to the sibling repo's main branch, then pulled into Leksihjelp via `sync-vocab`. This has cross-app blast radius — `papertek-webapps` and `papertek-nativeapps` both consume the same vocabulary — but all changes in this plan are additive (typo growth) or corrective (fixing wrong genus / phrase-infinitive pollution) and therefore non-breaking for sibling apps. A concrete Rollback Protocol is documented in Task 3 for the unlikely case that a downstream consumer regresses despite the additive shape.

**Autonomous-flag semantics (addresses checker warning):** This plan's frontmatter declares `autonomous: false` because Task 2 is a `checkpoint:human-verify`. Task 1 performs heavy autonomous file edits inside the sibling repo (`papertek-vocabulary`), but those edits are LOCAL and UNCOMMITTED — they stay as staged changes until Task 2's human-verify checkpoint approves them. If the user rejects at Task 2, Task 1 is re-opened: no rollback needed because nothing was committed or pushed. The `autonomous: false` flag therefore gates the *push*, not the staged edits.

The fixture suite gates the data change: after `sync-vocab` pulls the expanded typos into Leksihjelp's `data/*.json`, `npm run check-fixtures` must show higher recall on `nb/typo.jsonl` (the new 4-6 seeded cases now pass), maintain `[nn/typo] F1 ≥ 1.000` on its 10 existing cases (no NN collision regression — explicit assertion, not just an implicit all-files-exit-0 check), and zero regression on clean corpora (Pitfall 4 from research — dialect typo collisions).

Purpose:
- Unlock Phase 4 SC-01 and Phase 3 word-prediction ranking work by giving the curated-typo rule more material.
- Honour the public commitment on the landing page ("Veien videre: expanded typo bank").
- Remove the three workarounds documented in Phase 1 SUMMARY where fixtures tiptoed around known data issues.
- Make success criterion #2 ("higher recall on NB typo without regressing NN") operationally measurable rather than hand-waved against a saturated F1=1.000 baseline.

This plan is the only one in Phase 2 that is NOT autonomous — because it involves commits to a sibling repo that has cross-app consumers, a human-verify checkpoint pauses before the `papertek-vocabulary` commit is pushed so the user can eyeball the dry-run diff for unexpected collisions with `papertek-webapps` or `papertek-nativeapps` conventions. The Leksihjelp-side sync + fixture check is fully automated.

Output:
- 4-6 new hand-authored NB typo fixture cases in `fixtures/nb/typo.jsonl` (seeded pre-sync, FAIL against current data, PASS post-sync).
- Sibling-repo commits at `/Users/geirforbord/Papertek/papertek-vocabulary` on `main` (landed and pushed or ready-to-push — see checkpoint for the decision).
- Updated `extension/data/nb.json` and `extension/data/nn.json` with the expanded typo arrays, via `npm run sync-vocab`.
- A single `feat(02-03)` commit in Leksihjelp containing the fixture additions AND the data/*.json diff.
- `npm run check-fixtures` continues to exit 0 with operationally-verifiable recall growth on `[nb/typo]` AND explicit `[nn/typo] F1 ≥ 1.000`.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-RESEARCH.md

# Phase 1 handoff — the three data-quality issues this plan fixes are documented here
@.planning/phases/01-foundation-vocab-seam-regression-fixture/01-03-SUMMARY.md

# Tools this plan uses (existing — do NOT create new scripts)
@scripts/sync-vocab.js
@scripts/check-fixtures.js

# Fixture conventions — authoring guide and span rules
@fixtures/README.md
@fixtures/nb/typo.jsonl
@fixtures/nn/typo.jsonl

# Sibling repo's existing typo-expansion script (the actual workhorse)
@/Users/geirforbord/Papertek/papertek-vocabulary/scripts/expand-nb-nn-typos.js

# CLAUDE.md policy — Papertek Vocabulary section
@CLAUDE.md

<interfaces>
<!-- Key file shapes this plan touches. -->

From extension/data/nb.json (per entry, the typos field):
```json
{
  "nounbank": {
    "hus_noun": {
      "word": "hus",
      "genus": "n",
      "typos": ["huus", "hous"]   ← DATA-02 grows these arrays (additive)
    }
  }
}
```

From papertek-vocabulary (same shape in vocabulary/lexicon/nb/*bank.json — the sync-vocab script preserves the field verbatim).

Fixture case schema (from fixtures/README.md):
```json
{"id":"nb-typo-XX","text":"...containing a typo...","expected":[{"rule_id":"typo","start":N,"end":M,"fix":"correct word"}],"must_not_flag":[]}
```

Three Phase-1-documented defects and their fixes:
1. NN verb-infinitive pollution — some entries have `word: "lese høyt"` (phrase) instead of `word: "lese"`. Fix: in the sibling repo, rename the offending entries so `word` is the bare infinitive; move the phrase to `explanation` or `examples` as appropriate.
2. NB noun `brev` has `genus: "m"` but the correct article is `et brev` (genus `n`). Fix: edit the single entry in `papertek-vocabulary/vocabulary/lexicon/nb/nounbank.json` and set `genus: "n"`.
3. Typos-in-validWords collision — some entries declare `typos: ["some_word"]` where `"some_word"` is also a valid form of a different entry. This causes the curated typo branch to be skipped. Fix options in priority order: (a) remove the colliding typo from the source `entry.typos` in the sibling repo (preferred — data quality); (b) if Leksihjelp's `sync-vocab.js` needs a dedupe guard as defence-in-depth, add it as a final-pass filter after `buildIndexes()` would produce the full validWords set. Option (a) is the CLAUDE.md-preferred fix ("data issues at the source").
</interfaces>

# Phase 1 SUMMARY quote (context for the three defects):
#
# > 1. NN verb-infinitive pollution. Some NN verb entries ship with phrase-form infinitives
# >    (e.g., `lese høyt` instead of bare `lese`). The `verbInfinitive` Map therefore maps
# >    conjugations to multi-word strings, which confuses modal-verb checks when the phrase
# >    appears in fixture text.
# > 2. NB noun `brev` tagged as `m` instead of `n`. The gender article for `brev` is `et brev`,
# >    but the noun bank currently ships `brev` with `genus: 'm'`.
# > 3. Typos-in-validWords causing curated branch bypass. Some `entry.typos` strings happen to
# >    also appear as valid word forms elsewhere in the bank (e.g., as a different word's base
# >    or conjugation). `validWords` is a union of all known forms, so the typo check's
# >    "if word is valid, skip typo branch" guard lets those slip through.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed SC-2 measurability — 4-6 new NB typo fixture cases that currently FAIL (pre-sync baseline); then fix three known-baseline data defects in papertek-vocabulary + dry-run typo expansion</name>
  <files>fixtures/nb/typo.jsonl, /Users/geirforbord/Papertek/papertek-vocabulary/vocabulary/lexicon/nb/nounbank.json, /Users/geirforbord/Papertek/papertek-vocabulary/vocabulary/lexicon/nn/verbbank.json (and any other offenders), /Users/geirforbord/Papertek/papertek-vocabulary/vocabulary/lexicon/nb/**/*.json (for typos-in-validWords dedupe), /Users/geirforbord/Papertek/papertek-vocabulary/scripts/dedupe-typos-vs-validwords.js (new)</files>
  <action>
This task has TWO parts. **Part 0 runs FIRST in Leksihjelp; Parts A-D run in the sibling repo.** The ordering matters: seeding the fixture cases BEFORE any data change is what makes the recall delta operationally measurable (addresses the checker's SC-2 measurability warning — without this, F1=1.000 pre-sync and F1=1.000 post-sync with no visible signal of improvement).

**Note on gating model:** This entire task performs heavy autonomous edits (both in Leksihjelp's fixtures and in the sibling repo's bank JSONs). All edits are **local and uncommitted** until Task 2's human-verify checkpoint approves them. If the user rejects at Task 2, the flow returns here — nothing needs rolling back because nothing was committed or pushed. The `autonomous: false` plan flag gates the *sibling-repo push in Task 3*, not the staged edits themselves.

═══════════════════════════════════════════════════════════════════
PART 0 — Seed 4-6 new pre-sync-FAILING NB typo fixture cases (Leksihjelp repo)
═══════════════════════════════════════════════════════════════════

Context: Phase 1's fixture suite has 11 NB typo cases at F1=1.000. Adding data to the typo bank without adding cases leaves SC-2's "higher recall" claim unmeasurable. This Part 0 seeds the operationally-verifiable gate.

Step 0.1 — Identify candidate new typos that are ABSENT from today's typo bank. Run:

```bash
cd /Users/geirforbord/Papertek/leksihjelp && node -e "
const fs = require('fs');
const nb = JSON.parse(fs.readFileSync('extension/data/nb.json','utf8'));
const allTypos = new Set();
for (const bank of Object.values(nb)) {
  if (typeof bank !== 'object' || !bank) continue;
  for (const e of Object.values(bank)) {
    if (e && Array.isArray(e.typos)) for (const t of e.typos) allTypos.add(t.toLowerCase());
  }
}
// Candidates to TEST — each must be a realistic Norwegian learner typo whose correct form is in validWords.
// Start with common patterns: double-consonant drops, silent H/D/T, common misspellings.
const candidates = [
  // [typo, expected_fix, description]
  ['begynelse', 'begynnelse', 'double-consonant drop: single n for nn'],
  ['forferdelig', 'forferdelig', 'DISTRACTOR — already correct, should NOT appear as typo'],
  ['forskjelig', 'forskjellig', 'double-l drop'],
  ['igjenem', 'gjennom', 'silent letter + transposition'],
  ['vanskelig', 'vanskelig', 'DISTRACTOR — correct'],
  ['opprining', 'opprinnelig', 'truncated + double-n drop'],
  ['sykel', 'sykkel', 'double-k drop in common noun'],
  ['fortsettele', 'fortsettelse', 'missing s in -lse ending'],
  ['oppmrksomhet', 'oppmerksomhet', 'missing e'],
  ['intresant', 'interessant', 'missing e + single-s'],
];
for (const [typo, fix, desc] of candidates) {
  const present = allTypos.has(typo);
  console.log((present ? 'SKIP (already in bank)' : 'USE    ').padEnd(25) + typo.padEnd(20) + '→ ' + fix.padEnd(20) + '  ' + desc);
}
"
```

Step 0.2 — From the `USE` rows of that output, pick **4-6 candidates** whose `expected_fix` is a known valid NB word (double-check each fix word is actually in the shipped vocabulary — the curated-typo branch only suggests fixes that are in validWords). Avoid any candidate whose typo string also happens to be a valid NN word (that's Pitfall 4 and would regress the NN clean corpus after sync).

Step 0.3 — Append those 4-6 cases to `fixtures/nb/typo.jsonl` using the schema documented in `fixtures/README.md`. IDs should follow the sequential convention used in the existing file (look at the last ID in the current file, e.g. `nb-typo-11`, and use `nb-typo-12`..`nb-typo-17`). Example format (adapt the span offsets to the actual text length):

```jsonl
{"id":"nb-typo-12","text":"Dette er en begynelse på noe bra.","expected":[{"rule_id":"typo","start":12,"end":21,"fix":"begynnelse"}],"must_not_flag":[]}
{"id":"nb-typo-13","text":"Han kjøpte en sykel i butikken.","expected":[{"rule_id":"typo","start":14,"end":19,"fix":"sykkel"}],"must_not_flag":[]}
```

Per `fixtures/README.md`: spans are end-EXCLUSIVE (`end = start + word.length`). Double-check each span with a simple offset count — mis-set ends silently turn every case red (Pitfall 5 from Phase 1).

Step 0.4 — Run the fixture suite and confirm the NEW cases FAIL with the current data (proves they're valid pre-sync baseline cases):

```bash
npm run check-fixtures:nb -- --rule=typo --verbose
```

Expected outcome: exit 1, `[nb/typo]` shows P=1.000 but R<1.000 (or F1<1.000) because 4-6 cases report "missing expected finding for rule_id=typo". **THIS IS INTENDED.** Do NOT commit the fixtures yet — leave them staged. The Task 3 post-sync run is what flips them to PASS, proving SC-2 recall delta.

Step 0.5 — Record the pre-sync baseline numbers in the task's execution notes (for the SUMMARY): something like "[nb/typo] pre-sync: 4/15 NEW cases failing, F1=0.783 (was 1.000 on the 11-case corpus)".

If all 4-6 seeded cases PASS at this step (no pre-sync failures), the typos you picked were already in the bank — go back to Step 0.1 and pick different ones. The whole point is that the seeded cases FAIL now and PASS after sync.

═══════════════════════════════════════════════════════════════════
PART A-D — Sibling repo changes (papertek-vocabulary)
═══════════════════════════════════════════════════════════════════

Parts A-D run ENTIRELY in the sibling repo `/Users/geirforbord/Papertek/papertek-vocabulary`. Do NOT modify anything inside `/Users/geirforbord/Papertek/leksihjelp/` during these parts — only the sibling repo. Work from an absolute-path context: every `cd` or filesystem call should be prefixed with `/Users/geirforbord/Papertek/papertek-vocabulary/`.

Per CLAUDE.md's Papertek Vocabulary rule, data issues are fixed at the source. Per the sibling repo's own CLAUDE.md (seen via research): "You may edit any file / commit to main without asking when instructed" — this plan is that instruction for the scope below.

PART A — Fix defect #2 (NB `brev` genus) — smallest, start here:

1. Open `/Users/geirforbord/Papertek/papertek-vocabulary/vocabulary/lexicon/nb/nounbank.json`.
2. Locate the `brev` entry. Confirm `genus: "m"` (or whatever the current wrong value is).
3. Change `genus` to `"n"`.
4. If the entry has a `pluralForms` / `definite` / `articles` field that encodes the wrong genus derivatively, also correct those (e.g. should produce `et brev`, `brevet`, `brev`, `brevene` for singular/def-sg/plural/def-pl in the neuter pattern).
5. Save.

PART B — Fix defect #1 (NN verb-infinitive pollution):

1. In `/Users/geirforbord/Papertek/papertek-vocabulary/vocabulary/lexicon/nn/verbbank.json`, find all entries whose `word` field contains a space (these are the phrase-form infinitives that should not be there).
2. For each, decide: (i) is the bare infinitive already a separate entry? If yes, delete the phrase entry. (ii) If no, rename the entry so `word` becomes just the bare infinitive; move the phrase to `examples` (if it's an illustrative usage) or `explanation` (if it's a collocation gloss).
3. The Phase 1 SUMMARY specifically called out `lese høyt` — start with that entry as the concrete example. Search for others by pattern: `grep -l '"word": "[a-zæøå]\+ [a-zæøå]' vocabulary/lexicon/nn/verbbank.json` or equivalent ripgrep.
4. Do NOT delete translations, examples, or other fields — move them, don't drop them.

PART C — Fix defect #3 (typos-in-validWords collision):

Preferred approach (source-side, per CLAUDE.md): in each NB/NN bank file, walk every `entry.typos: string[]` and remove any string that also appears as the `word` of some OTHER entry in the same language's lexicon (union across all banks). Implement this as a one-off cleanup script you drop inside `papertek-vocabulary/scripts/dedupe-typos-vs-validwords.js` (sibling repo's scripts directory) — follow the sibling repo's existing script style (see `expand-nb-nn-typos.js` as the canonical template). The script:
1. Load every bank JSON for NB (and separately NN).
2. Build a Set of all canonical `word` fields AND all inflected form fields (present tense, preterite, perfect, plurals — depends on what the sibling repo's banks expose). This mirrors the `buildIndexes → validWords` Set that Leksihjelp's seam produces.
3. For each entry, filter `entry.typos` to remove strings that appear in that Set (except the entry's OWN word — don't remove a valid self-reference; typos shouldn't include own word anyway).
4. Log every removal with `{entry.id, typo, collides_with}` so the change is auditable.
5. Write the files back.
6. Run `npm run validate:lexicon` in the sibling repo to confirm the JSON is still valid.

PART D — Additive typo growth via `expand-nb-nn-typos.js`:

1. Read the sibling repo's `scripts/expand-nb-nn-typos.js` to confirm its current modes. The research notes two: `--mode=zero` (only fills empty typos arrays) and `--mode=topup` (tops up entries below a CEFR target).
2. First: DRY RUN. `cd /Users/geirforbord/Papertek/papertek-vocabulary && node scripts/expand-nb-nn-typos.js --mode=topup --dry-run` (if the script supports --dry-run; if not, redirect its output to a log file and do NOT write back).
3. Capture the stdout to `/tmp/typo-expansion-dry-run.log` and inspect the proposed additions. Look for red flags:
   - Any proposed typo that equals a valid NB or NN word in the sibling repo's lexicon (the script's globalWords guard should already prevent these; confirm).
   - Any proposed typo that equals a word in DE/ES/FR/EN lexicon (the guard should also prevent these, but verify).
   - Unusual quantities — if the dry run proposes 20,000 new typos, that's too many; if it proposes 50, that's too few. Target (per research Open Question 2): ~+50% typo strings (NB: 11,385 → ~17,000+; NN: 7,417 → ~11,000+).
   - **Bonus sanity check:** cross-reference the 4-6 typo strings you seeded fixture cases for in Part 0 — the dry-run output SHOULD include most of them as newly-added typos (that's exactly the point). If none of your seeded typos are in the dry-run proposals, the expansion script may not cover the patterns you chose — consider swapping in patterns the script DOES propose.
4. If the dry run looks clean (no collision warnings, proportional additions), proceed to live run. If it flags collisions, investigate and tune script parameters before re-running.
5. LIVE RUN: `node scripts/expand-nb-nn-typos.js --mode=topup` (no --dry-run). This modifies the bank JSONs in place.
6. Run `npm run validate:lexicon` in the sibling repo. Must pass.
7. Run `npm run build:lexicon-search-index` if the sibling repo's release workflow requires it (inspect sibling repo's package.json scripts).

Do NOT commit in the sibling repo yet. Stage the changes (`git add -A` in the sibling repo) but wait for Task 2's human-verify checkpoint before pushing.

Summary of state after this task:
- **Leksihjelp side:** 4-6 new NB typo fixture cases staged in `fixtures/nb/typo.jsonl`. Pre-sync fixture run confirmed they FAIL (intended).
- **Sibling repo side:** NB `brev` genus corrected; NN verb phrase-form infinitives fixed; typos-in-validWords collisions removed via dedupe script (the dedupe script itself is NEW in the sibling repo's `scripts/` directory); `expand-nb-nn-typos.js --mode=topup` has run, adding ~+50% typos to NB and NN bank entries; sibling repo's validate step passes; changes are staged but not yet committed/pushed.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp &amp;&amp; node -e "const fs=require('fs');const lines=fs.readFileSync('fixtures/nb/typo.jsonl','utf8').split('\n').filter(l=&gt;l.trim() &amp;&amp; !l.trim().startsWith('//') &amp;&amp; !l.trim().startsWith('#'));const ids=lines.map(l=&gt;JSON.parse(l).id);const maxId=ids.length;if(maxId&lt;15)throw new Error('expected ≥15 NB typo cases after seeding (had 11 baseline + 4-6 new); got '+maxId);console.log('NB typo fixture now has '+maxId+' cases (baseline 11 + '+(maxId-11)+' seeded)');" &amp;&amp; echo '--- pre-sync fixture run (expecting FAILURES on new cases) ---' &amp;&amp; (npm run check-fixtures:nb -- --rule=typo || true) &amp;&amp; echo '--- sibling-repo defect-fix verification ---' &amp;&amp; cd /Users/geirforbord/Papertek/papertek-vocabulary &amp;&amp; node -e "const fs=require('fs'),path=require('path');const nb=JSON.parse(fs.readFileSync('vocabulary/lexicon/nb/nounbank.json','utf8'));const brevKey=Object.keys(nb).find(k=&gt;k.startsWith('brev_'));if(!brevKey){console.error('brev entry not found — sibling-repo shape may have changed');process.exit(1);}const brev=nb[brevKey];if(brev.genus!=='n')throw new Error('brev genus still wrong: '+brev.genus);console.log('brev genus corrected:', brev.genus);const nn=JSON.parse(fs.readFileSync('vocabulary/lexicon/nn/verbbank.json','utf8'));const offenders=Object.values(nn).filter(e=&gt;e.word &amp;&amp; e.word.includes(' '));if(offenders.length&gt;0)throw new Error('NN verbbank still has '+offenders.length+' phrase-form infinitives: '+offenders.slice(0,3).map(e=&gt;e.word).join(', '));console.log('NN verb phrase-form infinitives cleaned (0 remain)');" &amp;&amp; npm run validate:lexicon</automated>
  </verify>
  <done>
    Leksihjelp: `fixtures/nb/typo.jsonl` has ≥15 cases (11 baseline + 4-6 seeded); pre-sync `npm run check-fixtures:nb -- --rule=typo` shows the new cases FAILING (intended — proves the recall delta will be measurable post-sync). Sibling repo: NB `brev` has `genus: "n"`; NN verbbank has zero phrase-form infinitives; typos-in-validWords dedupe script ran and was staged as a sibling-repo new-file; `expand-nb-nn-typos.js --mode=topup` completed a live run producing ~+50% typo string growth; `npm run validate:lexicon` passes in the sibling repo; changes in BOTH repos are staged but not committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify sibling-repo diff + seeded NB fixture cases before pushing (cross-app blast radius)</name>
  <files>/Users/geirforbord/Papertek/papertek-vocabulary (staged changes only), /Users/geirforbord/Papertek/leksihjelp/fixtures/nb/typo.jsonl (staged changes only)</files>
  <what-built>
The sibling repository `/Users/geirforbord/Papertek/papertek-vocabulary` now has staged changes from Task 1: three defect fixes (NB brev genus, NN verb-phrase-infinitive cleanup, typos-in-validWords dedupe) plus an additive typo-bank expansion (~+50% typo strings across NB and NN, via the existing `expand-nb-nn-typos.js --mode=topup` tool which enforces the global no-collision invariant).

Leksihjelp also has staged changes: 4-6 new NB typo fixture cases in `fixtures/nb/typo.jsonl` that currently FAIL against the Phase-1 data bank (intentional — they prove the post-sync recall delta is real).

These changes will eventually flow to THREE consumers via the vocabulary API deployment: `papertek-webapps`, `papertek-nativeapps`, and Leksihjelp. STATE.md flags this as a "Blockers/Concerns" item specifically because of the cross-app radius. The human-verify checkpoint exists so the user can eyeball the proposed changes in the sibling repo before we push to main and trigger an API redeploy.
  </what-built>
  <action>
This is a human-verify checkpoint — no new automated action. The orchestrator pauses and presents the verification steps from <how-to-verify> to the user. The user runs those diff/spot-check commands and types "approved" (or rejects with a description) via the resume-signal. On approval, control passes to Task 3. On rejection, control returns to Task 1 for rework.

No files are written by Claude during this task. The user may run any of the listed inspection commands at will; they are read-only.
  </action>
  <how-to-verify>
Run these commands to see what's about to land:

**Leksihjelp fixture seeding (new in this revision):**

0. `cd /Users/geirforbord/Papertek/leksihjelp &amp;&amp; git diff fixtures/nb/typo.jsonl` — confirm 4-6 new NB typo cases seeded, IDs sequential after the existing last ID, schema matches `fixtures/README.md` (end-exclusive spans, `rule_id: "typo"`, `fix` word present in nb.json validWords). Each new case's `expected` typo string should be one that the post-sync sync-vocab will bring in as a new `entry.typos` item — so the case goes from FAIL pre-sync to PASS post-sync. This is the measurable recall delta for SC-2.

**Sibling repo (papertek-vocabulary) — about to push to main:**

1. `cd /Users/geirforbord/Papertek/papertek-vocabulary &amp;&amp; git status` — confirm staged files match expectations (nounbank.json for brev, verbbank.json for NN phrase-infinitives, multiple bank files for typo expansion, new scripts/dedupe-typos-vs-validwords.js).

2. `cd /Users/geirforbord/Papertek/papertek-vocabulary &amp;&amp; git diff --stat --cached` — magnitude check. Expect: tens to low-hundreds of lines changed across a handful of bank JSONs, plus one new script file. If the diff is 10,000+ lines, something went wrong — too many typos were added, or the dedupe was overzealous.

3. `cd /Users/geirforbord/Papertek/papertek-vocabulary &amp;&amp; git diff --cached vocabulary/lexicon/nb/nounbank.json | head -40` — spot-check: the `brev` entry's genus field changed from `m` to `n`, and plural/definite forms follow suit.

4. `cd /Users/geirforbord/Papertek/papertek-vocabulary &amp;&amp; git diff --cached vocabulary/lexicon/nn/verbbank.json | grep '"word":' | head -20` — confirm no lines show `word: "foo bar"` (phrase-form) after the change.

5. `cd /Users/geirforbord/Papertek/papertek-vocabulary &amp;&amp; node scripts/dedupe-typos-vs-validwords.js --verify` (if the dedupe script exposes a verify-idempotent flag) OR just run it a second time — it should report "0 removals" on the second run, confirming idempotency.

6. Cross-app gut check: scan the diff for changes OUTSIDE `vocabulary/lexicon/nb/` and `vocabulary/lexicon/nn/`. If you see changes to `vocabulary/lexicon/de|es|fr|en/` or `vocabulary/grammar/*`, stop — those are out of scope for DATA-02 and suggest the expansion script over-reached.

If all six checks (including the fixture-seeding one) look clean, approve with "approved" and Task 3 will push the sibling-repo commit(s), deploy the vocabulary API (if needed), run Leksihjelp's `npm run sync-vocab`, and re-run the fixture suite (which should now PASS the 4-6 seeded cases — the visible SC-2 recall delta). If anything looks wrong, reject with a description (e.g. "dry-run log shows new NN typo 'berre' which is a valid NN word — expansion script misconfigured") and Task 1 is re-opened to investigate.

Recommended commit message for the sibling repo (after approval):

```
data: expand NB/NN typo bank ~+50% + fix 3 Phase-1 Leksihjelp data defects

- nb/nounbank: correct `brev` genus from 'm' to 'n' (article is 'et brev')
- nn/verbbank: remove phrase-form infinitives (e.g. `lese høyt` → bare `lese`)
- dedupe typos-in-validWords across NB and NN banks (new scripts/dedupe-typos-vs-validwords.js)
- expand-nb-nn-typos --mode=topup: add ~X NB typos, ~Y NN typos; global no-collision invariant enforced

Coordinated with Leksihjelp Phase 2 DATA-02.
Cross-app impact: additive (typos[] growth, non-breaking); brev-genus correction is a bug-fix.
```

The user has final sign-off authority for pushing to the sibling repo's main branch — Leksihjelp cannot complete DATA-02 without this push because `sync-vocab` pulls from the deployed API.
  </how-to-verify>
  <verify>
    <automated>MISSING — This is a human-verify checkpoint. Automated proof cannot replace cross-app judgement; the user inspects git diff --cached in the sibling repo and the seeded fixture cases in Leksihjelp and approves or rejects. The verify guard for the CHANGES themselves runs in Task 3's automated verify block post-approval (including the post-sync fixture PASS on the pre-sync-FAILING cases — that's the operational SC-2 recall-delta gate).</automated>
  </verify>
  <done>
    User has inspected the staged sibling-repo diff AND the seeded NB fixture cases (via the six inspection commands listed in how-to-verify) and typed "approved" to proceed, OR the user has rejected with a description of what needs to change and Task 1 has been re-opened to address that feedback. No files are written during this task.
  </done>
  <resume-signal>Type "approved" to proceed to Task 3 (push + sync + fixture verification), or describe specific issues to go back to Task 1 for rework.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit + push sibling-repo changes, run npm run sync-vocab in Leksihjelp, verify fixture suite (with explicit [nb/typo] recall delta and [nn/typo] F1≥1.000 assertions)</name>
  <files>extension/data/nb.json, extension/data/nn.json, fixtures/nb/typo.jsonl (same-commit as data changes)</files>
  <action>
After Task 2's human-verify checkpoint is approved, execute the following in order. Each step has a failure mode listed — stop and surface to the user if any step fails.

STEP 1 — Commit in sibling repo (papertek-vocabulary):
`cd /Users/geirforbord/Papertek/papertek-vocabulary`
`git commit -m "$(cat <<'EOF'
data: expand NB/NN typo bank + fix Phase-1 Leksihjelp data defects

- nb/nounbank: correct brev genus 'm' → 'n'
- nn/verbbank: remove phrase-form infinitives (lese høyt → lese, etc.)
- dedupe typos-in-validWords via new scripts/dedupe-typos-vs-validwords.js
- expand-nb-nn-typos --mode=topup: additive growth, global no-collision enforced

Coordinated with Leksihjelp Phase 2 DATA-02 (cross-app additive change).
EOF
)"`

Failure mode: if pre-commit hooks fail in the sibling repo, fix the flagged issue in a new commit and try again — do not bypass hooks.

STEP 2 — Push to sibling repo's main:
`cd /Users/geirforbord/Papertek/papertek-vocabulary &amp;&amp; git push origin main`

Failure mode: if push is rejected (remote is ahead), `git pull --rebase origin main`, resolve any conflicts (unlikely — this is additive work), then re-push. If the remote is protected and requires a PR, stop and escalate to the user — CLAUDE.md authorises main-branch commits but if the sibling repo's branch protection changed, the plan needs adjustment.

═══════════════════════════════════════════════════════════════════
ROLLBACK PROTOCOL (cross-app regression response)
═══════════════════════════════════════════════════════════════════

If at any point AFTER Step 2 (i.e. once the sibling-repo push is live and the API has redeployed) you discover that `papertek-webapps` or `papertek-nativeapps` has regressed downstream — for example a CI failure in those repos, a user report of missing vocabulary entries, or a visible regression in Leksihjelp's fixture suite that traces to bad data — execute this protocol. It's also referenced from `.planning/STATE.md` Blockers/Concerns.

A. **Identify the offending sibling-repo commit SHA:**
   `cd /Users/geirforbord/Papertek/papertek-vocabulary &amp;&amp; git log --oneline -5` — the top commit is the DATA-02 one (its message starts with "data: expand NB/NN typo bank").

B. **Revert in sibling repo:**
   `cd /Users/geirforbord/Papertek/papertek-vocabulary &amp;&amp; git revert <SHA> --no-edit &amp;&amp; git push origin main`
   This produces a new revert commit rather than rewriting history (safer for the shared main branch).

C. **Wait for API redeploy** (~60 seconds), then verify the API manifest shows the pre-DATA-02 state:
   `curl -s https://papertek-vocabulary.vercel.app/api/vocab/v3/manifest | head -c 500`

D. **Re-sync Leksihjelp to reflect the rollback:**
   `cd /Users/geirforbord/Papertek/leksihjelp &amp;&amp; npm run sync-vocab &amp;&amp; git add extension/data/nb.json extension/data/nn.json &amp;&amp; git commit -m "revert(02-03): roll back DATA-02 sync after sibling-repo regression (sibling SHA <revert-SHA>)"`

E. **Also revert the Leksihjelp seeded fixture cases** (they were staged assuming post-sync typos would materialise):
   `cd /Users/geirforbord/Papertek/leksihjelp &amp;&amp; git revert <leksihjelp-feat-02-03-SHA> --no-edit` (or edit fixtures/nb/typo.jsonl back to baseline 11 cases if the feat commit also touched data).

F. **Notify user** — the plan's state is now "DATA-02 rolled back; investigate root cause in sibling repo before retrying". Do NOT silently retry.

Fast path if the regression is detected BEFORE Step 5 (i.e. between Step 2 and Step 5's sync), the sequence is simpler: run step B alone (revert + push in sibling repo), skip D-E (Leksihjelp was never synced), and notify. The Leksihjelp-side state is unchanged because the sync hasn't run yet.

═══════════════════════════════════════════════════════════════════

STEP 3 — Allow API deploy to complete. The sibling repo deploys to Vercel on push to main. Wait ~60 seconds after the push, then verify the API is live by fetching the manifest:
`curl -s https://papertek-vocabulary.vercel.app/api/vocab/v3/manifest | node -e "let d='';process.stdin.on('data',c=&gt;d+=c).on('end',()=&gt;{const m=JSON.parse(d);console.log('NB total:',m.languages.nb?.totalWords,'NN total:',m.languages.nn?.totalWords);})"`

The NB and NN totalWords counts should be the same as before DATA-02 (the expansion adds typo strings inside existing entries, not new entries). If totals moved significantly, something unexpected happened — investigate before proceeding.

STEP 4 — Run sync in Leksihjelp:
`cd /Users/geirforbord/Papertek/leksihjelp &amp;&amp; npm run sync-vocab`

This fetches the newly-deployed vocabulary and overwrites `extension/data/nb.json`, `extension/data/nn.json` (and all other language files, which are untouched in this plan — diff them to confirm only NB and NN show meaningful changes).

STEP 5 — Measure typo-bank growth in Leksihjelp:
`node -e "const fs=require('fs');for(const l of ['nb','nn']){const d=JSON.parse(fs.readFileSync('extension/data/'+l+'.json','utf8'));let entries=0,typos=0;for(const bank of Object.values(d)){if(typeof bank!=='object'||!bank)continue;for(const e of Object.values(bank)){if(e &amp;&amp; Array.isArray(e.typos)){entries++;typos+=e.typos.length;}}}console.log(l,'entries-with-typos:',entries,'total-typo-strings:',typos);}"`

Record the numbers in the SUMMARY. Compare against the pre-Phase-2 baseline from research (NB: 11,385 strings / 3,424 entries; NN: 7,417 / 2,948). Must show ≥+30% growth to satisfy success criterion #2's "visibly larger" standard.

STEP 6 — Fixture gate (with explicit per-class assertions):
`cd /Users/geirforbord/Papertek/leksihjelp &amp;&amp; npm run check-fixtures -- --json > /tmp/post-sync-fixture.json || true`

Then parse the JSON output and assert (this is the additional explicit-assertion step addressing the checker's "NN typo F1 recall growth not explicitly asserted" info note, and the reframed SC-2 measurability):

```bash
node -e "
const r = JSON.parse(require('fs').readFileSync('/tmp/post-sync-fixture.json','utf8'));
// Expected shape: each rule-class entry has {P, R, F1, passed, total}
// [nb/typo] must be F1 >= 1.000 on the GROWN corpus (includes the 4-6 seeded cases — proves SC-2 recall delta)
const nbTypo = r['nb/typo'] || r['nb']?.['typo'] || r.results?.['nb/typo'];
if (!nbTypo) { console.error('[nb/typo] missing from fixture output:', JSON.stringify(r).slice(0,500)); process.exit(1); }
if (nbTypo.F1 < 1.0) throw new Error('[nb/typo] F1='+nbTypo.F1+' < 1.000 — some seeded typo cases did not resolve after sync (expansion script did not add those typos — consider retuning or swap fixture cases for patterns the script DOES cover)');
if (nbTypo.total < 15) throw new Error('[nb/typo] total='+nbTypo.total+' < 15 — Part 0 fixture seeding may have been undone; rerun Part 0');
console.log('[nb/typo] F1='+nbTypo.F1+' on '+nbTypo.total+' cases (grown from 11 baseline — '+(nbTypo.total-11)+' new cases added and passing = SC-2 recall delta DEMONSTRATED)');
// [nn/typo] must be F1 >= 1.000 — explicit assertion (addresses checker info note)
const nnTypo = r['nn/typo'] || r['nn']?.['typo'] || r.results?.['nn/typo'];
if (!nnTypo) { console.error('[nn/typo] missing from fixture output'); process.exit(1); }
if (nnTypo.F1 < 1.0) throw new Error('[nn/typo] F1='+nnTypo.F1+' < 1.000 — NN typo regression from sync (Pitfall 4: new NB typos colliding with NN valid words or new NN typos inside NN validWords)');
console.log('[nn/typo] F1='+nnTypo.F1+' on '+nnTypo.total+' cases (NN typo corpus UNCHANGED in plan but F1=1.000 maintained — zero sync regression, NN typo recall explicitly asserted)');
// [nb/clean] and [nn/clean] must be F1 >= 1.000 (Pitfall 4: no new false positives on clean corpora)
for (const key of ['nb/clean', 'nn/clean']) {
  const c = r[key] || r.results?.[key];
  if (!c) { console.error(key+' missing'); process.exit(1); }
  if (c.F1 < 1.0) throw new Error(key+' F1='+c.F1+' < 1.000 — new typo collides with a valid word (Pitfall 4 triggered). ROLLBACK.');
  console.log(key+' F1='+c.F1+' on '+c.total+' cases (clean-corpus invariant held)');
}
console.log('all explicit assertions passed: [nb/typo] F1≥1.000 on grown corpus, [nn/typo] F1≥1.000, [nb/clean] F1≥1.000, [nn/clean] F1≥1.000');
"
```

Three possible outcomes:
(a) All four assertions pass. → **Success. SC-2 recall delta operationally demonstrated.** Proceed to STEP 7.
(b) `[nb/clean]` or `[nn/clean]` F1 drops below 1.000. → **Pitfall 4 triggered.** Execute Rollback Protocol (revert sibling-repo push, revert Leksihjelp sync, revert seeded fixture cases if needed). Do NOT ship.
(c) `[nn/typo]` F1 drops below 1.000 (NN typo regression even though the plan didn't add NN cases). → Same rollback path as (b). The NN corpus shouldn't move on its own; if it did, the expand-nb-nn-typos run did something unexpected in NN.
(d) `[nb/typo]` F1 < 1.000 with seeded-case failures. → The typos you seeded fixture cases for were NOT added by the expansion script. Two remediation options: (i) edit the 4-6 fixture cases to use typo patterns the expansion script DID add (inspect `/tmp/typo-expansion-dry-run.log` from Task 1 Part D to find candidates), re-run Task 3 from STEP 4; or (ii) hand-author the missing typos in the sibling repo's bank files and repeat the push+sync. Whichever is cleaner given the specific gap.

STEP 7 — Commit in Leksihjelp (single commit covering fixture seeds + data/*.json sync):
Only if STEP 6 exited 0 with all explicit assertions passing.
`cd /Users/geirforbord/Papertek/leksihjelp &amp;&amp; git add fixtures/nb/typo.jsonl extension/data/nb.json extension/data/nn.json &amp;&amp; git commit -m "$(cat <<'EOF'
feat(02-03): sync expanded typo bank + Phase-1 data defect fixes + new NB typo fixture cases (DATA-02)

- Pulled from papertek-vocabulary after upstream expansion/fix commit.
- NB entry.typos strings: before → after (see SUMMARY for exact counts).
- NN entry.typos strings: before → after.
- Known defect fixes land: brev genus corrected, NN phrase-infinitives cleaned, typos-in-validWords deduped.
- Added N new NB typo fixture cases targeting pre-sync-missing typos — now all PASS after sync (SC-2 recall delta operationally demonstrated).
- npm run check-fixtures exits 0; [nb/typo] F1=1.000 on grown corpus, [nn/typo] F1=1.000, clean-corpus invariants held.
EOF
)"`

Do NOT run `npm run package` or touch `manifest.json` or `backend/public/index.html` — bundle size and version bumps are owned by Plan 02-04 (the release-gate plan).

Failure mode: if commit is rejected by hooks, investigate — do NOT bypass. Most likely cause is a pre-commit linter (unlikely in this codebase) or an accidentally-staged file; double-check `git status` before retrying.

Final state:
- papertek-vocabulary has a landed commit on main (with the Rollback Protocol ready if downstream regresses).
- Leksihjelp has a single landed commit touching `fixtures/nb/typo.jsonl`, `extension/data/nb.json`, and `extension/data/nn.json`.
- Fixture suite exits 0 with `[nb/typo]` F1=1.000 on an EXPANDED 15-17-case corpus (up from 11-case baseline) AND `[nn/typo]` F1=1.000 explicitly asserted AND clean-corpus F1=1.000 invariants held.
- SC-2's "higher recall on nb/typo without regressing NN" is now operationally demonstrated — not hand-waved against a saturated F1=1.000 Phase-1 baseline.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp &amp;&amp; node -e "const fs=require('fs');const BASELINE={nb:{entries:3424,typos:11385},nn:{entries:2948,typos:7417}};for(const l of ['nb','nn']){const d=JSON.parse(fs.readFileSync('extension/data/'+l+'.json','utf8'));let entries=0,typos=0;for(const bank of Object.values(d)){if(typeof bank!=='object'||!bank)continue;for(const e of Object.values(bank)){if(e &amp;&amp; Array.isArray(e.typos)){entries++;typos+=e.typos.length;}}}const base=BASELINE[l];const growth=((typos/base.typos-1)*100).toFixed(1);console.log(l+': '+typos+' typo strings ('+growth+'% vs baseline '+base.typos+')');if(typos&lt;base.typos*1.3)throw new Error(l+' typo growth '+growth+'% &lt; +30% threshold');}console.log('typo growth OK');" &amp;&amp; node -e "const fs=require('fs');const lines=fs.readFileSync('fixtures/nb/typo.jsonl','utf8').split('\n').filter(l=&gt;l.trim() &amp;&amp; !l.trim().startsWith('//') &amp;&amp; !l.trim().startsWith('#'));if(lines.length&lt;15)throw new Error('[nb/typo] fixture corpus shrank back below 15 cases — the seeded cases are missing; got '+lines.length);console.log('[nb/typo] fixture corpus size: '+lines.length+' (≥15 required to demonstrate SC-2 recall delta)');" &amp;&amp; npm run check-fixtures</automated>
  </verify>
  <done>
    Sibling repo's main branch has a new commit containing the brev-genus fix, NN phrase-infinitive cleanup, typos-in-validWords dedupe, and expand-nb-nn-typos topup. Leksihjelp's `extension/data/nb.json` and `nn.json` have been updated via `npm run sync-vocab` and show ≥+30% growth in total typo strings for both languages. `fixtures/nb/typo.jsonl` now has ≥15 cases (11 baseline + 4-6 seeded); ALL of them PASS post-sync. `npm run check-fixtures` exits 0 with explicitly-asserted `[nb/typo] F1≥1.000` on the grown corpus, `[nn/typo] F1≥1.000` on its 10-case corpus (no NN regression from Pitfall 4), and clean-corpus F1≥1.000 invariants held. A single Leksihjelp-side `feat(02-03)` commit covers fixture seeds + data/*.json updates. A concrete Rollback Protocol is documented in Task 3 for downstream-consumer regression response (cross-referenced from STATE.md Blockers/Concerns).
  </done>
</task>

</tasks>

<verification>
- Sibling repo `/Users/geirforbord/Papertek/papertek-vocabulary` has a landed commit on main covering: NB brev genus correction, NN verb-phrase-infinitive cleanup, typos-in-validWords dedupe script + run, additive typo expansion via `expand-nb-nn-typos.js --mode=topup`.
- Leksihjelp's `extension/data/nb.json` contains ≥30% more total `entry.typos` strings than the pre-Phase-2 baseline (11,385 → ≥14,800).
- Leksihjelp's `extension/data/nn.json` contains ≥30% more total `entry.typos` strings than baseline (7,417 → ≥9,642).
- `fixtures/nb/typo.jsonl` grew from 11 cases to ≥15 cases (4-6 new cases seeded pre-sync that targeted typos absent from the pre-sync bank).
- `npm run check-fixtures` exits 0 with **explicit per-class assertions**: `[nb/typo] F1=1.000` on the GROWN 15-17-case corpus (SC-2 recall delta operationally demonstrated), `[nn/typo] F1=1.000` on its 10-case corpus (explicitly asserted, not implicit — addresses checker info note), `[nb/clean] F1=1.000` and `[nn/clean] F1=1.000` (Pitfall 4 not triggered).
- Leksihjelp commit touches `fixtures/nb/typo.jsonl`, `extension/data/nb.json`, `extension/data/nn.json` — no script or rule changes.
- NO schema changes to `entry.typos` (stays `string[]`) — verified by eyeballing the JSON diff.
- A concrete Rollback Protocol (git revert + re-sync + re-commit) is documented inside Task 3, and a STATE.md Blockers/Concerns entry cross-references it.
</verification>

<success_criteria>
DATA-02 is complete when:
1. Developer runs `npm run sync-vocab` and pulls an observably larger typo bank into `extension/data/*.json` (success criterion #2: ≥+30% typo strings per language).
2. `npm run check-fixtures` shows **explicit**, **per-class**, operationally-verifiable results: `[nb/typo] F1=1.000` on a GROWN corpus (11 → ≥15 cases) — proving recall delta against the saturated Phase-1 baseline — AND `[nn/typo] F1=1.000` (explicitly asserted, no regression from Pitfall 4 NN collisions) AND clean-corpus invariants held.
3. All three Phase-1-documented data-quality defects are fixed at the `papertek-vocabulary` source (preferred per CLAUDE.md) and reflected in the synced `extension/data/*.json`.
4. Cross-app coordination honoured: no schema changes, only additive field growth and pointed bug-fixes; `papertek-webapps` and `papertek-nativeapps` consume the same API without friction. A concrete Rollback Protocol is in place and cross-referenced from STATE.md in case downstream regression is discovered post-push.
5. The sibling repo commit and Leksihjelp commit both land cleanly on their respective main branches with descriptive messages.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer-frequency-bigrams-typo-bank/02-03-SUMMARY.md` following the template at `/Users/geirforbord/.claude/get-shit-done/templates/summary.md`. Record:
- Exact pre/post typo string counts for NB and NN (baseline 11,385 / 7,417; post: X / Y; percentage growth).
- The 4-6 NB typo fixture cases seeded in Part 0: each case's typo, fix, and confirmation that the case went from FAIL pre-sync to PASS post-sync (this is the SC-2 recall-delta evidence).
- Sibling-repo commit SHA on papertek-vocabulary's main branch.
- Leksihjelp-side commit SHA.
- Fixture suite P/R/F1 numbers per class before and after the sync (specifically `[nb/typo]`, `[nn/typo]`, `[nb/clean]`, `[nn/clean]`); absolute confirmation that `[nn/typo] F1≥1.000` and clean corpora did not regress.
- Any Pitfall-4 near-miss: did the dry-run flag a collision that was averted? How?
- Confirmation that the Rollback Protocol is documented in Task 3 and cross-referenced from `.planning/STATE.md` Blockers/Concerns (include the STATE.md line reference).
- The three defect fixes landed: which entries were touched, sibling-repo file paths.
- Any Open-Question-2 discretion used (e.g. "target +50% growth, actual +47%, within tolerance").
</output>
</output>
