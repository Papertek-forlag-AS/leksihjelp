---
phase: 23-data-source-migration
plan: 08
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - BOOT-03
  - API-01
  - API-02
  - API-03
  - CACHE-01
  - CACHE-02
  - CACHE-03
  - SCHEMA-01
  - UPDATE-01
  - UPDATE-02
  - UPDATE-03
  - MIGRATE-01
  - GATES-02

must_haves:
  truths:
    - "Offline install scenario confirmed: error pill renders when network disabled, NB baseline still functional"
  artifacts: []
  key_links: []
---

<objective>
Browser-verify the offline install scenario deferred from plan 23-03's checkpoint. Confirm the v3.0 extension handles network failure gracefully on first install: error pill renders, NB baseline lookups still work.

Purpose: Close the last partial verification gap (BOOT-03 offline path) so the phase can be marked fully verified.
Output: Human confirmation of offline behavior; no code changes expected.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-data-source-migration/23-VERIFICATION.md
@.planning/phases/23-data-source-migration/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package extension for testing</name>
  <files></files>
  <action>
Run `npm run package` to build the current extension zip. Confirm it completes successfully and print the output path so the user knows which zip to load.
  </action>
  <verify>
    <automated>npm run package 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <done>Extension zip built successfully, path printed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify offline install fallback in browser</name>
  <action>Human verifies offline install scenario in Chrome browser.</action>
  <what-built>v3.0 extension with IndexedDB-based vocabulary bootstrap and NB baseline fallback. The offline error path in vocab-bootstrap.js emits lexi:hydration state:'error', and popup.js renders an "Ordlister utilgjengelig -- prøv igjen senere" pill.</what-built>
  <how-to-verify>
1. Open Chrome, go to chrome://extensions, enable Developer mode
2. Click "Load unpacked" and select the `extension/` directory (or unzip the packaged zip)
3. Open DevTools on the service worker (click "Inspect views: service worker" on the extension card)
4. In DevTools Network tab, check "Offline" (or disable your network adapter)
5. Remove and reload the extension (toggle it off/on, or remove + re-load unpacked) to trigger onInstalled
6. Open the extension popup
7. Confirm: the popup shows "Ordlister utilgjengelig -- prøv igjen senere" (or similar error pill)
8. Type a Norwegian word (e.g. "hund") in the dictionary search
9. Confirm: NB baseline lookup still works (the word is found, results render)
10. Re-enable network. The service worker should auto-retry bootstrap download.

Expected: Step 7 shows the error pill. Step 9 shows NB baseline results. Step 10 triggers successful download.
  </how-to-verify>
  <verify>Human approval</verify>
  <done>User confirms offline fallback works: error pill renders, NB baseline lookups functional.</done>
  <resume-signal>Type "approved" if offline fallback works correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
Human confirms offline install fallback behavior matches BOOT-03 requirements.
</verification>

<success_criteria>
- User has verified offline install scenario in Chrome with network disabled
- Error pill renders correctly when bootstrap download fails
- NB baseline dictionary lookups work without network
- Phase 23 BOOT-03 can be marked as fully verified (not just code-verified)
</success_criteria>

<output>
After completion, create `.planning/phases/23-data-source-migration/23-08-SUMMARY.md`
</output>
