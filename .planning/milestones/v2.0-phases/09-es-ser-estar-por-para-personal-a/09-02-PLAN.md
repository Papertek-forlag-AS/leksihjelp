---
phase: 09-es-ser-estar-por-para-personal-a
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - extension/content/spell-rules/es-ser-estar.js
  - extension/styles/content.css
  - extension/manifest.json
  - fixtures/es/ser-estar.jsonl
autonomous: true
requirements: [ES-01]

must_haves:
  truths:
    - "benchmark-texts/es.txt line 46 'Soy cansado' flips from unflagged to flagged by es-ser-estar rule"
    - "Legitimate ser/estar usage in acceptance fixtures does NOT flag"
    - "Adjectives tagged 'both' (aburrido, listo, rico) never produce a finding"
    - "Accent-stripped copula forms (esta, estan) are recognized as estar conjugations"
  artifacts:
    - path: "extension/content/spell-rules/es-ser-estar.js"
      provides: "ES-01 copula-adjective mismatch rule"
      contains: "es-ser-estar"
    - path: "fixtures/es/ser-estar.jsonl"
      provides: "Regression fixtures for ES-01"
      min_lines: 45
  key_links:
    - from: "extension/content/spell-rules/es-ser-estar.js"
      to: "self.__lexiGrammarTables"
      via: "lazy-init getTables() at first check() call"
      pattern: "ES_COPULA_ADJ"
    - from: "extension/content/spell-rules/es-ser-estar.js"
      to: "host.__lexiSpellRules"
      via: "rule registration push"
      pattern: "__lexiSpellRules"
---

<objective>
Ship the ES-01 ser/estar copula-adjective mismatch rule. Flags when a conjugated ser form is used before an estar-only adjective (or vice versa).

Purpose: "Soy cansado" is the #1 ser/estar error Norwegian students make. This rule catches predicate-adjective copula mismatches via a closed-set lookup table.
Output: es-ser-estar.js rule file + fixture suite + CSS dot-colour binding.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-es-ser-estar-por-para-personal-a/09-RESEARCH.md
@.planning/phases/09-es-ser-estar-por-para-personal-a/09-01-SUMMARY.md
@extension/content/spell-rules/grammar-tables.js
@extension/content/spell-rules/de-prep-case.js
@extension/content/spell-check-core.js
@extension/styles/content.css

<interfaces>
<!-- From grammar-tables.js (after Plan 01) — ES tables on self.__lexiGrammarTables: -->
ES_SER_FORMS: Set of conjugated ser forms (soy, eres, es, somos, ...)
ES_ESTAR_FORMS: Set of conjugated estar forms (estoy, estas, esta, estamos, ...)
ES_COPULA_ADJ: { cansado: 'estar', alto: 'ser', aburrido: 'both', ... }

<!-- From spell-check-core.js — rule registration pattern: -->
host.__lexiSpellRules = host.__lexiSpellRules || [];
// Each rule: { id, languages, priority, check(ctx) => Finding[] }
// Finding: { rule_id, start, end, original, fix, message }

<!-- From spell-check-core.js — ctx shape passed to check(): -->
ctx.lang: string ('es')
ctx.tokens: Array<{ word, display, start, end }>
ctx.sentences: Array from Intl.Segmenter
ctx.getTagged(i): { pos, isFinite, isSubject, ... }
ctx.vocab: { verbInfinitive, isAdjective, knownPresens, knownPreteritum, ... }

<!-- From de-prep-case.js — lazy-init getTables() pattern (reference implementation): -->
let _tables = null;
function getTables() {
  if (!_tables) {
    const host = typeof self !== 'undefined' ? self : globalThis;
    _tables = host.__lexiGrammarTables || {};
  }
  return _tables;
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create es-ser-estar.js rule + fixture suite</name>
  <files>extension/content/spell-rules/es-ser-estar.js, fixtures/es/ser-estar.jsonl, extension/styles/content.css, extension/manifest.json</files>
  <behavior>
  Positive cases (MUST flag):
  - "Soy cansado" -> flag "Soy", suggest "Estoy" (cansado requires estar)
  - "Es enfermo" -> flag "Es", suggest "Esta" (enfermo requires estar)
  - "Estoy alto" -> flag "Estoy", suggest "Soy" (alto requires ser)
  - "Esta inteligente" -> flag "Esta", suggest "Es" (inteligente requires ser)
  - "Somos enfermos" -> flag "Somos", suggest "Estamos"
  - "Estan guapos" -> flag "Estan", suggest "Son" (guapo requires ser)

  Acceptance cases (MUST NOT flag):
  - "Estoy cansado" (correct estar + estar-adj)
  - "Soy alto" (correct ser + ser-adj)
  - "Es inteligente" (correct ser + ser-adj)
  - "Esta enfermo" (correct estar + estar-adj)
  - "Soy aburrido" (both -- should not flag)
  - "Estoy aburrido" (both -- should not flag)
  - "Es listo" (both -- should not flag)
  - "Esta listo" (both -- should not flag)
  - "Es un profesor" (ser + noun, not adjective)
  - "Esta en la casa" (estar + preposition, not adjective)
  - "El es muy alto" (ser + adverb + ser-adj -- correct, should not flag)

  Fixture ratio: at least 30 positive + 15 acceptance (>=2x acceptance ratio).
  </behavior>
  <action>
  **1. Create `fixtures/es/ser-estar.jsonl`** with the fixture suite. Follow the project fixture format: one JSON object per line with `{ text, lang, expected }` where `expected` is an array of `{ rule_id, original, fix }` for positive cases and `[]` for acceptance cases. Target >= 30 positive + >= 15 acceptance fixtures.

  **2. Create `extension/content/spell-rules/es-ser-estar.js`** as an IIFE registering onto `host.__lexiSpellRules`:

  - **Rule metadata:** `id: 'es-ser-estar'`, `languages: ['es']`, `priority: 50` (same priority band as de-prep-case).
  - **Lazy-init getTables():** Read `ES_SER_FORMS`, `ES_ESTAR_FORMS`, `ES_COPULA_ADJ` from `host.__lexiGrammarTables` at first `check()` call. Follow the `de-prep-case.js` pattern exactly.
  - **check(ctx) logic:**
    1. Guard: return early if `ctx.lang !== 'es'` or no `ctx.sentences`.
    2. Import `tokensInSentence` from core context if available, or iterate tokens directly.
    3. For each token, check if lowercased word is in `ES_SER_FORMS` or `ES_ESTAR_FORMS` to determine `usedCopula`.
    4. If copula found, scan next 1-3 tokens for an adjective in `ES_COPULA_ADJ`:
       - Skip intervening adverbs (`muy`, `tan`, `bastante`, `realmente`, `demasiado`, `un poco`).
       - Skip intervening `no` / `ya` / `todavia`.
       - If the next non-skip token is NOT in `ES_COPULA_ADJ`, skip.
       - If `requiredCopula === 'both'`, skip (ambiguous, no flag).
       - If `requiredCopula === usedCopula`, skip (correct usage).
       - Otherwise, flag the copula token with a suggestion to use the correct copula form.
    5. **Conjugation form mapping:** Build a simple map from ser-form to estar-form and vice versa by pairing forms in the same person/number/tense slot. For the suggestion `fix`, map the used form to the corresponding correct copula form. E.g. `soy`(ser,1s,pres) -> `estoy`(estar,1s,pres), `es`(ser,3s,pres) -> `esta`(estar,3s,pres). Store this mapping as a const `COPULA_FORM_MAP` in the rule file.
  - **explain function:** `explain: (finding) => ({ nb: '...', nn: '...', severity: 'warning' })`. Norwegian explanation: "Adjektivet '{adj}' brukes med {correct}, ikke {wrong}." / NN variant.

  **3. Add `.lh-spell-es-ser-estar` CSS binding** in `extension/styles/content.css` with P2 warn-tier dot colour (amber/orange, matching the de-prep-case pattern).

  **4. Add `es-ser-estar.js` to `extension/manifest.json`** content_scripts js array, after `grammar-tables.js` and alongside other es-* rule files.

  No data component in papertek-vocabulary -- the copula-adjective set is a closed linguistic class maintained inline per project philosophy.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp && npm run check-fixtures && npm run check-explain-contract && npm run check-rule-css-wiring && npm run check-network-silence</automated>
  </verify>
  <done>"Soy cansado" flags with es-ser-estar rule suggesting "Estoy"; "Estoy cansado" does not flag; "both" adjectives never flag; all release gates pass; fixture suite has >= 30 positive + >= 15 acceptance.</done>
</task>

</tasks>

<verification>
1. `npm run check-fixtures` exits 0 — ser-estar fixtures pass with >= 30 positive + >= 15 acceptance
2. `npm run check-explain-contract` exits 0 — es-ser-estar has explain with nb, nn, severity
3. `npm run check-rule-css-wiring` exits 0 — .lh-spell-es-ser-estar CSS binding exists
4. `npm run check-network-silence` exits 0 — no fetch/XHR in es-ser-estar.js
5. `npm run check-bundle-size` exits 0
</verification>

<success_criteria>
- es-ser-estar.js registered and fires on "Soy cansado" (benchmark line 46)
- Acceptance fixtures for correct ser/estar usage and "both" adjectives all pass
- Accent-stripped forms (esta, estan) recognized correctly
- Fixture ratio >= 2:1 acceptance vs positive
- All release gates pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-es-ser-estar-por-para-personal-a/09-02-SUMMARY.md`
</output>
