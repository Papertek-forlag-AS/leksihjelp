---
phase: 08-de-case-agreement-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/content/spell-rules/grammar-tables.js
  - extension/content/vocab-seam-core.js
  - extension/content/vocab-seam.js
  - extension/manifest.json
  - benchmark-texts/de.txt
  - benchmark-texts/expectations.json
autonomous: true
requirements: [DE-01, DE-02, DE-03, DE-04]

must_haves:
  truths:
    - "grammar-tables.js exports PREP_CASE, DEF_ARTICLE_CASE, INDEF_ARTICLE_CASE, SEPARABLE_PREFIXES, SEIN_VERBS tables on self.__lexiGrammarTables"
    - "vocab-seam-core builds participleToAux Map from de.json verbbank perfektum data"
    - "Benchmark de.txt contains lines with 'mit den Schule', 'ich habe gegangen', 'das Schultasche' patterns for Phase 8 rules to target"
  artifacts:
    - path: "extension/content/spell-rules/grammar-tables.js"
      provides: "Shared closed-set linguistic tables for DE governance rules"
      contains: "PREP_CASE"
    - path: "extension/content/vocab-seam-core.js"
      provides: "participleToAux index for DE-03"
      contains: "participleToAux"
  key_links:
    - from: "extension/content/spell-rules/grammar-tables.js"
      to: "self.__lexiGrammarTables"
      via: "IIFE global export"
      pattern: "__lexiGrammarTables"
    - from: "extension/content/vocab-seam-core.js"
      to: "participleToAux"
      via: "buildLookupIndexes return"
      pattern: "participleToAux"
---

<objective>
Ship the shared `grammar-tables.js` data primitive and extend `vocab-seam-core.js` with a `participleToAux` index. Add missing DE benchmark lines for Phase 8 rules.

Purpose: All four DE rules (Plans 02+03) depend on these shared tables and indexes. Shipping them first prevents duplication and establishes the contract.
Output: `grammar-tables.js` IIFE loaded before rule files; `participleToAux` Map in vocab-seam; benchmark text extended with Phase 8 target patterns.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-de-case-agreement-governance/08-RESEARCH.md
@extension/content/vocab-seam-core.js
@extension/content/spell-check-core.js
@extension/manifest.json
@benchmark-texts/de.txt
@benchmark-texts/expectations.json

<interfaces>
<!-- From vocab-seam-core.js — buildLookupIndexes return shape (line ~800): -->
return { nounGenus, verbInfinitive, validWords, isAdjective, knownPresens, knownPreteritum, verbForms, nounForms, typoFix, compoundNouns };

<!-- From vocab-seam-core.js — buildIndexes (line ~805): -->
function buildIndexes({ raw, bigrams, freq, sisterRaw, lang, isFeatureEnabled } = {})
// Returns object with: wordList, nounGenus, verbInfinitive, validWords, ...

<!-- From spell-check-core.js — rule registration pattern: -->
host.__lexiSpellRules = host.__lexiSpellRules || [];
// Each rule: { id, languages, priority, check(ctx) → Finding[] }

<!-- From de-v2.js — SEPARABLE_PREFIXES already defined there (line ~42): -->
const SEPARABLE_PREFIXES = ['ab', 'an', 'auf', 'aus', 'bei', 'ein', 'fest', 'her', 'hin', ...]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create grammar-tables.js shared data primitive + extend vocab-seam with participleToAux</name>
  <files>extension/content/spell-rules/grammar-tables.js, extension/content/vocab-seam-core.js, extension/content/vocab-seam.js, extension/manifest.json</files>
  <action>
  **1. Create `extension/content/spell-rules/grammar-tables.js`** as an IIFE that exports onto `self.__lexiGrammarTables`:

  Tables to include:
  - `PREP_CASE`: Object mapping German prepositions to their required case(s). Keys: `durch/für/gegen/ohne/um` → `'acc'`, `aus/bei/mit/nach/seit/von/zu` → `'dat'`, `an/auf/hinter/in/neben/über/unter/vor/zwischen` → `'acc/dat'` (two-way), `wegen/statt/trotz/während` → `'gen'`.
  - `DEF_ARTICLE_CASE`: Object mapping definite article forms (`der/die/das/den/dem/des`) to arrays of `{ genus, case }` entries (see 08-RESEARCH.md code example for exact values — articles are ambiguous across cases).
  - `INDEF_ARTICLE_CASE`: Same pattern for indefinite articles (`ein/eine/einen/einem/einer/eines`).
  - `SEPARABLE_PREFIXES`: Set of German separable prefixes (move from de-v2.js to grammar-tables.js as canonical source): `ab, an, auf, aus, bei, ein, fest, her, hin, los, mit, nach, um, vor, weg, zu, zurück, zusammen, weiter, vorbei, herum, heraus, hinaus`.
  - `SEIN_VERBS`: Set of verb infinitives that require `sein` as auxiliary in Perfekt. Built from the 42 sein-verbs identified in research: common ones include `gehen, kommen, fahren, fliegen, laufen, fallen, sterben, werden, sein, bleiben, passieren, geschehen, wachsen, entstehen, verschwinden, reisen, wandern, erscheinen, gelingen, begegnen, ankommen, abfahren, aufstehen, einschlafen, aufwachen, umziehen, zurückkehren, eintreten, auswandern, stattfinden`.
  - `BOTH_AUX_VERBS`: Set of verb infinitives that accept BOTH haben and sein (skip in DE-03): `fahren, fliegen, laufen, schwimmen, ausziehen, wegfahren`.

  Include `module.exports` dual-export footer matching the project IIFE pattern.

  **2. Extend `vocab-seam-core.js` buildLookupIndexes** to build a `participleToAux` Map:
  - After the existing `verbInfinitive` population loop, add a new loop over `wordList` entries.
  - For entries from `verbbank` that have `conjugations.perfektum.participle` and `conjugations.perfektum.auxiliary`, map `participle.toLowerCase()` → `auxiliary` value (`'haben'` or `'sein'` or `'both'`).
  - Add `participleToAux` to the return object of `buildLookupIndexes`.
  - In `buildIndexes`, destructure `participleToAux` from `buildLookupIndexes` result and include it in the returned indexes object.

  **3. Extend `vocab-seam.js`** to expose a `getParticipleToAux()` getter that returns the `participleToAux` Map from the indexes, following the exact pattern used by `getNounGenus()`.

  **4. Add `grammar-tables.js` to `manifest.json`** content_scripts js array. It must load BEFORE any rule file that consumes it. Insert it right after `spell-check-core.js` and before `quotation-suppression.js`.

  **5. Update `de-v2.js`** to read `SEPARABLE_PREFIXES` from `host.__lexiGrammarTables.SEPARABLE_PREFIXES` instead of the local const, with a fallback to a local copy if grammar-tables hasn't loaded (for backward compatibility). Keep the local const as fallback only.

  No new data component in papertek-vocabulary — all tables are closed linguistic sets that belong in rule-side constants per project philosophy.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp && node -e "
      const gt = require('./extension/content/spell-rules/grammar-tables.js');
      const assert = require('assert');
      assert(gt.PREP_CASE.mit === 'dat', 'mit should be dative');
      assert(gt.PREP_CASE.durch === 'acc', 'durch should be accusative');
      assert(gt.PREP_CASE.in === 'acc/dat', 'in should be two-way');
      assert(gt.DEF_ARTICLE_CASE.dem.some(e => e.case === 'dativ'), 'dem must map to dativ');
      assert(gt.SEPARABLE_PREFIXES instanceof Set, 'SEPARABLE_PREFIXES must be a Set');
      assert(gt.SEPARABLE_PREFIXES.has('auf'), 'must contain auf');
      assert(gt.SEIN_VERBS instanceof Set, 'SEIN_VERBS must be a Set');
      assert(gt.SEIN_VERBS.has('gehen'), 'must contain gehen');
      assert(gt.BOTH_AUX_VERBS instanceof Set, 'BOTH_AUX_VERBS must be a Set');
      assert(gt.BOTH_AUX_VERBS.has('fahren'), 'must contain fahren');
      console.log('grammar-tables.js: PASS');
    " && node -e "
      const core = require('./extension/content/vocab-seam-core.js');
      const de = require('./extension/data/de.json');
      const idx = core.buildIndexes({ raw: de, lang: 'de', isFeatureEnabled: () => true });
      const assert = require('assert');
      assert(idx.participleToAux instanceof Map, 'participleToAux must be a Map');
      assert(idx.participleToAux.size > 0, 'participleToAux must not be empty');
      assert(idx.participleToAux.get('gegangen') === 'sein', 'gegangen must require sein');
      console.log('participleToAux: PASS (' + idx.participleToAux.size + ' entries)');
    " && npm run check-fixtures</automated>
  </verify>
  <done>grammar-tables.js loads and exports all five tables; participleToAux populates from de.json verbbank; de-v2.js reads SEPARABLE_PREFIXES from grammar-tables; all existing fixtures still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add DE benchmark lines for Phase 8 patterns + update expectations.json</name>
  <files>benchmark-texts/de.txt, benchmark-texts/expectations.json</files>
  <action>
  **1. Add new lines to `benchmark-texts/de.txt`** that contain the Phase 8 target patterns. Insert them after the existing prose paragraph (after line 46 "Ich wasche mich die hände vor dem essen."), preserving the comment header:

  Add these lines (each on its own line, natural student-error prose):
  - `Gestern ich bin mit den Schule nach Hause gegangen.` — contains "mit den Schule" (acc article after dative prep, DE-01 target)
  - `Ich habe gegangen zum Laden um Milch zu kaufen.` — contains "habe gegangen" (haben with sein-verb, DE-03 target)
  - `Das Schultasche ist sehr schwer und ich brauche ein neues.` — contains "Das Schultasche" (wrong compound gender, DE-04 target; Tasche is feminine → die Schultasche)
  - `Am Morgen ich aufstehe und ich mache Frühstück.` — contains "ich aufstehe" unsplit in main clause (DE-02 target). Note: line 39 already has "Dann ich aufstehe" which is a V2 error — this new line isolates the separable-verb issue in a non-V2 context.

  **2. Update `benchmark-texts/expectations.json`** with new entries for the Phase 8 lines. Use 1-based line numbers matching the final file. All four new DE rules are P2 warn severity:
  - The "mit den Schule" line → `{ "rule_id": "de-prep-case", "severity": "warning", "priority_band": "P2" }`
  - The "habe gegangen" line → `{ "rule_id": "de-perfekt-aux", "severity": "warning", "priority_band": "P2" }`
  - The "Das Schultasche" line → `{ "rule_id": "de-compound-gender", "severity": "warning", "priority_band": "P2" }`
  - The "ich aufstehe" line → `{ "rule_id": "de-separable-verb", "severity": "warning", "priority_band": "P2" }`

  Count lines carefully to get the correct 1-based line numbers for the new entries. Existing entries must keep their correct line references (verify the existing entries still point to correct lines after insertion).

  **3. Update the de.txt comment header** FUTURE / UNPLANNED section to mark the Phase 8 items as COVERED.
  </action>
  <verify>
    <automated>cd /Users/geirforbord/Papertek/leksihjelp && node -e "
      const fs = require('fs');
      const lines = fs.readFileSync('benchmark-texts/de.txt', 'utf8').split('\n');
      const exp = JSON.parse(fs.readFileSync('benchmark-texts/expectations.json', 'utf8'));
      const assert = require('assert');
      // Verify new patterns exist somewhere in the text
      const text = lines.join('\n');
      assert(text.includes('mit den Schule'), 'missing mit den Schule');
      assert(text.includes('habe gegangen'), 'missing habe gegangen');
      assert(text.includes('Das Schultasche'), 'missing Das Schultasche');
      // Verify expectations reference valid line numbers
      for (const [key, val] of Object.entries(exp.entries)) {
        if (!key.startsWith('de.')) continue;
        const lineNum = parseInt(key.split('.')[1]);
        assert(lineNum > 0 && lineNum <= lines.length, 'invalid line ref: ' + key);
      }
      console.log('Benchmark + expectations: PASS');
    "</automated>
  </verify>
  <done>de.txt has 4 new lines with Phase 8 error patterns; expectations.json has matching entries with correct line numbers; existing entries still valid.</done>
</task>

</tasks>

<verification>
1. `node -e "require('./extension/content/spell-rules/grammar-tables.js')"` succeeds
2. `npm run check-fixtures` exits 0 (no regression)
3. `npm run check-benchmark-coverage` exits 0 (existing expectations still met; new entries will fail until Plan 02/03 ship the rules — that is expected)
4. `npm run check-network-silence` exits 0 (grammar-tables.js has no fetch/XHR)
</verification>

<success_criteria>
- grammar-tables.js exports PREP_CASE, DEF_ARTICLE_CASE, INDEF_ARTICLE_CASE, SEPARABLE_PREFIXES, SEIN_VERBS, BOTH_AUX_VERBS
- vocab-seam-core builds participleToAux Map with ≥40 entries from de.json
- de-v2.js reads SEPARABLE_PREFIXES from grammar-tables (no local duplication)
- manifest.json loads grammar-tables.js before rule files
- de.txt has benchmark lines for all four DE-0x patterns
- All existing release gates pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-de-case-agreement-governance/08-01-SUMMARY.md`
</output>
