---
phase: 12-pronoun-pro-drop-es-fr
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - extension/content/spell-rules/es-pro-drop.js
  - extension/content/spell-rules/es-gustar.js
  - fixtures/es/pro-drop.jsonl
  - fixtures/es/gustar.jsonl
autonomous: true
requirements: [PRON-01, PRON-02]

must_haves:
  truths:
    - "ES 'yo voy' is flagged as redundant subject pronoun at hint severity"
    - "ES 'Yo pienso' is flagged as redundant subject pronoun at hint severity"
    - "ES 'yo fui' is flagged as redundant subject pronoun at hint severity (preteritum coverage)"
    - "ES 'El no gusta ayudar' is flagged as gustar-class syntax error at warn severity"
    - "ES 'Me gusta la musica' does NOT flag"
    - "ES 'Yo creo que si' IS flagged (pedagogical — hint is acceptable)"
  artifacts:
    - path: "extension/content/spell-rules/es-pro-drop.js"
      provides: "ES pro-drop-overuse hint rule"
      exports: ["id: 'es-pro-drop'"]
    - path: "extension/content/spell-rules/es-gustar.js"
      provides: "ES gustar-class syntax flagger"
      exports: ["id: 'es-gustar'"]
    - path: "fixtures/es/pro-drop.jsonl"
      provides: ">=30 positive + >=60 acceptance fixtures for pro-drop"
      min_lines: 90
    - path: "fixtures/es/gustar.jsonl"
      provides: ">=30 positive + >=60 acceptance fixtures for gustar"
      min_lines: 90
  key_links:
    - from: "extension/content/spell-rules/es-pro-drop.js"
      to: "extension/content/vocab-seam-core.js"
      via: "ctx.vocab.esPresensToVerb and ctx.vocab.esPreteritumToVerb"
      pattern: "esPresensToVerb\\.get\\|esPreteritumToVerb\\.get"
    - from: "extension/content/spell-rules/es-gustar.js"
      to: "extension/content/spell-rules/grammar-tables.js"
      via: "ES_GUSTAR_CLASS_VERBS"
      pattern: "ES_GUSTAR_CLASS_VERBS"
---

<objective>
Implement PRON-01 (ES pro-drop-overuse hint) and PRON-02 (ES gustar-class syntax flagger) with full fixture suites.

Purpose: Flag the two most common Norwegian-transfer pronoun errors in Spanish student writing.
Output: Two rule files + two fixture files, all release gates passing.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pronoun-pro-drop-es-fr/12-RESEARCH.md
@.planning/phases/12-pronoun-pro-drop-es-fr/12-01-SUMMARY.md

<interfaces>
From extension/content/spell-check-core.js:
```javascript
// SUBJECT_PRONOUNS.es = new Set(['yo', 'tu', 'el', 'ella', 'nosotros', 'vosotros', 'ellos', 'ellas', 'usted', 'ustedes'])
// ctx.getTagged(i) returns { word, pos, isSubject, isFinite, ... }
// tokensInSentence(ctx, sentenceIdx) returns { start, end }
// ctx.sentences = array of sentence boundaries
// ctx.vocab.esPresensToVerb = Map: conjugated_form -> { inf, person }
// ctx.vocab.esPreteritumToVerb = Map: conjugated_form -> { inf, person }
```

From extension/content/spell-rules/grammar-tables.js (Plan 01 adds):
```javascript
const ES_GUSTAR_CLASS_VERBS = new Set([
  'gustar', 'encantar', 'interesar', 'importar', 'molestar',
  'fascinar', 'aburrir', 'doler', 'faltar', 'sobrar',
  'parecer', 'quedar', 'apetecer', 'costar', 'bastar',
]);
```

Person labels from esPresensToVerb (accent-stripped):
```
yo, tu, el/ella, nosotros, vosotros, ellos/ellas
```
Note: the labels use accented forms in the Map: `tú`, `él/ella`, `ellos/ellas`.
Verify actual label format by checking esPresensToVerb.get('voy').person at runtime or in vocab-seam-core.js.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PRON-01 es-pro-drop rule with fixtures</name>
  <files>extension/content/spell-rules/es-pro-drop.js, fixtures/es/pro-drop.jsonl</files>
  <behavior>
    - yo + 1sg present verb (yo voy, yo pienso, yo quiero) -> hint: redundant subject pronoun
    - yo + 1sg preteritum verb (yo fui, yo comí) -> hint: redundant subject pronoun
    - tu + 2sg present verb (tu hablas, tu quieres) -> hint: redundant subject pronoun
    - Sentence-initial yo still flagged (pedagogical goal to teach pro-drop default)
    - No pronoun before verb -> no flag
    - 3rd person pronouns (el, ella) -> NOT flagged (higher FP risk, start conservative)
    - Correct yo usage with emphasis/contrast -> flagged anyway at hint (acceptable at hint tier)
    - Non-ES language -> no flag
    - Intervening words between pronoun and verb (yo no voy) -> still flag if within 2-token window
  </behavior>
  <action>
Create extension/content/spell-rules/es-pro-drop.js following the self-registering IIFE pattern (see es-subjuntivo.js as template):

1. Register rule with id 'es-pro-drop', languages ['es'], priority 65.
2. Build PRONOUN_TO_PERSON mapping: yo -> person label for 1sg, tu/tu -> person label for 2sg. Only map yo and tu initially (conservative start).
3. In check(ctx): iterate through sentences. For each sentence, iterate tokens. When a subject pronoun (yo or tu, case-insensitive) is found:
   - Scan forward up to 2 tokens for a verb form that appears in esPresensToVerb OR esPreteritumToVerb.
   - Allow intervening tokens like 'no', 'nunca', 'tampoco', 'tambien', 'siempre' between pronoun and verb (negation/adverb fillers).
   - Look up verb person. Compare with pronoun's expected person.
   - If match: emit finding at hint severity with message suggesting to drop the pronoun ("En espanol, el pronombre 'yo' es redundante cuando el verbo ya indica la persona").
   - Suggestion/fix: empty string (removing the pronoun; or omit fix field to indicate awareness-only).
4. explain() returns {nb, nn, severity: 'hint'} with student-friendly explanation in both Norwegian registers about pro-drop.

Create fixtures/es/pro-drop.jsonl with >=30 positive and >=60 acceptance cases:
- Positive: yo + present verbs, yo + preteritum, tu + present, yo + intervening 'no', sentence-initial yo
- Acceptance: no pronoun, ella/el + verb (not flagged), nosotros + verb (not flagged), just the verb alone, non-ES text, yo in non-verb context

Verify person label format by reading esPresensToVerb construction in vocab-seam-core.js (lines 858-866) before hardcoding the mapping.
  </action>
  <verify>
    <automated>npm run check-fixtures 2>&1 | grep -E "es.*pro-drop|PASS|FAIL"</automated>
  </verify>
  <done>es-pro-drop rule flags "yo voy", "Yo pienso", "yo fui" at hint severity; >=30 positive + >=60 acceptance fixtures pass; benchmark lines es.39 and es.41 would flip</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PRON-02 es-gustar rule with fixtures</name>
  <files>extension/content/spell-rules/es-gustar.js, fixtures/es/gustar.jsonl</files>
  <behavior>
    - Subject + gustar-class verb WITHOUT dative clitic -> warn (e.g., "El no gusta ayudar")
    - Subject + no + gustar-class verb WITHOUT dative clitic -> warn (negation intervening)
    - Correct dative construction "Me gusta la musica" -> no flag
    - Correct dative construction "Le gusta leer" -> no flag
    - "A ella le gusta" -> no flag (has dative clitic)
    - Non-gustar-class verb "El no quiere ayudar" -> no flag
    - Non-ES language -> no flag
    - Gustar-class verb with dative clitic present within 3 tokens before verb -> no flag
  </behavior>
  <action>
Create extension/content/spell-rules/es-gustar.js following the self-registering IIFE pattern:

1. Register rule with id 'es-gustar', languages ['es'], priority 60.
2. Access ES_GUSTAR_CLASS_VERBS from grammar-tables (via host.__lexiGrammarTables or the established import pattern -- check how es-subjuntivo.js accesses grammar-tables).
3. Define ES_DATIVE_CLITICS = new Set(['me', 'te', 'le', 'nos', 'os', 'les']).
4. In check(ctx): iterate through sentences. For each sentence:
   - Scan for conjugated verb forms. For each token, check esPresensToVerb (or esPreteritumToVerb). If the returned inf is in ES_GUSTAR_CLASS_VERBS:
     - Walk 1-3 tokens backward from the verb. Check if any is a dative clitic.
     - Allow negation tokens (no, nunca, tampoco) between clitic and verb.
     - If NO dative clitic found: flag the verb token at warn severity.
     - Suggestion copy: propose dative restructuring. For the benchmark case "El no gusta ayudar", suggest "A el no le gusta ayudar". For general case, suggest adding the appropriate dative clitic.
5. explain() returns {nb, nn, severity: 'warning'} explaining that gustar-class verbs use dative experiencer, not SVO.

Create fixtures/es/gustar.jsonl with >=30 positive and >=60 acceptance:
- Positive: subject + gustar forms, subject + no + gustar, subject + encantar/interesar/importar, bare gustar without clitic
- Acceptance: "me gusta", "le gusta leer", "nos encanta", "les interesa", "a ella le gusta", correct dative constructions for all gustar-class verbs, non-gustar verbs, non-ES text
  </action>
  <verify>
    <automated>npm run check-fixtures 2>&1 | grep -E "es.*gustar|PASS|FAIL"</automated>
  </verify>
  <done>es-gustar rule flags "El no gusta ayudar" at warn severity with dative suggestion; >=30 positive + >=60 acceptance fixtures pass; benchmark line es.32 would flip</done>
</task>

</tasks>

<verification>
- `npm run check-fixtures` exits 0 (or the es/pro-drop and es/gustar sections pass)
- `npm run check-explain-contract` exits 0 for the two new rule files
- `npm run check-rule-css-wiring` exits 0 for the two new rule files
- `npm run check-network-silence` exits 0
</verification>

<success_criteria>
- es-pro-drop.js flags yo/tu + matching verb at hint severity
- es-pro-drop.js covers both present and preteritum forms
- es-gustar.js flags gustar-class verbs without preceding dative clitic at warn severity
- es-gustar.js provides dative restructuring suggestion
- Both rules have >=30 positive + >=60 acceptance fixtures passing
- All release gates exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/12-pronoun-pro-drop-es-fr/12-02-SUMMARY.md`
</output>
