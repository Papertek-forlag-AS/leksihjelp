---
phase: 10-fr-elision-auxiliary-pp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/content/spell-rules/grammar-tables.js
  - extension/content/vocab-seam-core.js
  - extension/content/spell-rules/fr-contraction.js
  - extension/data/grammarfeatures-fr.json
  - benchmark-texts/fr.txt
  - benchmark-texts/expectations.json
autonomous: true
requirements: [FR-01, FR-02, FR-03]

must_haves:
  truths:
    - "FR avoir/etre conjugation tables available via grammar-tables.js"
    - "buildParticipleToAux reads both perfektum and passe_compose keys"
    - "fr-contraction.js no longer handles le/la + vowel elision (scope narrowed to prepositional contractions only)"
    - "grammar_fr_pp_agreement feature toggle exists in grammarfeatures-fr.json, defaults OFF"
    - "Benchmark fr.txt contains j'ai alle line for FR-02 validation"
  artifacts:
    - path: "extension/content/spell-rules/grammar-tables.js"
      provides: "FR_AVOIR_FORMS, FR_ETRE_FORMS, FR_ETRE_VERBS, FR_ETRE_PARTICIPLES exports"
      contains: "FR_AVOIR_FORMS"
    - path: "extension/content/vocab-seam-core.js"
      provides: "Extended buildParticipleToAux reading passe_compose"
      contains: "passe_compose"
    - path: "extension/data/grammarfeatures-fr.json"
      provides: "grammar_fr_pp_agreement feature toggle"
      contains: "grammar_fr_pp_agreement"
  key_links:
    - from: "extension/content/vocab-seam-core.js"
      to: "extension/data/fr.json"
      via: "buildParticipleToAux reads passe_compose.participle + passe_compose.auxiliary"
      pattern: "passe_compose"
    - from: "extension/content/spell-rules/grammar-tables.js"
      to: "FR etre/avoir rule (Plan 02)"
      via: "FR_AVOIR_FORMS, FR_ETRE_FORMS, FR_ETRE_VERBS consumed by fr-etre-avoir.js"
      pattern: "FR_AVOIR_FORMS"
---

<objective>
Add FR shared grammar tables (avoir/etre conjugations, DR MRS VANDERTRAMP fallback set), extend buildParticipleToAux to read `passe_compose` key, refactor fr-contraction.js to narrow scope, add PP agreement feature toggle, and add benchmark lines for Phase 10 validation.

Purpose: Provide the data foundation and infrastructure fixes all three FR rules depend on; unblock Plans 02 and 03.
Output: grammar-tables.js with FR tables, vocab-seam fix, narrowed fr-contraction, grammarfeatures toggle, benchmark lines.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fr-elision-auxiliary-pp/10-RESEARCH.md

<interfaces>
From extension/content/spell-rules/grammar-tables.js:
- IIFE pattern appending to `host.__lexiGrammarTables`
- Existing: DE_PREP_CASE, DE_*_ARTICLE_CASE, SEPARABLE_PREFIXES, HABEN_FORMS, SEIN_FORMS, DE_PARTICIPLE_TO_AUX
- Existing: ES_SER_FORMS, ES_ESTAR_FORMS, ES_COPULA_ADJ, ES_POR_PARA_TRIGGERS, ES_HUMAN_NOUNS, ES_COPULA_VERBS
- Pattern: tables assigned as properties on host.__lexiGrammarTables object

From extension/content/vocab-seam-core.js (lines 807-819):
```javascript
function buildParticipleToAux(raw) {
    const participleToAux = new Map();
    if (!raw || !raw.verbbank) return participleToAux;
    for (const entry of Object.values(raw.verbbank)) {
      if (!entry.conjugations || !entry.conjugations.perfektum) continue;
      const perf = entry.conjugations.perfektum;
      if (!perf.participle || !perf.auxiliary) continue;
      const participle = perf.participle.toLowerCase();
      if (!participleToAux.has(participle)) {
        participleToAux.set(participle, perf.auxiliary);
      }
    }
    return participleToAux;
}
```

From extension/content/spell-rules/fr-contraction.js:
- Currently handles le/la + vowel/h -> l' (overlaps with FR-01 elision)
- Must be narrowed to only prepositional contractions: de le -> du, a le -> au, de les -> des, a les -> aux
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FR tables to grammar-tables.js and extend buildParticipleToAux</name>
  <files>extension/content/spell-rules/grammar-tables.js, extension/content/vocab-seam-core.js</files>
  <action>
**grammar-tables.js:** Add the following FR tables to the IIFE, following the same pattern as DE/ES tables:

1. `FR_AVOIR_FORMS` — Map of conjugated avoir forms to `{ person, tense }`:
   - Present: ai, as, a, avons, avez, ont
   - Imparfait: avais, avait, avions, aviez, avaient
   - Note: `avais` covers both je/tu imparfait (same form)

2. `FR_ETRE_FORMS` — Map of conjugated etre forms to `{ person, tense }`:
   - Present: suis, es, est, sommes, etes, sont
   - Imparfait: etais, etait, etions, etiez, etaient
   - Include accent-stripped variants alongside accented: etais AND étais, etc.
   - Note: `es` collision with Spanish — rule-level language filter handles this

3. `FR_ETRE_VERBS` — Set of DR MRS VANDERTRAMP infinitives (supplement for data gaps):
   aller, arriver, descendre, devenir, entrer, monter, mourir, naitre, naître, partir, passer, rentrer, rester, retourner, revenir, sortir, tomber, venir

4. `FR_ETRE_PARTICIPLES` — Map of participle form -> infinitive for data-gap verbs:
   devenu -> devenir, devenue -> devenir, ne -> naitre, née -> naitre, rentre -> rentrer, rentree -> rentrer, retourne -> retourner, retournee -> retourner
   Include both accented and unaccented forms (né/ne, née/nee, rentré/rentre, retourné/retourne).

Assign all four to `host.__lexiGrammarTables.FR_AVOIR_FORMS`, `FR_ETRE_FORMS`, `FR_ETRE_VERBS`, `FR_ETRE_PARTICIPLES`.

**vocab-seam-core.js:** Extend `buildParticipleToAux` (line ~811) to also read `passe_compose`:

Change:
```javascript
if (!entry.conjugations || !entry.conjugations.perfektum) continue;
const perf = entry.conjugations.perfektum;
```
To:
```javascript
const perf = entry.conjugations?.perfektum || entry.conjugations?.passe_compose;
if (!perf) continue;
```

This single change makes the existing `participleToAux` Map work for both DE and FR verbs. The Map already stores `participle -> auxiliary` which is the same shape for both languages.
  </action>
  <verify>
    <automated>node -e "
const gt = require('./extension/content/spell-rules/grammar-tables.js');
const t = (typeof self !== 'undefined' ? self : globalThis).__lexiGrammarTables;
const assert = (cond, msg) => { if (!cond) { console.error('FAIL:', msg); process.exit(1); } };
assert(t.FR_AVOIR_FORMS && t.FR_AVOIR_FORMS.ai, 'FR_AVOIR_FORMS missing ai');
assert(t.FR_ETRE_FORMS && t.FR_ETRE_FORMS.suis, 'FR_ETRE_FORMS missing suis');
assert(t.FR_ETRE_VERBS && t.FR_ETRE_VERBS.has('aller'), 'FR_ETRE_VERBS missing aller');
assert(t.FR_ETRE_VERBS.has('devenir'), 'FR_ETRE_VERBS missing devenir');
assert(t.FR_ETRE_PARTICIPLES && t.FR_ETRE_PARTICIPLES.devenu, 'FR_ETRE_PARTICIPLES missing devenu');
console.log('PASS: FR grammar tables loaded');
" && node -e "
const vsCore = require('./extension/content/vocab-seam-core.js');
const src = require('fs').readFileSync('./extension/content/vocab-seam-core.js', 'utf8');
const assert = (cond, msg) => { if (!cond) { console.error('FAIL:', msg); process.exit(1); } };
assert(src.includes('passe_compose'), 'buildParticipleToAux does not read passe_compose');
console.log('PASS: buildParticipleToAux reads passe_compose');
"</automated>
  </verify>
  <done>FR auxiliary tables exported from grammar-tables.js; buildParticipleToAux reads both perfektum and passe_compose keys</done>
</task>

<task type="auto">
  <name>Task 2: Narrow fr-contraction.js, add PP toggle, add benchmark lines</name>
  <files>extension/content/spell-rules/fr-contraction.js, extension/data/grammarfeatures-fr.json, benchmark-texts/fr.txt, benchmark-texts/expectations.json</files>
  <action>
**fr-contraction.js:** Refactor to ONLY handle prepositional contractions. Remove the `le/la + vowel/h -> l'` handling (now owned by fr-elision.js in Plan 02). The rule should ONLY flag:
- `de le [noun]` -> `du [noun]`
- `a le [noun]` -> `au [noun]` (where `a` is the preposition `à`)
- `de les [noun]` -> `des [noun]`
- `a les [noun]` -> `aux [noun]`

Keep the same id `fr-contraction`, priority 15, severity `error`. Update the explain strings to reflect preposition contractions rather than elision. The check function should look for `de/a` followed by `le/les` (2 tokens -> 1 contraction), not le/la + vowel.

**grammarfeatures-fr.json:** Add a new feature toggle for PP agreement:
```json
{
  "id": "grammar_fr_pp_agreement",
  "name": "Accord du participe passé",
  "nameEn": "Past Participle Agreement",
  "description": "Samsvar av partisipp med foranknytt direkte objekt (la pomme que j'ai mangée)",
  "descriptionEn": "Past participle agreement with preceding direct object",
  "category": "verbs",
  "appliesTo": ["verb"],
  "dataPath": "conjugations.passe_compose"
}
```
Add it to the features array. Do NOT add it to any preset (defaults OFF -- only appears if user manually enables it, as per success criteria).

**benchmark-texts/fr.txt:** Add a new line after the existing text containing the être/avoir benchmark error. Insert before the last line (`C'est un homme grand et intelligent.`):
```
J'ai allé au parc avec mes amis hier.
```
This gives the benchmark a line where `j'ai allé` (avoir + être-verb participle) can be flagged by FR-02.

**benchmark-texts/expectations.json:** Add forward-looking expectations for the new FR rules. The expectations reference benchmark line numbers — after adding the new line, add entries like:
- For elision errors already in the text: `je ai` (line with "je avais" is already `je ai` pattern — check exact line), `si il` (line "Si il pleut")
- For être/avoir: the new `j'ai allé` line
- Note: Exact line numbers must be determined from the actual fr.txt content after the edit. The executor should count carefully.

Do NOT add expectations for FR-03 (PP agreement) since it is behind an opt-in toggle that defaults OFF.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const fr = fs.readFileSync('./extension/content/spell-rules/fr-contraction.js', 'utf8');
const assert = (cond, msg) => { if (!cond) { console.error('FAIL:', msg); process.exit(1); } };
// fr-contraction should NOT contain vowel-onset logic anymore
assert(!fr.includes('VOWELS') || !fr.includes('new Set'), 'fr-contraction still has VOWELS set');
assert(fr.includes('du') || fr.includes('au'), 'fr-contraction missing prepositional contractions');
console.log('PASS: fr-contraction narrowed');

const gf = JSON.parse(fs.readFileSync('./extension/data/grammarfeatures-fr.json', 'utf8'));
const ppToggle = gf.features.find(f => f.id === 'grammar_fr_pp_agreement');
assert(ppToggle, 'grammar_fr_pp_agreement toggle missing');
const inPreset = gf.presets.some(p => p.features.includes('grammar_fr_pp_agreement'));
assert(!inPreset, 'grammar_fr_pp_agreement should NOT be in any preset');
console.log('PASS: PP agreement toggle added, not in presets');

const bench = fs.readFileSync('./benchmark-texts/fr.txt', 'utf8');
assert(bench.includes(\"J'ai allé\") || bench.includes(\"j'ai allé\"), 'Benchmark missing j\\'ai alle line');
console.log('PASS: Benchmark fr.txt has etre/avoir error line');
" && npm run check-fixtures 2>&1 | tail -5</automated>
  </verify>
  <done>fr-contraction narrowed to preposition contractions only; grammar_fr_pp_agreement toggle added (defaults OFF); benchmark fr.txt has j'ai alle line; existing fixtures still pass</done>
</task>

</tasks>

<verification>
1. `npm run check-fixtures` exits 0 (no regressions from fr-contraction refactor)
2. `npm run check-explain-contract` exits 0
3. `npm run check-rule-css-wiring` exits 0
4. FR grammar tables loadable from Node: `FR_AVOIR_FORMS`, `FR_ETRE_FORMS`, `FR_ETRE_VERBS`, `FR_ETRE_PARTICIPLES`
5. `buildParticipleToAux` source contains `passe_compose`
6. `grammar_fr_pp_agreement` feature exists in grammarfeatures-fr.json, not in any preset
</verification>

<success_criteria>
- grammar-tables.js exports FR avoir/etre tables and DR MRS VANDERTRAMP fallback set
- buildParticipleToAux works for both DE (perfektum) and FR (passe_compose) verbs
- fr-contraction.js only flags prepositional contractions, not vowel elision
- PP agreement feature toggle exists and defaults OFF
- Benchmark fr.txt has at least one etre/avoir error line
- All existing release gates pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-fr-elision-auxiliary-pp/10-01-SUMMARY.md`
</output>
