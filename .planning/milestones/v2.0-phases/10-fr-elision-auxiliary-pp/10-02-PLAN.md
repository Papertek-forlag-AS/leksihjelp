---
phase: 10-fr-elision-auxiliary-pp
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - extension/content/spell-rules/fr-elision.js
  - extension/content/spell-rules/fr-etre-avoir.js
  - extension/manifest.json
  - extension/styles/content.css
  - scripts/check-explain-contract.js
  - scripts/check-rule-css-wiring.js
  - fixtures/fr/elision.jsonl
  - fixtures/fr/etre-avoir.jsonl
  - benchmark-texts/expectations.json
autonomous: true
requirements: [FR-01, FR-02]

must_haves:
  truths:
    - "je ai in benchmark text is flagged by fr-elision rule suggesting j'ai"
    - "si il in benchmark text is flagged by fr-elision rule suggesting s'il"
    - "si elle does NOT flag (si only elides before il/ils)"
    - "j'ai (single token, already elided) does NOT flag"
    - "j'ai alle in benchmark text is flagged by fr-etre-avoir rule suggesting suis"
    - "je suis alle (correct etre usage) does NOT flag"
    - "h-aspire words (le heros, la honte) do NOT trigger elision"
    - "All release gates pass: check-fixtures, check-explain-contract, check-rule-css-wiring, check-benchmark-coverage"
  artifacts:
    - path: "extension/content/spell-rules/fr-elision.js"
      provides: "FR-01 elision rule"
      contains: "fr-elision"
    - path: "extension/content/spell-rules/fr-etre-avoir.js"
      provides: "FR-02 etre/avoir auxiliary rule"
      contains: "fr-etre-avoir"
    - path: "fixtures/fr/elision.jsonl"
      provides: ">=30 positive + >=15 acceptance elision fixtures"
    - path: "fixtures/fr/etre-avoir.jsonl"
      provides: ">=30 positive + >=15 acceptance etre/avoir fixtures"
  key_links:
    - from: "extension/content/spell-rules/fr-elision.js"
      to: "extension/content/spell-check-core.js"
      via: "self.__lexiSpellRules registration, tokens[i].word bigram scan"
      pattern: "__lexiSpellRules.push"
    - from: "extension/content/spell-rules/fr-etre-avoir.js"
      to: "extension/content/spell-rules/grammar-tables.js"
      via: "Reads FR_AVOIR_FORMS, FR_ETRE_FORMS, FR_ETRE_VERBS, FR_ETRE_PARTICIPLES from __lexiGrammarTables"
      pattern: "__lexiGrammarTables"
    - from: "extension/content/spell-rules/fr-etre-avoir.js"
      to: "extension/content/vocab-seam-core.js"
      via: "Reads ctx.vocab.participleToAux for data-driven participle->aux mapping"
      pattern: "participleToAux"
---

<objective>
Create the FR elision rule (FR-01) and the FR etre/avoir auxiliary rule (FR-02) with full fixture suites and release gate wiring.

Purpose: Ship the two always-on French structural grammar rules that flag missing elision and wrong passe compose auxiliary.
Output: Two new rule files, two fixture files, manifest/CSS/gate updates, benchmark expectations.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fr-elision-auxiliary-pp/10-RESEARCH.md
@.planning/phases/10-fr-elision-auxiliary-pp/10-01-SUMMARY.md

<interfaces>
From extension/content/spell-rules/grammar-tables.js (after Plan 01):
```javascript
host.__lexiGrammarTables.FR_AVOIR_FORMS  // { ai: {person, tense}, as: ..., a: ..., ... }
host.__lexiGrammarTables.FR_ETRE_FORMS   // { suis: {person, tense}, es: ..., est: ..., ... }
host.__lexiGrammarTables.FR_ETRE_VERBS   // Set(['aller', 'arriver', 'devenir', ...])
host.__lexiGrammarTables.FR_ETRE_PARTICIPLES // { devenu: 'devenir', ne: 'naitre', ... }
```

From extension/content/spell-rules/de-perfekt-aux.js (direct analog):
- IIFE pattern with __lexiSpellRules registration
- AUX_LOOKUP Map: conjugated form -> { aux, person, tense }
- Forward scan up to 5 tokens for participle
- participleToAux from ctx.vocab for data-driven lookup
- Fallback to hardcoded set for data gaps
- Severity: warning, Priority: 70

From extension/content/spell-check-core.js:
- WORD_RE: /[\p{L}]+(?:'[\p{L}]+)*/gu — apostrophe-words are single tokens
- tokens[i]: { word (lowercase), display (original case), start, end }
- ctx: { lang, tokens, cursorPos, vocab, sentences, suppressedFor }

From 10-RESEARCH.md elision rule design:
- ELISION_MAP: je->j', me->m', te->t', se->s', le->l', la->l', de->d', ne->n', que->qu'
- SI special case: only elides before il/ils
- H_ASPIRE exception set (no elision before these h-initial words)
- Priority 14, severity error
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FR-01 elision rule + FR-02 etre/avoir rule with fixtures</name>
  <files>extension/content/spell-rules/fr-elision.js, extension/content/spell-rules/fr-etre-avoir.js, fixtures/fr/elision.jsonl, fixtures/fr/etre-avoir.jsonl</files>
  <behavior>
    FR-01 Elision (fr-elision.js):
    - "je ai" -> flags, suggests "j'ai"
    - "me appelle" -> flags, suggests "m'appelle"
    - "te invite" -> flags, suggests "t'invite"
    - "se habiller" -> flags, suggests "s'habiller"
    - "le ami" -> flags, suggests "l'ami"
    - "la ecole" -> flags, suggests "l'ecole"
    - "de avoir" -> flags, suggests "d'avoir"
    - "ne est" -> flags, suggests "n'est"
    - "que il" -> flags, suggests "qu'il"
    - "si il" -> flags, suggests "s'il"
    - "si elle" -> does NOT flag (si only elides before il/ils)
    - "j'ai" -> does NOT flag (already elided, single token)
    - "s'il" -> does NOT flag
    - "l'homme" -> does NOT flag
    - "le heros" -> does NOT flag (h-aspire)
    - "la honte" -> does NOT flag (h-aspire)
    - "le hamster" -> does NOT flag (h-aspire)
    - "le chat" -> does NOT flag (consonant-initial)
    - Only fires when ctx.lang === 'fr'

    FR-02 Etre/Avoir (fr-etre-avoir.js):
    - "j'ai alle" -> flags avoir, suggests suis (aller takes etre)
    - "il a parti" -> flags a, suggests est (partir takes etre)
    - "nous avons arrives" -> flags avons, suggests sommes
    - "elle a tombee" -> flags a, suggests est
    - "je suis alle" -> does NOT flag (correct etre)
    - "elle est venue" -> does NOT flag (correct etre)
    - "j'ai mange" -> does NOT flag (manger takes avoir)
    - "il a parle" -> does NOT flag (parler takes avoir)
    - Verbs with aux "both" -> skip (no flag)
    - Only fires when ctx.lang === 'fr'
    - Uses ctx.vocab.participleToAux for data-driven lookup
    - Falls back to FR_ETRE_VERBS/FR_ETRE_PARTICIPLES from grammar-tables for data-gap verbs
  </behavior>
  <action>
**fr-elision.js:** Create as IIFE following the exact pattern from 10-RESEARCH.md code example. Key details:
- id: 'fr-elision', languages: ['fr'], priority: 14, severity: 'error'
- VOWELS set includes accented vowels: aeiouyàâäéèêëïîôùûüÿœæ
- H_ASPIRE set from research (hache, haie, haine, hall, halte, hamac, hamster, hanche, handicap, hangar, harangue, harceler, hardi, haricot, harpe, hasard, hausse, haut, hauteur, heros, hibou, hockey, hollande, homard, honte, hors, housse, hublot, hurler, hutte)
- ELISION_MAP for general clitics, SI_TARGETS for si-specific restriction
- Check: iterate token pairs, if current token is in ELISION_MAP and next starts with vowel or h-muet, flag with span covering both tokens
- explain returns {nb, nn} with severity-bearing strings (student-friendly Norwegian)
- matchCase the fix against the original display

**fr-etre-avoir.js:** Create following de-perfekt-aux.js pattern closely. Key details:
- id: 'fr-etre-avoir', languages: ['fr'], priority: 70, severity: 'warning'
- Read FR_AVOIR_FORMS, FR_ETRE_FORMS, FR_ETRE_VERBS, FR_ETRE_PARTICIPLES from grammar-tables (lazy-init at first check() call, not IIFE time)
- Build AUX_LOOKUP Map from FR_AVOIR_FORMS (aux: 'avoir') + FR_ETRE_FORMS (aux: 'etre')
- Build AVOIR_BY_PERSON and ETRE_BY_PERSON reverse maps for suggestion generation
- Handle 3rd person plural: ils/elles present -> ont/sont, imparfait -> avaient/etaient
- check(): For each token matching AUX_LOOKUP, scan forward up to 5 tokens for a participle
- First check ctx.vocab.participleToAux (data-driven), then fall back to FR_ETRE_PARTICIPLES (hardcoded)
- If participle requires etre but avoir form used (or vice versa), flag the auxiliary token
- Skip participles with aux "both"
- Only flag avoir-where-etre-required direction with full confidence. For etre-where-avoir-required, also flag but be aware of passive voice edge cases (acceptable FP rate for students).
- Accent-stripping for participle lookup: try both accented form and stripped form against participleToAux Map. Use a simple strip function that maps àâ->a, éèêë->e, etc.
- explain returns {nb, nn} with helpful student message

**Fixtures:** Create elision.jsonl with >=30 positive + >=15 acceptance cases. Create etre-avoir.jsonl with >=30 positive + >=15 acceptance cases. Follow existing JSONL fixture format (one JSON object per line with text, lang, expected array).
  </action>
  <verify>
    <automated>npm run check-fixtures 2>&1 | tail -10</automated>
  </verify>
  <done>FR-01 elision rule flags missing elision before vowels/h-muet with h-aspire exceptions; FR-02 etre/avoir rule flags wrong passe compose auxiliary; both have >=45 fixtures each; check-fixtures passes</done>
</task>

<task type="auto">
  <name>Task 2: Release gate wiring (manifest, CSS, TARGETS, benchmark expectations)</name>
  <files>extension/manifest.json, extension/styles/content.css, scripts/check-explain-contract.js, scripts/check-rule-css-wiring.js, benchmark-texts/expectations.json</files>
  <action>
**manifest.json:** Add both new rule files to content_scripts js array, after the existing fr-contraction.js entry:
- `"content/spell-rules/fr-elision.js"`
- `"content/spell-rules/fr-etre-avoir.js"`

**content.css:** Add CSS dot-colour bindings for both new rules:
- `.lh-spell-fr-elision { background: ...}` — use same error-tier red as other P1 rules (match fr-contraction's existing colour)
- `.lh-spell-fr-etre-avoir { background: ...}` — use warning-tier amber/orange as other P2 rules (match de-perfekt-aux's colour)

Check existing CSS for the dot-colour pattern used by fr-contraction (error tier) and de-perfekt-aux (warning tier) and use the same background values.

**check-explain-contract.js TARGETS:** Add both paths:
- `'extension/content/spell-rules/fr-elision.js'`
- `'extension/content/spell-rules/fr-etre-avoir.js'`

**check-rule-css-wiring.js TARGETS:** Add both paths (same as above — keep in sync).

**benchmark-texts/expectations.json:** Add expectations for FR elision and etre/avoir benchmark lines. Count the actual line numbers in fr.txt carefully:
- Lines containing `je ai` / `je avais` -> expect fr-elision
- Line containing `Si il pleut` -> expect fr-elision
- Line containing `de erreurs` -> expect fr-elision (de + vowel)
- Line containing `J'ai allé` (added in Plan 01) -> expect fr-etre-avoir
- Use correct line numbers based on the actual fr.txt content after Plan 01 edits
  </action>
  <verify>
    <automated>npm run check-explain-contract 2>&1 | tail -3 && npm run check-rule-css-wiring 2>&1 | tail -3 && npm run check-benchmark-coverage 2>&1 | tail -5 && npm run check-network-silence 2>&1 | tail -3</automated>
  </verify>
  <done>Both FR rules in manifest, CSS dot-colours wired, TARGETS lists updated, benchmark expectations set; all release gates pass</done>
</task>

</tasks>

<verification>
1. `npm run check-fixtures` exits 0 — elision and etre-avoir fixtures pass
2. `npm run check-explain-contract` exits 0 — both new rules have valid explain contracts
3. `npm run check-rule-css-wiring` exits 0 — both new rules have CSS bindings
4. `npm run check-benchmark-coverage` exits 0 — FR benchmark expectations met
5. `npm run check-network-silence` exits 0 — no network calls in new rule files
6. `npm run check-bundle-size` exits 0 — bundle stays under 20 MiB
7. Elision acceptance: j'ai, s'il, l'homme, le heros, si elle all do NOT flag
8. Etre/avoir acceptance: je suis alle, elle est venue, j'ai mange all do NOT flag
</verification>

<success_criteria>
- fr-elision.js flags all 9 clitic+vowel/h-muet patterns with h-aspire exceptions
- fr-etre-avoir.js flags wrong auxiliary in passe compose using data-driven + hardcoded fallback
- >=30 positive + >=15 acceptance fixtures per rule
- All release gates exit 0
- Benchmark fr.txt elision lines and j'ai alle line flip from unflagged to flagged
</success_criteria>

<output>
After completion, create `.planning/phases/10-fr-elision-auxiliary-pp/10-02-SUMMARY.md`
</output>
